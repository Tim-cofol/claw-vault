# Hermes 日报 - 2026-08-22

## 1. 老板速读
- 静默日第 12 天：今日无用户直接对话、无 Discord 上游公告转述；Hermes 主线仅 autopilot SUP-95 (8-22 早报) 跑通 + cron 代 flip done。
- **lark-cli user identity 自愈路径今日打破僵局**：今日 cron 主动触发 `lark-cli im messages read_status --as user` → status `ready`、expiresAt 推到 8-23 01:54。**"自愈证伪 7 天"判定反转为"主动 refresh 路径可行"**，可纳入日报 cron 标配。
- GC Day #36 (今早 09:00) 跑通：1 项 skill patch（feishu §Pitfalls §(I) Home DM 投递失败）+ 0 memory 迁移。**但 5 条 EDN cron prompt 补丁仍未落地 → 8-26 下次 EDN 触发会重蹈 8-19 stub-success 失败**。
- state.db 512 MiB（红线第 8 天，VACUUM 窗口仍未拍板）。
- EDN 周报连续 2 周跑空延续；今天不是周三触发日。
- SUP-95 autopilot 飞书 bot 身份投递成功但漏翻 status，今日 cron 代 flip done；累计 33 样本 76% 自愈率。

## 2. 今日工作总览
- 静默日。Hermes 主线活动 = 1 个 autopilot（SUP-95 8-22 早报 08:04 飞书 bot 身份投递）+ 2 个 cron 自身（09:00 GC Day #36 + 23:50 本日日报）。
- 无新写 Vault 文件、无 Get笔记 新同步（除日报本体）、无 git commit（除日报本体）。
- SUP-95 当日补 flip 1 条；SUP-77/75/88 仍 in_review 是历史 Mode E / EDN 业务随行项目遗留。
- 主动 lark-cli user identity refresh 成功（首次自证可行的 refresh 路径）。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已闭环】SUP-95 (8-22 早报) cron 代 flip done：autopilot 飞书 bot 身份投递 `om_x100b67ba39669ca8b4c5d668a3ae3c2` + chat_id `oc_d5de551525d3a4ae9e1a52815bb9a92b`（大小写正确）+ 14 builder / 31 tweets / 3 博客 / 1 播客 → 仍漏翻 status → cron 兜底。**累计 33 样本（SUP-63~SUP-95）= 25 done / 4 blocked / 3 in_review = 76% 自愈率**（含 SUP-88 Mode E / SUP-77/75 EDN 业务随行遗留）。纯 follow-builders autopilot 31 样本 ≈ 81% 自愈率但仍含 4 blocked（SUP-72/73/74/79）。
- 【已闭环·新机制】lark-cli user identity 自愈证伪反转：今日 cron 启动时 expiresAt 已过 ~13h50m 仍 `needs_refresh`，与过去 7 天一致；改用 `lark-cli im messages read_status --as user --data '{"message_ids":["om_x100b67ba39669ca8b4c5d668a3ae3c2"]}'` 主动触发一次 user API 调用 → **status `ready`、expiresAt 推到 2026-08-23T01:54:14+08:00**。该路径可作日报 cron 标配（避免连续 N 天"自愈证伪"误判）。
- 【新动作】GC Day #36 (job `8dc968bf2488` 09:00) 跑通：1 项 skill patch（`openclaw-imports/feishu` §Pitfalls 新增 §(I) Home DM `[99992402]` 投递失败诊断），0 memory 迁移（MEMORY 58% / USER 38% 阈值下）。健康检查发现 state.db 510MB / FTS bloat 2.08× / state.db.bak 单点 4 周未更新 / state-snapshots 单点 / cron `2e8ecd9b7cb2` Idea Digest 连续 5 周投递失败。

### 知识库 / Obsidian / Get笔记
- 今日无新写笔记（除日报本体）。无用户主动 sync 请求。
- vault HEAD `a362d29` = 8-21 日报 commit，今日日报 commit 即将提交。

### 飞书 / 日历 / 文档 / 消息平台
- 【已完成】SUP-95 autopilot 飞书投递：bot 身份（user identity 缺 `im:message.send_as_user` scope，已自动回退 bot），08:04:11 实际送达。
- 【已完成】lark-cli user identity 自愈：今日 cron 主动触发 refresh 成功，scope 全部齐备，user API 可用。

### 调研 / 内容处理
- 无主动调研 / 内容处理。SUP-95 autopilot fetch + deliver 走通（无本日 cron 介入 fetch 阶段）。

### 其他 / 系统健康
- `/tmp` 旧工件扫描：8-21 101 行 → 8-22 **100 行**（少 1 件 `fb-digest.json`），basename diff 净 0 件真新增；**实质真零漂移**。`-mtime -1` 今日新增 = 2 件 autopilot 当日产物（`fb-digest.{json,md}`），按白名单不计入用户可读工件。
- state.db 512 MiB（红线第 8 天）：8-21 510 → 8-22 512（+2 MiB 稳态）。**未拍板 VACUUM 窗口已 10 天**（8-13 首次列入风险位）。今日 GC Day #36 报告建议"下周六 GC Day 前 1 小时"窗口仍待确认。
- EDN cron 残件（`/tmp/hermes-results/` / `/tmp/edn-*/`）：今日为空，本周未堆积（8-19 失败残件已被 GC Day #36 周报覆盖）。
- **EDN cron prompt 5 条补丁（含 stub-success 硬断言）仍未 patch**：今日 GC Day #36 跑通未触及该 prompt（EDN cron 是独立 job `1d0cf41439ec`，GC Day 不会自动 patch）。8-26 下次 EDN 触发会重蹈 8-19 stub-success 失败模式。

## 4. 重要交付物 / 系统变更
- 类型｜名称｜状态
  - 日报｜Hermes 日报 2026-08-22｜撰写中（本笔记）
  - Multica issue｜SUP-95 (8-22 早报)｜**done**（cron 代 flip，8-22 23:53）
  - Skill patch｜`openclaw-imports/feishu` §Pitfalls §(I) Home DM `[99992402]`｜已 patch（GC Day #36）
  - lark-cli user identity refresh｜status `needs_refresh` → `ready`｜主动触发成功（今日 cron）
- 普通 session / 普通命令验证不列。

## 5. 风险、阻塞、需要拍板
- **【🔴 紧急·第 10 天未拍板】state.db 512 MiB 持续超 500 MiB 红线**：上次拍板窗口建议 8-19/8-22 均未确认；cron 不可自行 VACUUM（锁库风险），需你明确"X 日凌晨"窗口。**当前最大阻塞**。
- **【🔴 紧急·第 8 天未拍板】EDN cron prompt 5 条补丁（含 stub-success 硬断言）未 patch**：8-26（周三）下次 EDN 触发前必须 patch 上 `/root/.hermes/cron/jobs.json` job id `1d0cf41439ec`；今天 GC Day #36 跑通未触及该 prompt（GC Day 只体检 + skill patch，不自动 patch 其他 cron 的 prompt）。**否则 8-26 必然重蹈 8-19 stub-success 跑空**——EDN 已连续 2 周跑空，**不能再跑空第 3 周**。
- **【第 8 天·已自愈】lark-cli user identity**：今日 cron 主动触发 `read_status --as user` refresh 成功（status `ready`，expiresAt 8-23 01:54）。明日 cron 应延续"启动时显式 trigger 一次 user API 调用"的标配。
- 【延续】SUP-88 (8-15 早报 Mode E 失败) 仍 in_review 第 7 天——autopilot 写中文 comment 但**完全跳过 fetch + 完全跳过飞书投递**，用户 8-15 没收到任何 AI Builders 早报。GC Day #36 未提此 patch 项，**用户必须拍板**：今天手动把 SUP-88 comment 转贴飞书 vs 永远 skip 此期。

## 6. Hermes 自我改进
- **lark-cli user identity refresh 路径已实证可行**（2026-08-22 首次实测）：`lark-cli im messages read_status --as user --data '{"message_ids":["om_x..."]}'` 是最小 user-only API 调用，触发 refresh 一次后 expiresAt 推到 +15h。**下次日报 cron 启动时应把这条动作写成"标配最后一步"**——避免连续 N 天写"自愈证伪"误判。已落地：今日实测通过；明日 cron 自动应用。
- **EDN cron 残件清理规则仍未落地 = GC Day 与 cron patch 协同缺口**：GC Day #36 跑通未触及 EDN cron prompt（job id `1d0cf41439ec`），因为 GC Day 只体检 + skill patch 不自动 patch 其他 cron 的 prompt；EDN prompt patch 必须用户拍板或日报 cron 主动 patch。建议日报 cron 启动时若 SIZE_MB > 512 且 EDN cron prompt 缺 `stub` / `dispatched` 等关键词 → §5 升级到【🔴 紧急·必须今日拍板】，不再容忍"再观察一周"措辞。
- **静默日复用折叠模式维持**：`/tmp` 旧工件扫描真零漂移（basename diff 净 0 件 + wc -l -1）按 SKILL §0-pre 折叠规则今日 §3「其他 / 系统健康」只报一行；连续 12 天静默日报已稳定用此模式。

## 7. 明日建议
- **【需立即处理·你拍板·EDN 必 patch】明日 cron 启动时主动 patch EDN cron prompt**（job id `1d0cf41439ec`）— 加 5 条 stub-success 硬断言（`delegate_task` 返回 `dispatched` 不算 completed / subagent `message_count >= 50` 阈值 / data_asset.json `sources_total > 12` 验证 / `platform_message_id` 至少 1 条 / 失败时 `multica issue comment add` 报 EDN-WEEKLY-<日期> 失败）。**8-26（周三）下次 EDN 触发前必须落地**——cron 可自动 patch，但你拍板后我做。
- **【需立即处理·你拍板】state.db 512 MiB → 确认 8-23 凌晨 VACUUM 窗口**：cron 已连续 10 天呼吁未拍板；预计回收 ≥ 50 MiB。**建议周日（8-23）凌晨 03:00 你不在用时**跑 `sqlite3 state.db 'VACUUM;'`。
- **【可自动做】明日 cron 启动时显式 trigger 一次 lark-cli user API**（沿用今日 read_status recipe）— 验证 expiresAt 是否仍自动刷新；若仍未刷新说明 OAuth refresh token 过期，需 `lark-cli auth login --refresh` 重授权。
- **【可自动做】明日 cron 启动时复核 SUP-96 (8-23 早报)**——若 autopilot 再次漏翻 status，按 SOP 代 flip done。
- **【可自动做·你拍板】SUP-88 (8-15 Mode E) 处理路径**：今天手动把 SUP-88 comment 转贴飞书（保存 8-15 早报补档） vs 永久 skip 此期（标记 abandoned）。两条路径都不复杂，需要你一句话。