# Hermes 日报 - 2026-06-26

## 1. 老板速读
- **今天主线 = autopilot 早报第 6 天 + 飞书晚间长对话**：上午 SUP-30 (AI Builders 每日早报) autopilot 跑通；晚上 22:49 起你在飞书连发 3 个问题（YouTube 视频总结 / idea recall 系统 / FTS5 性能），我在飞书一条条答完。
- **idea-recall 系统已立项、待你最后 2 个拍板**：方案 = `30-Resources/Ideas/` 目录 + SQLite FTS5 + 被动召回（关键词白名单触发）+ 每周 cron 推 dormant idea；FTS5 benchmark 实测完成（500 条 p95 < 0.2ms / 2000 条 p95 < 0.44ms / 2C4G VPS 内存 < 22MB / 并发 291 QPS 无冲突），性能完全 OK。
- **新发现（值得立刻记录）**：`~/.follow-builders/config.json` 声明 `delivery.method: "feishu"`，但 skill 自带的 `deliver.js` 不支持 feishu —— autopilot 一直靠 `lark-cli im +messages-send` 绕过。runbook 里这一步写法需要固化为 skill / memory，避免下次 onboarding 的人重踩。
- **`/tmp` 旧工件扫描**：实际结果 11 ARCHIVED + 51 NOT_ARCHIVED（51 件与昨天完全一致，全部为 Hermes 内部 scratch，不归档）；今天无新增用户产出挂起。**今天第二轮"无幽灵延续项"。**
- **延续风险 = 7 项未变**：last30days X 源 cookies / MI squad 架构 / Discord token 重置 / lark-cli scope 补全 / PR #41283 / .bak 清理 / SUP-25 in_review。

## 2. 今日工作总览
- **Cron 自动跑 = 1 条**：SUP-30 autopilot 早报 08:02 启动 → 08:07 done → 飞书群 08:06 Asia/Shanghai 送达；当前日报自身 `cron_7be51b336d4f_20260626_235004`。
- **Multica ACP = 1 条真活动**：`1117c6bb-...` SUP-30 autopilot 早报 done（126 条 msg，工具调用密集），无 stub。
- **用户直接对话 = 3 条问题（同一飞书 session）**：YouTube 视频总结 → idea-recall 系统可行性 + 最佳实践 → FTS5 benchmark 性能评估。
- **子 agent 派发 = 0 条**：今天没有 quick-create / local coding agent / quick-search 派发。
- **状态盘点**：Multica workspace 今日 `done` = SUP-30；SUP-29 / SUP-28 / SUP-27 / SUP-26 维持 done；SUP-25 仍 in_review 第 10+ 天。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【自驱完成】SUP-30 autopilot 早报 cron 08:02 启动、08:06 飞书送达、08:07 翻 `done`——连续 6 天无人工干预。本期 6 位 builder 有实质 AI 内容（Aaron Levie / Guillermo Rauch / Swyx / Google Labs / Ryo Lu / Dan Shipper），podcast 为 AI & I by Every 整段解读。
- 【新发现待固化】`~/.follow-builders/config.json` 声明 `delivery.method: "feishu"` 但 skill 自带 `deliver.js` 只支持 telegram/email/stdout——autopilot 6 天来都是用 `lark-cli im +messages-send --as bot` 绕过。**这个发现已经在 SUP-30 issue 评论里写了，但要落成 memory / skill 才不会再被忘掉。**
- 【保持】lark-cli user identity `needs_refresh` 第 4 天；scope 列表里**无** `im:message.send_as_user`。今天 SUP-30 投递走 bot 身份 fallback（identity: bot，message_id `om_x100b6ce86b05d0a4b34489b8da13a15`），投递成功但身份错配已积累 4 天。

### 飞书 / 日历 / 文档 / 消息平台
- 【新增】22:49:51 你在飞书发来 YouTube 链接 `https://youtu.be/Sb96po6fS67k`（Hermes Agent + Claude Code 组合用法）；我按视频整理中文要点：Hermes vs Open Claw 差异（self-evolving skills / memory 带 token 上限 / sandbox）、Anthropic 6/15 后对第三方 agent 额外计费、用 Hermes 当 MCP server 给 Claude Code 调用的三种用法（Slack → PRD skill / cron 跑 Claude Code 非交互 / 健康巡检）。
- 【新增】23:02:10 你提 idea-recall 系统可行性——我搜了 best practices（Generative Agents / Letta / Mem0），结论：50–500 条想法最佳实践 = Obsidian Markdown + SQLite FTS5 + 被动召回 + 周 cron 主动推送；理由 vs 向量库（语义匹配质量 / 隐私 / 复杂度递减）。
- 【新增】23:40:43 你担心 FTS5 性能——我跑 benchmark 给你：50/100/500/2000 条想法的建索引时间、查询 p95、DB 大小、内存、并发（3 reader + 1 writer / 5 秒 / 0 错误 / 1454 queries / p99 = 3.31ms）；结论 FTS5 完全 OK，瓶颈是 Python 进程启动而非 FTS5。
- 【持续小坑】lark-cli user identity `needs_refresh` 第 4 天 + scope 列表无 `im:message.send_as_user`；Discord token improper 自 6-22 15:18 起超 130 小时（今天 6:00+），你明文 token 仍未作废。

### 知识库 / Obsidian / Get笔记
- 【未新归档】今天没有任何用户产出工件产生；`/tmp` 扫描 51 件 NOT_ARCHIVED 与昨天完全一致——全部归类为 Hermes 内部 scratch（verdict 模板照搬）。
- 【保持】昨天闭环的 11 件内容工件在 Vault 状态稳定；`beautiful-feishu-whiteboard/{CATALOG,SKILL,RULES,README.zh}.md` 4 件 NOT_ARCHIVED 已在 workspace 安装为完整 skill 源，README.md 已归档留入口。

### OpenClaw / 代码项目 / GitHub
- 【Multica】SUP-30 (`AI Builders 每日早报 2026-06-26`) 翻 `done`；SUP-29 维持 done（昨天）；SUP-25 仍 in_review 第 10+ 天。
- 【保持】PR #41283 仍未查；P0 清理 26 份 `.bak` 仍可自动做。

### 调研 / 内容处理
- 【完成】YouTube 视频总结（Hermes Agent + Claude Code 组合用法）已发飞书。
- 【完成】idea-recall 系统最佳实践调研：覆盖 Letta / Mem0 / Generative Agents / Self-RAG / HyDE；推荐 FTS5-first 路径。
- 【完成】SQLite FTS5 benchmark 报告：性能 + 内存 + 并发 + 磁盘全部实测。

## 4. 重要交付物 / 系统变更
- 早报｜SUP-30 AI Builders 每日早报（2026-06-26）｜已完成｜飞书群 `oc_d5de551525d3a4ae9e1a52815bb9a92b` 08:06 Asia/Shanghai 送达，message_id `om_x100b6ce86b05d0a4b34489b8da13a15`（bot 身份）
- 调研｜YouTube 视频总结（Hermes Agent + Claude Code 组合用法）｜已完成｜视频 ID `Sb96po6fS67k`（已发飞书）
- 调研｜idea-recall 系统最佳实践 + 方案 + benchmark 报告｜已完成｜落档飞书对话（含 FTS5 实测数据：500 条 p95 < 0.2ms / 2000 条 p95 < 0.44ms / 291 QPS / 0 冲突）
- Multica 状态｜SUP-30 (`AI Builders 每日早报 2026-06-26`) 翻 `done`｜已完成｜今日 08:07
- 新发现｜`follow-builders` skill 的 `deliver.js` 不识别 feishu 投递方法｜未动｜已写进 SUP-30 issue 评论，待落成 memory / skill

## 5. 风险、阻塞、需要拍板

### 5.1 `/tmp` 旧工件扫描
今天 23:50 `find /tmp -maxdepth 2 -type f -mtime +0` 实际结果：

**【全部归档完成 · 无新增挂起】** 11 ARCHIVED + 51 NOT_ARCHIVED；51 件 NOT_ARCHIVED **全部归类为 Hermes 内部 scratch**（与昨天完全一致，无新增）：30 件 `hermes-cwd-*.txt` / 8 件 `hermes-results/call_*.txt` / 4 件 `beautiful-feishu-whiteboard/{CATALOG,SKILL,RULES,README.zh}.md` / 9 件杂项 Hermes scratch。

**今天第二轮"无幽灵延续项"**，与昨天并列。

### 5.2 [新增 · 需你拍板] idea-recall 系统最后 2 个问题
FTS5 性能已实测无问题。等你拍：
1. **关键词白名单**（被动召回触发用）：`agent, hermes, claude, recall, idea, PRD, cron, skill, memory, workflow, plan, deploy, openclaw`？要加/减？
2. **开始动手**？我会按 6 步建：文件夹 → FTS5 索引脚本 → idea-recall skill → 周 cron → 更新 MEMORY → 更新 AGENTS.md。

### 5.3 [新增 · 需你拍板] SUP-30 autopilot 投递路径要不要固化为 skill
发现 `~/.follow-builders/config.json` 写 `delivery.method: "feishu"` 但 skill 自带的 `deliver.js` 不识别 —— 6 天来 autopilot 一直靠 `lark-cli im +messages-send` 绕过。要不要：
- A1 把 `deliver.js` 扩到支持 feishu（一劳永逸，但需要写 Node 代码）
- A2 把 "prepare-digest → lark-cli im +messages-send" 这条路径写进 follow-builders skill runbook，下次 onboarding 不会重踩

### 5.4 [延续第 4 天 · 需你拍板] `last30days` X 源的最后一步
yt-dlp 装好、bird-installed ✅，**就差你从浏览器导出 `auth_token` + `ct0` 写进 `~/.config/last30days/.env`**。两条路径：A1 贴给 Hermes 写 / A2 你自己写 .env 写完告诉 Hermes 跑 diagnose。**等到了就 5 分钟能跑通 X 源。**

### 5.5 [延续第 5 天 · 需你拍板] MI squad 多 skill 调研架构
fa96d603 已出完整方案（路径 1 三段 agent 拆 / 路径 2 xiaoma 兼任投递），3 个问题等你回：路径 1 还是路径 2？手动还是自动派活？Obsidian vault 目录是 `/root/obsidian-vault` 根 还是 `Projects/MI/` 子目录？

### 5.6 [延续第 6 天 · 需你拍板] Discord token improper
已超 130 小时；6-23 16:37 你贴的明文 token Hermes 拒绝用，那个 token 必须作废，必须去 Discord 重置一次新 token 走 `DISCORD_BOT_TOKEN` 环境变量。

### 5.7 [延续第 4 天 · 需你拍板] lark-cli user identity scope 补全
SUP-27 / SUP-28 / SUP-29 / SUP-30 四天 autopilot 投递均发现 user 身份缺 `im:message.send_as_user` scope；今天 `lark-cli auth status` 确认 user identity `needs_refresh`（expiresAt 2026-06-26T10:05:41，刷新窗口已开但未触发）且 scope 列表里**无** `im:message.send_as_user`。目前 bot 身份 fallback 工作正常，但 autopilot 想统一走 user 身份投递，需 `lark-cli auth login --scope "im:message.send_as_user"` 重授权。**今天未做。**

### 5.8 [延续第 N 天] PR #41283 / P0 清理 26 份 .bak / Weixin disconnected / SUP-25 in_review 第 10+ 天
全部未动。SUP-25（飞书 vibe 助手）建议升 `done` 或明确下一步责任人。

## 6. Hermes 自我改进
- **idea-recall FTS5 benchmark 跑通 = "先测再说" 第 N 次验证**：你担心性能我没拍脑袋说"够用"，而是写了 50/100/500/2000 条想法的 benchmark + 3 reader + 1 writer 并发测试——结果远好于预期。**值得固化为 runbook 模板**："性能问题 → 写 benchmark → 跑数据 → 给数字"。
- **autopilot 早报 cron 连续 6 天零失败 + 新发现 follow-builders skill 与 deliver.js 配置错位**：明天应该把 SUP-30 issue 评论里的发现（"config.json 声明 feishu 但 deliver.js 不识别"）patch 进 follow-builders skill 的 SKILL.md，避免下次 onboarding 的人重踩。
- **3 个用户问题都是产品方向（不是闲聊）**：YT 视频总结 / idea-recall 系统 / FTS5 benchmark——日报模板应该把"飞书产品方向深度问答"作为独立分组常驻（不只是"消息平台"）。

## 7. 明日建议
- 【需要你确认】idea-recall 系统最后 2 问：关键词白名单 + 开始动手。FTS5 性能已实测 OK，方案已完整，就差你这 2 个拍板。
- 【需要你确认】SUP-30 autopilot 投递路径要不要固化为 skill：A1 扩 `deliver.js` 支持 feishu / A2 把 runbook 写进 follow-builders skill。
- 【可自动做】明天 autopilot SUP-31 早报应自动创建（cron 08:00 Asia/Shanghai），无需手动干预。
- 【可自动做】明天 23:50 日报 cron 启动时，再扫一遍今天的延续项（last30days / MI squad / Discord token / lark-cli scope）；哪条挂满 7 天就在 §5 风险位升级措辞，不再用"延续第 N 天"模糊化。
- 【需要你确认】`last30days` X 源 cookies 选 A1（贴给 Hermes）或 A2（自己写 .env）——这条挂 4 天，autopilot EDN 周报的 X 帖子数直接受影响。
- 【可自动做】SUP-25（飞书 vibe 助手）已在 in_review 第 10+ 天，cron 建议直接升 `done` 或挂 blocker 标黄；26 份 `.bak` 按"保留 2 份最新"清理；lark-cli `auth login --scope "im:message.send_as_user"` 重授权（如不阻塞 autopilot 早报可推迟到周末一起做）。
- 【可自动做】把 SUP-30 issue 评论里的 follow-builders 投递路径发现 patch 进 follow-builders skill 的 SKILL.md / runbook。