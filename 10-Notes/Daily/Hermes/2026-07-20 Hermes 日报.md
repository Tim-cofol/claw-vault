# Hermes 日报 - 2026-07-20

## 1. 老板速读
- 周一工作日，但今天 0 用户直接对话、0 用户驱动调研；主要产出来自身 cron 与 idea-recall weekly digest。
- 09:00 idea-recall weekly digest 已扫描 6 条历史想法，全部 skipped → 主动判定 `[SILENT]` 静默不投递（连续 4 周低密度）。
- **SUP-57 今日 AI Builders 早报存在真实异常**：autopilot 08:01 抓数据后中断，0 飞书投递；issue 仍停在 `in_review`，待人工核查。
- `/tmp` 旧工件实扫 109 行：11 ARCHIVED + 98 扫描器标记 NOT_ARCHIVED；和昨天一致，全部为 Hermes 内部 scratch，无新增用户待找回产出。
- 飞书 user identity 仍 `needs_refresh`，缺 `im:message.send_as_user`；bot 路径目前可用，今天没新触发投递，未影响业务。
- 结构性风险未变：state.db ≈ 421MB、checkpoint ≈ 116MB；上周已升级为等你拍板，今天继续搁置。

## 2. 今日工作总览
- 【已完成·自驱】09:00 idea-recall weekly digest 扫描 6 条想法 → 全部 skipped → cron 自身判定 `[SILENT]`。
- 【异常·自驱】SUP-57 AI Builders 每日早报 08:00 创建 issue，08:01 抓完数据后未继续（无 tweet/blog，仅 1 集播客），未生成最终 digest 与飞书投递，状态保持 `in_review`。
- 【已核对】本日报启动前完成 `/tmp` 双扫描（-mtime +0 / -mtime -1）、昨日风险位复读、vault git HEAD 比对（`1b46942` = 2026-07-19 日报）、飞书 user identity 自检。
- 【已完成】本日报本身已生成、双写流程执行。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【自驱】09:00 idea-recall weekly digest cron（`2e8ecd9b7cb2`）扫描到 6 条历史想法，全部 skipped → cron 自行 `[SILENT]` 静默。本周已无新增 idea-capture，连续 4 周低密度，建议把 weekly digest 改为双周一次或合并到每月 GC Day（见 §6）。
- 【静默】其余 3 个 enabled cron 今日未触发（EDN 周一 08:30 周末才跑、GC Day 仅周六、daily report 23:50 = 本任务）。
- 【实扫】`/tmp` 旧工件 109 行（`bash scripts/scan_tmp_artifacts.sh | wc -l` 实测），与昨天 109 行一致：11 ARCHIVED + 98 NOT_ARCHIVED，其中 96 行是 `hermes-cwd-*` / `hermes-results/call_*` / `scan_2026-*.txt` 等 Hermes/容器 scratch。24h 内新增用户可读工件 = `fb-digest_intro.md` / `fb-prepare.json` / `fb-summarize_blogs.md` / `fb-summarize_podcast.md` / `fb-summarize_tweets.md` / `fb-translate.md` 共 6 件，全部属于 SUP-57 autopilot prep 阶段产物（见 §4）。

### Multica / 自驱工作流
- 【异常】SUP-57 今日 AI Builders 早报：00:00:20Z 创建 → 08:01 抓数据写入 `/tmp/fb-prepare.json`（tweets=0 / blogs=0 / podcasts=1）→ 之后无动作。issue 至今 `in_review`，无中文结果评论，无 message_id。
- 【未代 flip】今日 SUP-57 不符合昨天日报的"代翻 done"规则（无结果评论 / 无 message_id）；按 SKILL 要求显式标"未验证"，**不能**静默翻 done。
- 【观察】这是 follow-builders autopilot 第二次出现"issue 已建但 deliver 中断"（上次是 SUP-39 stall 24h 后 SUP-41 自愈）。今天 issue 创建 4 小时仍无续动，不符合 6-30 观察的"acp channel probe 会在 24h 内自愈"模式，需要人工核查 acp probe 心跳是否正常。

### 飞书 / 消息平台
- 【治理项】user identity 仍为 `needs_refresh`；scope 列表里 `im:message.send_as_user` 仍缺失；今天没有用户驱动或 autopilot 投递触发，没碰这条路径。
- 【bot 状态】bot identity 仍 ready；如 SUP-57 续动成功将以 bot 身份投递（与昨日 SUP-56 一致）。

### 知识库 / Obsidian / Get笔记
- 【稳态】本日报双写流程本身正常；vault git HEAD = `1b46942`（昨天日报），今日 working tree 干净，不存在前一日未推送遗留。
- 【观察】idea-recall 索引今天 0 新增 → 30-Resources/Ideas/ 已连续多周无新写入，与 weekly digest 的 skipped 6 条同向信号。

## 4. 重要交付物 / 系统变更
- cron｜Hermes Idea Recall Weekly Digest 2026-07-20｜已跑通，扫描 6 全部 skipped → SILENT
- issue｜SUP-57 AI Builders 每日早报｜异常：autopilot prep 阶段抓数据后中断，状态 `in_review`，待人工核查
- 工件｜`/tmp/fb-prepare.json`｜0 tweets + 0 blogs + 1 podcast｜SUP-57 prep 阶段产物
- 工件｜`/tmp/fb-{digest_intro,summarize_blogs,summarize_podcast,summarize_tweets,translate}.md`｜6 件 prompt 模板｜SUP-57 prep 阶段产物
- 日报｜2026-07-20 Hermes 日报｜已生成、已纳入日常双写流程

## 5. 风险、阻塞、需要拍板
- 【需要你确认】SUP-57 今日异常：08:01 抓完数据后 autopilot 无续动，issue 已卡 `in_review` 4 小时未自愈。建议核查 acp channel 心跳/调度是否正常；若 autopilot 已停摆决定是否手工跑一次补发 7-20 早报。
- 【需要你确认】state.db ≈ 421MB / checkpoint ≈ 116MB：上周已升级等你拍板，今天继续搁置。
- 【观察】idea-recall 0 新增 + weekly digest 连续 skipped：建议把 weekly digest 改为双周一次或合并到 GC Day（见 §6）。
- 【观察】飞书 user identity 缺 `im:message.send_as_user`：bot 路径今天未受影响，但任何用户身份投递场景仍会卡。

## 6. Hermes 自我改进
- SUP-57 异常暴露 follow-builders skill 仍缺"prep 阶段失败 → 明确标记 → 不留半成品 issue"的兜底。建议下次 GC Day 把"prep 抓完数据后 15 分钟内未开始 deliver → issue 加 comment 标 `prep-only` 并状态翻 `blocked`"写进 skill 强制收尾段，与 SUP-5x 的"deliver 后自动 flip done"配成完整闭环。
- idea-recall 0 新增 + weekly digest skipped 6 → 调整为双周或合并 GC Day 可降低 cron 噪声，让 weekly digest 真的"找到值得回顾的想法"才有意义。

## 7. 明日建议
- 【需要你确认】处理 SUP-57 异常：人工核查 acp probe 心跳 / 决定是否手工补发 7-20 早报。
- 【可自动做】idea-recall 改为双周 digest（写进 jobs.json cron `expr`）。
- 【可自动做】EDN 周三 08:30 准备正常跑，按今天周报节奏无异常。
- 【需要你确认】决定 state.db / checkpoint 缩容策略（继续搁置则不动）。
- 【可自动做】明天 23:50 日报按惯例继续生成；若 SUP-57 仍未恢复则在 §3 重复标"延续异常第 N 天"。