# Hermes 日报 - 2026-07-23

## 1. 老板速读
- 今日核心产出是 SUP-60 AI Builders 早报：覆盖 17 位 Builder、36 条 X 动态、1 篇博客和 1 期播客，已投递飞书并由执行链路自行翻为 `done`。
- 重点信号包括 Google 双 Flash 模型上线、Codex/ChatGPT Work 突破 1000 万用户、Factory 的“Dark Factory”自主软件工程层，以及 Claude 接入 Apple Foundation Models。
- follow-builders 首次完整执行“投递 → 回写证据 → 翻状态”收尾，日报无需代办 close-out，前期流程修复开始生效。
- `/tmp` 旧工件实扫 98 行：13 ARCHIVED + 85 NOT_ARCHIVED；较昨日新增 1 件 EDN 合并数据 JSON，属于昨日周报中间资产，无新增今日用户产出。
- 飞书 user identity 仍为 `needs_refresh`，且缺 `im:message.send_as_user`；SUP-60 通过 bot 成功送达，当前不阻塞，但身份路径仍未根治。
- 明日优先建议：确认是否把 follow-builders 默认投递方式从 `stdout` 固化为飞书；处理长期悬置的 SUP-57 与 SUP-25。

## 2. 今日工作总览
- 【已完成】AI Builders 早报完成采集、筛选、中文整理、飞书投递和 issue 状态收尾。
- 【已完成】对 17 位 Builder 的 36 条动态去除个人见闻与低价值转发，保留模型、Agent 工程、开发工具与产业信号。
- 【已核对】今日无用户直接对话；主要活动来自 ACP/autopilot 自驱任务。
- 【已核对】`/tmp` 新鲜扫描仅见 Hermes/容器 scratch，无新增用户可读工件。

## 3. 按项目 / 责任域汇报

### Multica / AI Builders
- 【已完成】SUP-60 早报已投递飞书，回写 message_id 后直接翻为 `done`，没有遗留 `in_review`。
- 【结论】本期值得持续追踪的主线是轻量模型提速、Agent 工程平台化、组织记忆可迁移，以及 Claude 与端侧系统框架结合。
- 【已过滤】Nikunj Kothari、Peter Steinberger、Dan Shipper、Matt Turck 的个人见闻或转发未进入正文，降低信息噪声。

### 飞书 / 消息平台
- 【已完成】目标群 chat_id 使用已验证的小写 `oc_...`，正式消息及连通测试均送达。
- 【观察】当前 follow-builders 配置仍以 `stdout` 为默认投递，今天依靠 issue 指令覆盖为飞书；下次仍存在配置与任务描述不一致风险。
- 【需补 scope】user identity 缺 `im:message.send_as_user`，且 token 当前等待自动刷新；今天 bot 路径可用，但不能等同于 user 身份正常。

### 知识库 / Obsidian / Get笔记
- 【已核对】昨日 Vault 已推送且工作树干净，无跨日 Git 遗留。
- 【观察】昨日 EDN 合并数据 JSON 仍留在 `/tmp`，它是可再生成的中间资产，不作为今日新增用户交付物。

## 4. 重要交付物 / 系统变更
- 早报｜SUP-60 AI Builders 早报｜已投递并 `done`｜17 位 Builder、36 条 X、1 篇博客、1 期播客
- 流程改进｜follow-builders 自动 close-out｜首次验证生效｜投递后自行回写证据并翻状态

## 5. 风险、阻塞、需要拍板
- 【需要你确认】是否将 follow-builders 默认投递方式固化为飞书，并写入已验证的小写 chat_id？建议确认后一次改好，避免每期靠 issue 文本覆盖。
- 【需要你确认】SUP-57 是否补发 7 月 20 日早报并关闭；若不补发，应明确取消，别继续悬置。
- 【需要你确认】SUP-25 飞书 vibe 助手需求已长期停留 `in_review`：继续推进或关闭二选一。
- 【观察】飞书 user scope 仍缺 `im:message.send_as_user`；若需要 user 身份投递，必须重新 OAuth 授权，bot 成功不能掩盖该缺口。

## 6. Hermes 自我改进
- follow-builders 的“deliver → comment → status done”今天已由执行链路自行完成；7 月 25 日 GC Day 应核对是否已成为稳定规则，而不是单次偶然行为。
- `/tmp` 扫描继续同时报告扫描器总数与用户可读工件判断，避免把 EDN/Hermes 中间资产误报成用户待归档成果。

## 7. 明日建议
- 【需要你确认】把 follow-builders 默认 delivery 固化为飞书，消除配置漂移。
- 【需要你确认】对 SUP-57 做“补发并关闭 / 放弃并关闭”的明确决策。
- 【需要你确认】关闭或继续推进 SUP-25，清理长期看板占位。
- 【可自动做】7 月 25 日 GC Day 验证 close-out、EDN fallback 阈值与旧扫描快照迁移三项规则是否真正落地。
