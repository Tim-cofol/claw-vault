# Hermes 日报 - 2026-08-19

## 1. 老板速读

- 静默日延续第 9 天：**0 用户直接对话、0 派发子 agent**；仅 EDN 周报 cron + daily-report cron 自身 + SUP-92 autopilot 1 次。
- **EDN 周报 cron 8-19 关键失败**：cron 触发正常、3 subagent 已派发、但 subagent 在 `delegate_task` 同步返回后**早早 die**（仅 3/5/1 条 msg），母 agent 停在 "All 3 subagents dispatched. Waiting for results." 后**无任何 tool call**——本周 weekly digest **0 产出**：无 `/root/.hermes/cron/output/edn-insight-data/2026-08-19.json`、无飞书长住文档追加、无 origin 摘要推送、`last_status='ok'` 是 cron 监控层的误判。这是 **8-29 EDN 双层失败模式变种**——子任务卡死但 cron 仍报 ok，**下周连续 2 周 EDN 跑空**的破坏性远超单次失败。
- **SUP-92 (8-19 AI Builders 早报) autopilot 第二次全自驱闭环**：00:00:25 创建 → 00:02:53 飞书送达 `om_x100b677b675600a8b2c07924621955e`（14 builder / 30 X / 1 podcast No Priors / `oc_xxx` chat_id 自愈）+ 00:02:56 autopilot 自翻 `done`（耗时 91 秒）。**17 样本累计 = 8 全自愈 / 8 cron代flip / 1 blocked = 53% 自愈率**（16 样本 44% → 17 样本 53%，**SUP-91 / SUP-92 连续 2 天全自驱闭环**，这是新出现的"连续全自愈"模式信号，但样本量 2 太小，不足以推翻 patch 紧迫性）。
- **state.db 507 MiB**（503 → 507，+4 MiB），**踩 500 MiB 红线第 5 天**；连续 12 天呼吁 VACUUM 窗口未拍板，cron 自跑会锁库（必须用户拍板）。
- **lark-cli user identity `needs_refresh` 第 4 天**（expiresAt 2026-08-19T10:02:28，今日 23:50 cron 启动时已过 ~13h48m）；今日 SUP-92 autopilot 飞书送达仍走 bot 身份（**chat_id 自愈成功 + status flip 自驱完成均不触发 user API refresh**）——自愈路径已确认证伪第 4 天。
- **明日必拍板（连续 ≥ 4 天呼吁升级）**：(a) state.db VACUUM 窗口（连续 12 天未拍板）+ (b) `lark-cli auth login --refresh` 手动重授权（连续 4 天证伪）+ (c) **EDN cron prompt 加 completion token + subagent 在 polling-loop 里硬断言**（**关键补丁**：本周已连续两周 EDN 跑空，不能再"再观察一周"）。
- **8-22 GC Day cron（周六 09:00）准备就绪**，强约束：把 (a) EDN cron prompt completion-token 双轨 patch + (b) follow-builders skill 双轨 patch + (c) state.db VACUUM 拍板窗口 + (d) lark-cli user identity 重授权一并纳入。

## 2. 今日工作总览

- **0 用户直接对话**。
- **0 派发子 agent**（EDN cron 自身派的 3 个 subagent 全部早早 die，不算有效子 agent 工作）。
- **autopilot 成功 1 次**：SUP-92 (8-19 早报) 飞书送达 + chat_id 自愈 + status flip 自驱 91 秒。**SOP 升级**：连续 2 天（SUP-91 / SUP-92）autopilot 全自驱闭环，是新出现的"连续自愈"信号但样本量不足，不放松 patch 紧迫性。
- **autopilot 失败延续未变**：SUP-88 (8-15) Mode E 第 5 天未自愈，不能 cron 代 flip；SUP-79 (8-06) chat_id 错配 `blocked` 第 14 天未拍板。
- **EDN 周报 0 产出**（用户工作流硬性损害）：本周继 8-12 后**已连续两周 EDN 跑空**，EDN 长期缺失触发 follow-builders skill 外的第二条补丁需求。
- **lark-cli 自愈路径确认第 4 天未生效**：user identity 仍 `needs_refresh`，今日 autopilot 飞书送达继续走 bot 身份。
- **状态**：典型「静默日第 9 天 + SUP-92 autopilot 全自驱闭环 + EDN 周报连续第 2 周失败 + state.db 持续踩红线 + lark-cli 自愈连续 4 天证伪」。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统

- 【首二次实证】**SUP-92 (8-19 早报) autopilot 第二次全自驱闭环**：00:00:25 创建 → 00:02:53 飞书送达 → 00:02:56 自翻 done（耗时 91 秒）。与昨日 SUP-91 形成"连续 2 天全自驱闭环"——是新出现的稳定信号但**样本量仅 2**（17 样本累计自愈率 53% 较 16 样本 44% 回升），不能据此放松 follow-builders skill patch 紧迫性；chat_id 自愈率 = 100%（SUP-92 显式记录"issue 描述里给的 ID 大写 `OC_...`，实际是小写 `oc_...`"），status flip 自愈率 = 8/17 = 47%。
- 【持续】**state.db 507 MiB**（503 → 507，+4 MiB，**单日涨幅回到 trend +5-10 MiB/天 区间**，结束 8-18 +1 MiB 的短暂回落），**踩 500 MiB 红线第 5 天**；cron 不自作主张跑 VACUUM（锁库），但用户连续 12 天未拍板处理。
- 【持续】**lark-cli user identity `needs_refresh` 第 4 天**：今日 cron 启动时距 expiresAt ~13h48m，autopilot 飞书送达仍走 bot 身份；自愈路径已稳定确认不可靠。

### 知识库 / Obsidian / Get笔记

- 【进行中】本日 8-19 日报生成、走 skill 标配双写（vault → git commit/push → 同步 getnote）。
- 【未变】`scripts/getnote_sync_obsidian.py` 仍位于 hermes skill 路径，vault 内无副本；沿用 OpenAPI fallback + 幂等映射 `getnote_sync_map.json`。

### 飞书 / 日历 / 文档 / 消息平台

- 【静默】今日无用户主动会话。
- 【已证伪·第 4 天】`lark-cli auth status` user identity 仍 `needs_refresh`（expiresAt 2026-08-19T10:02:28，今日 23:50 cron 启动时已过 ~13h48m）；scope 列表齐（含 `im:message.send_as_user`）；今日 SUP-92 autopilot 飞书送达仍走 bot 身份。**自愈路径已稳定确认不可靠**，必须用户拍板手动重授权。
- 【活跃】SUP-92 飞书送达 + chat_id 自愈成功 + status flip 自驱完成（连续 2 天自驱）。

### OpenClaw / 代码项目 / GitHub

- 【无活动】今日无任何项目推进。

### Multica / 自驱工作流

- 【成功】**SUP-92 (8-19 早报) autopilot 全自驱闭环**：14 builder / 30 X / 1 podcast No Priors（Sarah Guo × Elad Gil 聊万亿公司节奏、AMC、TAM 想象力、加州亿万富翁税、AI 安全）。要点：① YC Garry Tan 开源 "Personal AGI" 模板（70 个 skills + 个人知识库，MIT 协议），② Nikunj Kothari："AI 行业没人有护城河，只剩 VC 自己"，③ Anthropic Claude Code 新增 `/design` 命令，④ Replit Amjad Masad 看好"pitch 里完全不出现 AI"的 AI-native 团队，⑤ Vercel Guillermo Rauch：Cursor Origin + Vercel 部署打通。
- 【**关键失败·连续 2 周**】**EDN-Weekly-Market-Insight cron (`1d0cf41439ec`) 8-19 8:30 触发但 weekly digest 0 产出**：3 subagent 派发后，subagent 仅产生 3/5/1 条 msg 早早 die，母 agent 停在 "Waiting for results" 后 0 tool calls；无 `/tmp/edn-2026-08-19/` 数据资产、无飞书长住文档追加、无 origin 摘要推送、`last_status='ok'` 是 cron 监控层误判。**根因变种**：与 8-29 EDN 双层失败模式不同——本次是 **subagent 同步返回后立刻 die**，无 600s 超时、无 brotli decoder 异常；更像 subagent 在 `delegate_task` 模式返回的 stub session 后**没有真正消费数据**就开始说"成功"。**本周继 8-12 后已连续 2 周 EDN 跑空**——EDN 用户工作流硬性损害。**cron 处置**：本次因 0 实质产出 + 无飞书送达证据，**不能 cron 代 flip**（无 issue 可翻）；下次 8-26 cron 必须先看 dispatcher 是不是同一子任务。
- 【持续】**SUP-88 (8-15 早报) Mode E 第 5 天未自愈**：8-15 早报完全未投到飞书，今日 0 落地动作；累计已 5 天，cron 不能代 flip（无 om_x 飞书证据）。
- 【持续】**SUP-79 (8-06 早报) chat_id 错配 `blocked` 第 14 天**：autopilot 跑完 digest 但飞书投递因 `OC_d5de551525d3a4ae9e1a52815bb9a92b` 大写失败，digest 仍贴在 comment 待用户拍板；下游已 4 次累计 autopilot 跳过此坑（SUP-80 / 83 / 85 / 92 均显示"已用小写发送"自愈）——**chat_id 自愈率 = 100% 但对 8-06 历史 issue 已无影响**。

### 调研 / 内容处理

- 【完成】SUP-92 autopilot 飞书投递 + 中文汇总完成（14 builder / 30 推文 / 1 播客）。
- 【失败·连续 2 周】EDN 本周 weekly digest **0 产出**：EDN 长期缺失触发工作流硬性损害（连续 2 周 = 错过 2 次完整趋势分析）。

## 4. 重要交付物 / 系统变更

- Multica issue｜**SUP-92**｜**已完成**：autopilot 全自驱闭环（14 builder / 30 X / 1 podcast / 飞书送达 `om_x100b677b675600a8b2c07924621955e` + 自翻 `done` 91 秒）。
- cron 失败｜**EDN-Weekly-Market-Insight (`1d0cf41439ec`) 本周周报**｜**未完成**：weekly digest 0 产出；与 8-12 失败形成**连续 2 周 EDN 跑空**。`last_status='ok'` 是 cron 监控层误判。**下次 8-26 cron 必须先看 dispatcher 是不是同一子任务**。
- Multica issue｜**SUP-88**｜**未闭环（连续第 5 天，Mode E）**：autopilot 启动但仅写中文 comment、未拉 builder 数据、未投递飞书；cron 不能代 flip（无 om_x）。
- Multica issue｜**SUP-79**｜**未闭环（连续第 14 天，chat_id `blocked`）**：autopilot 跑完 digest 但飞书投递因 `OC_` 大写失败，digest 已贴在 comment。

## 5. 风险、阻塞、需要拍板

- **【🔴 紧急】EDN 周报连续 2 周失败**：本周 weekly digest 0 产出；下周 8-26 触发前**必须用户拍板**：是 GC Day 8-22 落地 EDN cron prompt 强制 completion-token + subagent hard-assertion patch，还是放弃本周后下次重试。连续 2 周缺失已是用户工作流硬性损害。
- **【🔴 紧急·第 5 天】state.db 507 MiB 持续踩 500 MiB 红线**：+4 MiB/天 trend 已稳定 5 天，连续 12 天呼吁 VACUUM 窗口未拍板。**需要用户拍板**：是 GC Day 8-22 落地 cron prompt "SIZE > 490 → 自动低峰 VACUUM"，还是手动等到本周低峰停机跑 VACUUM。cron 自跑会锁库必须等。
- **【🔴 紧急·第 4 天】lark-cli user identity `needs_refresh`**：autopilot 飞书送达继续走 bot 身份（chat_id 自愈 + status flip 自驱均不触发 user API refresh）。**需要用户拍板**：手动 `lark-cli auth login --refresh` 重授权（连续 4 天证伪自动自愈路径）。
- 【持续·等待用户拍板】SUP-88 (8-15) Mode E 第 5 天：是否手动转贴飞书 vs 跳过本期。
- 【持续·等待用户拍板】SUP-79 (8-06) chat_id blocked 第 14 天：人工补投目标 chat_id。

## 6. Hermes 自我改进

- **EDN 周报 cron prompt 必须加 completion-token 硬断言**（GC Day 8-22 落地）：本周已连续 2 周跑空，根因是 3 subagent 同步返回的 stub session 后**没有强制写入 `data_asset.json` 才算完成**——必须在 cron prompt 的 Step 5 加：(a) subagent 完成时返回 completion token；(b) 母 agent 在 polling-loop 里检查 token + 数据资产 JSON 落地，token 缺失则视为 subagent die 而非"已成功"；(c) 同时加 `disown delegate_task 模式必须先检查 stub session 的 message_count >= 50 才返回 success`，避免 3 条 stub msg 假装返回成功。**反模式**：把 8-19 失败归为"subagent 偶发"——连续 2 周样本已经构成稳定信号。
- **follow-builders skill 双轨 patch 紧迫性维持**（不让 SUP-91 / SUP-92 连续 2 天全自驱误导）：chat_id 大小写兜底（自愈率 100% 已稳定可写硬规则）+ status flip 强制收尾段（自愈率仍 47% 不可靠）必须分别写入；**不能合并为一次 patch**，也不能因连续 2 天自驱样本（SUP-91 / SUP-92）就降级紧迫性——这是 2 次单样本而非能力级证明。
- **`/root/.hermes/state/state_db_size.log` 连续无缺口维护**：今日追加 8-19 507MB，log 已连续 19 天无缺口；cron 启动时若发现 log 最后一行不是昨天日期 → 主动从昨日日报反查补一行（不要跳）。

## 7. 明日建议

- 【可自动做】明天 8-22 GC Day cron 自动跑通前，cron 可预生成 EDN cron 失败根因诊断 + follow-builders skill 双轨 patch diff + state.db VACUUM 拍板方案，作为 GC Day 输入材料（无需用户预先拍板）。
- 【需要你确认】**EDN cron 跑空根因诊断 + completion-token patch 是否本周落地**：本周 8-26 EDN 触发前必须拍板，否则下周连续 3 周 EDN 跑空的破坏性将远超 EDN 失败本身。
- 【需要你确认】**state.db VACUUM 窗口**：连续 12 天未拍板；建议 X 日凌晨 02:00-04:00（系统低峰）+ cron 提前 1h 通知，让所有 cron 暂停 30 分钟。
- 【需要你确认】**`lark-cli auth login --refresh` 重授权**：连续 4 天证伪自动自愈路径，必须手动重授权才能让 SUP-5x 元模式后续飞书送达走 user 身份（避免 bot 身份继续污染）。
- 【可自动做】明天 8-20 / 8-21 / 8-22 daily-report cron 继续观察 SUP-88 / SUP-79 是否自愈；若连续 7 天未自愈则升级 §5 措辞为"已 ghost"。
