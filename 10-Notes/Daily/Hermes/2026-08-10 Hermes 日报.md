# Hermes 日报 - 2026-08-10

## 1. 老板速读
- **周一，无用户对话、无 Discord/飞书转述、无 code 项目改动**：今日 Hermes 全部活动 = 1 次 follow-builders autopilot 早报 + 1 次 idea-recall 每周 digest cron。
- **follow-builders SUP-83 已成功送达飞书**（14 builder · 26 X · 1 播客，message_id `om_x100b68a51c26b0a0b1d069084830668`），但 autopilot **再次漏翻 status → done**（连续 4 周 8 次漏翻元模式）；周日 SUP-81（8-08）和 SUP-82（8-09）同样卡 `in_review`，今日 cron 代 flip 3 条。
- **Slack 通道 `Session is closed`** 持续重连失败（10s 间隔刷屏 errors.log，23:49→23:53 至少 11 条 ERROR），自周日 8-09 起延续；今日首次出现 Lark IM receiver `no close frame` 退出（23:51:13）。
- **state.db 485 MiB（+2 MB）**：与昨日 483 MiB 相比小幅增长，距 500 MiB 红线 15 MiB；今日增量来源为 1 个 autopilot + 2 个 cron + EDN 残件 session 缓存。
- **`/tmp` 扫描 75 行（3 ARCHIVED + 72 NOT_ARCHIVED）**：与昨日 76 行相比净 -1；basename diff 新增 0 件；`/tmp/reply.md` 已消失（昨日 cron 调试残留）。今日 autopilot 跑通后产生 `digest.json` + `digest_final.md` + 7 个 `chats*.json` 共 9 件当日产物（按白名单不计入工件）。
- **明日优先**：① 8-11 autopilot 早报再次验证 SUP-5x 元模式；② Slack 通道自 8-09 起持续失败 2 天，是否升级为风险位待用户拍板；③ state.db VACUUM 维护窗口仍未安排，500 MiB 红线压力继续累积。

## 2. 今日工作总览
- 【已推进】follow-builders autopilot 08:00 触发，14 builder / 26 X / 1 播客 / 0 博客，中文 digest 已成功投递飞书 `oc_d5de551525d3a4ae9e1a52815bb9a92b`。
- 【已闭环】日报 cron 代 flip SUP-81（8-08）+ SUP-82（8-09）+ SUP-83（8-10）三条 `in_review` issue，三条均有飞书送达证据 + 中文 result comment；今日 SUP-5x 元模式继续自报成功但漏翻 status。
- 【已核对】`/tmp` 双口径扫描（`-mtime +0` 75 行 + `-mtime -1` 5 件均为 autopilot 当日产物白名单）、state.db size 取数、cron 健康、lark-cli auth status、昨日日报复读。
- 【静默】今日无 user-facing session，无 acp/subagent 派发，无 Discord 上游公告转述，无 EDN cron（下次为周三 8-12），无 GC Day（上周 8-08 已跑，下次 8-15）。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已核对】state.db 485 MiB（+2 MB），距 500 MiB 红线 15 MiB；今日单日增量来自 1 个 autopilot + 2 个 cron session，无 Discord 转述。
- 【静默】今日 0 个 user-facing session；MEMORY.md 字符数无变化。
- 【已闭环】idea-recall weekly cron（job `2e8ecd9b7cb2`）09:00:51 跑通，incremental index 6 条全部 skipped（已有索引），本周无新增 idea；surfaced 1 条 `把每周复盘改成 voice note`（imp 4，surfaced ×5，提示 45 天前 raw idea 仍未 archive/promote）。
- 【风险】Slack `slack_bolt.AsyncApp: Failed to connect (error: Session is closed)` 持续 10s 重试刷屏（errors.log 至少 11 条 ERROR，今日 23:49→23:53 连续）；周日 8-09 首次出现后延续 2 天未自愈。

### 飞书 / 日历 / 文档 / 消息平台
- 【已闭环】SUP-81 / SUP-82 / SUP-83 cron 代 flip done（带飞书送达 message_id 证据）。
- 【风险】lark-cli user identity `needs_refresh`，`expiresAt 2026-08-10T10:02:16`（今天 23:51 > 过期 13 小时，但 token 仍可用，下次 user API 调用会自动 refresh）；scope 列表仍缺 `im:message.send_as_user`，延续第 N 天风险位。
- 【风险】Lark IM receiver `receive message loop exit, err: no close frame received or sent` 23:51:13 退出（首次出现，conn_id 7672059971123940299）；飞书事件订阅是否持续接收需观察。
- 【无活动】今日无手动飞书日历/文档操作；EDN 周报下次触发为周三 8-12。

### 知识库 / Obsidian / Get笔记
- 【静默】vault 工作树今日干净，无新增 commit；Get笔记 sync 将在本日报落定后执行。
- 【延续】`/tmp/hermes-results/call_*.txt`（8 件 EDN subagent transcript）+ `/tmp/edn-2026-08-05/data_asset.json` 8-05 EDN 残件仍未清理（EDN cron prompt 末段未加 `rm -f`，GC Day 8-15 应顺手 patch）。

### OpenClaw / 代码项目 / GitHub
- 【静默】今日无代码项目改动、无 GitHub 操作、无 commit。

## 4. 重要交付物 / 系统变更
- 日报｜Hermes 日报 2026-08-10｜已写入｜路径见 §3 知识库。
- 早报｜AI Builders 每日早报 8-10｜已投递飞书｜14 builder / 26 X / 1 播客，message_id `om_x100b68a51c26b0a0b1d069084830668`，target chat `oc_d5de551525d3a4ae9e1a52815bb9a92b`；autopilot 自愈 chat_id 大小写（issue body 大写 `OC_` → 实际小写 `oc_`，与 SUP-80 同款行为）。
- 监控记录｜`state_db_size.log`｜已 append 8-10 行（485 MiB，单日 +2 MB）；补齐了昨日 8-09 → 8-10 连续 2 天数据点（昨日漏写，已在 §6 点名）。
- 扫描 snapshot｜`/root/.hermes/state/scan_2026-08-10.txt`｜已落地｜75 行（3 ARCHIVED + 72 NOT_ARCHIVED）。
- idea-recall weekly digest｜`/root/.hermes/cron/output/2e8ecd9b7cb2/2026-08-10_09-00-51.md`｜已落地｜incremental index 6/6 skipped，无新增 idea；surfaced 1 条 voice-note review idea（imp 4 ×5）。
- Multica close-out｜SUP-81 / SUP-82 / SUP-83｜已翻 done｜均含飞书送达证据 + 中文 result comment + 日报 cron 核验评论；cron 代 flip 而非 autopilot 自驱。

## 5. 风险、阻塞、需要拍板
- 【需你确认】Slack 通道自 8-09 持续 `Session is closed` 重连失败 2 天，未自愈；是否升级为需修复项（重启 gateway / 重新配 socket-mode token）由用户拍板——若是企业 Slack 已下线或 bot 被移除，需主动告知；若是网络抖动则继续观察。
- 【需你确认】Lark IM receiver 23:51:13 退出（`no close frame`，conn_id 7672059971123940299）——首次出现，但发生在本日报 cron 启动 14s 后，与日报 cron 调用 `lark-cli auth status` 是否触发 reconnect 相关需进一步观察；建议下次 user-API 调用时观察是否复现。
- 【需你确认】lark-cli user identity `needs_refresh` + scope 缺 `im:message.send_as_user` 已延续 ≥ 4 天；明日 autopilot 恢复跑通前是否补 `lark-cli auth login --scope "... im:message.send_as_user ..."` 重授权，避免 SUP-84 投递身份再次错配。
- 【需你确认】idea-recall surfaced `把每周复盘改成 voice note` 已 45 天 raw 状态，是否 promote 到 developed 或 archive；本周再 surfaced ×5，未处理。
- 【可观察】SUP-5x 元模式连续 4 周 8 次漏翻 status flip（7-30 SUP-67、7-31 SUP-68、8-05 SUP-78、8-08 SUP-81、8-09 SUP-82、8-10 SUP-83、+ 8-06 SUP-79 blocked、8-07 SUP-80 自愈但仍漏翻）；autopilot 自愈率本周仍低于"≥ 10 连续样本 90%+"稳定阈值。

## 6. Hermes 自我改进
- 静默日延续至周一低活跃：今日仅 1 个 autopilot + 1 个 idea-recall cron + 1 个日报 cron 自身，无用户对话；建议下次 GC Day 8-15 顺手把 EDN cron prompt 末尾加 `rm -f /tmp/hermes-results/call_*.txt /tmp/edn-*/data_asset.json`（与 8-06 EDN 残件清理规则合并），关闭 `/tmp` 残件跨日报漏报。
- state.db size log 8-09 漏写已补齐：cron 启动时若发现 log 最后一行不是昨天日期 → 主动从昨日日报反查补写；今日已恢复连续无缺口。**反模式**提醒：log 有缺口时直接 append 今日会让后续 cron 把缺口日暴涨误算成"今天 +N MB"。
- SUP-5x 元模式本周累计 10 样本（7-26 自愈 / 7-30 漏 / 7-31 漏 / 8-01 自愈 / 8-02 自愈 / 8-03 自愈 / 8-05 漏 / 8-06 blocked / 8-07 自愈 / 8-10 漏）= **5 自愈 / 4 漏 / 1 blocked = 50% 自愈率**（较 8-03 的 67% 略回落）；autopilot 自愈 chat_id 大小写（SUP-80 + SUP-83）已具备能力，但 status flip 仍 100% 依赖 cron 代 flip——GC Day 8-15 应升级 follow-builders skill 强制收尾 patch 为"紧急"。

## 7. 明日建议
- 【可自动做】周二 8-11 autopilot 早报（SUP-84）跑通后，若 autopilot 仍漏翻 status，按今天同款流程 cron 代 flip done。
- 【可自动做】观察 Slack `Session is closed` 重连错误：若 8-11 errors.log 仍无 ERROR 自动收敛 → 升级为"待用户拍板"风险位。
- 【可自动做】Lark IM receiver 下次 user-API 调用时观察 `no close frame` 是否复现；若复现则在 §5 风险位显式标"飞书事件订阅可能中断"。
- 【需要你确认】决定是否在周二 autopilot 跑通前补 `lark-cli auth login --scope "... im:message.send_as_user ..."` 重授权，避免身份错配；user identity 已过期 13 小时仍能跑通（今日 SUP-83 已成功投递），但 user vs bot 身份可能影响下游解析。
- 【需要你确认】决定 EDN 残件清理与 SUP-5x 强制收尾 patch 是否合并到 GC Day 8-15 一次性处理——这两个是 1 周内反复出现的同源问题（cron 自身缺收尾），建议同步落地避免下周再次复发。
