# Hermes 日报 - 2026-09-12

## 1. 老板速读

- **autopilot 静默 9 天后今日复活！** — SUP-108 (9-12 早报) autopilot 00:03 跑通 18 builders / 35 tweets / 1 podcast，飞书 bot 实际送达 `om_x100b657d258d8ca0b2610ce103ef58c`（8806 bytes），但漏翻 `done` → cron 23:53 已代 flip done。中断 9 天（9-04 ~ 9-11）后 autopilot 链路恢复。
- **GC Day #42 (9-12 周六 09:00) 已落地，无 skill patch** — MEMORY/USER 字符占比 58%/38%（绿区），本次只审计未改动；§-anchor 6/6 OK；state.db 585 MiB 超红线 + FTS trigram_data bloat 2.24× 再次被点出。
- **lark-cli refresh 标配连续 20 天闭环** — 今日 cron 启动时 user `needs_refresh` → `calendar +agenda` → `ready`，expiresAt 推到 9-13 01:51。
- **state.db 586 MiB 红线第 30 天** — 单日 +1 MiB（稳态），连续 30 天呼吁 VACUUM 未拍板。
- **Idea Digest 99992402 投递失败连续第 10 周** — cron `last_status='ok'` 但 `last_delivery_error` 持续失败，下次触发 9-14 周一 09:00。
- **EDN cron 今天未触发** — 上次 9-09 跑通，下次 9-16 周三 08:30（连续 1 周窗口）。

## 2. 今日工作总览

- **autopilot 静默解除 + SUP-108 跑通** — 00:03 autopilot 完整跑完 follow-builders skill 流程，飞书 bot 身份实际送达，中断 9 天后首次回归。autopilot 仍漏翻 status，cron 按快速路径加核验评论 + `multica issue status SUP-108 done`。
- **lark-cli refresh 标配自动闭环** — 20 天连续，`calendar +agenda` 触发后 expiresAt 推到次日 01:51。
- **GC Day #42 健康检查** — 周六 09:00 跑通，MEMORY/USER 字符检查全绿，cron jobs 状态 4 个全 ok（但 Idea Digest 有 delivery_error），gateway 运行中但日志 WARNING 持续。
- **`/tmp` 工件真零漂移稳态第 8 天** — 今日 79 行 vs 昨日 82 行（-3.7%，稳态），3 件昨日 cron 内部 scratch（`builders.json` / `today_not.txt` / `yday_not.txt`）今日消失（容器/Hermes 自动清理），无新增用户可读工件。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已闭环·重大】autopilot 静默 9 天后今日恢复 — SUP-108 (9-12) autopilot 00:00:18 创建 → 00:03:49 中文 result comment + 飞书 bot 送达 `om_x100b657d258d8ca0b2610ce103ef58c`（8806 bytes / 5119 chars / 18 builders / 35 tweets / 1 podcast / 0 blogs）。autopilot 内部 `OC_` → `oc_` 大小写自愈正确；执行判断合理（跳过 6 位 builder 纯 meme / 跳播客视频不附假链接 / 显式说明零博客）。**但 autopilot 仍漏翻 status** → 23:53 cron 按快速路径补核验评论 + `multica issue status SUP-108 done` + `issue get` 验证，全程 ~3 秒。
- 【已闭环】lark-cli user identity refresh 标配连续 20 天自动闭环（9-23 起累计，今日 +1）。
- 【异常·已确认】Idea Digest cron (`2e8ecd9b7cb2`) 投递失败连续第 10 周——`last_status='ok'` 但 `last_delivery_error='live adapter send failed: [99992402] field validation failed; ...'`。下次触发 9-14 周一 09:00。

### 知识库 / Obsidian / Get笔记
- 【正常】日报 cron 23:50 启动，本篇走双写流程，推送到 Get笔记 MyClaw。
- 【真零漂移稳态第 8 天】`/tmp` 工件扫描：今日 79 行 vs 昨日 82 行（-3.7%，稳态范围 ±10% 内）。3 件昨日 cron 内部 scratch（`builders.json` / `today_not.txt` / `yday_not.txt`）今日消失（疑似容器层或 cron 自动清理），无新增用户可读工件。今日 mtime 0-day 龄工件 0 件，全部为历史残留（EDN 残件 15 件 + 8-15 oc-evolution 子树 26 件 + autopilot digest 副本 6 件 + 其他历史 32 件）。

### 飞书 / 日历 / 文档 / 消息平台
- 【正常】lark-cli user identity 已 refresh（user status ready，expiresAt → 9-13 01:51，连续 20 天闭环）。
- 【已闭环】今日 SUP-108 飞书 bot 送达 `om_x100b657d258d8ca0b2610ce103ef58c`（8806 bytes / 5119 chars，autopilot 跑通后回读验证完整落地）。
- 【观察】EDN 周报飞书长住文档今日未变更（按周节奏）；EDN cron 下次触发 9-16 周三 08:30。
- 【异常】Idea Digest 飞书投递 99992402 失败连续第 10 周；workaround 待拍板。

### 调研 / 内容处理 / EDN / GC Day
- 【GC Day #42 已落地】周六 09:00 跑通——MEMORY 1275/2200 (58%) + USER 523/1375 (38%) 全绿；§-anchor 6/6 OK；0 skill patch / 0 memory 段落迁移。健康检查：state.db 585 MiB 🔴 超红线 + FTS trigram_data bloat 2.24×（建议 vacuum + FTS rebuild，需停 gateway 窗口）；state-snapshots 只 1 个 396MB 无 rolling retention 🔴；checkpoints 110M（legacy-20260607 55M 待归档）；gateway 3 类 WARNING 持续。破坏性操作全部未执行，等用户拍板。
- 【静默日】0 条新调研动作。

## 4. 重要交付物 / 系统变更

- 报告 | 2026-09-12 Hermes 日报.md | 即将写 vault + 双写 Get笔记 MyClaw
- 状态 | SUP-108 (9-12 早报) | ✅ done（autopilot 00:03 跑通飞书送达 + cron 23:53 代 flip），autopilot 静默 9 天后恢复
- 状态 | GC Day #42 (9-12 周六 09:00) | ✅ 跑通，0 skill patch，5 项清理待用户拍板
- 状态 | state.db 586 MiB（+1 MiB 单日）| 500 MiB 红线 +86 MiB，VACUUM 未拍板第 30 天
- 状态 | lark-cli user identity | 已主动 refresh，expiresAt → 9-13 01:51（连续 20 天闭环）
- 状态 | Idea Digest cron `last_delivery_error` | 99992402 连续第 10 周，workaround 待拍板
- 状态 | EDN cron 下次触发 | 9-16 周三 08:30（连续 1 周窗口）
- 状态 | `/tmp` 旧工件扫描 | 79 行真零漂移稳态第 8 天，basename 集合几乎不变（-3.7%）

## 5. 风险、阻塞、需要拍板

- **[观察·autopilot 已恢复] SUP-108 (9-12) 跑通但漏翻 done** — autopilot 内部 `OC_` 大小写自愈正确（chat_id `oc_xxx`）、飞书 bot 实际送达、comment 完整齐备；仅缺最后一步 `multica issue status SUP-108 done`。今日 cron 按快速路径代 flip ~3 秒闭环。**之前担心的 autopilot 静默根因（acp 触发器 / 上游 feed / API 异常）似乎自愈**——但根因未明确定位，下次 autopilot 触发（9-13 早报）能否稳定自驱仍是观察项。
- **[🔴 紧急] state.db 586 MiB VACUUM 未拍板**：已 30 天呼吁，连续 13 天踩红线后稳态增长（+1-3 MiB/天）。建议尽快给凌晨窗口拍板（cron 不可自动 VACUUM 锁库）；GC Day #42 已点出"停 gateway → FTS rebuild + VACUUM 目标 < 350 MB"为本周最影响磁盘项。
- **[🔴 紧急] Idea Digest 99992402 连续 10 周失败**：cron `last_status='ok'` 但 `last_delivery_error` 持续失败。下次触发 9-14 周一 09:00；workaround（改 `lark-cli im +send` 绕开 gateway live-adapter）已挂 6 周未拍板；GC Day #42 应明确拍板路径。
- **[观察] GC Day #42 五项清理待用户拍板**：① FTS rebuild + VACUUM（需停 gateway）；② checkpoints `legacy-20260607-001849/` 55M 归档；③ state-snapshots rolling retention cron 加；④ Idea Digest 投递通道切换；⑤ Discord routing key 7636cdf9 stale session 根因排查。0 项已在 cron 自动执行。

## 6. Hermes 自我改进

- **autopilot 静默 9 天后自愈，cron 代 flip 流程持续保留必要**：今日 SUP-108 autopilot 完整跑通后仍漏翻 status，cron 23:53 按快速路径 ~3 秒闭环——SUP-5x 元模式"autopilot 漏翻 done"持续成立（chat_id 自愈 100% 但 status flip 仍依赖 cron 兜底）。GC Day #42 (9-12) 落地窗口内仍需把 follow-builders skill status flip 强制收尾 patch 落地，autopilot 自驱仅在 9-12 单样本验证 1 次，**单样本不构成放宽 patch 紧迫性**。
- **GC Day #42 0 改动落地 + 五项清理议题已挂 1 天**：本次 GC Day 跑通但 0 skill patch / 0 memory 迁移（MEMORY/USER 全 lean）；五项清理（FTS rebuild / checkpoints 归档 / state-snapshots retention / Idea Digest 投递 / Discord routing）全部在 GC Day 报告中显式列但 0 项已被用户拍板执行——下次 GC Day 报告（9-19）前若无任何执行，会变成下一个"5 议题连续 N 周挂起"模式。
- **`/tmp` 真零漂移稳态判定已稳定第 8 天**：连续 8 天 ±10% 范围内，basename 集合今日净 -3 件（昨日 cron 内部 scratch 消失，疑似容器层清理），按 SKILL.md §0-pre 零漂移稳态判定可继续折叠成一行；今日 0 件新增用户可读工件，EDN 残件 15 件跨 3+ 天龄继续堆在 `/tmp` 但不影响判定。

## 7. 明日建议

- 【可自动做】lark-cli refresh 标配自动闭环（cron 自跑，连续 21 天起算）。
- 【可自动做】`/tmp` 工件扫描 + state.db size log 续写（cron 自跑）。
- 【可自动做】autopilot 9-13 早报触发后 cron 按快速路径判代 flip 必要（chat_id 自愈 100% / status flip 仍是元模式漏翻点）。
- 【可自动做】EDN cron 9-16 周三 08:30 下次触发按今日三件验证（data_asset / 飞书长住文档 / origin）判定健康。
- 【需你确认】GC Day #42 五项清理议题拍板（建议优先级：① FTS rebuild + VACUUM 窗口期 → ② state-snapshots rolling retention → ③ checkpoints legacy 归档 → ④ Idea Digest 投递通道切换 → ⑤ Discord routing key 排查）；全部挂 1 天未拍板，FTS rebuild 是当前最影响磁盘项。
- 【需你确认】autopilot 9-12 自愈根因验证——是否需要在 9-13 早报跑通后人工查 acp 触发器日志（SUP-108 完整跑通说明 autopilot 链路本身已恢复，但 9-04 ~ 9-11 中断根因仍不明确）。