# Hermes 日报 - 2026-08-07

## 1. 老板速读
- **SUP-80（8-07 早报）今日自愈 + cron 收尾**：autopilot 跑完 AI Builders 早报（12 builder / 15 tweet / 1 播客）后**自己纠正了任务描述里大写 `OC_xxx` 为小写 `oc_xxx`** 并飞书实际送达（om_x100b68664450e4a0b49e0b8e094535b，2026-08-07 08:02:50）——但仍漏翻 status。本次日报 cron 已代 flip done。SUP-5x 元模式本周第 9 样本 = 6 自愈 / 2 漏 / 1 blocked = 67% 投递成功率。
- **今天是你亲自在飞书开工**：17:24 飞书一条"hello"轻问，agent 答"嗨，Tim，准备好开工了"。这是过去 5 天里**唯一一条用户主动发起**的消息。
- **`/tmp` 实扫 76 行（3 ARCHIVED + 73 NOT_ARCHIVED）**：相比昨日 75 行净 +1 行（basename diff = `digest-raw.json`，mtime 8-06 08:00——昨日 SUP-79 autopilot 输出 1-day 龄才被今日 `-mtime +0` 抓到，**典型昨日漏报今日补捞**，不是真新增）。
- **`state.db` 479 MiB**（昨日 476 MiB，+3 MiB，已过 475 MiB 警戒线，距 500 MiB 红线 21 MiB）；增量来源主要是今日 autopilot 跑完 SUP-80 飞书实际送达产生的 `messages.platform_message_id` 与 tool_calls JSON 列表。
- **SUP-79（8-06 早报）仍 blocked**：飞书投递因大写 `OC_` 失败未补发；明日 SUP-81 autopilot 启动时若仍未纠正，可能连续第 4 次撞同坑。
- **明日重点**：8-08 周六 GC Day #21 自动体检 + 拍板 SUP-79 chat_id + 处理 SUP-57 abandoned + state.db 维保窗口。

## 2. 今日工作总览
- 【已闭环】SUP-80 代翻 done，附核验评论（含 message_id、chat_id 自动纠正说明、SUP-5x 元模式第 9 样本统计）。
- 【已服务】17:24 飞书轻问（"hello"），agent 答"准备开工"，零工具调用、零下游动作。
- 【已转述】02:49 Discord 上游公告（hermes-agent 语音加速 + Polymarket 技能从内置移到可选），按惯例只做中文化转发到 Discord 频道，无 vault 写入。
- 【已核对】`/tmp` basename diff、state.db 大小（log 写入 `state/state_db_size.log`）、lark-cli auth status（user identity needs_refresh，下次 user API 自动 refresh）。

## 3. 按项目 / 责任域汇报

### Multica / AI Builders
- 【已闭环】**SUP-80 (8-07 早报)**：本次日报 cron 代翻 done。autopilot **首次自愈 chat_id 大小写坑**——任务描述里的 `OC_d5de551525d3a4ae9e1a52815bb9a92b` 自动纠正为小写 `oc_xxx` 后飞书实际送达（om_x100b68664450e4a0b49e0b8e094535b，bot 身份，2026-08-07 08:02:50）；中文 builder 覆盖 12 位（Swyx、Madhu Guru、Guillermo Rauch、Aaron Levie 等），15 条 tweet + 1 播客（Sara Tavel / Benchmark）。**关键修复证据**：comment `151d4b9d-...` 显式说明"任务描述里写的 `OC_xxx` 是大写形式,实际 open_chat_id 小写开头,已用小写发送"——本会话第 3 次 chat_id 大小写坑的首次自愈变体（与 SUP-76 8-04 同款机制，与 SUP-79 8-06 投递失败形成对照）。
- 【遗留】**SUP-79 (8-06 早报)**：仍 blocked，digest 内容贴评论区等你确认目标小写 chat_id。今日 SUP-80 已自愈同坑，可参考 SUP-80 实际投递的小写 `oc_d5de551525d3a4ae9e1a52815bb9a92b` 补发 SUP-79。
- 【元模式累计】**SUP-5x 本周 9 样本 = 6 自愈 / 2 漏 / 1 blocked = 67% 投递成功率**：7-26 SUP-63 自愈 / 7-30 SUP-67 漏（cron 代 flip）/ 7-31 SUP-68 漏（cron 代 flip）/ 8-01 SUP-69 自愈 / 8-02 SUP-70 自愈 / 8-03 SUP-71 自愈 / 8-05 SUP-78 漏（cron 代 flip）/ 8-06 SUP-79 blocked / **8-07 SUP-80 自愈（今日代 flip）**。仍低于 ≥ 10 样本 90%+ 自愈率稳定阈值。

### 飞书 / Discord / 消息平台
- 【轻问已答】17:24 飞书用户 hello，agent 答"嗨，Tim，准备开工了"，零下游动作。
- 【转述完成】02:49 Discord 上游公告（hermes-agent 语音加速 `hermes update` + Polymarket 技能从内置移到可选，PR #80515）已中文化转贴到 Discord 频道，无 vault 写入。

### EDN / 需求洞察
- 【未触发】今日无 EDN cron 触发（8-07 周五不触发，下一轮 8-12 周三）；8-05 周报已成功落盘，今日无新动作。

### Hermes / Agent 系统
- 【已核对】`lark-cli auth status`：user identity `needs_refresh`（expiresAt 2026-08-06 10:01，下次 user API 调用自动 refresh）；scope 列表齐备，`im:message.send_as_user` 仍缺失（与昨日一致，今日 SUP-80 autopilot 用 bot 身份投递成功）。
- 【已监控】`state.db` 大小 479 MiB（+3 MiB 今日，已过 475 MiB 警戒线，距 500 MiB 红线 21 MiB）。SIZE BY source 全时口径仍以 discord 311 / cron 137 / acp 63 / subagent 27 为主，今日新增 SUP-80 autopilot session 3 个。
- 【已记录】state.db 大小趋势写入 `/root/.hermes/state/state_db_size.log`（4 天数据：8-04 463MB / 8-05 475MB / 8-06 476MB / 8-07 479MB）。

## 4. 重要交付物 / 系统变更
- 早报｜AI Builders Digest 2026-08-07｜已代翻 done｜SUP-80（om_x100b68664450e4a0b49e0b8e094535b，autopilot 自愈 chat_id 大小写）
- 转述｜hermes-agent 语音加速 + Polymarket 技能移到可选｜已中文化转贴 Discord｜`20260807_024857_d1c49f00`
- 轻问答｜飞书 hello → 开工准备｜已答｜`20260807_172415_be3220a0`
- 自愈率追踪｜SUP-5x 本周 9 样本 = 6/2/1 = 67%｜新增 SUP-80 自愈 + SUP-79 blocked 入样本
- `state.db` 监控｜479 MiB｜+3 MiB 今日，已过 475 MiB 警戒线；写入 `state/state_db_size.log`

## 5. 风险、阻塞、需要拍板
- 【需要你确认】SUP-79 chat_id 修复：参考 SUP-80 已自愈的小写 `oc_d5de551525d3a4ae9e1a52815bb9a92b` 补发 8-06 早报 digest 到同一群。
- 【需要你确认】`state.db` 480 MiB 黄区前是否安排低峰 VACUUM 窗口（已过 475 MiB 警戒线，距 500 MiB 红线 21 MiB）。
- 【坑积累】飞书 chat_id 大小写规范：今日 SUP-80 autopilot **首次自愈**（与大写硬编码模板冲突的自动纠正），是 8-04 SUP-76 同款机制；但 SUP-79 仍因未自愈停在 blocked。GC Day 8-08 应把 follow-builders skill 的 issue description 默认 chat_id 改为小写 `oc_xxx`，彻底关闭根因。
- 【待观察】SUP-5x 元模式：今日 autopilot 自愈 chat_id + 自跑投递成功，但**仍漏翻 status**——意味着 autopilot 收尾不全比 chat_id 大小写坑更稳定。GC Day 应优先 patch "deliver → comment → flip status → comment done" 强制收尾段。
- 【连续 ≥ 4 天未闭环】SUP-57（2026-07-18 早报）自报飞书投递未完成，连续 4 天建议 abandoned 未动作；明日 cron 不再重复建议。

## 6. Hermes 自我改进
- **SUP-80 首次 autopilot 自愈 chat_id 大小写坑**：comment 显式说明"任务描述里写的 `OC_xxx` 是大写形式,实际 open_chat_id 小写开头,已用小写发送"——证明 autopilot 端可识别 + 自动纠正 + 成功投递。本会话 3 次撞同坑：7-30 SUP-67 漏（cron 代 flip）/ 8-04 SUP-76 自愈 / 8-06 SUP-79 blocked / **8-07 SUP-80 自愈**。建议 GC Day #21 把这条 autopilot 端自动纠正逻辑提炼成通用 patch（`OC_xxx.lower().replace('OC_','oc_')` 兜底），写进 follow-builders skill 提示词的开头几行。
- **`/tmp` 折叠判定再次验证**：今日 basename diff 净 +1 件（`digest-raw.json`，mtime 8-06 08:00）——又是 0-day 龄工件跨 24h 才被今日 `-mtime +0` 抓到。**6 周内第 3 次复现**此模式（7-28 / 8-06 / 8-07），SKILL.md §0-pre 强制 `-mtime -1` 对偶扫描已落地但每次仍依赖 cron 当天自觉跑——是否要写进 cron prompt 强制执行段，由下次 GC Day 决定。

## 7. 明日建议
- 【需要你确认】处理 SUP-79 chat_id：参考 SUP-80 自愈的小写 chat_id 直接补发 8-06 早报 digest；或标 SUP-79 abandoned 等 8-08 GC Day 处理。
- 【需要你确认】翻 SUP-57 为 `abandoned`（连续 4 天未动作）；如再不动，8-08 GC Day cron 代硬翻。
- 【需要你确认】`state.db` 480 MiB 黄区前是否安排低峰 VACUUM 窗口（已过 475 MiB 警戒线 4 MiB）。
- 【可自动做】8-08 周六 GC Day #21 自动体检（默认 cron 触发），重点 patch：(1) follow-builders skill issue description 默认 chat_id 改小写 `oc_xxx`；(2) SUP-5x 元模式强制收尾段（"deliver → comment → flip status"）；(3) cron prompt 强制 `-mtime -1` 对偶扫描段（避免再次依赖自觉）。
- 【可自动做】明天 cron 启动时再跑 `/tmp` basename diff + state.db size + lark-cli auth status 三连，验证 SUP-80 autopilot 产物是否被本日清理、`-mtime +0` 折叠能否进入第 4 天稳态。
