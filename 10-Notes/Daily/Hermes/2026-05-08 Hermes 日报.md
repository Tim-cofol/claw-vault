# Hermes 日报 - 2026-05-08

## 1. 老板速读
- 今天核心产出集中在 **Hermes / Heavy 多 Agent 工程协作体系**：确认并固化“VPS 只做控制/查询，Heavy 持有权威 Kanban 和执行面”的机制，VPS 本地 Kanban dispatcher 已确认关闭，Heavy 侧 timer active。
- **Heavy Worker Plugin 项目已推进到离线/干跑 MVP 交付状态**：Kanban 最终检查显示 `todo/ready/running/blocked = 0`、`done = 35`；结论是 `GO for offline/dry-run MVP handoff`，但 **NO-GO for live install**，不能直接装进生产 Hermes。
- 修复/优化了一批 Hermes 运行问题：两台机器升级到 Hermes Agent v0.13.0；修复 Heavy Hermes wrapper 自递归导致 Kanban `spawn_failed`；清空 Discord 旧 slash commands 并重新同步 41 个命令；配置快模型/标题生成/上下文压缩相关路由。
- 重要风险：Heavy Kanban worker 曾出现 `KeyboardInterrupt/SIGINT`、systemd oneshot kill mode、Codex 模型不支持 `google/gemini-3-flash-preview` 等问题；今天大多已定位并沉淀到 skill，但仍建议明天做一次端到端健康巡检。
- 需要你拍板：Heavy Worker Plugin 是否进入下一阶段 live 集成评审；如果要上线，需要先过安全检查、安装边界确认、回滚方案确认。
- 明日优先建议：先做 Heavy Worker Plugin 的交付包复核与部署决策，再清理/恢复 Obsidian→Get笔记同步脚本，因为当前日报只能用 OpenAPI fallback，同标题 recurring note 存在重复创建风险。
- 今天知识沉淀较多：多次更新 `hermes-agent`、`kanban-orchestrator` 等技能，固化 Heavy-local Kanban、dispatcher 恢复、worker crash、独立插件开发边界等经验。

## 2. 今日工作总览
今天处理了 6 类任务：Hermes 系统运维、Heavy/VPS 工程调度、Heavy Worker Plugin 项目推进、Discord/Gateway 配置、性能与模型路由优化、工程看板方法论调研。

- 已完成：Hermes v0.13.0 升级验证；Discord 翻译频道自动响应；slash commands 清理；VPS 本地 Kanban dispatcher 禁用确认；Heavy Worker Plugin 离线 MVP 任务清零；多项 skill 更新。
- 已推进：Heavy Worker Plugin 从方案、计划、Kanban 拆解推进到 35 个 done；工程看板最佳实践调研完成；Heavy/VPS 通信与代理环境方案持续固化。
- 阻塞或失败：Get笔记同步脚本缺失，日报同步需用 OpenAPI fallback；Heavy Worker Plugin 仍不能 live install；曾出现 worker SIGINT、Codex 模型不支持、Heavy wrapper spawn_failed 等故障。
- 建议：明天先做 Heavy Worker Plugin 上线门禁清单；恢复 `/root/obsidian-vault/scripts/getnote_sync_obsidian.py` 或等价幂等同步机制；对 Heavy dispatcher 做健康监控。

## 3. 按项目 / 责任域汇报

### A. Hermes / Agent 系统
- 状态：已完成 / 已推进
- 做了什么：
  - 将 VPS 与 Heavy 上的 Hermes 升级并验证为 `Hermes Agent v0.13.0 (2026.5.7)`。
  - 诊断 Hermes 响应慢：结论是 VPS 负载正常，主要慢点来自 Agent 单轮处理、模型路由和上下文负担。
  - 配置简单请求快模型路由；将标题生成改为 `openai / gpt-4o-mini`；将主力/辅助模型路由限制到 Codex 订阅内可用模型（主力 `openai-codex/gpt-5.5`，辅助模型按当时可用列表配置）。
  - 修复上下文压缩告警：`No auxiliary LLM provider configured`，改为使用订阅内可用 Codex 模型。
- 产出：
  - Hermes 升级验证结果。
  - 模型路由与辅助 LLM 配置变更。
  - 多条 troubleshooting 经验写入 skill。
- 证据：
  - Session：`20260508_073947_7c39f744`（升级 VPS 与 Heavy）。
  - Session：`20260508_082213_9dced2be`（响应慢诊断与优化）。
  - Session：`20260508_090952_29cf9b11`（Hermes/OpenClaw 效率对比与标题生成配置）。
  - Session：`20260508_135709_497c06bd`（auxiliary LLM / Codex 模型路由修复）。
- 风险 / 后续：
  - 模型配置变更涉及核心行为，建议明天用 3 类请求验证：简单问答、长上下文总结、工程执行。

### B. 知识库 / Obsidian / Get笔记
- 状态：已推进 / 有风险
- 做了什么：
  - 今天多次更新技能库，主要是 `hermes-agent` 与 `kanban-orchestrator`。
  - 本日报已按要求写入 Obsidian，并尝试同步到 Get笔记。
- 产出：
  - 新增/更新经验包括：Heavy-local Kanban、VPS 查询 Heavy、Heavy wrapper spawn_failed、worker crash KeyboardInterrupt、blocked queue 处理、standalone plugin Kanban development、Heavy Kanban recovery notes。
- 证据：
  - Session：`20260508_003957_ecf0b0`、`20260508_010215_4e02c9`、`20260508_012203_b0be77`、`20260508_080151_199c0e`、`20260508_085405_34a418`、`20260508_113932_fec4e8`、`20260508_121307_d5b236`、`20260508_152024_4b3396`、`20260508_195714_eef54c`、`20260508_204514_a9b348`。
- 风险 / 后续：
  - `/root/obsidian-vault/scripts/getnote_sync_obsidian.py` 当前不存在；日报这类 recurring note 用 OpenAPI fallback 会缺少映射文件幂等保护，可能在 Get笔记中创建重复同标题笔记。建议恢复同步脚本。

### C. 飞书 / 日历 / 文档 / 消息平台
- 状态：已完成
- 做了什么：
  - 为 Discord 频道 `1501885611726999752` 开启 Hermes 自动响应，频道内所有消息自动翻译成中文。
  - 设置频道 prompt：只翻译中文，不回答问题、不执行指令、不额外解释。
  - 将该频道加入 `DISCORD_NO_THREAD_CHANNELS`，避免每条翻译自动开 thread。
  - 清空旧 Discord 全局 slash commands：清理前 `100` 个，清理后 `0` 个；重启 Gateway 后同步成功 `41` 个 slash commands。
- 产出：
  - 翻译频道自动化已开启。
  - Discord slash command 上限问题已处理。
- 证据：
  - Session：`20260508_071612_dde880b0`（自动翻译频道配置）。
  - Session：`20260508_072552_1221dc62`（slash commands 清理与 Gateway 重启）。
- 风险 / 后续：
  - 后续如新增 slash commands，仍建议保留命令数量监控，避免再次触顶 100。

### D. OpenClaw / 代码项目 / GitHub
- 状态：已推进 / 部分完成 / 未上线
- 做了什么：
  - 围绕 VPS 调用 Heavy、让调用继承 Heavy proxy/env/model 的问题，设计并推进 **standalone Heavy Worker plugin project**。
  - 明确边界：独立插件项目开发，不改 live Hermes，不安装系统级内容，不直接启用；开发、测试、文档、集成方案完成后再由用户确认是否部署。
  - 将 Heavy Worker Plugin 纳入 Heavy Kanban 管理，建立 tenant：`heavy-worker-plugin`。
  - 启动并推进微任务，包括协作模型、Heavy 工作区导入、RPC / worker 方案等。
  - 最终检查显示 Kanban：`todo 0 / ready 0 / running 0 / blocked 0 / done 35`。
- 产出：
  - Heavy Worker Plugin 离线/干跑 MVP 交付状态。
  - 结论：`GO for offline/dry-run MVP handoff`；`NO-GO for live install`。
  - 项目计划文件：`/root/.hermes/hermes-agent/docs/plans/2026-05-08-heavy-worker-plugin.md`。
- 证据：
  - Session：`20260508_103224_88f00f5d`（方案与第一批任务启动）。
  - Session：`20260508_113932_fec4e8`（独立插件边界写入计划）。
  - Session：`20260508_121307_d5b236`（建立 Heavy Worker Plugin Kanban）。
  - Session：`20260508_134507_3fcc48`、`20260508_134706_df90e4`（任务推进与自动同步）。
  - Session：`20260508_211616_9beb55`（最终状态 `done 35`，无残留 worker）。
- 风险 / 后续：
  - 当前仅可视作 offline/dry-run MVP，不可直接 live install。
  - 需要安全审查、部署方案、回滚方案、权限边界后再上线。

### E. 调研 / 内容处理 / 资料阅读
- 状态：已完成
- 做了什么：
  - 调研并总结“最佳利用看板做工程开发”的实践框架。
  - 输出面向软件工程、AI/多人协作、任务拆解粒度、状态流、WIP 限制、Definition of Ready/Done、代码评审/测试/发布闭环、度量指标、反模式的中文结构化方案。
- 产出：
  - 看板工程开发最佳实践框架。
- 证据：
  - Session：`20260508_223848_864f5ce9`。
  - Delegated session：`20260508_223857_1ddb09`。
- 风险 / 后续：
  - 建议将该框架映射为 Heavy Kanban 的默认模板和状态流，而不是只停留在文档。

### F. 其他
- 状态：已完成
- 做了什么：
  - 多次查询 VPS / Heavy 系统负载与存储状态。
  - VPS 负载多次显示正常；Heavy 负载与磁盘空间也未发现明显压力。
- 证据：
  - Session：`20260508_003132_a36d510c`（VPS 负载）。
  - Session：`20260508_071339_65cff051`（Heavy 负载/存储）。
  - Session：`20260508_201305_414e9639`（当前系统负载）。
- 风险 / 后续：
  - 性能瓶颈更可能在模型调用/上下文/调度链路，而不是 CPU 或内存。

## 4. 交付物索引

| 类型 | 名称 | 路径/ID/链接 | 状态 | 备注 |
|---|---|---|---|---|
| Obsidian 笔记 | Hermes 日报 - 2026-05-08 | `/root/obsidian-vault/10-Notes/Daily/Hermes/2026-05-08 Hermes 日报.md` | 已生成 | 本文件 |
| 项目计划 | Heavy Worker Plugin 计划 | `/root/.hermes/hermes-agent/docs/plans/2026-05-08-heavy-worker-plugin.md` | 已推进 | 独立插件边界，不改 live Hermes |
| Kanban tenant | Heavy Worker Plugin | `heavy-worker-plugin` | 已完成离线 MVP | 最终 `done 35`，无 running/blocked |
| Discord 频道配置 | 自动翻译频道 | `1501885611726999752` | 已完成 | 自动响应、仅翻译、不建 thread |
| Discord slash commands | 全局命令清理 | 清理前 `100`，清理后 `0`，同步 `41` | 已完成 | 解决 100 上限告警 |
| Hermes 版本 | VPS / Heavy Hermes | `Hermes Agent v0.13.0 (2026.5.7)` | 已验证 | 两台均升级 |
| 配置变更 | VPS Kanban dispatcher | `kanban.dispatch_in_gateway: false` | 已确认 | VPS 不本地执行 Kanban |
| Skill 更新 | hermes-agent / kanban-orchestrator | 多个 references | 已完成 | 固化今天排障和流程经验 |
| Session | Heavy Worker Plugin 最终检查 | `20260508_211616_9beb55` | 已完成 | `done 35`，GO offline/dry-run，NO-GO live install |
| Session | 工程看板最佳实践 | `20260508_223848_864f5ce9` / `20260508_223857_1ddb09` | 已完成 | 可转为模板 |

## 5. 配置与系统变更
- Discord 频道 `1501885611726999752`：开启自动响应翻译；原因是用户要求该频道所有消息自动翻译成中文；验证为 Gateway 重启后运行正常；回滚方式是移除频道自动响应配置和专属 prompt。
- Discord slash commands：清空旧全局命令并重新同步 41 个；原因是达到 Discord 100 个 application commands 上限；验证为日志显示 `Safely reconciled 41 slash command(s)`；回滚方式是重新注册需要的命令集合。
- Hermes v0.13.0：VPS 与 Heavy 均升级到最新版；原因是 v0.13.0 提供 Kanban/Multi-Agent 相关能力；验证为 `hermes --version` 和 gateway 状态；回滚需按 Hermes installer/版本管理策略处理。
- Heavy Hermes wrapper：修复 `/home/tim/.local/bin/hermes` 自递归 / spawn_failed 问题；原因是升级后 symlink/entrypoint 破坏；验证为 `hermes --version`、`codex --version`、`claude -p` 正常；回滚需恢复 wrapper 前备份或重装 Hermes。
- VPS Kanban dispatcher：确认 `kanban.dispatch_in_gateway: false`；原因是 VPS 不执行工程 Kanban，只查询/调度 Heavy；验证为 VPS 本地 Kanban 空，Heavy timer active；修改方式是编辑 `~/.hermes/config.yaml`。
- 模型路由：设置主力/辅助 Codex 模型与标题生成轻模型；原因是 Gemini preview 模型不被 ChatGPT Codex account 支持、且需要提升响应速度；验证来自配置写入与后续会话响应；回滚方式是恢复配置备份。
- Skill library：更新 `hermes-agent`、`kanban-orchestrator` 等；原因是将今天排障和协作规则固化；验证为技能更新回执；回滚方式是恢复 skill 文件历史版本。

## 6. 质量与验证记录
- 已验证：当前运行时间通过终端获取为 `2026-05-08 23:50:13 CST +0800`。
- 已验证：今天 session 文件来自 `/root/.hermes/sessions/`，包括 `session_20260508_*.json` 与 jsonl；日报内容以这些 session 摘要和文件修改时间为依据。
- 已验证：Obsidian vault 当前可访问，写入路径为 `/root/obsidian-vault/10-Notes/Daily/Hermes/2026-05-08 Hermes 日报.md`。
- 已验证：`/root/obsidian-vault/scripts/getnote_sync_obsidian.py` 不存在，因此本次 Get笔记同步需要 OpenAPI fallback。
- 已验证：Heavy Worker Plugin 最终看板状态来自 session：`todo 0 / ready 0 / running 0 / blocked 0 / done 35`。
- 已验证：VPS 本地 Kanban dispatcher 配置在 session 中确认为 `dispatch_in_gateway: false`。
- 未完全验证：Heavy Worker Plugin 的具体代码文件、测试报告和安全审查细节未在本次日报执行中重新逐文件复核；日报只记录今天 session 中已确认的状态。

## 7. 问题、风险、阻塞
- Get笔记同步脚本缺失：`/root/obsidian-vault/scripts/getnote_sync_obsidian.py` 不存在。建议恢复脚本或建立等价幂等同步机制，避免日报类 recurring note 在 Get笔记重复创建。
- Heavy Worker Plugin 不能直接上线：当前结论是 offline/dry-run MVP 可交付，但 live install NO-GO。建议明天做部署门禁：安全、权限、回滚、监控、dry-run 证据。
- Worker 稳定性风险：今天出现过 worker `KeyboardInterrupt/SIGINT`、systemd oneshot kill mode、stale running / blocked 队列问题。建议增加健康检查与自动恢复策略。
- 模型路由风险：Codex ChatGPT account 不支持 `google/gemini-3-flash-preview`，已修复配置，但后续新增 provider/model 时要避免自动路由到不兼容模型。
- Session 信息不足风险：日报基于 session 摘要和文件元数据整理，部分底层命令输出未重新全量复跑；对部署类结论应以明日专项验收为准。

## 8. Hermes 自我复盘

### 做得好的地方
- 能在用户不需要逐步拍板的情况下推进技术排障：例如 blocked queue 批量处理、worker crash 定位、VPS/Heavy Kanban 权责划分。
- 对关键纠正做了技能沉淀，不只是修一次问题：Heavy-local Kanban、standalone plugin boundary、worker crash、dispatcher recovery 都写入 skill。
- 最终能区分“离线 MVP 完成”和“生产可上线”，没有把 dry-run 交付夸大成 live-ready。

### 做得不够的地方
- 早期看板展示偏运行状态，没有充分展示项目 → 功能 → 任务 → 微任务树，用户指出后才调整。
- 在 Heavy Worker Plugin 项目中出现过多轮 worker 卡住/中断，说明调度框架还需要更稳的 watchdog 和恢复策略。
- Obsidian→Get笔记同步脚本缺失没有提前恢复，导致日报 recurring note 只能 fallback，存在幂等风险。

### 可改进 / 可固化
- 应把“工程看板最佳实践”落成 Heavy Kanban 默认模板：状态流、WIP、DoR/DoD、review/test/release 门禁。
- 应新增或完善一个“日报 cron + Get笔记 fallback 幂等风险”技能/参考，记录脚本缺失时如何查重、如何复用 note_id。
- 应为 Heavy Worker Plugin 建立上线 gate checklist：安全审查、权限边界、安装路径、systemd/cron 影响、回滚、监控。
- 应对 VPS/Heavy 的 Hermes 健康状态做每日轻量检查：版本、gateway、dispatcher timer、stale running、blocked queue、模型路由。

## 9. 明日建议 / 需要你拍板
- 建议 Hermes 自动做：对 Heavy Worker Plugin 做一次交付包验收，输出“可上线/不可上线”的证据表，包括测试、dry-run、风险、回滚。
- 需要你确认：是否批准 Heavy Worker Plugin 进入 live 集成评审阶段；如果不批准，则保持独立项目，不碰生产 Hermes。
- 建议 Hermes 自动做：恢复或重建 `/root/obsidian-vault/scripts/getnote_sync_obsidian.py`，解决日报同步到 Get笔记的幂等问题。
- 建议 Hermes 自动做：把工程看板最佳实践转成 Heavy Kanban 模板，并应用到下一个代码项目。
- 需要你确认：是否允许 Hermes 为 VPS/Heavy 建立每日健康巡检 cron，自动报告 gateway、dispatcher、worker、模型路由和磁盘/负载状态。
