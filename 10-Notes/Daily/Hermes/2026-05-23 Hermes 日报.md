# Hermes 日报 - 2026-05-23

## 1. 老板速读
- 今天核心工作集中在 Hermes 自身稳定性与性能排障：确认 VPS 资源健康，主要瓶颈在 GPT-5.5 / openai-codex backend 的 300 秒级卡顿和大上下文放大效应。
- 已把模型 fallback 配置从错误的 `feedback` 思路修正为真正的 fallback，并验证 MiniMax-M2.7 通过 `minimax-cn` provider 可连通。
- 明确了 Discord 当前仍是重工具入口：默认暴露 terminal、file、execute_code、cron、skill 管理等能力，普通聊天不应长期背这套 full access surface。
- Obsidian skill 及相关依赖安装/使用流程今天有推进，日报双写流程继续按 Obsidian + Git + Get笔记执行。
- 主要风险是 GPT-5.5 backend 不稳定仍会拖慢多子区并发；建议尽快做平台轻量 toolset、smart model routing、Weixin 通道清理。
- 需要你拍板：是否把 VPS Discord/Feishu 默认入口改成 light profile，把重执行默认迁到 devbox/Kanban。
- 明日优先建议：先做低风险配置瘦身，再做 skills/tool schema 懒加载设计。

## 2. 今日工作总览
- 处理 Hermes Gateway / 模型调用失败排障，状态：已完成根因分析，部分 fallback 配置已修正。
- 处理 VPS 运行状态检查，状态：已完成，系统资源健康，问题不在 CPU/内存/磁盘。
- 处理 Discord slash command 权限与可见性分析，状态：已确认 Hermes 命令对所有频道/角色开启，Discord 内置命令无法完全隐藏。
- 处理 Skill / SOUL / user profile / memory / tool schema 体积分析，状态：已完成现状盘点并形成优化方向。
- 处理 Obsidian / Get笔记相关安装与日报流程，状态：已推进，仍需关注 recurring note 幂等风险。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已完成】排查多 Discord 子区 GPT-5.5 持续失败；判断主因是 openai-codex backend 非流式调用 300s stall / 500，本地大上下文和多工具循环是放大器。
- 【已完成】分析 Hermes 与模型交互最长路径；确认最长路径是 agent turn 内部多轮 GPT-5.5 调用，而不是 Discord 发送、本地 terminal 或 VPS 资源。
- 【已推进】修正 fallback 配置；主模型保持 GPT-5.5，fallback 指向 `minimax-cn / MiniMax-M2.7`，并验证 gateway loader 能读到配置。

### VPS / Gateway 运维
- 【已完成】检查 VPS 状态；负载、内存、磁盘、inode、systemd failed units 均正常，Hermes Gateway 与 dashboard 可运行。
- 【风险】发现 Weixin session expired 持续刷日志，Discord 旧投递目标存在 Unknown Channel；建议清理或禁用无效通道，减少噪音和失败重试。
- 【已完成】确认慢响应不是 Feishu/Discord 平台发送慢，而是模型调用链和上下文/工具负载导致。

### Skill / 上下文 / 工具体系
- 【已完成】盘点当前 Skill：136 个 enabled；不会每轮注入全部 Skill 正文，但 Skill 索引、SOUL、memory、user profile、工具 schema 会形成较大的固定上下文。
- 【已完成】确认 Discord 当前实际是重工具入口；建议拆出 `hermes-discord-light`，普通消息不默认暴露 terminal、execute_code、patch、cronjob、skill_manage、MCP 等重工具。
- 【建议】后续应实现 Category 懒索引 / top-level router skill / lazy tool schema，减少每轮固定上下文。

### Discord / 消息平台
- 【已完成】检查 Discord slash command 管理页；Hermes 命令对 `@everyone` 与所有频道均为允许状态。
- 【已确认】如果 `/` 面板仍出现非 Hermes 命令，大概率来自 Discord 内置命令或用户级 App，不是服务器里另一个 bot。

### 知识库 / Obsidian / Get笔记
- 【已推进】Obsidian skill 安装与依赖方向今天有处理；日报继续按 Obsidian 本地 Markdown、GitHub、Get笔记 MyClaw 双写。
- 【风险】定时日报这类 recurring note 必须优先用同步脚本维护映射；如果退回 OpenAPI fallback，可能重复创建同名 Get笔记。

## 4. 重要交付物 / 系统变更
- 配置｜Hermes fallback providers｜已修正｜`minimax-cn / MiniMax-M2.7`
- 备份｜Hermes fallback 配置备份｜已创建｜`/root/.hermes/config.yaml.bak-fallback-minimax-cn-20260523`
- 诊断结论｜GPT-5.5 timeout 根因分析｜已完成｜用于后续性能优化决策
- 诊断结论｜Hermes 最长路径分析｜已完成｜明确优化目标是减少 GPT-5.5 调用次数与输入体积
- 诊断结论｜Discord slash command 权限｜已完成｜Hermes 命令已允许所有频道/角色
- 日报笔记｜Hermes 日报 - 2026-05-23｜已生成｜`10-Notes/Daily/Hermes/2026-05-23 Hermes 日报.md`

## 5. 风险、阻塞、需要拍板
- GPT-5.5 / openai-codex backend 仍可能 300s 卡顿；建议启用 smart model routing，并把简单消息切到 cheap model。
- Discord/Feishu 入口默认工具集过重；建议你确认是否允许我改为 light profile，重执行转 devbox/Kanban。
- Weixin session expired 持续存在；如果不用微信入口，建议禁用；如果要用，需要重新登录/刷新 iLink。
- Memory 与 user profile 接近上限；建议做一次压缩，把流程类内容迁到 skill，保留稳定偏好。
- Supadata MCP 不应默认注入普通 Discord 会话；建议改为 YouTube/网页任务按需启用。

## 6. Hermes 自我改进
- 需要固化一套“Gateway 性能排障 runbook”：从 provider latency、tool schema、session size、gateway response time、平台发送链路分层定位。
- 需要把 Discord light profile 的实现方案整理成可执行 plan，避免只停留在建议。
- 日报继续保持管理向，不把证据链、命令输出、session 细节塞进正文。

## 7. 明日建议
- 【可自动做】创建并验证 `hermes-discord-light` / `hermes-feishu-light` 工具集方案，先 dry-run 对比 tool schema 体积。
- 【需要你确认】是否启用 smart model routing，让短消息走 `gpt-5.4-mini`，复杂任务再升级 GPT-5.5。
- 【可自动做】清理 Weixin 失效通道或给出禁用配置补丁，降低 Gateway 噪音。
- 【可自动做】压缩 memory/user profile，把 YouTube、Get笔记、日报等流程细节迁回 skill。
- 【需要你确认】是否把 VPS 定位为轻入口/控制面，把代码与长任务默认派给 devbox/Kanban。