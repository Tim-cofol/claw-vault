# Hermes 日报 - 2026-07-10

## 1. 老板速读
- 今日真实用户活动：1 条 Discord 对话（cofol1986），三个回合：丢 YouTube 链接 → 要主内容摘要 → 问 SDD 如何和 Git 衔接。
- 唯一 autopilot：SUP-47「AI Builders 每日早报」已 `in_review`，08:09 飞书送达，本轮按规则未自动 `done`，等日报复审。
- 主交付物：Owain Lewis《How I Code With AI Agents (Spec-Driven Development)》中文摘要 + SDD × Git 衔接的 6 条具体决策（spec 进 Git、task/git unit 映射表、Refs: 锚点、PR 模板、git alias）。
- /tmp 旧工件扫描结果：101 行（11 ARCHIVED + 90 NOT_ARCHIVED），**与昨天完全一致，无漂移**；今天新增的 `/tmp/yt_summary/RhaF4LVAVng.{json,txt}` 是用户刚要的 YouTube transcript 文件，0 天龄。
- 飞书身份问题延续昨日：user identity 仍 `needs_refresh`，scope 列表里**不含** `im:message.send_as_user` / `im:message.group_at_message_send_as_user`，所以 SUP-47 早报继续走 bot 投递。
- autopilot 状态债继续累：SUP-39/40/41/42/43/44/47 多条历史 + 今日早报停在 `in_review`，dashboard 噪声没减。
- 明日优先建议：补飞书 OAuth scope、让 autopilot 自己在投递成功且无异常时翻 `done`、批量清理历史 SUP、`/tmp/yt_summary` 是否要归档进 Vault。

## 2. 今日工作总览
- 【已完成】Discord 频道处理 1 条 cofol1986 三回合对话：YouTube 视频抓取 → 主内容中文摘要 → SDD × Git 衔接问答。
- 【已完成】SUP-47 follow-builders 自动早报：拉 fresh feed、中文 remix、bot 身份投递飞书 DM、issue 留言并 `in_review`。
- 【已核对】lark-cli 状态：user `needs_refresh`，scope 列表里**没有**发送消息所需授权（修复路径仍是补 OAuth scope，需要你拍板）。
- 【已完成】本日报按 daily-report 模板生成并准备双写。

## 3. 按项目 / 责任域汇报

### Multica / 自驱试运行
- 【已完成】SUP-47「AI Builders 每日早报」完成飞书投递：14 位 builder + 3 篇 Anthropic 博客 + 1 期 AI & I by Every 播客；issue `in_review`，message_id `om_x100b6a37ca72f0a8b28fa94d59221a2`，08:09:48 送达。
- 【观察】autopilot 默认 `in_review`、靠日报复审翻 `done` 的闭环模式今天没改：早报已成功送达且 0 异常，按昨天结论本应前置到 autopilot 自身，但仍未生效。
- 【风险】SUP-39/40/41/42/43/44/47 七条历史/今日早报停在 `in_review` / `in_progress`，dashboard 长期噪声。

### Discord / 消息平台
- 【已完成】1 条 cofol1986 三回合对话：YouTube 链接（Owain Lewis《Spec-Driven Development》, 19 分钟）→ 主内容中文摘要（spec/PRD/架构文档区别、spec 框架之争、与你工作流对照）→ SDD × Git 衔接回答（spec 进 Git、task 粒度映射、Refs 约定、PR 模板、git alias）。
- 【已完成】按 youtube-content skill 输出 chapters + summary，覆盖视频 80 段 transcript 22K 字。
- 【未涉及】vault / Get笔记写入：用户没要求保存，且 §5.5 风险窗口内未升级。

### Hermes / Agent 系统
- 【已核对】今日 state.db 真实活动：3 个 session（1 Discord 用户 + 1 SUP-47 ACP stub + 1 cron），无 acp channel 异常、无 plugin 加载变化。
- 【观察】/tmp 扫描 101 行与 7-09 报告数字完全一致（11 ARCHIVED + 90 NOT_ARCHIVED），`scan_tmp_artifacts.sh` 计数纪律有效。
- 【观察】今天 21:56 出现 `/tmp/yt_summary/RhaF4LVAVng.{json,txt}` 新工件：用户请求触发的 youtube-content transcript 落地，0 天龄；是否要归档进 Vault 见 §5。

### 知识库 / Obsidian / Get笔记
- 【无新增用户笔记】今天用户没要求保存到 Obsidian / Get笔记；daily 报告本身按双写流程落档。
- 【已完成】本日报按双写流程：Obsidian 写入 + git commit + Get笔记 同步。

## 4. 重要交付物 / 系统变更
- YouTube 摘要｜Owain Lewis《Spec-Driven Development》｜已交付｜本地 `/tmp/yt_summary/RhaF4LVAVng.txt` + Discord 对话 `20260710_215426_a07a45d1`
- 问答｜SDD × Git 衔接六条具体决策｜已交付｜同 Discord session；包括 spec 进 git、task/git 粒度映射、Refs: 锚点、PR 模板、git alias、与 Kanban 衔接
- 早报｜SUP-47「AI Builders 每日早报」｜已投递飞书，issue `in_review`｜message_id `om_x100b6a37ca72f0a8b28fa94d59221a2`
- 日报｜2026-07-10 Hermes 日报｜已归档｜`10-Notes/Daily/Hermes/2026-07-10 Hermes 日报.md`

## 5. 风险、阻塞、需要拍板
- 【需要你确认】飞书 user 发送 scope 仍未补：user identity `needs_refresh` + 列表里**没有** `im:message.send_as_user` / `im:message.group_at_message_send_as_user`。修复动作仍是 `lark-cli auth login --scope "..."`（OAuth refresh，需你拍板），cron 不应自作主张。
- 【需要你确认】autopilot `done` 政策：今天 SUP-47 已成功送达且无异常，issue 仍停在 `in_review`；昨天日报已经喊过一次，今天再喊一次——是时候定"SUP 早报投递成功 + 无异常 = 自动 done"。
- 【可自动做】批量清理 SUP-39 → SUP-44 历史 `in_progress`/`in_review`：已投递且无后续动作的项 `done`；今天连 SUP-47 也一起凑齐，dashboard 一次性收口。
- 【需要你确认】`/tmp/yt_summary/RhaF4LVAVng.{json,txt}`（21:56 新建，22K 字 transcript）：用户今天用了、没要求归档；接下来会话可能被清掉，要不要顺手存一份到 `30-Resources/Ideas/` 或 `30-Resources/Knowledge/`？
- 【观察】`/tmp` 扫描 101 行里 90 行 NOT_ARCHIVED 仍主要是 Hermes 内部 scratch / EDN 临时产物；与昨天完全一致，不是漂移。

## 6. Hermes 自我改进
- Discord 会话作为"用户对话"在日报里要明确口径：今天 cofol1986 在 Discord 提了三回合问题，已按"用户真实活动"记入 §3，不再被"0 个用户 session"模板误判为静默日。
- `/tmp` 工件扫描今天与昨天计数完全一致（11 ARCHIVED + 90 NOT_ARCHIVED = 101），但今天**多了一条 `/tmp/yt_summary/` 子目录**是用户产出。建议规则：扫描脚本应在保留"+0 天龄系统 scratch 不进 ARCHIVED 判定"的前提下，**单独列当天 < 24h 的新工件**，避免用户产出被淹没在"全 +20 天噪声"里。

## 7. 明日建议
- 【需要你确认】补飞书 OAuth scope：优先 `im:message.send_as_user` + `im:message.group_at_message_send_as_user`，否则所有自动投递继续身份错配。
- 【需要你确认】定 SUP 早报 issue close-out 规则：投递成功 + 无异常 = 立刻 `done`，避免每天靠日报补救。
- 【可自动做】批量清理 SUP-39 → SUP-47 的 `in_review` / `in_progress`，一次性收到 `done`。
- 【需要你确认】`/tmp/yt_summary/RhaF4LVAVng.{json,txt}` 是否归档 Vault（22K 字 transcript + 摘要），可作 Spec-Driven Development 工作流的参考材料。
- 【可自动做】把 `/tmp` 扫描脚本改进一下：把"+0 天龄新工件"作为单独分组（而不是合并进 NOT_ARCHIVED 计数），方便用户区分"今天新增了什么 vs 历史遗留"。
