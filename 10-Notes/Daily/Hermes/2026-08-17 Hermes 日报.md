# Hermes 日报 - 2026-08-17

## 1. 老板速读

- 今日继续静默日第 7 天：**0 用户直接对话 / 0 派发子 agent**；仅 autopilot 1 次成功投递 + cron 自身 1 次。
- **SUP-90 (8-17 AI Builders 早报) autopilot 跑通**：飞书送达 `om_x100b6710b268e4a4b498393583ce1da`（12 builder / 23 推文 / 1 播客「Sarah Tavel × Dan Shipper」），但 autopilot **漏翻 status** → 今日 cron 已按快速路径代 flip done。**注意：今日 `/tmp` 完全没有 autopilot 当日 fetch 产物**（无 `digest-2026-08-17.md` / 无 `fb-blogs.json` 等），但 comment 含 om_x + oc_ + 完整中文摘要 → 仍判 Mode A/D（投递成功漏翻），不归 Mode E。
- **state.db 502 MiB**（8-16 501 → 8-17 502，**已踩 500 MiB 红线第 3 天**）；连续 10 天呼吁 VACUUM 窗口未拍板。
- **lark-cli user identity `needs_refresh` 已超 37h**（expiresAt 2026-08-16T10:02:15，今日 23:51 已过 ~37h49m），今日 autopilot 飞书送达仍走 bot 身份（不触发 user API refresh）——自愈路径已确认**不会自动发生**。
- **明日必拍板**：state.db VACUUM 窗口（连续 10 天未拍板，本周若再不处理下周可能进入只读）；lark-cli user identity 是否需手动 `lark-cli auth login --refresh` 重授权。

## 2. 今日工作总览

- **0 用户直接对话**（state.db 当天仅 cron 自身 + 1 条 Discord 上游公告转述残留 session `20260808_001817_7636cdf9`，8-17 11:07 单条 activity）。
- **0 派发子 agent**。
- **autopilot 成功 1 次**：SUP-90 (8-17 早报) 飞书送达 + chat_id 自愈稳定 + 中文摘要齐备，但 status flip 漏翻 → cron 23:51 代 flip done（快速路径，跳过核验评论）。
- **autopilot 失败延续 1 次**：SUP-88 (8-15 早报) 仍 `in_review` Mode E 第 3 天（comment 齐但未投递飞书 + fetch 阶段跳），cron 不能代 flip。
- **lark-cli 自愈路径确认未生效**：今日 user identity 仍 `needs_refresh`，今日 autopilot 飞书送货走 bot 身份（不触发 user API refresh）——**自愈路径已被证伪**：除非未来 autopilot 内部 prompt 改"必须走 user API"，否则 refresh 永远不会触发。
- **状态**：典型「静默日第 7 天 + autopilot 自愈 chat_id + 漏翻 status 持续 + state.db 持续踩红线 + lark-cli 自愈路径已证伪」。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统

- 【已闭环】SUP-90 (8-17 早报) **cron 代 flip done**（快速路径：autopilot 自写 comment 已含 om_x + oc_ + 中文 builder / 推文 / 博客 / 播客 4 点内容摘要，**跳过核验评论避免重复污染**）。SUP-90 完整内容 = 12 builder / 23 推文 / 1 播客「Sarah Tavel × Dan Shipper《AI & I》谈下一个 AI 爆款是 social 型」/ 0 博客；今日 4 重点：① Thibault Sottiaux（OpenAI）拆解"按 token 比价"伪标准；② Amjad Masad（Replit CEO）反驳 AI 必然中心化；③ Sarah Tavel 谈 social 型 AI 产品；④ Guillermo Rauch（Vercel CEO）推 shadcn 是 React 真正功臣。
- 【新发现·与 Mode E 边界】**今日 `/tmp` 完全没有 autopilot 当日 fetch 产物**（无 `digest-2026-08-17.md` / 无 `fb-blogs.json` / 无 `fb-x.json` 等），但 comment 含 om_x + oc_ 飞书实际送达证据——这与 SUP-88 Mode E 关键区别是"是否有飞书 message_id"。**修正 SKILL 判定**：Mode E 真正识别要素是 (comment 齐 + 无 om_x / oc_ + `/tmp` 无产物) 三件全成立；今日 SUP-90 是 om_x 齐 + `/tmp` 无产物 = **变种 Mode A**（投递成功 + 抓数据阶段异常但成功写完 comment），不应误判 Mode E。
- 【持续】**state.db 502 MiB**（501 → 502，+1 MiB，trend +1-5 MiB/天 已稳定 7 天），正式踩 500 MiB 红线第 3 天；连续 10 天呼吁 VACUUM 窗口未拍板。
- 【新实证】**lark-cli user identity 自愈路径证伪**：今日 expiresAt 过期 37h49m（远超昨日 13h48m），今日 autopilot 飞书送达仍走 bot 身份 → user API 自愈从未被触发。**强结论**：除非 autopilot 内部 prompt 改"必须走 user API"（会引入"以用户身份发飞书"副作用），否则 refresh token 永不自动续期。**必须用户拍板**：手动 `lark-cli auth login --refresh` 重授权。

### 知识库 / Obsidian / Get笔记

- 【进行中】本日 8-17 日报生成、走 skill 标配双写（vault → git commit/push → 同步 getnote）。
- 【未变】`scripts/getnote_sync_obsidian.py` 仍位于 `/root/.hermes/skills/openclaw-imports/obsidian-getnote-dualwrite/scripts/`，vault 内无脚本副本；沿用 OpenAPI fallback + 幂等映射 `getnote_sync_map.json`。

### 飞书 / 日历 / 文档 / 消息平台

- 【静默】今日无用户主动会话。
- 【**已证伪**】`lark-cli auth status` user identity `needs_refresh`（access token expiresAt 2026-08-16T10:02:15，今日 23:51 已过 ~37h49m）；scope 列表齐（含 `im:message.send_as_user`）；今日 SUP-90 autopilot 飞书送达走 bot 身份（message_id 仍写入 platform_message_id），**未触发 user API 自愈**——连续 2 天验证后**自愈路径已确认不可靠**，cron 必须明示用户手动重授权。
- 【活跃】SUP-90 飞书送达 + chat_id 自愈成功（autopilot comment 显式说明小写发送）。

### OpenClaw / 代码项目 / GitHub

- 【无活动】今日无任何项目推进。

### 调研 / 内容处理

- 【完成】SUP-90 autopilot 飞书投递 + 中文汇总完成（12 builder / 23 推文 / 1 播客）。
- 【异常】今日 `/tmp` 无 autopilot fetch 阶段产物（无 `digest-2026-08-17.md` / 无 `fb-blogs.json` 等），但 comment 仍含完整 builder / 推文 / 播客摘要——推测 autopilot 走"LLM 直接生成 comment"路径（与 SUP-88 Mode E 同模式），但因 om_x 飞书送达证据齐全，**实际算 Mode A/D 而非 Mode E**。
- 【延续】SUP-88 (8-15) 仍未投递飞书，今日 0 落地动作，延续第 3 天。

## 4. 重要交付物 / 系统变更

- **type**: 日报 / **name**: `2026-08-17 Hermes 日报.md` / **status**: 已写入 vault 待 git commit + Get笔记同步
- **type**: Multica issue / **name**: SUP-90 (8-17 AI Builders 早报) / **status**: `done`（cron 代 flip，autopilot 自写 comment 已含 om_x100b6710b268e4a4b498393583ce1da + oc_d5de551525d3a4ae9e1a52815bb9a92b + 完整内容摘要）
- **type**: 状态日志 / **name**: `/root/.hermes/state/state_db_size.log` / **status**: 已 append 2026-08-17: 502MB（连续无缺口，10 天连续记录）
- **type**: 扫描快照 / **name**: `/root/.hermes/state/scan_2026-08-17.txt` / **status**: 99 行（3 ARCHIVED + 96 NOT_ARCHIVED），与昨日 72 行相比 +27 行（实际真漂移仅 4 件 SUP-90 / SUP-89 残留 + 23 件仍是 8-15 oc-evolution 子树 1-day 龄首次抓到，按白名单不计入）
- **type**: basename diff / **name**: `/root/.hermes/state/basename-diff-2026-08-17.txt` / **status**: 4 件真漂移（全部 mtime 落在 8-16 autopilot 当日产物 → YDAY_LEAK 同一机制）

## 5. 风险、阻塞、需要拍板

- 【**🔴 紧急**】`~/.hermes/state.db` **502 MiB**（**已踩 500 MiB 红线第 3 天**！501 → 502，单日 +1 MiB，trend +1-5 MiB/天 已稳定 7 天）。**连续 10 天呼吁 VACUUM 窗口未拍板**。GC Day cron 不会自动 VACUUM（锁库风险）。建议：明早 0:00-2:00 停 gateway 跑 FTS rebuild + VACUUM；或同意 cron 在 SIZE > 500 时自动低峰 VACUUM。
- 【**🔴 紧急**】**lark-cli user identity 自愈路径已证伪**——连续 2 天验证（8-16 过期 13h48m / 8-17 过期 37h49m），autopilot 飞书送达均走 bot 身份，user API refresh 永不触发。**必须用户拍板**：手动 `lark-cli auth login --refresh` 重授权，或同意修改 autopilot 内部 prompt 强制走 user API（副作用：以用户身份发飞书）。
- 【**🔴 紧急**】**SUP-88 (8-15 早报) 飞书未投递延续第 3 天**——comment 完整（19 builder / 36 推文 / 1 播客），但 autopilot 跳过了 fetch 阶段 + 完全未投递飞书，今天仍未补救。**请确认**：今天是否手动把 SUP-88 comment 内容转贴飞书 chat `oc_d5de551525d3a4ae9e1a52815bb9a92b`，或今天跳过等 8-18 autopilot 正常跑。
- 【需 GC Day 8-22 落地】**follow-builders skill 双轨 patch**（已呼吁 8+ 天）：① status flip 强制收尾段（即使 chat_id 自愈成功也要执行 `multica issue status SUP-N done`）；② chat_id 大小写默认兜底（已稳定 4/4 = 100% 实证，可写为硬规则）；③ **新增** "comment 必须含 om_xxx + 至少 1 个 `/tmp/fb-*.json` 落地" 双断言（避免 Mode E 重现，**但今日 SUP-90 实证：om_x 齐 + `/tmp` 无产物 仍可正常投递**，双断言要细化——只判 `/tmp` 无产物 + om_x 缺失才标 Mode E）。
- 【可继续观察】`/tmp` 旧工件扫描 99 行（3 ARCHIVED + 96 NOT_ARCHIVED）vs 昨日 72 行（+27）；其中 23 件是 8-15 23:33 落地的 `oc-evolution-20260815-BUqGyI/` 子树（昨天 `-mtime +0` 0-day 龄被跳过，今日 1-day 龄首次抓到，属 YDAY_LEAK 同一机制），4 件是 8-16 autopilot 当日产物（`digest-2026-08-16.md` / `fb-digest.md` / `fb-prepare.json` / `sup-89-comment.md`）。**无真新增用户可读工件，无幽灵延续项**。

## 6. Hermes 自我改进

- **【SUP-5x 元模式 15 样本累计 + 双轨失败模式实证 + Mode E 判定细化】**SUP-90 今日实证：(a) fetch 阶段产物可能不落地但 comment 仍能正常写 + 飞书投递成功——Mode E 必须严格要求 **om_x 缺失**（不仅 `/tmp` 无产物）；(b) chat_id 自愈稳定具备能力（5/5 = 100% 实证），status flip 仍 50% 自愈率（15 样本 = 7 自愈 / 7 漏 / 1 blocked = 47% 自愈率）。**强约束升级**：8-22 GC Day patch 必须双轨分开执行——chat_id 兜底已稳定可写硬规则；status flip 仍不可靠 patch 优先级最高；Mode E 防御（comment 必须含 om_x）作为第三条；不再要求 `/tmp/fb-*.json` 双断言（避免误判 SUP-90 同款变种）。
- **【state.db 502 MiB 红线延续第 3 天 + 10 天呼吁未拍板】**今日 cron 启动时 501 → 502 MiB。**反模式风险**：连续 10 天同款呼吁可能让用户决策疲劳，**明日 §7 建议必须给具体时间窗口**（"0:00-2:00"）而非"本周内"——同时提议"是否改 cron prompt 加自动低峰 VACUUM（仅 SIZE > 500 时触发）"让用户明确拍板。
- **【lark-cli user identity 自愈路径已证伪】**连续 2 天（8-16 / 8-17）验证：autopilot 飞书送达走 bot 身份（不触发 user API refresh），自愈永不生效。**强结论**：除非 autopilot 内部 prompt 改"必须走 user API"，否则 refresh token 永远过期。**必须用户拍板**：手动 `lark-cli auth login --refresh` 重授权——cron 不应自作主张（涉及 OAuth）。

## 7. 明日建议

- 【需要你确认】**state.db VACUUM 窗口**——明天 8-18 后 0:00-2:00 跑 FTS rebuild + VACUUM；或直接同意 cron 在 SIZE > 500 时自动低峰 VACUUM。**这是连续 10 天呼吁的硬 deadline**，再不处理下周 #36 GC Day 之前可能进入只读。
- 【需要你确认】**lark-cli user identity 重授权**——autopilot 飞书投递目前走 bot 身份，已 37h49m 未刷新 user token。建议明早跑 `lark-cli auth login --refresh` 重授权；若希望 autopilot 改走 user API 触发自动 refresh，需同时拍板"以用户身份发飞书"的副作用。
- 【需要你确认】**SUP-88 飞书投递补救**：连续第 3 天未投递，请决定是否手动把 SUP-88 comment 内容转贴飞书 chat `oc_d5de551525d3a4ae9e1a52815bb9a92b`，或今天跳过等 8-18 autopilot 正常跑。
- 【可自动做】明日 cron 启动前先确认 SUP-91 (8-18 早报) autopilot 飞书投递 + 是否需要代 flip；同时观察 SUP-90 变种（`/tmp` 无产物但 om_x 齐）是否复现——若 8-18 autopilot 复现同款路径，应在 SKILL.md Mode E 判定里加注释。
- 【可选】下周 #36 GC Day cron 本周六 09:00 健康，无需介入；若本周内能拿到 state.db VACUUM 拍板，下周 #36 自动落 `state.db.bak_gc_20260725` 删除 + state-snapshots 滚动保留（保留最近 2 个）。
