# Hermes 日报 - 2026-07-31

## 1. 老板速读
- 今日是真正的全静默日：用户直接对话、飞书/Discord 轻问、子 agent 派发均为 0；唯一活动是 autopilot 08:00 SUP-68 触发（飞书送达成功，但漏翻 status）+ 日报 cron 自身。
- **SUP-5x 元模式本周第 3 次复现（紧急）**：SUP-68 (00:06) 飞书成功送达 `om_x100b698a233d60a4b24c09199180846`、中文输出齐备，autopilot **仍漏翻 `done`**——7-26 自愈 1 次 + 7-30 漏 1 次 + 7-31 又漏，**50% 自愈率不成立**。GC Day #20 (8-01) 必须升级为紧急 patch follow-builders skill 强制收尾段。
- SUP-68 已由 cron 代 flip `done`（带飞书回读 + 元模式复现证据评论）；SUP-57 (10 天) / SUP-39 (28 天) / SUP-25 (36 天) 仍按昨日决定累积到 GC Day #20 一并处置。
- `/tmp` 旧工件 77 行（3 ARCHIVED + 74 NOT_ARCHIFIED），与昨日 74 行相比 NOT_ARCHIVED +3；basename 集合新增 3 件（`not_today_basename.txt` / `not_yday_basename.txt` / `call_019fae935ade7e12819dfbe6.txt`），**全部是 cron 内部 scratch**（昨日 cron 跑 basename diff 调试残留 + 今日 scan 自身 scratch），不是用户可读新工件。
- EDN 周报 cron 自 7-29 起 error，今日未触发（按"周三 08:30" 计划，下一次 8-05），本周缺位持续；按昨日决定不补救，等下周三自动覆盖。
- 明日 8-01（周六）09:00 是 GC Day #20 触发窗口；建议本周 3 项 patch 合并落地：① follow-builders skill 自驱收尾硬约束 ② EDN cron prompt Step 5 数据资产 fallback 路径 ③ GC Day 报告同步进 vault 双写。

## 2. 今日工作总览
- 【已完成·代 flip】SUP-68（AI Builders 每日早报 — 2026-07-31）autopilot 00:06 飞书送达成功但 status 仍 in_review；日报 cron 23h+ 后跑 `multica issue status SUP-68 done` 闭环，并加 result comment 标记本周第 3 次 SUP-5x 元模式复现。
- 【无活动】用户直接对话 / 飞书轻问 / 子 agent 派发：全 0。state.db 当天只有本 cron session（1 条 user msg），0 acp / 0 subagent / 0 平台消息 ID 写入；jobs.json 4 条 cron：日报 cron（本 session）/ GC Day #20 (8-01) / EDN 仍 error / idea-recall 周一已跑。
- 【已转述·今日 0 条】Discord `#hermes-agent-notifications` 今日无上游公告（昨日 1 条 Buzz 工作区集成已转）。
- 【信息】EDN 周报本周缺位（已 3 天）持续到下周三 8-05 cron 自动覆盖前不再二次喊；本周 76 条证据只存于 subagent session，下次触发时被覆盖。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已完成】SUP-68 代 flip `done` 闭环（autopilot 08:00 跑完 deliver + comment 但 status 仍 in_review，cron 23h+ 后代 flip + 加 result comment 记录飞书 message_id 与元模式复现证据）。
- 【信息】jobs.json 4 条 cron：日报 cron `ok` / GC Day #20 `ok` (上次 7-25) / EDN `error` (7-29) / idea-recall `ok` (7-27)；本周日 8-01 09:00 GC Day #20 必触发。
- 【信息】**SUP-5x 元模式本周第 3 次复现**：7-26 SUP-63 自愈 + 7-30 SUP-67 漏 + 7-31 SUP-68 漏 = 33% 自愈率。`autopilot 自驱收尾稳定`假设不成立，GC Day #20 必 patch follow-builders skill（"deliver → comment → flip status → comment done"硬约束）。

### 飞书 / 日历 / 文档 / 消息平台
- 【已送达】SUP-68 飞书 00:06 送达成功（chat_id `oc_d5de551525d3a4ae9e1a52815bb9a92b`，bot 身份，markdown post 格式，message_id `om_x100b698a233d60a4b24c09199180846`），中文输出齐备。
- 【信息】`lark-cli` user identity 仍 `needs_refresh`（expiresAt `2026-07-30T10:03:20+08:00` 已过期一天），scope `im:message.send_as_user` 仍缺失；今日 SUP-68 autopilot 飞书送达走 bot fallback 正常，**未**影响交付。按 SKILL §0-pre 升级规则，本条不列风险位。

### Multica / AI Builders
- 【已完成】SUP-68 代 flip `done`：详见 §3 Hermes / Agent 系统。
- 【信息】SUP-57 (10 天 in_review) / SUP-39 (28 天 in_progress) / SUP-25 (36 天 in_review) 仍按昨日决定累积到 GC Day #20 一并处置。

### 调研 / 内容处理
- 【无活动】EDN 周报本周缺位持续到下周三 8-05 cron 自动覆盖前不再二次喊；今日未触发任何调研/内容处理 session。

## 4. 重要交付物 / 系统变更
- SUP-68 闭环｜AI Builders 每日早报（2026-07-31）｜done（cron 代 flip）｜飞书 message_id `om_x100b698a233d60a4b24c09199180846`，中文输出齐备
- SUP-68 result comment（cron 加）｜Multica SUP-68 评论区｜已完成｜内容：本周第 3 次 SUP-5x 元模式复现证据 + 飞书回读 + GC Day #20 紧急 patch 决策
- `/tmp` 扫描快照｜`/root/.hermes/state/scan_2026-07-31.txt`｜77 行｜3 ARCHIVED + 74 NOT_ARCHIVED；与昨日 74 行（3 + 71）相比 NOT_ARCHIVED +3（basename 集合新增 3 件 cron 内部 scratch）

## 5. 风险、阻塞、需要拍板
- 【紧急·需 GC Day #20 落地】**follow-builders skill 强制收尾 patch**：本周 3 次 SUP-N 漏 flip 元模式（7-26 自愈 1 + 7-30 漏 1 + 7-31 漏 1 = 33% 自愈率），autopilot 自驱收尾不稳定已确认；GC Day #20 (8-01 09:00) 必须升级为紧急 patch，把 "deliver → comment → flip status → comment done" 硬约束写进 follow-builders skill 强制收尾段；不要再"再观察一周"。
- 【需 GC Day #20 一并处置】SUP-57 (10 天) / SUP-39 (28 天) / SUP-25 (36 天)：按昨日决定 SUP-57 / SUP-39 cron 自动 close，SUP-25 留你拍板；GC Day #20 一并执行。
- 【信息】EDN 周报本周缺位（已 3 天）：按昨日决定"直接放弃本周等下周三自动覆盖"，今天不再二次喊；下周三 8-05 cron 触发时建议抽查上周 76 条证据是否仍可恢复（subagent session 在 state.db 里应还可见），如不可恢复再决定是否补跑。
- 【信息】GC Day #19 4 项清理决定（已挂 7 天）：VACUUM / legacy 20260607 checkpoint / jobs.json 4 个近似重复备份 / state-snapshots 滚动保留策略。GC Day #20 (8-01) 一并处置时建议把"GC Day 报告同步进 vault 双写"作为本批 fix 之一（已 7 天审计链断裂）。

## 6. Hermes 自我改进
- **SUP-5x 元模式升级判定（紧急）**：本周 7-26 SUP-63 自愈 + 7-30 SUP-67 漏 + 7-31 SUP-68 又漏 = 33% 自愈率（3 样本中 2 次失败），**昨日判断的"50/50 概率稳定自愈"已证伪**。GC Day #20 (8-01) 必须把 follow-builders skill 强制收尾段从"建议 patch"升级为"紧急 patch"——同时把日报 cron 代 flip 的判定（`multica issue comment list` 有飞书 message_id / 中文输出 + `multica issue get` status=in_review）固化为 skill sub-routine，避免再走"读昨天日报才知道"路径。
- **`/tmp` 计数漂移收敛观察**：今日 `wc -l` 77 vs 昨日 74 = +3 件 NOT_ARCHIVED，但 basename diff 仅显示 3 件全部是 cron 内部 scratch（`not_today_basename.txt` / `not_yday_basename.txt` 是昨日 cron basename diff 调试残留，`call_019fae935ade7e12819dfbe6.txt` 是今日 scan 自身 scratch）；**用户可读新工件仍为 0**。这是 `wc -l` 数字 vs basename 集合差异的口径双报持续生效的验证（按 preflight "wc -l 报数 + basename 集合判定"双口径）；考虑把 cron 自身 scratch 写入 `/root/.hermes/state/cron-scratch-<date>.txt` 而非 `/tmp`，避免后续 cron 自污染。
- **静默日 cron 模板精简机会**：连续 ≥ 2 天全静默日（日报 cron + autopilot 单一活动），§2 / §3 重复内容增多（"无活动" / "EDN 仍 error" 等）。下次 GC Day 可考虑给静默日配 ≤ 600 字精简模板，把"延续风险清单"压成一行；今天暂用原模板保持完整性。

## 7. 明日建议
- 【可自动做】GC Day #20 (8-01 周六 09:00) 准备：把今日 §6 SELF-IMPROVEMENT 里的 3 项 patch 清单（SUP-5x 强制收尾紧急 patch + EDN 孤儿移动路径 + GC Day 报告同步 vault 双写）打包成一次 patch 跑完；预计 1 项 follow-builders skill 紧急 patch + 1 项 EDN cron prompt patch + 1 项 cron-scratch 路径优化 + 4 项长留 issue 处置 + 1 项 GC Day 报告双写。
- 【可自动做】明天 8-01 不期望 SUP-69 autopilot 触发（按 SUP-67/68 的 24h 节奏 8-01 8:00 跑，应关注是否还漏 flip——本周第 4 次将升级为事故级别）。
- 【需你确认】SUP-25 处置：36 天 in_review，从 6-22 看到现在；如果不是还在跑，建议直接 close 释放看板；如果你还想留就明确说一声"保留 + 期望完成日期"，cron 加 `awaiting_user_decision` 标签。
- 【信息】EDN 周报本周缺位（已 3 天）按昨日决定不补救，等下周三（8-05）cron 自动覆盖；下周 EDN 触发后建议抽查上周 76 条证据是否仍可恢复（subagent session 在 state.db 里应还可见）。
- 【信息】8-01 之后会看到 GC Day #20 报告同步进 vault（如果 patch 成功落地），届时 vault 审计链补上；如未落地 cron 会按 SKILL §0-pre 升级规则记入自我改进。