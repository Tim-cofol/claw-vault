# Hermes 日报 - 2026-09-19

## 1. 老板速读
- SUP-115 (9-19 AI Builders 早报) autopilot 跑通：15 builder / 25 tweet，主题"Claude Code Projects 升级为本期主旋律"，飞书已送达（bot 身份），但 autopilot 漏翻 status → cron 已代 flip done。
- L4 静默状态正式解除：autopilot 自 9-15 SUP-111 起连续 5 天跑通（SUP-111 / SUP-112 / SUP-113 / SUP-114 / SUP-115），L4 假设"结构性 > 临时性"被部分证伪，根因暂判 acp 通道/上游 feed 临时性故障。
- state.db 实际踩 600 MiB 红线（630173696 bytes），红线第 36 天未拍板 VACUUM。
- lark-cli user identity 主动 refresh 成功（needs_refresh → ready，expiresAt 推到明日 01:51），连续 27 天闭环。
- `/tmp` 真零漂移稳态第 4 天：54 行（vs 昨日 54 行，+0%），按 ≥ 4 天稳态触发条件开始折叠报告口径。

## 2. 今日工作总览
- 09:02 GC Day #44 跑通（last_status=ok），MEMORY/USER 体检 + §-anchor audit。
- 08:00 autopilot (follow-builders) 跑通 SUP-115 AI Builders 早报 + 飞书 bot 身份投递 + 漏翻 status。
- 23:50 日报 cron 启动：lark-cli refresh 标配触发 → SUP-115 代 flip done → 日报写作 + git push + Get笔记同步。

## 3. 按项目 / 责任域汇报

**Hermes / Agent 系统**
- 【已闭环】SUP-115 (9-19 早报) cron 代 flip done：comment 含 `om_x100b65e9409828acb3c10beda246625` + chat_id `oc_d5de551525d3a4ae9e1a52815bb9a92b` + 完整 builder 摘要（15 builder / 25 tweet / 0 podcast / 0 blog / 主旋律 Claude Code Projects）→ 走快速路径直接 `multica issue status SUP-115 done`，全程 ~3 秒。
- 【已闭环】lark-cli refresh 标配连续 27 天：今日 user 段 `needs_refresh` → 主动 `lark-cli calendar +agenda`（零依赖 recipe，无 flag）→ status `ready`，expiresAt 2026-09-20 01:51（+15h）。
- 【已闭环】GC Day #44 09:02 跑通：MEMORY.md 1805 字符（占 82%）/ USER.md 1009 字符（占 73%），体检无超限；state.db 健康检查 + skill §-anchor audit 跑通。

**Multica / AI Builders / follow-builders autopilot**
- 【已闭环】L4 静默状态正式解除：9-04 ~ 9-11 静默 8 天后，9-12 SUP-108 跑通 + cron 代 flip → 9-13 SUP-109 自翻 → 9-14 SUP-110 cron 代 flip → 9-15 SUP-111 autopilot 跑通 → 9-16 SUP-112 cron 代 flip → 9-17 SUP-113 cron 代 flip → 9-18 SUP-114 cron 代 flip → **今日 9-19 SUP-115 cron 代 flip**。**连续 5 天 autopilot 跑通**（9-15 ~ 9-19 全部 create + 飞书送达），但 **status flip 自愈率仍仅 1/5 = 20%**（仅 9-13 SUP-109 自翻），cron 代 flip 流程仍是兜底必选。
- 【关键修正】L4 静默根因部分证伪：9-04 立 reference 时假设"L4 ≥ 4 天时长超出短时 acp 抖动，根因结构性 > 临时性"——9-12 SUP-108 自愈后连续 5 天 autopilot 跑通说明根因**很可能为临时性 acp 通道/上游 feed/API 故障**（无任何代码/config/prompt 改动）。

**调研 / 内容处理**
- 【今日无产出】EDN 周报下次触发 = 9-23 Wed 08:30（cron schedule `30 8 * * 3`）；今日非 EDN 触发窗口。EDN cron 上次真成功 = 9-09（mode 6 残件清理补丁已在 §0-pre.15 落地），9-16 模式 5（docs 落 + data_asset + origin 双缺位）数据缺口仍在。
- 9-16 EDN 飞书长住文档本周增量已写入（约 25K chars / 71 H1 / 15 H2 / 2 H3），用户实际已收到 weekly digest，仅结构化资产（data_asset JSON + origin 推送）不完整。

**其他**
- 【今日无新交付】周六静默日，user-driven Discord / 飞书轻问 = 0 session。

## 4. 重要交付物 / 系统变更
- 飞书 AI Builders 早报｜2026-09-19｜已送达｜SUP-115 (`om_x100b65e9409828acb3c10beda246625`, bot 身份) + 主题"Claude Code Projects 升级"。
- GC Day #44 cron output｜2026-09-19 09:02｜已生成｜`/root/.hermes/cron/output/8dc968bf2488/2026-09-19_09-02-47.md`（MEMORY 体检 + state.db 健康检查）。
- 日报自身｜2026-09-19｜已闭环｜本文件 / 即将 git push + Get笔记同步。

## 5. 风险、阻塞、需要拍板
- 🔴 **state.db 600 MiB 红线第 36 天 + VACUUM 窗口仍未拍板**：连续 5+ 周用户未拍板，今日起 §5 措辞维持 [🔴 紧急]。VACUUM 期间其他 cron 报 `database is locked`，必须用户拍板窗口期。**建议**：明日 0:00-2:00（cron 低活动期）手动跑 `sqlite3 /root/.hermes/state.db VACUUM`。
- 🔴 **EDN 8 条补丁仍未落地 GC Day 9-19（今天）**：连续 4+ 周 EDN 5 种失败模式（7-29 brotli / 8-19 stub-success / 9-02 mode 4 / 9-16 mode 5）+ 4 条旧补丁（timeout / subagent 落 JSON / 飞书 retry / tmp 孤儿）+ 1 条 stub-success 硬断言 + 1 条 data_asset 半自动兜底 + 1 条残件清理 + 1 条 EDN prompt 顺序重排 = **8 条独立补丁**。GC Day #44 cron 09:02 跑通但**未自动 patch EDN cron prompt**（GC Day 只体检 MEMORY/USER + 健康检查，不 patch 其他 cron）。
- 🟡 **Idea Digest 99992402 持续 17 周未投递**：`last_status='ok'` + `last_delivery_error='live adapter send failed: [99992402]'`——cron 跑通但飞书投递持续失败。workaround（切 `lark-cli im +messages-send` 绕开 gateway live-adapter）已 patch 进 `references/cron-health-judgment.md` §3 但未拍板切换 cron 配置。
- 🟡 **follow-builders skill status flip 强制收尾 patch 仍未落地**：SUP-5x 元模式连续 5 天 autopilot 跑通但 status flip 自愈率仅 20%——GC Day #44 (9-19) 应落地窗口已过（GC Day 自身不 patch 其他 cron 的 prompt）。下次窗口 = GC Day #45 (9-26 Sat)。

## 6. Hermes 自我改进
- 9-19 SUP-115 cron 代 flip 是 L4 解除后连续 5 天 autopilot 跑通的第 5 次兜底——**status flip 强制收尾 patch 紧迫性仍维持**（chat_id 自愈 100% / status flip 自愈仅 20% / 5/5 漏翻）。**GC Day cron 自身不 patch EDN / follow-builders skill 的 prompt**——必须用户拍板或单独的 cron prompt patch 任务。
- `/tmp` 真零漂移稳态第 4 天触发折叠口径：54 行 = 昨日 54 行（+0%）+ basename diff 空集 → §5.1 折叠成一行（"与昨天完全相同，第 N 轮'无幽灵延续项'第 4 天"）。今日新增 6 件 mtime < 24h 工件（`/tmp/digest-payload.{err,json}` / `fb-digest.txt` / `hermes-cwd-*` / `hermes-snap-*` / `paperclip.log`）全部为 autopilot 当日产物白名单 + Hermes 内部 scratch，**不算用户可读新工件**。
- lark-cli refresh 标配连续 27 天闭环完全自动化，今日 `lark-cli calendar +agenda`（零依赖 recipe）成功 trigger，未来 cron 启动时若 user 段 `needs_refresh` 第一反应 = 主动 trigger（已固化）。

## 7. 明日建议
- 【可自动做】明天 9-20 cron (cron schedule `0 9 * * 0` 周日) 无触发；日报 cron 9-20 23:50 仍按标配执行。
- 【需你确认】**VACUUM 窗口拍板**：连续 36 天呼吁未拍板，请回复 9-20 凌晨 0:00-2:00 是否可执行 `sqlite3 /root/.hermes/state.db VACUUM`（预计 3-5 分钟，期间其他 cron 报 `database is locked`，但 0:00-2:00 是低活动窗口）。
- 【需你确认】**EDN 8 条补丁是否一次性 patch EDN cron prompt**（job id `1d0cf41439ec`）：连续 4+ 周 5 种失败模式，建议 GC Day #45 (9-26 Sat) 前先手动 patch 上去（独立 cron prompt patch 任务即可，不等 GC Day）。
- 【需你确认】**Idea Digest 99992402 workaround 是否切换 cron 配置**：把 `deliver=origin` 改为 `lark-cli im +messages-send` 直接绕开 gateway live-adapter（8-27 SUP-100 autopilot 已自学此路径）。
- 【观察】autopilot 连续 5 天跑通趋势观察：L4 假设已被部分证伪（临时性 > 结构性），若 9-20 ~ 9-22 仍 100% 跑通且 status flip 自愈率提升到 ≥ 50%，下次 GC Day 可考虑"放宽 follow-builders skill status flip 强制收尾 patch 紧迫性"；若 status flip 自愈率仍 < 30%，patch 紧迫性维持。