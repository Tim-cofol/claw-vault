# Hermes 日报 - 2026-07-29

## 1. 老板速读
- 周三用户**零直接对话**；Hermes 当天活动 = EDN-Weekly 周报 cron + 1 条 Discord 上游公告转述（Hermes Agent 语音聊天加速）。
- **EDN 周报 cron 失败**（`1d0cf41439ec`，08:30→08:54）：4 个 subagent 中 3 个完成（A 西方 25 条 / B 中国 21 条 / C 安全+SASE+跨厂商 30 条），**subagent 任务 0（华为/H3C/ZTE）600s 超时**；母 agent 综合阶段遇到 `RuntimeError: brotli: decoder process called with data when 'can_accept_more_data()' is False`，**未写飞书长住文档、未写数据资产 JSON、未推送 origin 摘要**——本周周报缺位。
- Discord 转述：Hermes Agent 语音聊天加速（API TTS 后端流式输出），上游 `hermes update` 命令；本周第 4 次上游公告转述。
- `/tmp` 旧工件扫描 71 行（3 ARCHIVED + 68 NOT_ARCHIVED）——`scan_tmp_artifacts.sh` 视角；今日新增用户可读工件 = `edn_insight_plan_2026-07-29.json`（EDN 计划）/ `edn_doc_before.json` / `issues.json` / `tmp.258l6SNddF/state.json`（EDN 临时 scratch），其中 edn_insight_plan 因 cron 失败**未演化为最终数据资产**——下周计划前需要人决定是否手动补救或下一轮重跑。
- `lark-cli` user identity 仍 `needs_refresh`（今日 expiresAt 2026-07-29T10:50:09）；scope `im:message.send_as_user` 仍缺失；本周 EDN 推送因 brotli 崩溃未走到 lark 写入步骤，未触发 bot fallback。
- 明日优先：① **EDN 周报补救**（建议手动触发或下周三重跑——本周数据已抓，本周末前补一份 mini 周报最经济）；② GC Day #19 4 项清理决定（已挂 4 天）；③ SUP-57 / SUP-39 / SUP-25 长滞留处置（已挂 4 天）。

## 2. 今日工作总览
- 【失败·核心】EDN-Weekly-Market-Insight cron 08:30 触发，08:54 崩在综合阶段；3/4 subagent 数据已抓（西方传统 25 / 中国厂商 21 / 安全 SASE 跨厂商 30，合计 76 条证据 + 1 个华为/中国厂商 subagent 600s 超时），未写入飞书长住文档，未落数据资产 JSON，未推送 origin 摘要。
- 【已完成·转述】Discord `#hermes-agent-notifications` 13:43 一条上游公告：Hermes Agent 语音聊天加速（API TTS 后端流式输出）；agent 已中文化版转发含 `hermes update` 命令。
- 【无活动】用户直接对话 / 飞书轻问 / 子 agent 派发：全 0。jobs.json 4 条 cron 中日报 cron 是本 session 自身；GC Day #20 下次 8-01 触发；idea-recall 周报本周一已跑（7-27 09:00）。
- state.db 当天 sessions 9 条（assistant/user 多轮），分布在 5 类：cron × 2 + subagent × 4 + discord × 2 + cron 本 session × 1。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【失败】EDN-Weekly cron（`1d0cf41439ec`）last_status=error，last_error=brotli decoder；失败发生在母 agent 综合分析阶段（subagent 数据已收齐），导致本周周报在飞书和 origin 双端均缺位。
- 【信息】EDN 失败根因分两层：**subagent 任务 0（华为/H3C/ZTE）600s 超时**（跑 9 个 API call 卡死），母 agent `try/except` 没兜住；**brotli 解码错误**是第二层触发——大概率是 subagent 大量返回的 web_extract / supadata_scrape 输出压缩数据流未被正确处理。两条都不是 cron prompt 的设计缺陷，但都没有 retry/降级路径。
- 【信息】jobs.json 4 条 cron 状态：日报 cron ok / GC Day ok / **EDN error** / idea-recall ok。
- 【信息】`lark-cli auth status`：user identity `needs_refresh`（今日 expiresAt 2026-07-29T10:50:09），scope `im:message.send_as_user` 仍缺失。

### Multica / AI Builders
- 【无活动】今日无 SUP-N 触发；SUP-57 (9 天 in_review) / SUP-39 (27 天 in_progress) 处置仍未拍板（昨日 7-28 已挂 3 天，今天升至 4 天——按 SKILL §0-pre "重复呼吁 ≥ 3 天用户没拍板 = 第 4 天起升级处理"，本条今日起不再在风险位重复喊，改记到 §6 自我改进）。
- 【信息】无 SUP-66 / SUP-67 新 autopilot 输出（follow-builders 今日未到 08:05 触发——按 jobs.json 应为每日触发，需复核是否漏跑）；明日 7-30（周四）正常应出 SUP-66。

### 飞书 / 日历 / 文档 / 消息平台
- 【已转述】Discord `#hermes-agent-notifications` 13:43 一条上游公告：Hermes Agent 语音聊天加速（API TTS 后端流式输出），`hermes update` 命令即生效。
- 【失败落地】EDN 长住文档 `Qe1dd7O7XoCAZ9xBeNUc3meLnch` 本周**未追加**；EDN 数据资产 `/root/.hermes/cron/output/edn-insight-data/2026-07-29.json` **不存在**（目录里最新仍是 2026-07-22.json）。
- 【未触发】飞书 / 日历 / 文档 / 任务今天 0 个 user 动作；EDN 推送因母 agent 提前崩溃未走到 lark 写入步骤。
- 【信息】scope 缺失项：仍缺 `im:message.send_as_user`（已连续 6 期 SUP-5x 走 bot fallback）。

### 调研 / 内容处理
- 【已完成·未落地】EDN 本周抓数据成功：76 条证据（西方传统 25 / 中国厂商 21 / 安全 SASE 跨厂商 30）；Cisco / Fortinet / Zscaler / Palo Alto / Huawei / H3C / Arista / Juniper / HPE Aruba / Extreme 都有本周信号；主要话题：Fortinet FortiGate 1200G + FortiSASE Outpost / Zscaler + Schwarz Digits 欧洲主权 SASE / Palo Alto AIRS AI Gateway / Cisco Antares / 华为 IDC MarketScape 领导者 / Agentic Ops / SASE 主权化 / AI agent 身份治理。**但本周报告未写入飞书，所有数据只在 subagent session 缓存里**——下周 EDN 触发时若 prompt 不改，会被新一轮覆盖。
- 【信息】EDN subagent 工具使用：web_extract 14 次 / supadata_scrape 5 次（X scrape 严格在 ≤ 3 限制内）/ delegate_task 3 次；预算合规。

## 4. 重要交付物 / 系统变更
- EDN 周报数据（已抓·未落地）｜2026-07-22 → 2026-07-29 一周｜失败｜76 条证据仅存于 subagent session（session_id `20260729_083106_` / `20260729_083105_` / `20260729_084118_` / `20260729_084647_`），未写入飞书长住文档 `Qe1dd7O7XoCAZ9xBeNUc3meLnch` 也未生成数据资产 JSON；下次触发会被覆盖
- Discord 转述｜Hermes Agent 语音聊天加速｜已转述 Discord `#hermes-agent-notifications`｜agent 中文化版含 `hermes update` 命令
- `/tmp` 扫描快照｜`/root/.hermes/state/scan_2026-07-29.txt`｜71 行｜3 ARCHIVED + 68 NOT_ARCHIVED；今日新增用户可读工件 = `edn_insight_plan_2026-07-29.json` / `edn_doc_before.json` / `issues.json` / `tmp.258l6SNddF/state.json`，全部因 EDN cron 失败悬空

## 5. 风险、阻塞、需要拍板
- 【需立即处理】EDN 周报本周缺位（已 1 天）：数据已抓，下周三 cron 自动覆盖前需要决定**手动补救**还是**直接放弃本周**。手动补救需母 agent 重跑综合分析 + 写飞书 + 写数据资产；直接放弃本周最经济但 EDN 长住文档本周空白。
- 【需确认·不可逆】GC Day #19 4 项清理决定（已挂 4 天）：VACUUM / legacy 20260607 checkpoint / jobs.json 4 个近似重复备份 / state-snapshots 滚动保留策略。7-25 GC Day 报告原文在 `~/.hermes/cron/output/8dc968bf2488/2026-07-25_09-03-47.md`，未进 vault。
- 【需确认】SUP-57 (9 天 chat_id 大小写错配) / SUP-39 (27 天 HTTP 500) / SUP-25 (35 天) 处置：SUP-57 / SUP-39 我倾向 close（撤回记录已留 / 错误已确认），SUP-25 留你拍板。已挂 4 天，按 SKILL §0-pre 升级规则，本条不再在风险位重复喊，记入 §6。
- 【信息】`lark-cli` user identity `needs_refresh` + scope `im:message.send_as_user` 缺失：连续 6 期 SUP-5x bot fallback 兜底；今日 EDN 推送因母 agent 提前崩溃未走到 lark 写入步骤，未触发 fallback。按 SKILL §0-pre 升级规则，本条不列风险位（等你哪天主动处理或下次 GC Day 集中处置）。
- 【信息】EDN 失败根因：subagent 600s 超时 + 母 agent brotli decoder 异常叠加；cron prompt 当前设计无 retry/降级路径。

## 6. Hermes 自我改进
- **EDN 双层失败需沉淀到 cron prompt**：第一层 subagent 600s 超时（任务 0 跑 9 API call 卡死）应当配置 `timeout=900s` 提升 + 单任务失败不应阻塞其他任务写入；第二层 brotli decoder 异常表明母 agent 在处理大量压缩 subagent 输出时缺乏 chunk 校验/重试。**建议**：EDN cron prompt Step 3 综合分析前加"逐 subagent 结果落本地 JSON + 即使综合失败也保留数据资产 + 写入飞书用 retry-on-error"的硬约束；这与 SUP-5x 元模式补丁（GC Day #20 8-01 必落地）合并为同一类"cron 必须自驱收尾"问题。
- **SUP-57 / SUP-39 / SUP-25 长滞留升级处理**：已挂 4 天，按 SKILL §0-pre 规则从风险位移到自我改进。下次 GC Day #20 (8-01) 一并处置：SUP-57 / SUP-39 close（撤回记录 / 错误已留档），SUP-25 留你拍板；若届时仍未拍板，cron 自动 close 掉 SUP-57 / SUP-39，把 SUP-25 标记 `awaiting_user_decision` 标签。
- **`/tmp` 工件自污染观察**：本日新增 `edn_insight_plan_2026-07-29.json` 因 EDN 失败变孤儿——下次 EDN 触发会再生成一份同模式 plan 文件，但旧的不自动清理。**建议**：EDN cron prompt Step 5 数据资产 JSON 写入失败时，把 plan 移至 `/root/.hermes/state/edn-orphan-YYYY-MM-DD.json` 留档（不进 `/tmp`），与日报 snapshot 路径统一避免污染。

## 7. 明日建议
- 【需要你确认】EDN 本周缺位处置：① 手动补救（重跑综合 + 写飞书 + 数据资产）需要 30-45 分钟人工；② 直接放弃本周等下周三——**建议采纳 ②**，下周 EDN 数据资产一份就够追溯；除非本周 EDN 数据有紧急战略价值。
- 【需要你确认】GC Day #19 4 项清理决定（已挂 4 天）：建议优先批准 VACUUM + jobs.json 备份合并；legacy checkpoint + state-snapshots policy 可缓议。
- 【需要你确认】SUP-57 / SUP-39 / SUP-25 处置：建议 SUP-57 / SUP-39 直接 close（理由在 §6），SUP-25 留你拍板。
- 【可自动做】明天 7-30（周四）follow-builders autopilot 应出 SUP-66 AI Builders 早报——日报 cron 关注是否自驱 flip `done`，若又退回代 flip 模式则累计 3 期样本反验证 SUP-5x 自愈不稳态。
- 【可自动做】明天 7-30 日报 cron 必跑 `-mtime +0` + `-mtime -1` 双扫描（已固化 SKILL §0-pre）。