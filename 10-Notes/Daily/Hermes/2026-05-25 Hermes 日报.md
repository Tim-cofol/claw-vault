# Hermes 日报 - 2026-05-25

## 1. 老板速读
- 今天主要产出是排查 Devbox Hermes 运行状态、Tailscale exit node 和模型配置，没有做大规模代码改动。
- Devbox Hermes Gateway 实际是运行中的，CLI 也能成功调用；早上的失败更像 OpenAI Codex 间歇性连接/超时，不是服务挂死。
- Devbox 已启用 Tailscale exit node，IPv4 确认走 VPS 新加坡出口；但 IPv6 仍直连，存在绕过 exit node 的风险。
- Devbox 目前没有 fallback model/key，Codex 抽风时会直接失败，这是今天最明确的系统风险。
- 技能库后台完成一次收敛：把 5 个窄技能归并进 2 个 umbrella skill，降低后续检索和维护成本。
- 需要拍板：是否给 Devbox 配置 fallback，以及是否禁用 IPv6 或补齐 IPv6 exit node 路由。
- 明日优先建议：先把 Devbox 的模型 fallback 和网络出口策略补齐，再做更大的 Hermes 性能/工具集优化。

## 2. 今日工作总览
- 处理 Devbox Hermes 启动状态：已确认 Gateway 正常运行、CLI 可用，并定位到间歇性 API 连接问题。
- 处理 Devbox Tailscale 网络出口：已确认 IPv4 走 VPS exit node，发现 IPv6 泄漏/绕过风险。
- 处理当前模型配置查询：已确认当前 VPS 会话使用 `gpt-5.5` + `openai-codex`，smart model routing 关闭。
- 处理通知转述：转发 Deepseek V4 Flash FREE 回到 Nous Portal 的信息。
- 后台 curator 做了技能库整理：完成两组 class-level umbrella consolidation，未删除技能，只做归档和合并。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已完成】检查 Devbox Hermes Gateway；服务处于 active/running，未重启，避免打断现有 gateway/kanban 状态。
- 【已完成】验证 Devbox Hermes CLI；用完整路径调用成功返回 OK，说明本地执行链路可用。
- 【风险】Devbox 非 login shell 下 `hermes` 不在 PATH；直接 `ssh devbox 'hermes ...'` 会失败，后续自动化要用完整路径或修 PATH。

### 网络 / Devbox 运维
- 【已完成】检查 Tailscale exit node；IPv4 出口已走 VPS，新加坡公网 IP 与 VPS 一致。
- 【风险】IPv6 没走 Tailscale exit node；默认请求可能从本地 IPv6 出口直连，建议禁用 IPv6 或补齐 exit node IPv6 路由。
- 【建议】Devbox 需要配置模型 fallback；否则 OpenAI Codex 网络/API 间歇故障会直接造成用户侧失败。

### 知识库 / Skills
- 【已完成】后台 curator 将 `hermes-model-fallbacks`、`hermes-multi-host-operations`、`messaging-gateway-topic-behavior` 合并进 `hermes-operations`。
- 【已完成】后台 curator 将 `artifact-recovery`、`linux-server-maintenance` 合并进 `linux-host-recovery-maintenance`。
- 【已完成】合并采用归档而非删除，保留原技能内容可追溯，同时减少窄技能泛滥。

### 消息平台 / 通知
- 【已完成】转述 Deepseek V4 Flash FREE 回到 Nous Portal 的信息；这是信息同步，不涉及配置变更。
- 【已完成】回答当前模型配置；确认当前 VPS live config 为 `gpt-5.5` / `openai-codex`。

## 4. 重要交付物 / 系统变更
- 运维结论｜Devbox Hermes Gateway 状态｜已确认运行｜无需重启。
- 运维结论｜Devbox Tailscale exit node｜IPv4 已走 VPS，IPv6 未走｜需后续处理。
- 配置结论｜当前 Hermes 模型｜已确认｜`gpt-5.5` / `openai-codex`，smart routing 关闭。
- Skill｜hermes-operations｜已创建/合并｜承接 Hermes fallback、多主机运维、消息网关 topic/thread 经验。
- Skill｜linux-host-recovery-maintenance｜已创建/合并｜承接 artifact recovery 与 Linux server maintenance 经验。

## 5. 风险、阻塞、需要拍板
- Devbox 没有 fallback model/key；建议同步 VPS 上可用 fallback 配置，或明确 Devbox 只接受 Codex 单点失败风险。
- Devbox IPv6 会绕过 exit node；建议二选一：短期禁用 IPv6，或正式配置 VPS/Tailscale 的 IPv6 exit route。
- `hermes` 命令在非 login shell 不可用；建议修正 PATH，避免远程自动化脚本误判 Hermes 未安装。
- 今天未能从 session 中确认是否已经对 Devbox fallback 或 IPv6 做实际配置变更；不要把上述建议视为已落地。

## 6. Hermes 自我改进
- 技能库应继续向 class-level umbrella 收敛，避免每次故障都沉淀成一个窄技能，降低检索噪音。
- Devbox/VPS 运维检查可以固化为标准 runbook：服务状态、CLI 调用、provider 超时、fallback、Tailscale IPv4/IPv6 出口一次性检查。
- 对“服务没启动吗”这类问题，正确路径是先验证 service + CLI + provider，再决定是否重启；今天避免了不必要重启。

## 7. 明日建议
- 【可自动做】给 Devbox 增加模型 fallback 检查清单，并核对是否能复用 VPS 的 MiniMax fallback 配置。
- 【需要你确认】Devbox 是否允许短期禁用 IPv6，以消除 exit node 绕过风险。
- 【可自动做】修复 Devbox 非 login shell 的 Hermes PATH，保证 SSH 自动化命令可直接调用。
- 【可自动做】把 Devbox Hermes 健康检查整理成一个脚本或 Hermes operation playbook。
- 【需要你确认】是否把技能库 umbrella consolidation 策略设为 curator 的长期默认规则。
