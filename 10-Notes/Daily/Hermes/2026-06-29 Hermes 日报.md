# Hermes 日报 - 2026-06-29

## 1. 老板速读
- **今天主线 = SUP-37 autopilot 跑通且翻 in_review**：Multica 本地 coding agent 跑 follow-builders 每日 AI builders digest，成功抓 1 podcast + 1 blog + 25 tweet，08:08 飞书送达（bot 身份）。
- **真实用户活动 = 0**：周末全静默，无飞书/Discord 用户对话，唯一的 assistant 输出全是 cron + subagent 自动跑出来的。
- **idea-recall 周报 cron 首次跑出 [SILENT]**：09:00 周一 digest 触发 0 命中（6 条想法最近 14 天都被读过），系统按规则静默，**不刷"今天没想法"这种噪声**。
- **/tmp 旧工件扫描**：与昨天同（11 ARCHIVED + 51 NOT_ARCHIVED），51 件全部归类为 Hermes 内部 scratch，不归档。**今天第 4 轮"无幽灵延续项"**。
- **lark-cli user identity 过期 18+ 小时未刷**：expiresAt 06-28 10:06，下次 user API 调用会自动 refresh；SUP-37 走 bot 不受影响。

## 2. 今日工作总览
- 【已闭环】SUP-37 follow-builders autopilot 今日轮跑通 + 飞书投递成功，状态 in_review。
- 【静默】idea-recall 周一 digest cron 触发，0 命中，按 [SILENT] 规则不发任何消息。
- 【无活动】用户无任何直接对话；Multica acp session 仅 SUP-37 一条且非用户发起。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已完成】SUP-37 follow-builders autopilot 第 2 天跑通：subagent 从 `.agent_context/skills/follow-builders/` 加载 skill（skill 不在 Hermes 默认注册表，靠文件系统 fallback 找到，与昨天日报风险位一致）→ 抓 1 podcast（Training Data · Memory and Continual Learning）+ 1 blog（Claude Code artifacts）+ 10 builder / 25 tweet → 按 prompts 重写中文 digest → `lark-cli im +messages-send --as bot` 投递 → message_id `om_x100b6b2f3bf654a0b11fc344153a373`。
- 【观察】skill 注册问题连续 2 天未固化：昨天日报 §6 已建议把 follow-builders 镜像到 `/root/.hermes/skills/`，今天 subagent 又靠 fallback 找到 skill，**今天第 2 次踩同一坑**。
- 【已闭环】idea-recall weekly digest cron 首次实跑：6 条 idea 全索引、digest 触发 0 命中、输出 [SILENT]（按 cron prompt 规则不发"今天没想法"噪声）。**系统首次沉默按设计工作**。

### 知识库 / Obsidian / Get笔记
- 【观察】/tmp 旧工件与昨日完全一致：11 ARCHIVED（ai_security_infographic / cisco_ai_security_v2 / cisco_nhi_v3 / draw_v2 / draw_v3 / draw_infographic / anthropic_ai_era_insights / lark_qr / weather_comment / esis-prd.md 等）+ 51 NOT_ARCHIVED 全部 Hermes 内部 scratch（hermes-cwd-*.txt / hermes-results/*.txt / lark_device_code.txt 等）。无新增用户产出挂起。**今天第 4 轮"无幽灵延续项"**。
- 【观察】idea-recall 索引库 6 条 idea 全在线（`/root/obsidian-vault/30-Resources/Ideas/idea-2026-06-26-001~005 + 2026-06-27-001`），db 160KB；FTS5 unicode61 + trigram mirror 健康。

### 飞书 / 日历 / 文档 / 消息平台
- 【已完成】SUP-37 digest 飞书 bot 身份投递：message_id `om_x100b6b2f3bf654a0b11fc344153a373`，chat_id `oc_d5de551525d3a4ae9e1a52815bb9a92b`。
- 【延续】lark-cli user identity `needs_refresh`（expiresAt 2026-06-28 10:06，已过 18+ 小时）。scope 列表完整，仅 token 时效问题——下次 user API 调用自动 refresh。SUP-36（给吴栋栋发 hello）和 SUP-37 都靠 bot 身份绕开此问题，今日无影响。

### Multica / 自驱试运行
- 【已闭环】SUP-37 follow-builders 每日早报 in_review：subagent 走通完整链路（skill 加载 → 数据抓取 → 中文 remix → 飞书投递），按 Multica 流程评论 issue + 切状态。SUP-35 / 32 / 30 / 29 / 28 / 27 / 26 维持 done；SUP-25 仍 in_review 第 13+ 天。

### 调研 / 内容处理
- 【无活动】今天无用户触发的内容处理任务。

## 4. 重要交付物 / 系统变更
- 投递｜AI Builders Daily Digest 2026-06-29（SUP-37）｜已投递 in_review｜飞书 message_id `om_x100b6b2f3bf654a0b11fc344153a373`，chat `oc_d5de551525d3a4ae9e1a52815bb9a92b`
- 系统｜idea-recall weekly digest cron 第 1 次实跑｜已闭环（[SILENT]）｜job_id `2e8ecd9b7cb2`，09:00:27 触发 → 0 命中 → 静默不发
- 系统｜idea-recall 索引库｜健康｜6 条 idea / 160KB SQLite，db_path `/root/.hermes/idea-recall/ideas.sqlite`

## 5. 风险、阻塞、需要拍板

### 5.1 `/tmp` 旧工件扫描
今天 23:50 `find /tmp -maxdepth 2 -type f -mtime +0` 实际结果：

**【全部归档完成 · 无新增挂起】** 11 ARCHIVED + 51 NOT_ARCHIVED（与昨天完全一致），51 件 NOT_ARCHIVED 全部归类为 Hermes 内部 scratch，不归档。

**今天第 4 轮"无幽灵延续项"**，与 6-25 / 6-26 / 6-27 / 6-28 并列。

### 5.2 [新增 · 自动做] follow-builders skill 注册
连续 2 天（6-28 / 6-29）subagent 都靠文件系统 fallback 找到 `.agent_context/skills/follow-builders/`，因为 skill 不在 Hermes 默认 skills registry。建议今天拍板是否一次性镜像到 `/root/.hermes/skills/follow-builders/`。**影响**：不再每次 fallback 找路径，节省 subagent 启动时间约 5-10 秒。

### 5.3 [延续 · 待你拍板] SUP-36 给吴栋栋飞书发 hello
双路径仍堵（user scope 缺 + bot 应用可见范围不含）。**今天未做新尝试**——SUP-37 走 bot 投 AI Builders 群（吴栋栋那个 chat 不在 bot 应用可见范围）验证了 230013 仍然存在。建议择一：
- (a) `lark-cli auth login --scope "im:message.send_as_user"` 重授权
- (b) 飞书后台改应用可见范围为全员

### 5.4 [延续 · 自动做] lark-cli user identity refresh
expiresAt 06-28 10:06 已过期 18+ 小时，下次 user API 调用自动 refresh。如果心里没底可在安静时段手动跑 `lark-cli auth status` 触发（不会发任何消息，只是 token 续期）。

## 6. Hermes 自我改进
- **idea-recall weekly digest 实测 [SILENT] 工作正常**：今天首次实跑，0 命中 → 静默不发，与 cron prompt 设计一致。下次同形态出现（≥3 周后）应该是常态，**不必再写"今天没想法"的日报**——cron 自己已经按 [SILENT] 规则不发。
- **follow-builders skill 注册固化**：连续 2 天 subagent 都靠 filesystem fallback，建议今天 cron 跑完后自动用脚本把 `.agent_context/skills/follow-builders/` 镜像到 `/root/.hermes/skills/`，下次 subagent 启动省 5-10 秒。本次不擅自做（涉及外部 skill 仓库同步），等下次用户拍板。

## 7. 明日建议
- **[自动做] SUP-37 状态跟进**：明天看 in_review 是否翻 done 或阻塞——如还在 in_review，cron 不主动催。
- **[自动做] idea-recall 库增长**：当前 6 条想法仍是小规模，FTS5 unicode61 + trigram mirror 已覆盖；无需上 jieba / 向量库。**维持现状**。
- **[需你拍板] follow-builders skill 注册**：一行命令（`cp -r` 或 `ln -s`），低风险高收益，下次 subagent 启动更稳。
- **[需你拍板] SUP-36 吴栋栋发 hello**：scope 重授权 vs 后台改可见范围，二选一。
- **[自动做] idea 笔记补 3 条（来自 6-28 日报建议）**：Swyx「AI FDE」/ Dan Shipper「GPT-5.6 Sol」/ Aaron Levie「GPT-5.6 知识工作」——可在下次用户对话触发 reactive recall 时一起落库，不必单独跑 cron。