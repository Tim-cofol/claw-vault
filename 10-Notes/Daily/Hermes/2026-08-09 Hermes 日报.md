# Hermes 日报 - 2026-08-09

## 1. 老板速读
- **周日静默日，无用户对话、无 autopilot、无 Discord/飞书转述**：state.db 今日只新增 1 个 session，即本日报 cron 自身。
- **系统总体可用**：4 个 enabled cron 全部 `ok`，Slack `Session is closed` 未再上报新错误，gateway 在线。
- **state.db 持平 483 MiB**：与昨日一致，距 500 MiB 红线约 17 MiB；周末 cron 少，单日 0 MB 增量符合预期。
- **lark-cli user identity `needs_refresh`**：今日无任何飞书投递动作，未触发自动 refresh；scope 列表仍缺 `im:message.send_as_user`，延续昨日风险位。
- **`/tmp` 实扫 76 行（3 ARCHIVED + 73 NOT_ARCHIVED）**：与昨日 75 行相比 +1 件 `all_not_arch.txt`（昨日 cron 调试产物，应排除）；新鲜用户可读工件扫描为 0 件。
- **明日优先**：周一 8-10 起 follow-builders autopilot 可能恢复；建议先用最小 user-API 调用触发一次 lark-cli token refresh，再处理 Idea Recall Weekly Digest 投递错误与 state.db 维护窗口。

## 2. 今日工作总览
- 【已核对】完成 `/tmp` 双口径扫描（`scan_tmp_artifacts.sh` 76 行 + `-mtime -1` 对偶 0 件）、state.db size 取数、cron 状态、lark-cli auth status、昨日日报复读。
- 【静默】今日无显著 Hermes 活动：无用户对话 session、无 acp/subagent 派发、无 Discord 转述、无飞书投递。
- 【维护】本日仅由日报 cron 自身产生 1 个 session，符合周日静默日预期。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【静默】今日 0 个 user-facing session；state.db 仍为 483 MiB（与昨日持平，单日 0 MB 增量）。
- 【稳定】4 个 enabled cron 状态全部 `ok`；上次 GC Day 跑通为 8-08（cron `8dc968bf2488`），下次为 8-15。
- 【待维护】MEMORY.md 1805/2200 字符（82%）、USER.md 1009/1375（73%），距上限仍有空间，无需本周迁移。

### 飞书 / 日历 / 文档 / 消息平台
- 【无活动】今日无任何 lark-cli 投递，无飞书日历/文档操作；lark-cli user identity 保持 `needs_refresh`，下次 user-API 调用会触发自动 refresh。
- 【延续风险】scope 列表未见 `im:message.send_as_user`；需补 OAuth scope，避免后续投递静默回退 bot 身份。
- 【稳定】Slack `Session is closed` 今日未新增错误；与昨日"观察是否自愈"一致，暂不升级处理。

### 知识库 / Obsidian / Get笔记
- 【已完成】本日报正文已写入 Obsidian `10-Notes/Daily/Hermes/2026-08-09 Hermes 日报.md`，作为今日审计记录保存。
- 【静默】vault 工作树今日干净，无新增 commit；Get笔记 sync 将在 §3 标准流程完成后执行。

### OpenClaw / 代码项目 / GitHub
- 【静默】今日无代码项目改动、无 GitHub 操作、无 commit。

## 4. 重要交付物 / 系统变更
- 日报｜Hermes 日报 2026-08-09｜已写入｜路径见 §3 知识库。
- 监控记录｜`state_db_size.log`｜已 append 8-09 行（483 MiB，连续第 4 天 ≤ 484 MiB 稳态）。
- 扫描 snapshot｜`/root/.hermes/state/scan_2026-08-09.txt`｜已落地｜76 行，ARCHIVED/NOT_ARCHIVED 已分类。

## 5. 风险、阻塞、需要拍板
- 【需你确认】lark-cli user identity `needs_refresh` 与 scope 缺 `im:message.send_as_user` 仍是延续风险位（连续 ≥ 3 天）。周一 autopilot 恢复前建议先做一次 `lark-cli auth login --scope "...,im:message.send_as_user,..."` 重授权，避免 8-10 早报 SUP-81 投递再次静默回退 bot 身份。
- 【需你确认】Idea Recall Weekly Digest `[99992402]` 投递错误仍未定位（昨日已记），周一可手动做一次最小消息回归。
- 【需你确认】state.db VACUUM 维护窗口仍未安排；483 MiB 距 500 MiB 红线约 17 MiB，今日 0 MB 增量提供少量缓冲，但下周 cron 恢复后增长会重启。
- 【可观察】Slack `Session is closed` 今日无新增；继续观察 1-2 天，若稳定沉默则降级为非风险项。
- 【无需升级】`/tmp` NOT_ARCHIVED 73 件中 63 件为 Hermes 内部 scratch（autopilot / EDN / hermes-cwd / hermes-results / fb-* / scan-* / build_digest / inspect / fts5_benchmark 等），剩余 10 件均为历史 session 残留，无今日新增用户可读工件；不展开列。

## 6. Hermes 自我改进
- 静默日扫描已收敛到 5 分钟内完成（双口径 `/tmp` + state.db size + cron 状态 + lark-cli auth + 昨日日报复读），无需复杂调试；但扫描结果中 63/73 件为 Hermes 内部 scratch 的事实说明 `/tmp` 整体清洁度长期偏低，下次 GC Day 8-15 可顺手把 autopilot / EDN 跑完后的内部残件清理纳入 cron prompt 标准收尾（与 8-06 EDN 残件清理规则合并）。
- 周日静默日的"真零漂移"判定已稳定——连续 2 天 basename diff ≤ +1 且新增件均在 cron 内部 scratch 白名单内；按 ≥ 4 天折叠规则，今天继续展开，明天若仍相同则可折叠成一行（参考 8-08 实测）。

## 7. 明日建议
- 【可自动做】周一 8-10 早晨 first user-API 调用时主动观察 lark-cli token 是否自动 refresh；若 `needs_refresh` 持续未解，主动报告一次，避免拖延到 autopilot 跑通后再发现。
- 【需要你确认】确认是否在周一对 follow-builders autopilot 跑通前补一次 `lark-cli auth login --scope` 重授权，避免 SUP-81 投递身份再次错配。
- 【可自动做】继续观察 Slack `Session is closed`；若 8-10 仍无新增，cron 可在 8-11 日报把它从风险位降级。
- 【可自动做】Idea Recall Weekly Digest 投递回归测试（最小消息），定位 `[99992402]` 字段错误根因。
- 【需要你确认】决定 state.db VACUUM 维护窗口的优先级——下周 cron 恢复后单日增量可能回升到 +5-20 MB，500 MiB 红线压力将重新出现。