# Hermes 日报 - 2026-08-23

## 1. 老板速读
- 静默日第 13 天：今日无用户直接对话、无 Discord 上游公告转述；Hermes 主线 = 1 个 autopilot（SUP-96 8-23 早报）+ 本日日报 cron 自身。
- **SUP-96 autopilot 首次"自翻 done + 自愈 chat_id + 完整闭环"**：08:03 飞书 bot 身份送达 `om_x100b6797c0ec6ca4b22d783d3160e69` + comment 显式说明 `OC_xxx → oc_xxx` 大小写兜底（连续 4 次自愈，SUP-80/83/85/92/96）→ autopilot **自翻 done**（updated_at 00:04:01Z, rev=4, 无 cron 介入）。**纯 follow-builders autopilot 累计 32 样本自愈率回升到 81%**。
- state.db 513 MiB（红线第 9 天，VACUUM 窗口仍未拍板）。
- EDN 周报连续 2 周跑空延续（8-12 / 8-19 stub-success 误报）；**8-26 下周三触发前 EDN cron prompt 5 条补丁仍未 patch**，今日 GC Day #36 仍未触及（EDN cron 是独立 job，GC Day 不自动 patch 其他 cron）。
- lark-cli user identity 今日 `needs_refresh`（昨日 cron 主动 trigger refresh 路径今日未执行——新日报 cron 应把这条动作写进标配最后一步）。

## 2. 今日工作总览
- 静默日。Hermes 主线活动 = 1 个 autopilot（SUP-96 8-23 早报 08:03 飞书 bot 身份投递 + autopilot 自翻 done）+ 1 个 cron 自身（23:50 本日日报）。
- 无新写 Vault 文件、无 Get笔记 新同步（除日报本体）、无 git commit（除日报本体）。
- SUP-96 当日不需补 flip（autopilot 自驱闭环）；SUP-88/77/75 仍 in_review 是历史 Mode E / EDN 业务随行项目遗留。
- lark-cli user identity refresh 今日 cron 启动时未执行主动 trigger（昨日成功路径今日未延续），仍为 `needs_refresh` 状态。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已闭环·首次】SUP-96 (8-23 早报) **autopilot 全自驱闭环**：08:03 飞书 bot 身份投递 `om_x100b6797c0ec6ca4b22d783d3160e69` + chat_id `oc_d5de551525d3a4ae9e1a52815bb9a92b`（大小写正确）+ 16 builder / 34 tweets / 1 播客（No Priors Chess.com CEO Erik Allebest）/ 0 博客 + comment 显式说明 `OC_xxx → oc_xxx` 自愈（**chat_id 自愈连续 4 次 SUP-80/83/85/92/96 + SUP-91 累计 5 次全部成功**）+ **autopilot 自己翻 done**（rev=4, updated_at 2026-08-23T00:04:01Z, 无 cron 介入）。**累计 34 样本 SUP-5x 元模式（SUP-63~SUP-96）= 26 done / 4 blocked / 3 in_review / 1 cron 代 flip = 76% 自愈率**；纯 follow-builders autopilot 32 样本 ≈ **81% 自愈率**（仍含 4 blocked: SUP-72/73/74/79）。
- 【延续】SUP-88 (8-15 早报 Mode E 失败) 仍 in_review 第 8 天——autopilot 写中文 comment 但**完全跳过 fetch + 完全跳过飞书投递**，用户 8-15 没收到任何 AI Builders 早报。**用户仍未拍板**：今天手动把 SUP-88 comment 转贴飞书 vs 永久 skip 此期。
- 【延续·EDN 业务随行】SUP-77 / SUP-75 仍 in_review（更新于 8-04），是 EDN 业务随行第三方对接项目遗留，非 autopilot 失败模式。
- 【未触发】lark-cli user identity refresh 标配动作：昨日 cron 主动 trigger `read_status --as user` 成功（status `ready`, expiresAt 推到 8-23 01:54），今日 cron 启动时未延续此动作（仍 `needs_refresh`）。**明日 cron 应把"启动时主动 trigger 一次 user API 调用"写成标配最后一步**。

### 知识库 / Obsidian / Get笔记
- 今日无新写笔记（除日报本体）。无用户主动 sync 请求。
- vault HEAD `5f6e06c` = 8-22 日报 commit，今日日报 commit 即将提交。

### 飞书 / 日历 / 文档 / 消息平台
- 【已完成】SUP-96 autopilot 飞书投递：bot 身份（user identity 缺 `im:message.send_as_user` scope，已自动回退 bot），08:03:46 实际送达。
- 【未触发】lark-cli user identity refresh：今日 cron 启动时未主动 trigger，沿用昨日路径的标配动作明日 cron 应自动应用。

### 调研 / 内容处理
- 无主动调研 / 内容处理。SUP-96 autopilot fetch + deliver + flip 走通（无本日 cron 介入任何阶段）。

### 其他 / 系统健康
- `/tmp` 旧工件扫描：8-22 100 行 → 8-23 **102 行**（+2 autopilot 当日产物 `/tmp/fb-digest.{json,md}` mtime 8-22 08:01-08:03），basename diff 净 0 件用户可读新工件；按 SKILL §0-pre 折叠规则今日 §3「其他 / 系统健康」只报一行。**`-mtime -1` 今日新增 = 3 件 Hermes 内部 scratch**（`hermes-cwd-*.txt` / `hermes-snap-*.sh` / `paperclip.log`），全部 cron 自身产物，不计入用户可读工件。
- state.db 513 MiB（红线第 9 天）：8-22 512 → 8-23 513（+1 MiB 稳态）。**未拍板 VACUUM 窗口已 11 天**（8-13 首次列入风险位）。今日 GC Day #36 报告建议"下周六 GC Day 前 1 小时"窗口仍待确认。
- EDN cron 残件（`/tmp/hermes-results/` / `/tmp/edn-*/`）：今日为空（8-19 失败残件已被 GC Day #36 报告覆盖）。
- **EDN cron prompt 5 条补丁（含 stub-success 硬断言）仍未 patch**：今日 GC Day #36 跑通未触及该 prompt（EDN cron 是独立 job `1d0cf41439ec`，GC Day 不会自动 patch）。**8-26（下周三）下次 EDN 触发会重蹈 8-19 stub-success 跑空**——EDN 已连续 2 周跑空，**不能再跑空第 3 周**。

## 4. 重要交付物 / 系统变更
- 类型｜名称｜状态
  - 日报｜Hermes 日报 2026-08-23｜撰写中（本笔记）
  - Multica issue｜SUP-96 (8-23 早报)｜**done**（autopilot 自翻，08-23 00:04）
  - 飞书消息｜SUP-96 中文早报｜**已投递** bot 身份 `om_x100b6797c0ec6ca4b22d783d3160e69`
- 普通 session / 普通命令验证不列。

## 5. 风险、阻塞、需要拍板
- **【🔴 紧急·第 11 天未拍板】state.db 513 MiB 持续超 500 MiB 红线**：上次拍板窗口建议 8-19/8-22 均未确认；cron 不可自行 VACUUM（锁库风险），需你明确"X 日凌晨"窗口。**当前最大阻塞**。
- **【🔴 紧急·第 9 天未拍板】EDN cron prompt 5 条补丁（含 stub-success 硬断言）未 patch**：8-26（下周三）下次 EDN 触发前必须 patch 上 `/root/.hermes/cron/jobs.json` job id `1d0cf41439ec`；今日 GC Day #36 跑通未触及该 prompt（GC Day 只体检 + skill patch，不自动 patch 其他 cron 的 prompt）。**否则 8-26 必然重蹈 8-19 stub-success 跑空**——EDN 已连续 2 周跑空，**不能再跑空第 3 周**。
- **【第 9 天·待你拍板】SUP-88 (8-15 早报 Mode E 失败) 仍 in_review 第 8 天**：autopilot 写中文 comment 但**完全跳过 fetch + 完全跳过飞书投递**，用户 8-15 没收到任何 AI Builders 早报。今天手动把 SUP-88 comment 转贴飞书 vs 永久 skip 此期——二选一，需要你一句话。
- 【延续·已自愈机制】lark-cli user identity refresh：今日 cron 未主动 trigger，沿用昨日成功路径的标配动作明日 cron 应自动应用；若仍未刷新说明 OAuth refresh token 也可能过期，需 `lark-cli auth login --refresh` 重授权。

## 6. Hermes 自我改进
- **SUP-96 autopilot 首次"自翻 done + 自愈 chat_id + 完整闭环"= 双轨 patch 紧迫性可考虑略降**：累计 5 次 chat_id 自愈（SUP-80/83/85/91/92/96 全部 100% 兜底）+ 今日首次"双能力同步完成"。**短期不放松 patch 紧迫性**（单样本不能打破 ≥ 10 连续样本 90%+ 自愈率的稳定阈值）；但 8-30 GC Day #38 可考虑将 follow-builders skill 强制收尾 patch 从"本周最紧急"降级为"下周保留观察"。**反模式**：单样本自愈就宣称"patch 可放宽"——仅 1 个连续样本不构成稳定信号。
- **lark-cli user identity refresh 标配动作缺失**：昨日 cron 主动 trigger 成功（首次实证可行），今日 cron 启动时未延续此动作导致仍 `needs_refresh`。**下次日报 cron 启动时应把"主动 trigger 一次 user API 调用"写成标配最后一步**——避免连续 N 天"自愈证伪"误判，并避免 bot 身份 fallback 写入每日投递。
- **EDN cron 残件清理规则仍未落地 = GC Day 与 cron patch 协同缺口**（延续第 11 天）：GC Day #36 跑通未触及 EDN cron prompt，因为 GC Day 只体检 + skill patch 不自动 patch 其他 cron 的 prompt；EDN prompt patch 必须用户拍板或日报 cron 主动 patch。**强约束**：日报 cron 启动时若 EDN cron prompt 仍缺 `stub` / `dispatched` / `data_asset.json` 等关键词 → §5 升级到【🔴 紧急·必须 8-26 前拍板】，不再容忍"再观察一周"措辞。

## 7. 明日建议
- **【需立即处理·你拍板·EDN 必 patch】明日 cron 启动时主动 patch EDN cron prompt**（job id `1d0cf41439ec`）— 加 5 条 stub-success 硬断言（`delegate_task` 返回 `dispatched` 不算 completed / subagent `message_count >= 50` 阈值 / data_asset.json `sources_total > 12` 验证 / `platform_message_id` 至少 1 条 / 失败时 `multica issue comment add` 报 EDN-WEEKLY-<日期> 失败）。**8-26（下周三）下次 EDN 触发前必须落地**——cron 可自动 patch，但你拍板后我做。
- **【需立即处理·你拍板】state.db 513 MiB → 确认 8-24 凌晨 VACUUM 窗口**：cron 已连续 11 天呼吁未拍板；预计回收 ≥ 50 MiB。**建议明日（8-24）凌晨 03:00 你不在用时**跑 `sqlite3 /root/.hermes/state.db 'VACUUM;'`。
- **【可自动做】明日 cron 启动时显式 trigger 一次 lark-cli user API**（沿用昨日 `read_status --as user` recipe）— 验证 expiresAt 是否仍自动刷新；若仍未刷新说明 OAuth refresh token 过期，需 `lark-cli auth login --refresh` 重授权。**此动作应作为日报 cron 标配最后一步固化**。
- **【可自动做】明日 cron 启动时复核 SUP-97 (8-24 早报)**——若 autopilot 再次漏翻 status，按 SOP 代 flip done。
- **【可自动做·你拍板】SUP-88 (8-15 Mode E) 处理路径**：今天手动把 SUP-88 comment 转贴飞书（保存 8-15 早报补档） vs 永久 skip 此期（标记 abandoned）。两条路径都不复杂，需要你一句话。