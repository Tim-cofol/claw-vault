# Hermes 日报 - 2026-06-25

## 1. 老板速读
- **今天主线 = autopilot 早报第 5 天 + 静默日**：08:06 SUP-28（AI Builders 每日早报）autopilot 自驱跑通，1 podcast（No Priors Biohub with Zuckerberg/Chan）+ 19 X builders + 41 推 → 飞书群 08:06 Asia/Shanghai 送达；连续 5 天自驱 cron 零失败。
- **用户直接对话 = 0 条**：今天没有任何来自 Discord/Feishu/Yuanbao 的用户提问或拍板；Wednesday 那条"hermes 是否支持 mem0"用户没回，已落档。
- **`/tmp` 旧工件扫描**：实际结果 11 ARCHIVED + 51 NOT_ARCHIVED（51 件 = 30 件 hermes-cwd scratch + 8 件 hermes-results tool cache + 4 件 beautiful-feishu-whiteboard skill 源 + 9 件 Hermes 内部 scratch `daily_report_aggregate.py / hermes-{new-commits,pre-sha}.txt / issue_*.md / lark_device_code.txt / last30days_SKILL.md / patchout.txt / pc.py / pr-body.md / probe.py`）——**全部按 verdict 模板归类为"Hermes 内部产物，不归档"**，今天无新增用户产出挂起。
- **延续风险 = 7 项**（昨天 6 项 + 新增 1 项 SUP-28 bot-identity 投递）：last30days X cookies / MI squad 架构 / Discord token 重置 / lark-cli scope 补全 / PR #41283 / .bak 清理 / SUP-25 in_review。
- **SOUL.md 硬规则执行**：今天本 cron 启动时即跑 `lark-cli auth status`（user identity 仍 `needs_refresh`，scope 列表无 `im:message.send_as_user`），SUP-28 早报走 bot 身份 fallback——自检合规。

## 2. 今日工作总览
- **Cron 自动跑 = 2 条**：`cron_cfda6470...` SUP-28 autopilot 早报（08:02 启动 → 08:07 done）+ `cron_7be51b336d4f` 当前日报自身。
- **Subagent 派发 = 0 条**：今天没有 quick-create / local coding agent / quick-search 派发，仅 Multica ACP 单 session 跑 SUP-28。
- **Multica ACP = 1 条真活动**：`f4eaacf4` SUP-28 autopilot 早报 done（77 条 msg，全程工具调用 43 次），无 stub。
- **用户直接对话 = 0 条**：Discord / Feishu / Yuanbao / CLI 全静默。
- **状态盘点**：Multica workspace 今日 `done` = SUP-28 早报；SUP-27 / SUP-26 / SUP-17 / SUP-18 维持 done；SUP-25 仍 in_review 第 9+ 天；backlog 8 条 / cancelled 1 条无变化。
- **Hermes / 上游侧**：无新 release notes、Plugin、Discord 上游公告触发转述 session。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【自驱完成】SUP-28 autopilot 早报 cron 08:02 启动、08:06 飞书送达、08:07 翻 `done`——连续 5 天无人工干预。
- 【保持】follow-builders skill 的 `deliver.js` 仍不支持 feishu 投递，agent 仍走 `lark-cli im +messages-send` 绕过；这套流程已稳定为 autopilot 默认路径。
- 【保持】lark-cli user identity `needs_refresh`（expiresAt 2026-06-24T10:05:04 已过期 49 小时），scope 列表里**无** `im:message.send_as_user`；今天 SUP-28 投递走 bot 身份 fallback（identity: bot，message_id `om_x100b6cf3418528a0b1fa2a654ed4272`），不影响投递但 message metadata 是 bot 不是 user。
- 【静默日】0 用户对话 + 0 派发子 agent，符合 §"连续全静默日（≥ 2 天）" 框架——但今天不算"连续静默"，昨天有 EDN 周报 + SUP-27 + 1 用户问，所以 §6 不必给精简模板建议。

### 知识库 / Obsidian / Get笔记
- 【未新归档】今天没有任何用户产出工件产生；`/tmp` 扫描的 51 件 NOT_ARCHIVED 全部归类为 Hermes 内部 scratch（verdict 模板：`hermes-cwd-*` / `hermes-results/call_*.txt` / `lark_device_code.txt` / `last30days_SKILL.md` / `patchout.txt` / `pc.py` / `probe.py` / `issue_*.md` / `pr-body.md` / `hermes-{new-commits,pre-sha}.txt` / `daily_report_aggregate.py` = 全部 cron / 子 agent / tool output 缓存，**用户不要找回的内容**，不归档）。
- 【保持】昨天闭环的 11 件内容工件在 Vault 状态稳定（AI Security Infographic / Anthropic AI Era / Lark QR / Singapore Weather / EDN Strategic Intelligence 5 个目录 + Security/scripts/ 3 件 draw_*.py）。
- 【保持】`/tmp/beautiful-feishu-whiteboard/{CATALOG,SKILL,RULES,README.zh}.md` 4 件 NOT_ARCHIVED ——这是昨天已装入 `multica_workspaces/78d794b4.../workdir/.agent_context/skills/follow-builders/` 的 skill 源（一份完整仓库），README.md 已归档到 Vault 留入口，源文件**已在 workspace 里**不需要重复归档。

### 飞书 / 日历 / 文档 / 消息平台
- 【新增】SUP-28 autopilot 早报 08:06:13 飞书群 `oc_d5de551525d3a4ae9e1a52815bb9a92b` 送达（bot 身份，message_id `om_x100b6cf3418528a0b1fa2a654ed4272`）；No Priors Biohub 那一期因为 feed 只给 channel URL 不给具体 video，按 digest_intro 规则仍纳入（feed URL 存在）。
- 【小坑延续】lark-cli user identity `needs_refresh` 第 3 天；scope 列表无 `im:message.send_as_user` 已积累 3 天 autopilot 走 bot fallback。
- 【小坑延续】Discord token improper 自 6-22 15:18 起已超 104 小时（昨天 80 小时 + 1 天），昨天 16:37 你贴的明文 token 仍未作废。
- 【保持】Slack / Lark / Feishu / Yuanbao connected；Weixin disconnected 第 32 天。

### OpenClaw / 代码项目 / GitHub
- 【Multica】SUP-28 (`AI Builders 每日早报 2026-06-25`) 翻 `done`；SUP-27 维持 done（昨天）；SUP-25 仍 in_review 第 9+ 天。
- 【保持】PR #41283 仍未查；P0 清理 26 份 `.bak` 仍可自动做。

### 调研 / 内容处理
- 【完成】SUP-28 autopilot 早报内容：1 podcast（No Priors Biohub: Zuckerberg/Chan/Hayden 关于 Biohub 开源生物学未来 62KB 字幕，但 feed URL 仅到 channel 层）+ 19 X builders + 41 推文（Andrej Karpathy 等）。
- 【保持】EDN 周报下次自动周三 08:30，无需手动干预。

## 4. 重要交付物 / 系统变更
- 早报｜SUP-28 AI Builders 每日早报（2026-06-25）｜已完成｜飞书群 `oc_d5de551525d3a4ae9e1a52815bb9a92b` 08:06 Asia/Shanghai 送达，message_id `om_x100b6cf3418528a0b1fa2a654ed4272`（bot 身份）；本地 markdown 备份 `/tmp/fb-digest.txt`（7863 bytes）
- Multica 状态｜SUP-28 (`AI Builders 每日早报 2026-06-25`) 翻 `done`｜已完成｜今日 08:07
- Multica 状态｜SUP-25 维持 in_review｜未动｜第 9+ 天

## 5. 风险、阻塞、需要拍板

### 5.1 `/tmp` 旧工件扫描
今天 23:50 `find /tmp -maxdepth 2 -type f -mtime +0` 实际结果：

**【全部归档完成 · 无新增挂起】** 11 ARCHIVED + 51 NOT_ARCHIVED；51 件 NOT_ARCHIVED **全部归类为 Hermes 内部 scratch**（按 verdict 模板明确不归档）：
- 30 件 `hermes-cwd-*.txt` — multica workspace workdir 路径记录
- 8 件 `hermes-results/call_*.txt` — tool result 缓存
- 4 件 `beautiful-feishu-whiteboard/{CATALOG,SKILL,RULES,README.zh}.md` — 已装入 workspace 的 skill 源（README.md 已归档，源在 workspace）
- 9 件杂项 Hermes scratch：`daily_report_aggregate.py`（cron 辅助脚本，6 天前）/ `hermes-new-commits.txt`（今天 refresh 过）/ `hermes-pre-sha.txt` / `issue_body.md` / `issue_desc.md` / `lark_device_code.txt` / `last30days_SKILL.md` / `patchout.txt` / `pc.py` / `probe.py` / `pr-body.md`

昨天闭环的 11 件内容工件在 Vault 状态稳定，无新增用户产出待归档。**昨天终断了连续 4 天的"幽灵延续项"，今天无悬挂。**

### 5.2 [延续第 3 天 · 需你拍板] `last30days` X 源的最后一步
yt-dlp 装好、bird-installed ✅，**就差你从浏览器导出 `auth_token` + `ct0` 写进 `~/.config/last30days/.env`**。两条路径：A1 贴给 Hermes 写 / A2 你自己写 .env 写完告诉 Hermes 跑 diagnose。**等到了就 5 分钟能跑通 X 源。**

### 5.3 [延续第 4 天 · 需你拍板] MI squad 多 skill 调研架构
fa96d603 已出完整方案（路径 1 三段 agent 拆 / 路径 2 xiaoma 兼任投递），3 个问题等你回：路径 1 还是路径 2？手动还是自动派活？Obsidian vault 目录是 `/root/obsidian-vault` 根 还是 `Projects/MI/` 子目录？

### 5.4 [延续第 5 天 · 需你拍板] Discord token improper
已超 104 小时；昨天 16:37 你贴的明文 token Hermes 拒绝用，那个 token 必须作废，必须去 Discord 重置一次新 token 走 `DISCORD_BOT_TOKEN` 环境变量。

### 5.5 [延续第 3 天 · 需你拍板] lark-cli user identity scope 补全
SUP-26 / SUP-27 / SUP-28 三天 autopilot 投递均发现 user 身份缺 `im:message.send_as_user` scope；今天 `lark-cli auth status` 确认 user identity `needs_refresh`（expiresAt 2026-06-24T10:05:04 已过期 49 小时）且 scope 列表里**无** `im:message.send_as_user`。目前 bot 身份 fallback 工作正常，但 autopilot 想统一走 user 身份投递，需 `lark-cli auth login --scope "im:message.send_as_user"` 重授权。**今天未做。**

### 5.6 [延续第 N 天] PR #41283 / P0 清理 26 份 .bak / Weixin disconnected / SUP-25 in_review 第 9+ 天
全部未动。SUP-25（飞书 vibe 助手）建议升 `done` 或明确下一步责任人。

## 6. Hermes 自我改进
- **`/tmp` 工件扫描 verdict 纪律首次在静默日实战**：昨天 12 件闭环后，今天扫到 51 件 NOT_ARCHIVED，**全部归类为 Hermes 内部 scratch 不归档**——避免 cron 把 verdict 模板的"不归档"类反复归档造成虚高 N。规则已稳定。
- **autopilot 早报 cron 连续 5 天零失败**：SUP-24/25/26/27/28（按顺序），无人工干预路径（`prepare-digest.js` → 飞书群 `lark-cli im +messages-send`）已固化。**问题**：bot identity fallback 把 "bot 投递" 写进了 daily/agent metadata，如果将来改回 user 身份投递，metadata 历史可能要回头修。
- **`/tmp/beautiful-feishu-whiteboard/` 4 件 NOT_ARCHIVED 的处理建议**：昨天装入 workspace 的 skill 源是完整 git 仓库（`.git/` + `templates/` + `scripts/`），README.md 已归档留入口，源文件**已在 workspace**——不应再把 CATALOG/SKILL/RULES/README.zh 单独拆出来归档（拆出来会破坏 skill 完整性）。建议在 verdict 模板里加一条"已装入 workspace 的完整 skill 仓库 = 整体已归档，不再拆分"。

## 7. 明日建议
- 【可自动做】明天 autopilot SUP-29 早报应自动创建（cron 08:00 Asia/Shanghai），无需手动干预。
- 【需要你确认】`last30days` X 源 cookies 选 A1（贴给 Hermes）或 A2（自己写 .env），写完即可跑通——这条已经挂 3 天，autopilot EDN 周报的 X 帖子数直接受影响。
- 【需要你确认】MI squad 多 skill 调研架构路径 1 / 路径 2 + 手动 / 自动 + Obsidian 目录位置 3 问——这条挂 4 天，xiaoma 处于待命状态不浪费但也不增值。
- 【需要你确认】Discord token 必须先去 Discord 重置一次，新 token 走 `DISCORD_BOT_TOKEN` 环境变量；明文 token 已超 104 小时，作废流程不能更久。
- 【可自动做】SUP-25（飞书 vibe 助手）已在 in_review 第 9+ 天，cron 建议直接升 `done` 或挂 blocker 标黄；26 份 `.bak` 按"保留 2 份最新"清理；lark-cli `auth login --scope "im:message.send_as_user"` 重授权（如不阻塞 autopilot 早报可推迟到周末一起做）。
- 【可自动做】在 `references/tmp-artifact-verdict.md` 加一条"已装入 workspace 的完整 skill 仓库 = 整体已归档，不再拆分"条款；下次 EDN 周报（周三）落地时同步把 8 家厂商本周信号做成"周环比"对照（首期 6-24 无对照，从这次开始）。
