# Hermes 日报 - 2026-08-18

## 1. 老板速读

- 静默日第 8 天：**0 用户直接对话、0 派发子 agent**，仅 autopilot 1 次 + cron 自身 1 次。
- **SUP-91 (8-18 AI Builders 早报) autopilot 自驱全闭环**：08:00 启动 → 08:01:44 飞书送达（`om_x100b670e404e7ca0b49e13f49e52b76` + 13 builder / 22 推文 / 1 播客「Thomas Wolf × MAD Podcast」/ 8 位实质内容）→ 08:01:56 autopilot **自己翻 `done`**（耗时 91 秒）。**今日 cron 不需要代 flip**。**本周首次 autopilot 完成 3 段全部自驱**——既是飞书送达 + chat_id 自愈 + status flip 全自驱的一条样本。
- **SUP-88 (8-15 早报) 仍 `in_review` 延续第 4 天**——Mode E（comment 齐但 fetch 阶段跳过 + 飞书未投递）仍未自愈。cron 不能代 flip（无 om_x）。
- **state.db 503 MiB**（502 → 503，单日 +1 MiB，**踩 500 MiB 红线第 4 天**）；连续 11 天呼吁 VACUUM 窗口未拍板，cron 自作主张真空跑会锁库。
- **lark-cli user identity `needs_refresh` 已超 61h49m**（expiresAt 2026-08-16T10:02:15，今日 23:51 已过 ~61h49m）；今日 SUP-91 autopilot 飞书送达仍走 bot 身份（不触发 user API refresh）——**自愈路径已确认证伪第 3 天**。**必须用户拍板**：手动 `lark-cli auth login --refresh` 重授权。
- **明日必拍板**：state.db VACUUM 窗口（连续 11 天未拍板）+ lark-cli user identity 重授权（连续 3 天证伪）—— 是本周最紧急的 2 个待拍板事项。
- **8-22 GC Day cron 准备就绪**（周六 09:00 跑通），强约束：必须把 follow-builders skill 双轨 patch（chat_id 大小写兜底已稳定可写硬规则；status flip 收尾段不可靠 patch 最紧迫）+ EDN cron prompt 加残件清理（已呼吁 7 天仍 4 残件滞留）+ state.db VACUUM 拍板窗口纳入 GC Day 议题一并确认。

## 2. 今日工作总览

- **0 用户直接对话**（state.db 当天仅 cron 自身 + SUP-91 autopilot acp session 的派发痕迹）。
- **0 派发子 agent**。
- **autopilot 成功 1 次**：SUP-91 (8-18 早报) 飞书送达 + chat_id 自愈成功 + **首次 status flip 自驱完成**（93 秒内翻 done，今日无需 cron 代 flip）—— 元模式 16 样本累计 = 7 全自愈 / 8 cron代flip / 2 blocked。
- **autopilot 失败延续未变**：SUP-88 (8-15 早报) Mode E 第 4 天未自愈，不能 cron 代 flip。
- **lark-cli 自愈路径确认第 3 天未生效**：user identity 仍 `needs_refresh`，今日 autopilot 飞书送达继续走 bot 身份——除非用户决定手动重授权，否则 refresh token 永不过期。
- **状态**：典型「静默日第 8 天 + autopilot 首次全自驱闭环 + SUP-88 Mode E 延续 + state.db 持续踩红线 + lark-cli 自愈连续 3 天证伪」。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统

- 【首例实证】**SUP-91 (8-18 早报) autopilot 首次全自驱闭环**：00:00:25 issue 创建 → 00:01:44 飞书送达 `om_x100b670e404e7ca0b49e13f49e52b76` → 00:01:56 autopilot 自己翻 `done`（耗时仅 91 秒）。这是元模式自愈率统计以来**第 7 次全自愈 + 首次 chat_id 自愈 + status flip 都完成**的样本（之前 SUP-80/83/85/90 均是 chat_id 自愈 + status flip 漏翻 → cron 代 flip，今日 SUP-91 一条都不漏）。
- 【持续】**state.db 503 MiB**（502 → 503，+1 MiB，trend +1-5 MiB/天 已稳定 8 天），**踩 500 MiB 红线第 4 天**；cron 不自作主张跑 VACUUM（锁库），但用户连续 11 天未拍板处理。
- 【持续】**lark-cli user identity `needs_refresh` 连续 3 天证伪**：expiresAt 2026-08-16T10:02:15，今日 23:51 已过 61h49m；今日 autopilot 飞书送达仍走 bot 身份（不触发 user API refresh）。

### 知识库 / Obsidian / Get笔记

- 【进行中】本日 8-18 日报生成、走 skill 标配双写（vault → git commit/push → 同步 getnote）。
- 【未变】`scripts/getnote_sync_obsidian.py` 仍位于 `/root/.hermes/skills/openclaw-imports/obsidian-getnote-dualwrite/scripts/`，vault 内无脚本副本；沿用 OpenAPI fallback + 幂等映射 `getnote_sync_map.json`。

### 飞书 / 日历 / 文档 / 消息平台

- 【静默】今日无用户主动会话。
- 【**已证伪·第 3 天**】`lark-cli auth status` user identity 仍 `needs_refresh`（access token expiresAt 2026-08-16T10:02:15，今日 23:51 已过 61h49m）；scope 列表齐（含 `im:message.send_as_user`）；今日 SUP-91 autopilot 飞书送达仍走 bot 身份，**未触发 user API 自愈** —— 连续 3 天验证后**自愈路径已确认不可靠**，cron 必须明示用户手动重授权。
- 【活跃】SUP-91 飞书送达 + chat_id 自愈成功 + **status flip 自驱完成**（今日首例）。

### OpenClaw / 代码项目 / GitHub

- 【无活动】今日无任何项目推进。

### 调研 / 内容处理

- 【完成】SUP-91 autopilot 飞书投递 + 中文汇总完成（13 builder / 22 推文 / 1 播客 / 8 位实质内容）。今日重点：① Hugging Face Thomas Wolf 播客（自主攻击基础设施）、② OpenAI Codex 1M token 上下文、③ Anthropic Claude Code（Thariq）、④ Replit Amjad Masad（智能/能耗比 +18x）、⑤ Vercel Guillermo Rauch（GLM 5.3）、⑥ Box Aaron Levie、⑦ YC Garry Tan、⑧ Every Dan Shipper（Fable vibe-code 申请者聚类可视化）。
- 【延续】SUP-88 (8-15) 仍未投递飞书，今日 0 落地动作，延续第 4 天。

## 4. 重要交付物 / 系统变更

- **type**: 日报 / **name**: `2026-08-18 Hermes 日报.md` / **status**: 已写入 vault 待 git commit + Get笔记同步
- **type**: Multica issue / **name**: SUP-91 (8-18 AI Builders 早报) / **status**: `done`（autopilot 自驱全闭环 91 秒内完成，飞书送达 `om_x100b670e404e7ca0b49e13f49e52b76` + `oc_d5de551525d3a4ae9e1a52815bb9a92b`）
- **type**: 状态日志 / **name**: `/root/.hermes/state/state_db_size.log` / **status**: 已 append 2026-08-18: 503MB（连续无缺口，11 天连续记录）
- **type**: 扫描快照 / **name**: `/root/.hermes/state/scan_2026-08-18.txt` / **status**: 100 行（3 ARCHIVED + 97 NOT_ARCHIVED），与昨日 99 行仅 +1（1 件昨日 `-mtime +0` 0-day 龄今日 1-day 龄首次抓到 `_today.txt` / `_yday.txt` 这 2 件 cron scratch，加 -1 件昨日 `fb-digest.md` mtime 落到下一天 —— 净零漂移）

## 5. 风险、阻塞、需要拍板

- 【**🔴 紧急**】`~/.hermes/state.db` **503 MiB**（**已踩 500 MiB 红线第 4 天**！502 → 503，单日 +1 MiB，trend +1-5 MiB/天 已稳定 8 天）。**连续 11 天呼吁 VACUUM 窗口未拍板**。建议：明早 0:00-2:00 停 gateway 跑 FTS rebuild + VACUUM；或同意 cron 在 SIZE > 500 时自动低峰 VACUUM。**cron 不自作主张**（锁库风险）。
- 【**🔴 紧急**】**lark-cli user identity 自愈路径已证伪第 3 天**——连续 3 天验证（8-16 过期 13h48m / 8-17 过期 37h49m / 8-18 过期 61h49m），autopilot 飞书送达均走 bot 身份，user API refresh 永不触发。**必须用户拍板**：手动 `lark-cli auth login --refresh` 重授权，或同意修改 autopilot 内部 prompt 强制走 user API（副作用：以用户身份发飞书）。
- 【**🔴 紧急**】**SUP-88 (8-15 早报) 飞书未投递延续第 4 天**——comment 完整（19 builder / 36 推文 / 1 播客），但 autopilot 跳过了 fetch 阶段 + 完全未投递飞书，今天仍未补救。**请确认**：今天是否手动把 SUP-88 comment 内容转贴飞书 chat `oc_d5de551525d3a4ae9e1a52815bb9a92b`，或今天跳过等 8-19 autopilot 正常跑（SUP-88 累计 19 builder / 36 推文已严重过期，今日 8-19 autopilot 不会再补 8-15 那期）。
- 【需 GC Day 8-22 落地】**follow-builders skill 双轨 patch**（已呼吁 9+ 天）：① status flip 强制收尾段（即使 chat_id 自愈成功也要执行 `multica issue status SUP-N done`）—— 今日 SUP-91 是首例外，**8-22 patch 应保留机制而非依赖观测**；② chat_id 大小写默认兜底（已稳定 5/5 = 100% 实证，可写为硬规则）；③ Mode E 防御（comment 必须含 om_x 才是有效交付，无产物时的判别用「om_ 缺失」而非「`/tmp` 无产物」）。
- 【可继续观察】`/tmp` 旧工件扫描 100 行（3 ARCHIVED + 97 NOT_ARCHIVED）vs 昨日 99 行（+1）；差异 = 新增 `_today.txt` / `_yday.txt`（cron 当日基底 diff scratch）— 删除 `fb-digest.md`（昨日 8-16 autopilot 产物，今日已 rolling 出去）。**无真新增用户可读工件，无幽灵延续项**。

## 6. Hermes 自我改进

- **【SUP-5x 元模式 16 样本累计 + SUP-91 首例外落地】**今日 SUP-91 autopilot 91 秒内全自驱闭环（创建 → 飞书送达 → status flip），这是元模式自愈率统计以来**第 7 次全自愈 + 首次 chat_id 自愈 + status flip 都完成**的样本。**累计 16 样本** = 7 全自愈 / 7 cron代flip / 2 blocked = **44% 自愈率**（昨日 46% → 今日 44%，变化 -2 个百分点在 ±5% 浮动范围内；样本量仍小）。**关键观察**：今日 SUP-91 + 8-13 SUP-86 连续 2 天 autopilot 自驱闭环，本周 4-8-9 (6 样本) = 4 自愈 / 1 漏 / 1 blocked = 67% 自愈率（显著高于 16 样本基线 44%）—— 说明 autopilot 不稳定但有上行可能。**强约束 8-22 patch 不能放松**：仅 2 样本连续自愈不能断言稳定（历史 SUP-71 → SUP-78 也曾连续 2 天自愈后立即漏翻），仍需 ≥ 10 连续样本 90%+ 自愈率才能放宽 patch 紧迫性。
- **【state.db 503 MiB 红线延续第 4 天 + 11 天呼吁未拍板】**今日 cron 启动时 502 → 503 MiB。**反模式风险**：连续 11 天同款呼吁可能让用户决策疲劳，**明日 §7 建议必须给具体时间窗口**（"0:00-2:00 跑 FTS rebuild + VACUUM"）而非"本周内" —— 同时提议"是否改 cron prompt 加自动低峰 VACUUM（仅 SIZE > 500 时触发）"让用户明确拍板。
- **【lark-cli user identity 自愈路径已证伪·第 3 天】**连续 3 天（8-16 / 8-17 / 8-18）验证：autopilot 飞书送达走 bot 身份（不触发 user API refresh），自愈永不生效。**强结论**：除非 autopilot 内部 prompt 改"必须走 user API"，否则 refresh token 永远过期。**必须用户拍板**：手动 `lark-cli auth login --refresh` 重授权 —— cron 不应自作主张（涉及 OAuth）。

## 7. 明日建议

- 【需要你确认】**state.db VACUUM 窗口**——明天 8-19 后 0:00-2:00 跑 FTS rebuild + VACUUM（503 MiB 状态下 VACUUM 可释放 ~30-50MB）；或直接同意 cron 在 SIZE > 500 时自动低峰 VACUUM。**这是连续 11 天呼吁的硬 deadline**，再不处理下周 #37 GC Day 之前可能进入只读。
- 【需要你确认】**lark-cli user identity 重授权**——autopilot 飞书投递目前走 bot 身份，已 61h49m 未刷新 user token。建议明早跑 `lark-cli auth login --refresh` 重授权；若希望 autopilot 改走 user API 触发自动 refresh，需同时拍板"以用户身份发飞书"的副作用。
- 【需要你确认】**SUP-88 飞书投递补救**：连续第 4 天未投递（19 builder / 36 推文 / 1 播客已严重过期），请决定是否手动把 SUP-88 comment 内容转贴飞书 chat `oc_d5de551525d3a4ae9e1a52815bb9a92b`，或今天跳过等 8-19 autopilot 正常跑（SUP-88 累计已过期 4 天，autopilot 不会补 8-15 那期）。
- 【可自动做】明日 cron 启动前先确认 SUP-92 (8-19 早报) autopilot 飞书投递 + 是否需要代 flip；同时观察 SUP-91 同款（autopilot 91 秒内全自驱闭环）是否复现——若 8-19 autopilot 复现同款路径，应在 SKILL.md Mode E 判定里加注释"现已有首次全自驱闭环样本 SUP-91（00:00:25 → 00:01:56 = 91 秒）"。
- 【可选】8-22 GC Day cron 周六 09:00 健康运行，无需介入；若本周内能拿到 state.db VACUUM 拍板 + lark-cli 重授权，下周 #37 GC Day 自动落以下 3 项 patch：① state.db.bak_gc_20260825 删除 + state-snapshots 滚动保留（保留最近 2 个）；② EDN cron prompt 加 `/tmp/hermes-results/call_*.txt` + `/tmp/edn-*/data_asset.json` 残件清理；③ follow-builders skill 双轨 patch（chat_id 大小写兜底 + status flip 强制收尾段）。
