# Hermes 日报 - 2026-06-06

## 1. 老板速读
- 今天最关键的产出是为 Hermes 云服务器迁移做了完整盘点，并生成了可回滚迁移脚本；当前卡点是新服务器 SSH 未授权。
- Get笔记“最新录音生成图片”任务出现语义误判：先做成了截图式卡片，后续已复盘并固化为“关键纪要信息图”规则。
- 已创建 `getnote-audio-infographic` 技能，并更新用户偏好：以后同类请求默认生成信息图，不再做普通摘要卡片。
- 微信公众号文章解析规则已确认：默认优先 Tavily Extract，正文抓不到时明确失败，不编造内容。
- Hermes / Obsidian / OpenClaw 迁移范围已基本识别，包含 sessions、skills、cron、vault、OpenClaw、systemd 服务和相关配置。
- 需要你拍板/执行：在新服务器 `43.130.13.217` 加入旧机 SSH 公钥，然后才能继续 dry-run 和正式迁移。
- 明日优先建议：先解除 SSH 阻塞，跑迁移 dry-run；再决定是否执行 `--apply` 完成搬家。

## 2. 今日工作总览
- 处理了 Get笔记录音图片任务；交付过图片卡片，但用户指出预期应为“关键纪要信息图”，任务质量未达预期。
- 完成了一次流程复盘，并把纠错规则写入技能和长期偏好，降低同类错误复发概率。
- 回答并固化了微信公众号文章解析策略，明确使用 Tavily Extract 作为首选路线。
- 为 Hermes 迁移到升级后云服务器做了资产盘点、服务检查和迁移脚本准备；执行阶段因 SSH 权限被阻塞。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已推进】盘点当前 Hermes 环境，包括主目录、源码/venv、sessions、skills、cron、gateway、dashboard、systemd 服务；迁移范围已明确。
- 【已完成】生成 `/root/hermes_migrate_to_43.130.13.217.sh`，脚本默认 dry-run，正式执行需显式 `--apply`，并包含目标机备份与验证步骤。
- 【阻塞】旧服务器到新服务器 SSH 登录失败，原因是目标机未接受当前公钥；迁移尚未真正执行。

### 知识库 / Obsidian / Get笔记
- 【已推进】Get笔记最新录音任务完成了内容拉取和图片生成尝试，但交付物形态偏离用户预期。
- 【已完成】将“Get笔记最新录音生成图片 = 关键纪要信息图”固化为技能与用户偏好，作为后续默认流程。
- 【已推进】本日报按双写流程保存到 Obsidian，并同步到 Get笔记。

### 调研 / 内容处理
- 【已完成】确认微信公众号文章解析策略：`mp.weixin.qq.com` 默认走 Tavily Extract，必要时升级 advanced，失败时明确说明正文未抓到。
- 【已完成】明确“总结链接”和“保存为笔记”是两类任务，只有用户要求保存/同步时才进入 Obsidian + Get笔记双写。

## 4. 重要交付物 / 系统变更
- 脚本｜Hermes VPS 迁移脚本｜已创建｜`/root/hermes_migrate_to_43.130.13.217.sh`
- 技能｜`getnote-audio-infographic`｜已创建｜用于 Get笔记最新录音生成关键纪要信息图
- 记忆｜Get笔记录音图片偏好｜已更新｜默认信息图，不是截图卡片
- 规则｜微信公众号解析路线｜已确认｜优先 Tavily Extract
- 笔记｜Hermes 日报 - 2026-06-06｜已保存｜`/root/obsidian-vault/10-Notes/Daily/Hermes/2026-06-06 Hermes 日报.md`

## 5. 风险、阻塞、需要拍板
- 新服务器 SSH 未授权：请先把旧机公钥加入 `43.130.13.217` 的 `/root/.ssh/authorized_keys`，否则迁移无法继续。
- 迁移脚本覆盖范围较大：建议先跑 dry-run，确认目标机备份路径、rsync 范围、服务重启行为后再执行 `--apply`。
- Get笔记录音图片任务暴露出上下文召回风险：后续同类任务必须先套用已固化 skill，不应凭字面理解。
- 若迁移发生在晚间 cron 附近，需注意日报、Get笔记同步、Hermes gateway 等任务可能跨机器重复运行。

## 6. Hermes 自我改进
- 已把“录音生成图片”的正确交付形态固化为 skill，避免同一类用户纠正重复发生。
- 对迁移类任务应默认采用“盘点 → dry-run → 备份 → apply → 验证”的可回滚流程，今天的迁移脚本已按这个模式实现。
- 对微信公众号等强反爬来源，应优先走专用抽取工具，并在失败时明确边界，不把壳内容当正文。

## 7. 明日建议
- 【需要你确认】先在新服务器添加旧机 SSH 公钥，然后通知我继续执行迁移 dry-run。
- 【可自动做】SSH 打通后，我可以先跑 `/root/hermes_migrate_to_43.130.13.217.sh --dry-run` 并报告差异与风险。
- 【需要你确认】dry-run 无异常后，再决定是否执行 `--apply` 完成正式迁移。
- 【可自动做】迁移后验证 Hermes gateway、dashboard、cron、sessions、skills、Obsidian symlink、Get笔记同步是否正常。
