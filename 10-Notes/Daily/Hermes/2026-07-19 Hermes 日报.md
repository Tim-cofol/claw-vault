# Hermes 日报 - 2026-07-19

## 1. 老板速读
- 周日静默：今天没有用户直接对话、代码提交或用户驱动调研；主要产出来自 ACP autopilot 与日报 cron。
- AI Builders 早报 SUP-56 已完成：汇总 1 集播客、3 篇官方博客和 11 位 builder 的有效更新，08:03 已投递飞书。
- 今日信号集中在 AI 基础设施与 agent 工程：OpenAI 算力供给、Anthropic Managed Agents、自托管 sandbox 与 MCP tunnel。
- 已纠正看板收尾缺口：核验飞书送达记录后，将 SUP-48 至 SUP-56 共 9 期早报全部置为 `done`，不再让已交付任务长期滞留 `in_review`。
- `/tmp` 旧工件实扫 109 行：11 ARCHIVED + 98 扫描器标记 NOT_ARCHIVED；较昨天增加的 2 件均为日报扫描快照，不是用户待找回产出。
- 结构性风险未变：state.db 约 421MB、checkpoint 约 116MB，涉及清理与 retention 策略，继续等待你确认。
- 明日优先：09:00 自动运行 idea-recall weekly digest，并检查召回结果与索引健康。

## 2. 今日工作总览
- 【已完成·自驱】ACP autopilot 08:00 创建并执行 SUP-56，生成中文 AI Builders 早报并成功投递飞书。
- 【已闭环】核验 SUP-48 至 SUP-56 的结果评论和 message_id，批量补齐 `in_review → done` 状态收尾。
- 【已核对】state.db 今日除本日报外无用户会话；Vault 昨日日报已提交推送，启动时 working tree 干净。
- 【已完成】执行 `/tmp` 新旧双扫描、昨日风险复读、Vault HEAD 与飞书授权自检。

## 3. 按项目 / 责任域汇报

### Multica / 自驱工作流
- 【已完成】SUP-56 由 ACP channel 触发，而非 jobs.json cron；抓取 1 集播客、13 位 builder 的 27 条推文和 3 篇博客，筛选后形成中文管理向简报。
- 【已验证】飞书 bot 投递成功并取得 message_id；issue 已从 `in_review` 改为 `done`。
- 【已闭环】SUP-48 至 SUP-55 的历史送达记录同样齐备，今天一次性清除 8 个遗留状态，加上 SUP-56 共关闭 9 项。

### 飞书 / 消息平台
- 【已完成】今日早报以 bot 身份投递到既定 P2P 会话；实际 chat_id 必须使用小写 `oc_` 前缀，已在 issue 评论中留痕。
- 【治理项】user identity 仍为 `needs_refresh`，且缺 `im:message.send_as_user`；本次 bot 路径可用，但身份治理问题没有消失，按既定升级策略不再每日重复催办。

### Hermes / Agent 系统
- 【稳态】4 个 enabled jobs 最近一次运行均为 `ok`；今天没有新增 cron 失败。
- 【实扫】`/tmp` 旧工件 109 行，较昨天增加 `scan_2026-07-17.txt`、`scan_2026-07-18.txt` 两个内部快照；最近 24 小时另有 5 个文件，其中 `digest.json` 为早报源数据缓存，其余为 Hermes/容器 scratch，用户待归档工件为 0。

## 4. 重要交付物 / 系统变更
- 飞书早报｜AI Builders 每日早报 2026-07-19｜已投递、已验证、SUP-56 `done`
- 看板治理｜SUP-48 至 SUP-56 历史早报｜9 项送达核验完成，状态全部收口为 `done`
- 日报｜2026-07-19 Hermes 日报｜已生成并纳入日常双写流程

## 5. 风险、阻塞、需要拍板
- 【需要你确认】state.db 约 421MB：建议先 dry-run 评估 FTS rebuild + VACUUM 的收益，再决定是否执行。
- 【需要你确认】checkpoint 约 116MB 且缺明确 retention：建议采用 3-snapshot rolling，改配置前先确认保留策略。
- 【观察】飞书 user 身份缺 `im:message.send_as_user`；bot 当前可投递，但如果以后要求以用户身份发送，仍需 OAuth 重授权。

## 6. Hermes 自我改进
- 已把“投递成功 + 可回读/有 message_id + 无人工审批门 = 立即 `done`”真正执行到看板状态，而不是继续写进日报当建议。
- `scan_tmp_artifacts.sh` 的 NOT_ARCHIVED 不能直接等同用户挂起项；日报继续做“扫描器结果 + 用户产出判定”两层分类，避免把内部 scratch 当风险。

## 7. 明日建议
- 【可自动做】09:00 运行 idea-recall weekly digest，若无合格旧想法则静默。
- 【可自动做】继续观察 SUP-57 是否在投递后直接收口为 `done`，验证新规则是否进入 autopilot 主流程。
- 【需要你确认】决定是否先做 state.db 缩容 dry-run。
- 【需要你确认】决定 checkpoint 是否采用保留最近 3 份的 rolling 策略。
