# Hermes 日报 - 2026-07-25

## 1. 老板速读

- 今天是个**周末静默 + 两次自动 cron 自驱** 的轻量日：用户没有主动发问，Hermes 由「日报 cron（昨晚 23:50）」和「GC Day cron（周六 09:00）」两条 cron 推动。
- 今日核心交付：① GC Day #19 完成体检（FTS5 rebuild 已做、state.db 备份齐备），列出 4 项需要你拍板的清理项；② SUP-62 AI Builders 早报已飞书送达，已按 SKILL §3 元模式代翻 `done`。
- 今日未做：任何破坏性操作（VACUUM / 删 legacy checkpoint / 清理冗余 jobs.json 备份 / 修 SUP-57 chat_id），全部留到明日及以后等你拍板。
- `/tmp` 工件扫描 85 行（3 ARCHIVED + 82 NOT_ARCHIVED），basename 与昨天对比有少量漂移（新增 `inspect*.py` / `pod.txt` / `prompts_full.txt` / `tweets.txt` 等 follow-builders 中间产物，删 `digest.json` / `hermes-cwd-*` / `patchout.txt` 等），全部为 skill 内部 scratch，无需你处理。
- 明日优先：① GC Day 4 项需确认项你拍板；② 7-29 EDN 周报实跑，验证 X scrape ≤ 3；③ 决定 SUP-57（chat_id 错配）和 SUP-25（vibe 助手需求）两个 7+ 天 in_review 的下一步。

## 2. 今日工作总览

- 【已完成】GC Day cron（08dc968bf2488，09:03 跑通）完成 MEMORY/USER 体检（56%/33%，无需迁移）、state.db FTS5 rebuild（19.8s，40387→40222）、生成 4 项需确认清理清单。
- 【已完成】日报 cron 代 flip SUP-62 为 `done`，核验 comment 已加（飞书送达 message_id 已在 SUP-62 结果评论里）。
- 【已转述】Discord `#hermes-agent-notifications` 转述了 Hermes Agent「沙箱凭证防火墙」公告（03:01），中文版已发频道，无配置变更。
- 【已派发】昨日日报遗留的 SUP-57（chat_id `OC_...` 大写 vs lark-cli 要求 `oc_...` 小写）和 SUP-25（飞书 vibe 助手需求）今天无新动作，仍在 in_review 等待拍板。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统

- 【已完成】GC Day #19（cron `8dc968bf2488`，09:03）跑通健康检查。MEMORY/USER 体检显示全部段落已是索引形式，**本周无可沉淀内容**，无新 skill 创建、无 patch。
- 【已完成】state.db FTS5 rebuild 执行（19.8s），备份 `state.db.bak_gc_20260725`（441MB）。WAL 仍 158MB（gateway 主动写入），VACUUM 必须停 gateway，本周未执行。
- 【已转述】Discord 公告「Hermes Agent 沙箱凭证防火墙」：`hermes egress setup`，沙箱用替代令牌、边界处替换为真实密钥；已中文转述到通知频道。
- 【需确认】GC Day 列出 4 项清理，全部涉及不可逆操作（VACUUM / 删 legacy 20260607 checkpoint / 清冗余 jobs.json 备份 / state-snapshots rolling policy），需你拍板（见 §5）。

### Multica / AI Builders

- 【已闭环】SUP-62 AI Builders 早报已飞书送达（00:05 创建，27 条 X / 1 podcast / 1 博客，覆盖 Cerebras Andrew Feldman、Claude Artifacts、Replit Autoscale、Vercel Python 冷启动等），目标群为 `oc_04ec3cdf998e30e7d852c866be3ac173`（小天群）。今日日报核验后翻 `done`，与昨日 SUP-61 同模式处理。
- 【需确认】SUP-57 仍 `in_review`（updated 7-20）：chat_id 错配问题，autopilot 当时因大小写不匹配无法投递，已撤回误发（message_id `om_x100b6ae217ec54a4b24299e1b819d11`），需你决定补发 7-20 早报或直接 close。
- 【需确认】SUP-25 仍 `in_review`（updated 6-22，已 33 天）：飞书 vibe 助手需求分析，需你决定继续推进或关闭。
- SUP-39 仍 `in_progress`（updated 7-2，已 23 天）：AI Builders 早报，无最近动作。

### 飞书 / 日历 / 文档 / 消息平台

- 【状态】`lark-cli auth status`：user identity 状态 `needs_refresh`（expiresAt `2026-07-25T10:04:48+08:00`），**下次 user API 调用会自动 refresh**；scope 列表中 `im:message.send_as_user` 不存在（autopilot SUP-62 因此默认走 bot 投递小天群）。今日未触发 user 身份投递。
- 【已转述】Discord 公告频道 1 条中文转述（沙箱凭证防火墙）。

## 4. 重要交付物 / 系统变更

- GC Day 报告｜Hermes GC Day #19｜已跑通｜`~/.hermes/cron/output/8dc968bf2488/2026-07-25_09-03-47.md`（完整报告原文）+ `state.db.bak_gc_20260725`（441MB 备份）
- AI Builders 早报｜SUP-62 (2026-07-25)｜已 `done`｜飞书送达 om_x100b690b93bd3ca0b28809f1384d040，27 条 X / 1 podcast / 1 博客
- 状态收尾｜SUP-62 由 in_review → done｜日报 cron 代 flip｜核验 comment id `0db59788-41e9-44c9-9acf-c4e9252b7765`（原结果评论已含 message_id，本次再加核验评论）
- 备份｜`state.db.bak_gc_20260725`｜441MB｜FTS rebuild 前快照，可逆
- 转述｜Hermes Agent 沙箱凭证防火墙公告｜Discord 频道｜03:01 中文版已发

## 5. 风险、阻塞、需要拍板

- 【需确认·不可逆】GC Day #19 4 项清理：
  1. `state.db` VACUUM（停 gateway 2-5 分钟，442MB → ~80MB，预计收益最大但需停机窗口）
  2. `~/.hermes/checkpoints/legacy-20260607-001849/`（55MB / 2866 文件，6-07 旧版本）
  3. `~/.hermes/cron/jobs.json.bak_20260724_2356{,_final,_2357_final,_2358_final}` 4 个 44K 近似重复备份
  4. state-snapshots 滚动保留策略（每日 03:00 / 保留 7 daily + 4 weekly？）
- 【需确认】SUP-57（chat_id 错配）决策：补发 7-20 早报到正确群 OR 关闭。
- 【需确认】SUP-25（飞书 vibe 助手需求）决策：继续推进 OR 关闭（已 in_review 33 天）。
- 【信息】SUP-39（in_progress 23 天）已不在主风险位列，但应留意是否被遗忘。
- 【信息】`lark-cli` user identity `needs_refresh`：下次 user API 调用自动 refresh；scope `im:message.send_as_user` 需 OAuth 重授权（仍待用户拍板）。

## 6. Hermes 自我改进

- SUP-62 再次撞 SOP：autopilot 飞书发送成功但停在 `in_review`，又靠日报 cron 代 flip → 把 **「deliver → comment → flip status → comment done」** 写进 `follow-builders` skill 收尾段，本周 GC Day #20 应落地 patch（参照 SKILL §0-pre SUP-5x 元模式 2026-07-18 段的明确要求）。
- SUP-57 教训沉淀：autopilot 描述里 `OC_xxx` 大写 chat_id 直接报错，**lark-cli 严格要求小写 `oc_xxx`**；下次 patch follow-builders skill 时把 chat_id 校验脚本加入 init 阶段，别让 agent 走到误发再撤回。
- GC Day 收尾报告今天又走的是 cron 输出文件路径（`~/.hermes/cron/output/<jobid>/<日期>.md`），**不是 vault**——意味着 GC Day 报告不进入双写生态（不被 Get笔记同步、不被 git 跟踪）。建议下周 GC Day 在 cron prompt 里加一段「报告同时落 vault `30-Resources/Playbooks/gc-day/` 并双写」，形成审计链。

## 7. 明日建议

- 【需要你确认】GC Day 4 项清理决定：建议优先批准 VACUUM（收益最大、可逆只是停机窗口）+ 同意 jobs.json 备份合并（低风险）；legacy checkpoint 和 state-snapshots policy 可缓议。
- 【需要你确认】SUP-57 / SUP-25 / SUP-39 三个长期悬挂项的处置：SUP-57 我倾向 close（已有撤回记录，autopilot 配置修好后下次照常），SUP-25 / SUP-39 留你拍板。
- 【可自动做】下次 follow-builders skill patch（GC Day #20 周六 8-01）把 chat_id 大小写校验 + deliver→flip status 收尾写进 prompt。
- 【可自动做】下周 GC Day #20 把 GC Day 报告同时落 vault 双写。
- 【可自动做】明天日报 cron 继续按 SUP-5x 元模式巡检：今日 SUP-61/62 已 `done`，待关注 SUP-57 / SUP-25 / SUP-39。