# Hermes 日报 - 2026-08-08

## 1. 老板速读
- **GC Day #21 已真实执行**：完成 MEMORY/USER 体检与索引核对，并把本周可复用的消息适配器与多 bot 群聊协作规则沉淀进 `hermes-operations` skill。
- **系统总体可用**：gateway 进程在线，4 个 enabled cron 最近状态均为 `ok`；Discord 今晨短暂 503 后未见持续性故障证据。
- **state.db 已到 483 MiB**：超过 480 MiB 维护关注线，距 500 MiB 红线约 17 MiB；在线 FTS 重建不能解决膨胀，低峰 VACUUM 需要你确认维护窗口。
- **Slack 仍在持续报 `Session is closed`**：这是当前最明显的通道层异常，需单独排查或停用无用连接，避免错误日志持续增长。
- **Idea Recall Weekly Digest 最近一次投递失败**：cron 状态表面为 `ok`，但飞书返回 `[99992402] field validation failed`，不能按成功交付处理。
- **`/tmp` 实扫 75 件**：3 件已归档、72 件未在 Vault 中匹配；与昨日相比无新增 basename，`digest.json` 已消失。新鲜用户可读工件扫描为 0 件。
- **明日优先**：先处理 Idea Digest 投递错误与 state.db 维护窗口，再决定是否清理历史临时文件和建立 snapshot 保留策略。

## 2. 今日工作总览
- 【已完成】执行 GC Day：检查记忆容量、skill 沉淀、gateway、cron、日志、磁盘和 state.db 健康。
- 【已完成】将消息适配器可用性、Weixin 移除状态、Tavily 搜索入口及多 bot 群聊协作规则写入现有 `hermes-operations` skill；未新建窄 skill。
- 【已核对】完成 `/tmp` 新旧工件扫描、state.db 按 source 统计、cron 状态检查及 lark-cli 认证状态检查。
- 【已转述】Discord 上游公告完成中文化处理，涉及语音唤醒、Buzz 集成、效率优化、可移植插件与 `/learn`。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已沉淀】GC Day 将 MEMORY.md 保持在 1275/2200 字符（58%），USER.md 为 523/1375（38%）；本轮无新增迁移，现有索引与行为规则结构完整。
- 【需维护】state.db 当前约 483 MiB；按 source 看，discord、cron、acp 是消息量前三大来源，在线 FTS 重建已验证无法有效缩容。

### 飞书 / 日历 / 文档 / 消息平台
- 【阻塞】Idea Recall Weekly Digest 上次发送至 Feishu 失败，错误为 `[99992402] field validation failed`；需检查发送 payload 和字段约束。
- 【异常】Slack 持续出现 `Session is closed` 重连错误；gateway 本身仍在线。
- 【已转述】Discord 上游公告已中文化转述，无额外 Vault 交付物。

### 知识库 / Obsidian / Get笔记
- 【已完成】本日报正文已写入 Obsidian，作为今日审计记录保存。
- 【待治理】`/tmp` 原始扫描仍有大量历史 autopilot、EDN 和 Hermes 内部残件，不能把扫描器的 72 件全部视为用户交付物。

## 4. 重要交付物 / 系统变更
- Skill｜`hermes-operations`｜已更新｜消息适配器与多 bot 群聊协作规则。
- GC 报告｜Hermes GC Day #21｜已完成｜包含记忆容量、系统健康和清理建议。
- 上游公告转述｜Hermes 语音唤醒、Buzz、效率优化、插件与 `/learn`｜已完成｜Discord 中文化转述。
- 监控记录｜`state_db_size.log`｜已补齐 8-06 缺口并记录 8-08 的 483MB。

## 5. 风险、阻塞、需要拍板
- 【需你确认】安排 state.db 低峰 VACUUM 窗口；当前 483 MiB，继续增长会逼近 500 MiB 红线。GC Day 未自行执行，以免锁库影响 gateway 与 cron。
- 【需处理】Idea Recall Weekly Digest 的 Feishu 投递失败。建议先检查 8-03 输出中的发送 payload，再做一次小范围重试验证。
- 【需处理】Slack `Session is closed` 重试风暴。建议确认 Slack 是否仍有使用价值；若需要保留，安排连接生命周期排查。
- 【需确认】`state-snapshots` 约 396M，当前仍缺少滚动保留策略；建议先确认保留周期，再改 cron 或清理旧快照。
- 【待确认】72 件 `/tmp` 未匹配项中包含大量内部 scratch 与历史残件；不建议直接批量删除，先按清单分类。

## 6. Hermes 自我改进
- GC Day 已把可复用的消息平台事实与群聊协作行为迁入现有 skill，避免继续占用 MEMORY；本轮不新增 skill，控制技能膨胀。
- 日报扫描器已按新鲜工件与旧工件双口径核对；今日无新增用户可读工件，但应继续保持该对偶扫描，避免 0-day 工件跨日漏报。
- state.db size log 发现 8-06 缺口并已补齐，后续按日期连续记录，避免 delta 诊断被错误基线污染。

## 7. 明日建议
- 【需要你确认】确定 state.db VACUUM 维护窗口。
- 【可自动做】定位 Idea Digest 的 `[99992402]` payload 字段错误，并用最小消息做投递回归。
- 【可自动做】继续监控 Slack `Session is closed`，确认是否自愈；若未自愈，再给出停用或修复方案。
- 【需要你确认】决定 `state-snapshots` 的滚动保留周期后，再执行清理或改配置。
- 【可自动做】继续按 basename diff 与 mtime 双扫描 `/tmp`，不把 cron/autopilot 内部产物误报为用户风险。
