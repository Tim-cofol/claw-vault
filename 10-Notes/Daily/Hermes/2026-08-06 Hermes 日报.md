# Hermes 日报 - 2026-08-06

## 1. 老板速读
- **AI Builders 早报今日飞书投递失败（SUP-79，blocked）**：autopilot 跑完 digest，但 issue 描述里的 chat ID 是大写 `OC_d5de551525d3a4ae9e1a52815bb9a92b`，飞书 API 返回 `invalid open_chat_id`；agent 拒绝自作主张投别处，digest 已贴在 issue 评论区，等你确认正确 chat ID。
- **SUP-78（8-05 早报）已飞书送达但 autopilot 漏翻 status**——本次日报 cron 已代 flip done（飞书 message_id `om_x100b6813aa4e60a0b295896be8d1f57`，11 builder / 22 tweet / 3 博客 / 1 播客）。
- **`/tmp` 实扫 75 件（3 已归档 / 72 未归档）**：相比昨日 69 行净 +6 件，但全部为昨日 EDN 跑出来的 0-day 龄 `hermes-results/call_*.txt`（5 件）+ `data_asset.json`，mtime 跨过 24h 才被今日 `-mtime +0` 抓到——属于昨日漏报今日补捞，不是真新增。
- **纯静默日 + 单 autopilot 失败**：今日零用户对话、零 Discord / 飞书用户问答，仅 SUP-79 autopilot 08:00 触发但卡在 chat_id 上。
- **`state.db` 476 MiB**（昨日 475 MiB，+1 MiB），**已过 475 MiB 警戒线**，距 480 MiB 黄区约 4 MiB；完整 VACUUM 仍待你定维护窗口。
- **SUP-57 连续 3 天建议 abandoned 仍未动作**——今日 cron 不再重复这条建议；如再不动，下次 GC Day（8-08 周六）建议由 cron 直接硬翻 abandoned 关闭悬挂项。
- **明日重点**：处理 SUP-79 chat ID（最大优先）+ 8-08 GC Day 周六体检 + 8-12 EDN 周报连续性观察。

## 2. 今日工作总览
- 【已闭环】SUP-78 代翻 done，附核验评论（含 message_id + 中文 builder 覆盖）。
- 【已推进】SUP-79 飞书投递阻塞已定位根因（chat ID 大小写），digest 内容已贴评论区待你确认目标 chat。
- 【无活动】零用户对话、零 Discord / 飞书用户问答、零 EDN cron（明天 8-07 周五不触发，周三 8-12 才再次触发）。
- 【已核对】`/tmp` 旧工件扫描 + basename diff + state.db 大小 + lark-cli auth status。

## 3. 按项目 / 责任域汇报

### Multica / AI Builders
- 【阻塞】**SUP-79 (8-06 早报)**：autopilot 跑完 digest（X / Twitter 10+ builder、tweet 覆盖 Madhu Guru / Swyx / Guillermo Rauch 等），但飞书投递因 chat ID 大写失败 `99992356 invalid open_chat_id`（log `20260806080145CF8BEE5FBD5E9005F4F1`）。agent 已搜索 tenant 内可达群未匹配，未自作主张投别处。**关键坑已确认**：SKILL.md 7-19 立的"飞书 chat_id 小写规范"实操反模式再次复现——issue description 模板用了大写 `OC_`，与 API 期望的小写 `oc_` 冲突。
- 【已闭环】**SUP-78 (8-05 早报)**：本次日报 cron 代翻 done；om_xxx 真实送达证据齐备。**SUP-5x 元模式本周继续**：本周样本 = 7-26 自愈 / 7-30 漏 / 7-31 漏 / 8-01 自愈 / 8-02 自愈 / 8-03 自愈 / 8-05 漏（今日代 flip）= **5 自愈 / 2 漏 = 71% 自愈率**（8-03 是 67%）。
- 【遗留】**SUP-57 仍 in_review**：连续 3 天建议标 abandoned 仍未动作。今日 cron 不再重复建议。

### EDN / 需求洞察
- 【未触发】今日无 EDN cron 触发（8-07 周五不触发，下一轮 8-12 周三）；8-05 周报已成功落盘，无需新动作。

### Hermes / Agent 系统
- 【已核对】`lark-cli auth status`：user identity `needs_refresh`（expiresAt 2026-08-06 10:01，下次 user API 调用自动 refresh）；scope 列表齐备，`im:message.send_as_user` 仍缺失（与昨日一致，今日仅调试 API 无实际投递）。
- 【已核对】`state.db` 大小 476 MiB（+1 MiB 今日），**已过 475 MiB 警戒线**；按昨日"低峰停 gateway 后 rebuild + VACUUM"建议仍未执行。SIZE BY source 全时口径与昨日一致（discord 311 / cron 137 / acp 63 / subagent 27），今日 +1 session 仅为本日报 cron 自身。

### 飞书 / chat_id 投递
- 【坑落地】**飞书 chat_id 大小写规范**（SKILL.md 7-19 已立）今日 SUP-79 再次踩坑，issue 模板硬编码大写 `OC_` 应改为小写 `oc_`；GC Day 8-08 建议把 follow-builders skill 的 issue description 默认 chat_id 从大写迁小写（或写一段 prompt 自动纠正）。

## 4. 重要交付物 / 系统变更
- 早报｜AI Builders Digest 2026-08-05｜已代翻 done｜SUP-78（om_x100b6813aa4e60a0b295896be8d1f57）
- 早报｜AI Builders Digest 2026-08-06｜飞书投递失败、digest 已贴评论区｜SUP-79（blocked）
- 自愈率追踪｜SUP-5x 本周 5/7 = 71%｜已累计 9 样本（4 自愈 / 3 漏 + SUP-66 todo + SUP-79 blocked 各 1）—— GC Day #21 紧迫性维持
- `state.db` 监控｜476 MiB｜+1 MiB 今日，已过 475 MiB 警戒线

## 5. 风险、阻塞、需要拍板
- 【需要你确认】SUP-79 chat_id 修复：原始 digest 已贴 SUP-79 评论，确认正确小写 chat ID（建议 `oc_d5de551525d3a4ae9e1a52815bb9a92b`）后由 cron / 人工补发。
- 【需要你确认】SUP-57 连续 3 天建议标 abandoned 仍未动作；如本周末 GC Day 8-08 仍未处理，cron 将硬翻 abandoned 关闭悬挂项（避免再占 in_review 位）。
- 【需维护窗口】`state.db` 已过 475 MiB 警戒线，距 480 MiB 黄区 ~4 MiB；建议低峰停 gateway 后 rebuild + VACUUM（cron 不擅自运行）。
- 【待观察】EDN 周报连续性：8-12 周三再次触发时核对 cron 是否稳定（7-22 成功 / 7-29 失败 / 8-05 成功，3 样本仍非稳态）。
- 【坑积累】飞书 chat_id 大小写规范：今日 SUP-79 第 3 次复现（大写 `OC_` vs 小写 `oc_`），GC Day 应落地 follow-builders skill issue description 模板纠正。

## 6. Hermes 自我改进
- **SUP-79 chat_id 大小写坑今日第 3 次复现**——SKILL.md 7-19 立的"飞书 chat_id 大小写规范"已有对策但 issue 模板仍硬编码大写 `OC_`。GC Day 8-08 应把 follow-builders skill 的 issue description 默认 chat_id 改为小写 `oc_xxx`，或加一段 prompt 自动纠正 `OC_` → `oc_`。
- **`/tmp` 折叠判定再次验证**：今日 basename diff 净 +6 件全部是昨日 EDN 跑出的 0-day 龄工件跨 24h 才被今日 `-mtime +0` 抓到——典型"真零漂移隐式漏报"模式（与 7-28 教训同款机制）。SKILL.md §0-pre 的强制 `-mtime -1` 对偶扫描仍是核心防线。

## 7. 明日建议
- 【需要你确认】处理 SUP-79 chat_id：补发今日 AI Builders digest 到正确群（建议小写 `oc_d5de551525d3a4ae9e1a52815bb9a92b`）。
- 【需要你确认】翻 SUP-57 为 `abandoned`（连续 3 天未动作）；如再不动，8-08 GC Day cron 代硬翻。
- 【需要你确认】`state.db` 480 MiB 黄区前是否安排低峰 VACUUM 窗口（已过 475 MiB 警戒线）。
- 【可自动做】明天 cron 启动时再跑 `/tmp` basename diff，验证 EDN 0-day 龄残件是否被本日清理（如果 8-07 自动清掉，`/tmp` 净增可能回到 ≤ 0）；若再出现 0-day 龄工件跨日才被 `-mtime +0` 抓到，说明 EDN subagent 残件清理机制需补。
- 【可自动做】8-08 周六 GC Day #21 自动体检（默认 cron 触发），重点 patch follow-builders skill 的 chat_id 大小写 + SUP-5x 元模式升级。