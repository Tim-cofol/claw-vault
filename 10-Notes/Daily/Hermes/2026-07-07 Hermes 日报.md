# Hermes 日报 - 2026-07-07

## 1. 老板速读
- **今天依然 autopilot 独白日**：0 用户直接对话、0 Discord 转述、0 调研落库，唯一真实产出 = SUP-44 follow-builders 7-7 早报跑通，连续第 5 天稳态。acp 派发 8:00（07-07 00:00 UTC）→ 飞书投递 `om_x100b6bf060e4e534b037e6d98b52e29` → mget 回读校验为 post → 翻 `in_review` 闭环。
- **今日早报内容偏弱**：1 期 No Priors（OpenAI / Noam Brown on test-time compute 改写 benchmark + safety + research）+ 14 builders / 25 tweets，0 blog。X 精选只剩 thenanyu / AmandaAskell / Cat Wu / Garry Tan / Zara Zhang / nikunj / Dan Shipper / Sam Altman 八位有 AI 实质；剩余 6 位按 prompts 末尾点名速览。
- **投递身份第 5 天走 bot**：lark-cli user identity `needs_refresh` 且 scope 列表里没有 `im:message.send_as_user`——这条是显式"scope 缺失"（OAuth grant 阶段就没拿到），不是 token 过期。`expiresAt: 2026-07-07T10:05:48` 又是滚动后时间戳，与昨天 `2026-07-06T10:05:50` 完全不同，说明 expiresAt 在被自动 refresh 推着走，token 时效层没问题；真正卡的是 scope 没补。
- **`/tmp` 扫描 99 行 = 11 ARCHIVED + 88 NOT_ARCHIVED**，比昨天多 2 件 NOT_ARCHIVED（昨天 86）；新增的全是 Hermes 内部 scratch（`/tmp/scan_today.txt`、`/tmp/hermes-cwd-557d18b738ef.txt`），**0 件用户产出待归档**。
- **延续风险位全部 +1 天**：SUP-36 吴栋栋 hello 第 9 天 / SUP-39 翻 closed 第 6 天 / SUP-42 + SUP-43 累计 in_review 等翻 done 第 2 天。SUP-44 本身已经闭环。
- **idea 库 8 天无新想法入库**：今天仍 0 入库，明天 09:00 cron 跑第三次周报，连续第三次 `[SILENT]` 的概率很高；如果不想再要这个 `[SILENT]`，用户得主动说一句"记一下 X"。

## 2. 今日工作总览
- 【已完成】follow-builders 7-7 早报（SUP-44）autopilot：acp 派发 8:00 → 106 条消息跑完 prep → 二次创作（1 podcast + 14 builders / 25 tweets）→ 飞书 bot 投递 → mget 复核 → 翻 in_review，连续 5 天稳定。
- 【无活动】今日 0 用户直接对话、0 Discord 上游公告转述、0 idea 入库、0 调研 / 笔记落库、0 飞书 / 日历 / 文档主动触发。
- 【未动】SUP-36 / SUP-39 / SUP-42 / SUP-43 4 条 issue 状态全部维持昨天，无任何用户或 cron 主动 close。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【观察】今天 sessions 表里只有两条：`cron_7be51b...20260707_235034`（本 cron 自己，刚跑）+ `6bd6ff4d-f542-45cc-a3ac-afc0eeec32cc`（SUP-44 acp subagent，57 条消息，跨 0:00 UTC = 8:00 CST 起跑）。jobs.json 仍 4 个 cron（日报 / GC Day / EDN 周报 / idea-recall digest）。
- 【观察】idea-recall 周一 digest cron `2e8ecd9b7cb2` 今天不触发（周一才跑，上次 7-6 触发返回 `[SILENT]`）；明天 8 天连续无入库的产物送到第 3 个周一会继续 `[SILENT]`。

### Multica / 自驱试运行
- 【已完成】SUP-44 follow-builders 7-7 早报：acp 派发 7-7 00:00 UTC → 106 条消息跑完 → 08:05 CST 完成 bot 投递 → mget 校验 → result 评论已贴 → 翻 `in_review`。
- 【新过程产物】`.follow-builders-2026-07-07.json` 中央 feed raw 已留底到 `workdir/`，供 retry / 调试用。
- 【延续第 9 天 · 待你拍板】SUP-36 吴栋栋 hello：双路径仍堵，状态 `blocked`。
- 【延续第 6 天 · 自愈已稳态】SUP-39 仍未翻 closed，连续 5 天 SUP-40/41/42/43/44 自驱跑通——按确立的"自愈模式"判定矩阵本不算延续风险位，但状态本身留审计尾。
- 【未动】SUP-42、43 状态 `in_review` 累计 2 天，等翻 done。

### 知识库 / Obsidian / Get笔记
- 【无活动】今天没有任何用户对话 / autopliot 结果触发笔记落 Vault；SUP-44 走 lark-cli 飞书不走笔记流程。
- 【观察】vault HEAD `c1c0ba4`（昨天 7-6 日报 commit），无 pending，working tree 干净。
- 【观察】idea 库结构性缺口延续：`30-Resources/Ideas/` 仍 6 条，8 天无新增——昨天日报已建议把 Lenny《Inner game》沉淀成 imp=4 idea，等拍板。

### 飞书 / 日历 / 文档 / 消息平台
- 【已完成】SUP-44 早报 bot 投递飞书 home DM `oc_d5de551525d3a4ae9e1a52815bb9a92b`，身份 `bot`。
- 【观察】lark-cli user identity `needs_refresh` 仍在 + scope 仍缺 `im:message.send_as_user`（今天 8 点跑 SUP-44 时 agent 已显式标"user identity 缺 im:message.send_as_user scope，已 fallback 到 bot，bot 已是该 chat 成员且投递成功"）；今天 `expiresAt: 2026-07-07T10:05:48+08:00` 是滚动后的新时间戳（与昨天 10:05:50 对齐换日），token 时效层不是问题，**真正卡点是 scope 没补**——必须 `lark-cli auth login --scope "im:message.send_as_user"` 重授权才能彻底解。
- 【无活动】飞书 IM / 日历 / 文档 / 邮件今日 0 用户触发。

## 4. 重要交付物 / 系统变更
- 早报｜SUP-44「AI Builders 每日早报 · 2026-07-07」｜`in_review`（已 bot 投递 + mget 校验）｜1 podcast（No Priors × Noam Brown / test-time compute）+ 14 builders / 25 tweets / 飞书 home DM / message_id `om_x100b6bf060e4e534b037e6d98b52e29`
- 中央 feed raw｜`.follow-builders-2026-07-07.json`｜`留底`｜acp subagent workdir 内，供 retry / 调试参考
- Hermes 日报｜本篇｜待双写｜`10-Notes/Daily/Hermes/2026-07-07 Hermes 日报.md`

## 5. 风险、阻塞、需要拍板

### 5.1 [延续第 9 天 · 待你拍板] SUP-36 给吴栋栋飞书 hello
双路径堵死已 9 天：(a) `lark-cli auth login --scope "im:message.send_as_user"` 重授权 / (b) 飞书后台改应用可见范围为全员。SUP-36 本身 `blocked`，agent 不会自己再尝试。今天拍板就闭环，不然明天又是第 10 天。

### 5.2 [/tmp] 旧工件扫描 99 行 = 11 ARCHIVED + 88 NOT_ARCHIVED
今天比昨天（97 行 = 11 + 86）多 2 件 NOT_ARCHIVED。新增的 2 件 `scan_today.txt` + `hermes-cwd-557d18b738ef.txt` 都是 Hermes 内部 scratch（cron 自己写出来的扫描快照），**用户产出 0 件待归档**。按 `tmp-artifact-verdict.md` 模板继续走"全 Hermes 内部产物不归档"。

### 5.3 [延续多日 · 自愈已稳态但审计尾未收] SUP-39 / SUP-42 / SUP-43 状态
SUP-39（7-2 卡死）连续第 6 天未翻 closed；SUP-42 + SUP-43 连续第 2 天 `in_review`。5 天自驱跑通后真正卡的是 Multica workspace dashboard 的"in_progress / in_review 计数"被这三条污染。建议用户或 cron 一次性翻 done / cancelled。

### 5.4 [观察 · 关键 path 阻断] lark-cli user identity 缺 `im:message.send_as_user` scope
今天 SUP-44 又走 bot fallback 是因为 **scope 没补**（OAuth grant 阶段就没有，不是 token 过期）。`expiresAt` 滚动正常说明 refresh 链路通；只要补一次 scope，所有 SUP-* 投递都能改回 user 身份。SUP-36 hello 路径之一就是补 scope，建议今天直接补、不再讨论。

### 5.5 [观察] idea 库 8 天无新想法入库 / 即将第三次周报 `[SILENT]`
明天 09:00 cron `2e8ecd9b7cb2` 第 3 次周报大概率继续 `[SILENT]`。触发链断点是**用户说"记一下 / 这个有意思"的频次太低**；agent 不会主动 prompt 入库。要么你主动说一句补 imp=4 idea，要么接受周报 [SILENT] 常态化。

## 6. Hermes 自我改进
- **acp channel 作为 autopilot 唯一入口已事实稳态 5 天**：SUP-40 / 41 / 42 / 43 / 44 全部跑通且无重复故障点，SUP-39 的 stall 是一次性窗口 bug。建议把"acp channel probe → follow-builders 派发 → 飞书投递"这条链路沉淀成 SOP（写进 follow-builders skill 文档"触发与运行机制"段），免得后续跟进者只看 jobs.json 又误判漏跑。
- **prepare-digest.js 仍会重复 digest 同一篇 blog**：昨天 7-6 已暴露 Foundation Models 重复 digest，今天 7-7 又以"0 blog"对冲（其实 7-6 feed 仍然只有那 1 篇 Claude Blog）。修补应该加 client-side seen-state（`state-feed.json` 落 hash set → 下次拉取时排除），但都没人动手——下一次修复机会是 EDN 周报 backlog 评审。
- **`/tmp` 扫描连涨 NOT_ARCHIVED 但全部 scratch，噪声模式稳定**：scan_tmp_artifacts.sh 输出含 hermes-cwd-*.txt / hermes-results/*.txt / scan_today.txt 等已知 Hermes 内部产物，每天 +1 ~ +2 是常态。建议脚本默认 exclude `*-cwd-*.txt` + `hermes-results/` + `scan_*.txt`，减少日报重复"全部 scratch"的噪声。

## 7. 明日建议
- **[需要你拍板 · 今天决策为佳] SUP-36 吴栋栋 hello 路径 a / b 二选一**：补 scope / 改可见范围，cron 不会自作主张。
- **[需要你拍板] 一次性翻 SUP-39 / SUP-42 / SUP-43 状态**：你回一句"翻 done"，cron 自动跑 multica issue status — 一劳永逸消除 dashboard 污染。
- **[可自动做 · 建议补一次] lark-cli user identity scope 重授权**：用户拍板即跑 `lark-cli auth login --scope "im:message.send_as_user"`，之后所有 SUP-* 自动改回 user 身份投递，SUP-36 hello 路径之一自然解锁。
- **[可自动做] GC Day 真做机制第一次自动跑**：明天（2026-07-11 周六 09:00，cron `8dc968bf2488`）按新机制自动体检 MEMORY/USER；预计 patch 现成 skill + 缩段落，可逆操作不需要拍板；新建 skill 仍需你点头。
- **[需要你拍板] Lenny《Inner game》沉淀 imp=4 idea**：补"个人能力 / 产品哲学"维度缺口；昨天 7-6 已建议，今天继续等。
