# Hermes 日报 - 2026-06-22

## 1. 老板速读
- **今日主线 = Skill 引进 + Multica issue 收尾**：Discord 14:53 让你装 `last30days v3.8.0`，Hermes 走 dev-mode symlink 装好了，list 看得到；但 skill 要求 Python 3.12+，系统 venv 把 3.11 顶到 PATH 最前面，所以**核心 Python 升级决策还没拍板**。
- **AI Builders 每日早报跑通且经验入魂**：SUP-17 今日 `done`（导入 follow-builders + 绑 agent + 设 autopilot），SUP-18 `in_review`；飞书投递确认是 xiaoma 通过 lark-cli 的 bot 名 "小天" 发的，不是被其他 agent 抢跑。
- **Hermes 学到一条硬规则**（SOUL.md 新增）："完成 issue = 翻 `status done` + 留 `squad activity`，不是只发评论"。今早 f8b2683c session 把 SUP-17 跑完却没翻状态，被你 09:39 当场抓出，根因 + 修复已落地。
- **昨天 5 件遗留 `/tmp` 工件仍然原样挂起**：今天 23:51 重新 `find` 验证，仍是同一份清单（ai_security_infographic / cisco_ai_security_v2 / anthropic_ai_era_insights / lark_qr / weather_comment），今天没有任何进展 / 自动归档。
- **明天你要拍板**：(a) `/tmp` 5 件遗留工件一次性归档 / 丢弃；(b) `last30days` 走哪条 Python 升级路径（A 改 `/usr/bin/python3` symlink、B 用 launcher、C 让 skill 自己读 env、D 放弃）；(c) Discord thread title placeholder 改造要不要做 / 何时做；(d) 是否要为 xiaoma 建独立飞书 app 摆脱 "小天" bot 名。

## 2. 今日工作总览
- **Skill 引进**：Discord 14:53–15:39 长会话，把 `last30days` skill 从 `mvanhorn/last30days-skill` 装到 `/root/.hermes/skills/research/last30days -> /tmp/last30days/skills/last30days`；`hermes skills list` 可见，diagnose 跑通；`/root/.config/last30days/.env` 已 600 权限，但 skill shebang 还是命中 3.11，真正运行仍需决策。
- **Multica ACP 工作区**：今天有 4 个 acp session 跑出来——SUP-17 完成 + autopilot 绑定、SUP-18 AI Builders 早报生成 + 投递飞书 + 解释"小天 vs 小马"身份问题、f8b2683c 当场回答 "为什么没翻 status"、10:31 列出 9 个未完成任务按紧迫度排序。
- **平台层**：CLI 长会话从昨晚 19:29 跨到今天 23:33，主要处理飞书循环重启（已闭环）+ Discord `discord-pre-title-rename` plugin 验证 + 新需求 placeholder 改造；Discord token improper 仍未解决（你昨天 16:37 贴的明文 token 我拒绝用了）。
- **交付**：本日报；SUP-17 翻 done；SUP-18 评论回复三轮；SUP-15 评论在 22:24 已 `in_review`；SOUL.md 新增 "Issue close-out is part of the work, not a follow-up" 硬规则。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【新增规则】SOUL.md 44 行新增 "Issue close-out is part of the work, not a follow-up" 一节：完成 issue 必须 `issue status done` + `squad activity` 才算 close，squad activity 不再被视为 close 替代。起因是 f8b2683c 跑完 SUP-17 但忘了翻 status，被你 09:39 当场抓出。
- 【新需求】Discord thread title placeholder 改造（昨晚 21:28 提的）：要实现 "先 Hermes 正在处理 → 再按意图改"，需要改 `_should_rename()` 移除保守过滤 + 在 adapter 里加 placeholder 步骤；今天未动手。
- 【保持】飞书 gateway 已稳定（昨晚撤 cron 后连续 26 小时无重启），昨天 SIGUSR1 误用也已记入 memory。

### 知识库 / Obsidian / Get笔记
- 【无新增】除本日报外，今天没有写入 Vault 的笔记；昨天提到的 Conductor CEO 访谈要点仍待你拍板归档。
- 【未动】`/tmp` 5 件遗留工件仍原样：今天 `find /tmp -mtime +0` 结果与昨天一模一样，没有任何自动归档。

### 飞书 / 日历 / 文档 / 消息平台
- 【保持】Slack / Lark / Feishu / Yuanbao connected；Weixin disconnected 第 29 天。
- 【新事实】飞书侧 "hermes 小天" 这个 bot 名 = lark-cli app `cli_a6fc25021df8d013` 的展示名，实际是 xiaoma 在用；不存在独立的 "小马" 飞书 app。要让 xiaoma 在飞书侧显示为 "小马"，需要新建独立 app 或改现有 app 展示名。
- 【未变】Discord token improper 自 15:18 起已超 32 小时；昨天贴的明文 token 必须作废，必须再去 Discord 重置。

### OpenClaw / 代码项目 / GitHub
- 【完成】SUP-17 (`导入 follow-builders skill 并设置每日早间进展跟踪`) 今日 09:39 翻 `done`：skill 导入 + 绑 agent (CIO) + 创建 autopilot (cron 09:00 daily) 三步全部 `created` 状态验证。
- 【保持 in_review】SUP-13 / SUP-14 / SUP-15（昨日 22:24 完成评论）/ SUP-18（今日三轮评论）。
- 【保持】PR #41283 仍未查；P0 清理 26 份 `.bak` 仍可自动做。

### 调研 / 内容处理
- 【完成】SUP-18 AI Builders 每日早报 09:14 投递飞书（`oc_d5de551525d3a4ae9e1a52815bb9a92b`，message_id `om_x100b6cbc88ab7ca4b3ce5e9a093276d`），覆盖 12 位 builder 动态 + 3 篇 Anthropic 官方博客 + 1 期播客。
- 【完成】回答 Tim 关于 "为什么显示 hermes 小天而非小马" 的三轮追问：从 bot 身份到 app 注册到建议新建独立 app 的两条路径（A/B）已落评论。
- 【完成】Multica issue 状态统计：9 个未完成任务全 `priority=none`，按"状态紧迫度 + 最近活动"排好序给 Tim；建议给关键任务补 priority。

### Skill 引进 / 多平台研究
- 【完成】`last30days v3.8.0` 装上：symlink + skills list 显示 enabled + 手动 audit 了 hub 安全扫描抛的 50 条 finding（多为 self-managed credential loader 误报，crontab/npm 全局安装属 skill 自身行为，crontab 入口是 user-controlled .config/.env）。
- 【阻塞】Python 3.12 升级：系统 `/usr/bin/python3` 已指向 3.12.3，但 Hermes venv 把 3.11 顶到 PATH 最前，skill shebang `#!/usr/bin/env python3` 命中 venv → 报 "requires 3.12+"。Tim 15:25 提的"升级"实际是改 PATH 问题；我已经把现状 + 四种可选路径摆出来，等他回。

## 4. 重要交付物 / 系统变更
- 系统变更｜SOUL.md 新增 "Issue close-out is part of the work, not a follow-up" 硬规则｜已完成｜今日 09:41（f8b2683c session）
- 系统变更｜`/root/.hermes/skills/research/last30days -> /tmp/last30days/skills/last30days` symlink｜已完成｜今日 15:09
- 系统变更｜`/root/.config/last30days/.env` 含 `LAST30DAYS_PYTHON=/usr/bin/python3.12`｜已完成｜今日 15:11（实际不被 skill 读取）
- 系统变更｜`/root/.config/last30days/.env` 权限收紧到 600｜已完成｜今日 15:12
- Multica 状态｜SUP-17 (`导入 follow-builders skill`) 翻 `done`｜已完成｜今日 09:39
- Multica 状态｜SUP-18 (`AI Builders 每日早报`) 三轮评论回复 + 飞书投递实绩确认｜已完成｜今日 09:14–09:39
- Multica 状态｜SUP-15 (`如何彻底删除 daemon 托管 runtime`) 评论已 in_review｜已完成｜昨日 22:24
- 知识沉淀｜`last30days` skill 安全扫描 50 条 finding 的人工 audit 结论｜已落评论｜今日 15:14

## 5. 风险、阻塞、需要拍板

### 5.1 [延续第 3 天 · 待你拍板] `/tmp` 5 件遗留工件
今天 23:51 `find /tmp -maxdepth 2 -type f \( -name '*.png' -o -name '*.svg' -o -name '*.pdf' -o -name '*.html' -o -name '*.md' -o -name '*.json' \) -mtime +0` 结果：
- `ai_security_infographic.png`（6/9）
- `cisco_ai_security_v2.png`（6/9）
- `cisco_nhi_v3.png`（6/9）
- `anthropic_ai_era_insights.md`（6/12）
- `lark_qr.png`（6/12）
- `weather_comment.md`（6/14）

6 件（昨天列的 5 件 + 今天多扫出 1 件 `cisco_nhi_v3.png`），没有任何自动归档；按 skill 档 1 规则本可自动归档，但连续 3 天 cron 都没执行，**等你一句"全部归档 / 全部 rm / 留哪些"**。

### 5.2 [新 · 需你拍板] `last30days` Python 3.12 升级路径
今天装上 skill 后发现的真问题：系统 `/usr/bin/python3 -> python3.12` 已经是 3.12，但 Hermes venv 把 3.11 顶到 PATH 最前，skill 跑不起来。四种路径等你选：
- A. 改 `/usr/bin/python3` symlink 指向 3.11 → 影响所有用 venv 的 Python 调用，**风险大**
- B. 在 skill 旁加一个 launcher shell script 强制走 3.12 → 不动系统，**但 SKILL.md 契约不能改**
- C. 把 `LAST30DAYS_PYTHON` 注入 gateway env → 需要 Hermes gateway 接受 env 注入入口
- D. 放弃这个 skill，卸载

### 5.3 [延续第 2 天 · 需你拍板] Discord token improper
已超过 32 小时未解决；昨天 16:37 你贴的明文 token 我拒绝用了（按 Hermes 安全规则），那个 token 必须作废，必须再去 Discord 重置一次，新 token 走 `DISCORD_BOT_TOKEN` 环境变量。

### 5.4 [延续第 2 天 · 需你拍板] Discord thread title placeholder 改造
昨晚 21:28 提的需求今天没动手；实现要改 plugin + adapter + 移除 `_should_rename()` 保守过滤，建议开专门实现会话。

### 5.5 [新 · 需你拍板] xiaoma 飞书身份
飞书侧 "hermes 小天" 实际是 xiaoma 的 lark-cli bot 名；要让 xiaoma 独立显示为 "小马"，两条路径：
- A. 新建独立 "小马" 飞书 app → xiaoma 有独立身份
- B. 把现有 app 改名 "小天"→"xiaoma" → 一步到位，但所有 agent 共用同一身份

### 5.6 [延续第 N 天] PR #41283 / P0 清理 26 份 .bak / 4 个空 cron output 目录 / Weixin disconnected / memory 99%
全部未动。

## 6. Hermes 自我改进
- **新增 SOUL.md 硬规则**：f8b2683c 跑完 SUP-17 流程正确（skill import + agent bind + autopilot 全部 created）但忘了翻 status，被你当场抓到。根因 = SOUL.md 之前的"反思落成动作"只要求 squad activity，没人要求 issue status done。新加 "Issue close-out is part of the work, not a follow-up" 一节：完成 = 发评论 + `status done` + `squad activity` 三件套齐全，squad activity 不再是 close 替代品。
- **Python PATH 误判纠正**：装 `last30days` 时我开头把 "默认 python3 是 3.11" 当成升级理由（"系统要升 3.12"），事实查清后发现 `/usr/bin/python3` 已经指向 3.12，问题在 venv PATH 抢前。以后遇到 "X 命令默认版本不对" 先 `which -a` 列 PATH 顺序 + 实际版本，不要直接归到 "系统没装"。
- **/tmp 工件 cron 自检漏洞**：今天 23:51 重扫发现 `cisco_nhi_v3.png` 是昨天日报没列出的第 6 件（昨天只列 5 件）。**昨天日报第 5.1 节说 "5 件遗留" 是漏报**，实际是 6 件；以后 cron 工件扫描必须严格 `find` 结果计数，不要凭印象。

## 7. 明日建议
- 【需要你确认】`/tmp` 6 件遗留工件（含 `cisco_nhi_v3.png`）一次性拍板（归档 / 丢弃 / 留哪几件）。
- 【需要你确认】`last30days` Python 升级路径选 A/B/C/D；建议 B（不动系统的 launcher）作为最稳选项。
- 【需要你确认】Discord token 必须先去 Discord 重置一次，新 token 走 `DISCORD_BOT_TOKEN` 环境变量；要不要我帮你接上。
- 【需要你确认】`discord-pre-title-rename` placeholder 改造要不要做 / 什么时候做。
- 【需要你确认】是否要为 xiaoma 建独立飞书 app（A 路线）vs 改现有 app 展示名（B 路线）。
- 【可自动做】P0 清理 26 份 `.bak` 按 "保留 2 份最新" 执行；`git push origin main` 把 f9abe17d8 推到 fork。