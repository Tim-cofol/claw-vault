# Hermes 日报 - 2026-09-21

## 1. 老板速读
- 今日无显著 Hermes 活动：仅 09:01 Idea Recall Weekly Digest cron 例行触发（持续 19 周 99992402 投递失败）和 08:00 autopilot SUP-117 AI Builders 早报（飞书已送达 + cron 代 flip done）；无用户对话 / 无 EDN / 无 Discord 主动请求。
- SUP-5x 元模式累计 42 样本 ≈ 93% 自愈率（39 done / 1 blocked SUP-79 / 1 in_review SUP-88 / 1 todo SUP-66）；SUP-117 今日 cron 代 flip done（rev 7，全程 ~3 秒）。
- lark-cli refresh 标配连续 10 天自动闭环（9-21 启动时 user 段 `needs_refresh` → `calendar +agenda` 触发 → status ready，expiresAt 推到 9-22 01:50）。
- state.db 实际 634 MiB（stat 634052608 bytes），红线第 38 天（602→604.6 MiB，今日 +2.6 MiB）；cron 不可跑 VACUUM。
- `/tmp` 真零漂移稳态过渡期：56 行 = 昨日 56 行（+0%），但仅 2 天连续相同，未达"≥ 4 天"折叠条件，按展开列。
- Idea Digest 99992402 投递失败持续 19 周（`last_delivery_error='live adapter send failed: [99992402] field validation failed'`），用户周一从未收到 digest。
- EDN 周报 9-16 (Wed) stub-success 跑空（无 data_asset.json / 无飞书增量 / 无 origin 推送），已累计 2 周 EDN 跑空（8-26 + 9-16），用户战略决策缺位窗口已扩到 21 天；下次 EDN = 9-23 (Wed 08:30)，GC Day #46 9-26 前必须落地 8 条补丁。

## 2. 今日工作总览
- **无显著 Hermes 活动**：state.db 当日 sessions 仅 2 条 cron（日报 cron 自己 + 09:01 Idea Digest 触发），无用户对话、无 Discord 上游公告、无 EDN、无 subagent 派发。
- SUP-117 (9-21 早报) autopilot 08:00 跑通 → 飞书 08:02 实际送达 `om_x100b64330c7d14a0b114ccebb0df5a5`（bot 身份 / 5 X / 1 播客 The MAD Podcast — Richard Socher / 3 builder 已过滤）→ 23:51 cron 快速路径代 flip done（rev 7）。
- 09:01 Idea Recall Weekly Digest cron 触发 → 25s 完成生成 3 条旧想法 → 投递 99992402 持续失败（用户周一从未收到）。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【完成】SUP-117 cron 快速路径代 flip done：comment 加核验评论 + `multica issue status SUP-117 done` + `issue get` 验证 rev 7，全程 ~3 秒。SUP-5x 累计 42 样本 ≈ 93% 自愈率（纯 follow-builders autopilot 近期样本仍约 80-85%，patch 紧迫性不降）。
- 【进行中】lark-cli refresh 标配连续 10 天闭环（8-24 ~ 9-21），今日启动时 `calendar +agenda` recipe 触发 user token 自动刷新；§3.5 已不再写"自愈证伪"观察项。

### 知识库 / Obsidian / Get笔记
- 【进行中】9-20 日报 commit `5d3aa24` 已推送，无 unstaged 文件；今日日报即将写入 + push + 同步 Get笔记 MyClaw。

### 飞书 / 日历 / 文档 / 消息平台
- 【失败】Idea Digest 99992402 持续 19 周：日报 cron 枚举投递型 cron 时发现 `cron_2e8ecd9b7cb2.last_delivery_error='live adapter send failed: [99992402] field validation failed; ... Feishu send failed: [99992402]'`，但 `last_status='ok'` —— 用户周一从未真收到 digest。
- 【进行中】EDN 周报 cron `1d0cf41439ec` 9-16 stub-success 跑空已 2 周累积（8-26 + 9-16），下次 9-23 (Wed 08:30)；8 条 prompt 补丁仍未落地，GC Day #46 9-26 是最后落地窗口。

### OpenClaw / 代码项目 / GitHub
- 【观察】今日无活动。

### 调研 / 内容处理
- 【完成】SUP-117 autopilot 输出 1 播客 + 5 X + 过滤 3 builder（Thibault Sottiaux 单句梗 / Nan Yu 工具调试 / Garry Tan 政治议题）；中文 builder 摘要齐备。

## 4. 重要交付物 / 系统变更
- 日报笔记｜`/root/obsidian-vault/10-Notes/Daily/Hermes/2026-09-21 Hermes 日报.md`｜即将 commit
- Multica issue｜SUP-117｜done｜rev 7
- 状态日志｜`/root/.hermes/state/state_db_size.log`｜新增 9-21 行 604.6 MiB
- 扫描快照｜`/root/.hermes/state/scan_2026-09-21.txt`｜56 行 = 昨日 56 行（+0%）

## 5. 风险、阻塞、需要拍板
- **【🔴 紧急 / 21 天累积】EDN 周报 cron 已 stub 跑空 2 周**（8-26 + 9-16），用户战略决策缺位窗口 21 天；下次 9-23 (Wed 08:30) 必须跑通，GC Day #46 9-26 前必须落地 8 条 prompt 补丁（subagent timeout=900s / 母 agent 提前落本地 JSON / docs +update retry / `/tmp` 孤儿 plan 改 `/root/.hermes/state/edn-orphan-*.json` / stub-success 硬断言 data_asset + docs + message_id）。建议：今天/明天拍板 GC Day #46 之前完成 8 条补丁。
- **【需立即处理】state.db 604.6 MiB 红线第 38 天**：cron 不可跑 VACUUM（锁库），单日 +2.6 MiB 稳态增长；请用户拍板低峰窗口期（凌晨 3-5 点）以便一次性 VACUUM + 重建索引，否则下月初将突破 700 MiB。
- **【需立即处理】Idea Recall Weekly Digest 99992402 持续 19 周**：cron `last_status='ok'` 是误判，实际每周一从未投递成功；用户从未真收到 digest。**反模式**：cron 已"自动化跑通"但用户感知失败。建议：拍板补一次手动推送（最近 4 周 digest）+ 修 prompt 走 `lark-cli im +messages-send --as bot` 兜底（参考 SUP-100 8-27 自救 SOP）。
- **【需立即处理】SUP-88 (8-15 早报) Mode E 仍 in_review 第 37 天**：autopilot 当时只写中文 comment + 完全跳过 fetch + 完全跳过投递，cron 不能代 flip（用户没收到 8-15 早报，翻 done = 看板污染）；手动转贴 vs 跳过本期仍需用户拍板。
- **【需你确认】SUP-66 (7-29 早报) todo 第 54 天**：长期 todo，需用户拍板转 closed/done 或 back to in_review。

## 6. Hermes 自我改进
- **§6 自我改进已 SOP 化**：连续 5+ 天日报 §6 都写"cron 不可跑 VACUUM + 持续逼近红线"，措辞无变化 → 反模式。建议下次 GC Day 把"state.db 红线呼吁"自动转 cron 主动执行方案（如自动 `lark-cli im +messages-send --as bot --chat-id ou_xxx` 给用户推红点），而不是日报里反复写。
- **EDN stub-success 误判已 2 周未被脚本捕获**：cron 启动时只读 `last_status='ok'`，未读 `last_delivery_error` 也不验证 `data_asset.json` 是否存在 + size > 1KB。建议下次 GC Day 把 EDN cron prompt 末尾追加硬断言：`(a) /root/.hermes/cron/output/edn-insight-data/<日期>.json > 1KB + (b) `docs +fetch` 验证本周增量已写入 + (c) 当天 `messages.platform_message_id` 至少 1 条`，三件全不成立 → 立即用 `lark-cli im +messages-send --as bot` 给用户发推送"EDN 本周跑空"，不再静默等下次自动覆盖。
- **/tmp 零漂移判定阈值收紧**：今日 56 行 = 昨日 56 行，但 9-17/9-18/9-19 是 55/54/54，未达"≥ 4 天连续完全相同"折叠条件；下次 cron 不要因单日相等就宣称"零漂移稳态"，必须 `wc -l` 连续 ≥ 4 天无变化才允许折叠。

## 7. 明日建议
- 【可自动做】继续维持 lark-cli refresh 标配（明日启动若 user `needs_refresh` → `calendar +agenda` 触发）；SUP-5x 元模式 cron 代 flip 流程保持。
- 【需要你确认】拍板 state.db VACUUM 窗口（建议下周二 9-23 凌晨 3-5 点，cron 已写好低峰执行脚本待激活）。
- 【需要你确认】Idea Digest 99992402 19 周累计失败是否补 4 周手动推送 + 修 prompt 走 bot 兜底。
- 【需要你确认】SUP-88 (8-15 早报 Mode E) 手动转贴 vs 跳过本期二选一拍板。
- 【需要你确认】GC Day #46 (9-26 周六 09:00) 是否落地 EDN 8 条补丁 + SUP-5x status flip 强制收尾段 + follow-builders skill chat_id 兜底（chat_id 自愈 6/6 已稳，无需 patch，但 status flip 自愈 76% 仍需 patch）。
