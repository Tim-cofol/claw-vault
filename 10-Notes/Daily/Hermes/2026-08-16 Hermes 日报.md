# Hermes 日报 - 2026-08-16

## 1. 老板速读

- 今日继续静默日第 6 天：**0 用户直接对话 / 0 派发子 agent**；仅 autopilot 1 次成功投递 + cron 自身 1 次。
- **SUP-89 (8-16 AI Builders 早报) autopilot 跑通**：飞书送达 `om_x100b673bafc2b4a8b1ce9e705517a85`（16 builder / 32 推文 / 1 博客「Claude × Apple Foundation」/ 1 播客「Mitch Troyanovsky 谈长时 agent」），chat_id 大小写自愈（`OC_` → `oc_`），但 autopilot **漏翻 status**，今日 cron 已按快速路径代 flip done。
- **state.db 501 MiB**（8-15 500 → 8-16 501，**已踩 500 MiB 红线第 2 天**，trend +1-5 MiB/天 稳定）；连续 9 天呼吁 VACUUM 窗口未拍板。
- **lark-cli user identity 仍 `needs_refresh`**（expiresAt 2026-08-16T10:02:15，今日 23:50 已过 ~13h48m），今日 autopilot 飞书投递**用了 bot 身份**（之前 user API 调用未触发 refresh 自愈路径未验证）。
- **明日必拍板**：state.db VACUUM 窗口（已踩红线第 2 天，明日 #36 GC Day 不会自动落）；SUP-88 8-15 早报**飞书未投递**是否要手动补（comment 齐但 0 om_x）。

## 2. 今日工作总览

- **0 用户直接对话**（state.db 当天仅 cron 自身 + 1 条 subagent 残留 user 消息）。
- **0 派发子 agent**。
- **autopilot 成功 1 次**：SUP-89 (8-16 早报) 完整跑通 fetch + 中文汇总 + 飞书送达 + chat_id 自愈，但 status flip 漏翻 → cron 代 flip done。
- **autopilot 失败延续 1 次**：SUP-88 (8-15 早报) 仍 `in_review`（Mode E：comment 齐但未投递飞书，无 om_x / oc_），cron 不能代 flip。
- **lark-cli 自愈路径未验证**：今日 cron 启动时 user identity 仍 `needs_refresh`，expired 13h48m；今日 autopilot 飞书送货成功用的是 bot 身份（因为 autopilot 走 bot 投递不触发 user API refresh）。
- **状态**：典型「周末静默 + autopilot 自愈 chat_id + 漏翻 status 持续 + state.db 持续踩红线」。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统

- 【已闭环】SUP-89 (8-16 早报) **cron 代 flip done**（快速路径，autopilot 自写 comment 已含 om_x + oc_ + 中文 builder / 推文 / 博客 / 播客 4 点内容摘要）。SUP-89 完整内容 = 16 builder / 32 推文 / 1 博客「Claude 接入 Apple Foundation Models framework」/ 1 播客「MAD Podcast — Mitch Troyanovsky（Basis）谈长时 agent 设计」；今日重点：① Aaron Levie 复盘 Cursor 退出（applied AI playbook 五要素）；② 长时 agent 设计应向人类组织方式学习；③ Claude × Apple Foundation 三行 Swift 集成。
- 【新发现】**chat_id 大小写自愈能力持续具备**（SUP-80 / 83 / 85 / 89 共 4 次全部自愈，100% 兜底成功率），但 **status flip 自愈率仍 50%**（14 样本累计 = 7 自愈 / 6 漏 / 1 blocked = 50% 自愈率）。**强约束**：双轨 patch 仍需 8-22 GC Day 落地——chat_id 兜底已稳定、status flip 仍不可靠。
- 【持续】**state.db 501 MiB**（500 → 501，+1 MiB，单日 +1-5 MiB 稳态已 7 天），正式踩 500 MiB 红线第 2 天；连续 9 天呼吁 VACUUM 窗口未拍板。
- 【未验证】`lark-cli` user identity 自愈路径仍未验证：今日 expiresAt 过期 13h48m，今日 autopilot 飞书送达走 bot 身份（不触发 user API refresh），自愈是否生效仍要等下一次 user API 调用。

### 知识库 / Obsidian / Get笔记

- 【进行中】本日 8-16 日报生成、走 skill 标配双写（vault → git commit/push → 同步 getnote）。
- 【未变】`scripts/getnote_sync_obsidian.py` 仍位于 `/root/.hermes/skills/openclaw-imports/obsidian-getnote-dualwrite/scripts/`，vault 内无脚本副本；沿用 OpenAPI fallback + 幂等映射 `getnote_sync_map.json`。

### 飞书 / 日历 / 文档 / 消息平台

- 【静默】今日无用户主动会话。
- 【待刷新】`lark-cli auth status` user identity `needs_refresh`（access token expiresAt 2026-08-16T10:02:15，今日 23:50 已过 ~13h48m），scope 列表齐（含 `im:message.send_as_user`）；今日 SUP-89 autopilot 飞书送达走 bot 身份（message_id 仍写入 platform_message_id），**未触发 user API 自愈**。
- 【活跃】SUP-89 飞书送达 + chat_id 自愈成功（autopilot comment 显式说明小写发送）。

### OpenClaw / 代码项目 / GitHub

- 【无活动】今日无任何项目推进。

### 调研 / 内容处理

- 【完成】SUP-89 autopilot 完整跑通：fetch 阶段（X / 博客 / 播客） + 中文汇总 + 飞书投递，`/tmp/fb-blogs.json` / `fb-podcast.json` / `fb-x.json` / `digest-data.json` / `chats*.json` 全部 8-16 08:00 落地 + `/tmp/digest-2026-08-16.md` 渲染完毕。
- 【延续】SUP-88 (8-15) 仍未投递飞书，今日 0 落地动作。

## 4. 重要交付物 / 系统变更

- **type**: 日报 / **name**: `2026-08-16 Hermes 日报.md` / **status**: 已写入 vault 待 git commit + Get笔记同步
- **type**: Multica issue / **name**: SUP-89 (8-16 AI Builders 早报) / **status**: `done`（cron 代 flip，autopilot 自写 comment 已含 om_x100b673bafc2b4a8b1ce9e705517a85 + oc_d5de551525d3a4ae9e1a52815bb9a92b + 完整内容摘要）
- **type**: 状态日志 / **name**: `/root/.hermes/state/state_db_size.log` / **status**: 已 append 2026-08-16: 501MB（连续无缺口）
- **type**: Autopilot 产物 / **name**: `/tmp/digest-2026-08-16.md` + `fb-*.json` (8-16 08:00-08:02) / **status**: 已生成（autopilot 当日产物，按白名单不计入归档扫描）

## 5. 风险、阻塞、需要拍板

- 【**🔴 紧急**】`~/.hermes/state.db` **501 MiB**（**已踩 500 MiB 红线第 2 天**！昨日 500 → 今日 501，单日 +1 MiB，trend +1-5 MiB/天 已稳定 7 天）。**连续 9 天呼吁 VACUUM 窗口未拍板**。GC Day cron 不会自动 VACUUM（锁库风险）。建议：明早 0:00-2:00 停 gateway 跑 FTS rebuild + VACUUM；或同意 cron 在 SIZE > 500 时自动低峰 VACUUM。
- 【**🔴 紧急**】**SUP-88 (8-15 早报) 飞书未投递延续第 2 天**——comment 完整（19 builder / 36 推文 / 1 播客），但 autopilot 跳过了 fetch 阶段 + 完全未投递飞书，今天仍未补救。**请确认**：今天是否手动把 SUP-88 comment 内容转贴飞书 chat `oc_d5de551525d3a4ae9e1a52815bb9a92b`，或今天跳过等 8-17 autopilot 正常跑。
- 【需 GC Day 8-22 落地】**follow-builders skill 双轨 patch**（已呼吁 8+ 天）：① status flip 强制收尾段（即使 chat_id 自愈成功也要执行 `multica issue status SUP-N done`）；② chat_id 大小写默认兜底（已稳定 4 次实证，可写为硬规则）；③ **新增** "comment 必须含 om_xxx + 至少 1 个 `/tmp/fb-*.json` 落地" 双断言（避免 Mode E 重现）。今日 SUP-89 实证双轨③ 已经过 fetch 阶段 + 飞书送达，只是 status flip 漏翻——两个失败模式应分开 patch。
- 【可继续观察】`lark-cli` user identity 自愈路径仍未验证——今日 13h48m 过期，无 user API 调用触发 refresh。明日 cron 启动时跑一次 `lark-cli auth status` 看 expiresAt 是否被更新。
- 【可继续观察】`/tmp` 旧工件扫描 96 行（3 ARCHIVED + 93 NOT_ARCHIVED）vs 昨日 72 行（+24）；其中 26 件是 8-15 23:33 落地的 `oc-evolution-20260815-BUqGyI/` 子树（昨天 `-mtime +0` 0-day 龄被跳过，今日 1-day 龄首次抓到，属 YDAY_LEAK 同一机制），2 件为今日 autopilot 当日产物（按白名单不计入）。**无幽灵延续项**。

## 6. Hermes 自我改进

- **【SUP-5x 元模式 14 样本累计 + 50% 自愈率 + 双轨失败模式实证】**SUP-89 今日实证两个失败模式**完全独立**：fetch + translate + 飞书投递（chat_id 自愈成功）+ 中文 comment 全部正常，**唯独 status flip 漏翻**。这与 SUP-88 Mode E（comment 齐 + 飞书未投递）属于**不同失败模式**。**强约束升级**：8-22 GC Day patch 必须双轨分开执行——chat_id 兜底已稳定 4/4 = 100% 可写硬规则；status flip 仍 0 可靠 patch 优先级更高；Mode E 防御（comment 必须含 om_x + `/tmp/fb-*.json`）作为独立第三条。
- **【state.db 501 MiB 红线延续第 2 天 + 9 天呼吁未拍板】**今日 cron 启动时仍 500 MiB（8-15 报告点 500），今天 +1 MiB=501 MiB。**反模式风险**：连续 9 天同款呼吁可能让用户决策疲劳，**明日 §7 建议必须给具体时间窗口**（"0:00-2:00"）而非"本周内"。
- **【lark-cli user identity 自愈实证发现】**今日 autopilot 飞书送达走 bot 身份（不触发 user API refresh），**自愈路径未验证与"autopilot 默认走 bot"互为因果**——autopilot 内部 prompt 若改"必须走 user API"，会触发 refresh，但会引入"飞书以用户身份发"而非 bot 的副作用。**强制规则**：GC Day 8-22 应评估 follow-builders skill 是保持 bot 投递（当前行为）还是强制 user 投递（触发 refresh）——这两个改动需用户拍板。

## 7. 明日建议

- 【需要你确认】**state.db VACUUM 窗口**——今天起 8-17 后 0:00-2:00 跑 FTS rebuild + VACUUM；或直接同意 cron 在 SIZE > 500 时自动低峰 VACUUM。**这是连续 9 天呼吁的硬 deadline**，再不处理下周 #36 GC Day 之前可能进入只读。
- 【需要你确认】**SUP-88 飞书投递补救**：今天 19 builder / 36 推文 / 1 播客摘要齐全但未投飞书，请决定是否手动把 SUP-88 comment 内容转贴飞书 chat `oc_d5de551525d3a4ae9e1a52815bb9a92b`，或今天跳过等 8-17 autopilot 正常跑。
- 【需要你确认】**GC Day 8-22 patch 清单升级**（SUP-5x 元模式 14 样本 + state.db 500 MiB 触线第 2 天）——建议 patch follow-builders skill 一次性落 3 件：① status flip 强制收尾（即使 chat_id 自愈成功也要执行 `multica issue status SUP-N done`）；② chat_id 大小写默认兜底（已实证 4/4，写硬规则而非概率事件）；③ **新增** "comment 必须含 om_xxx + 至少 1 个 `/tmp/fb-*.json` 落地" 双断言（避免 Mode E 重现）。
- 【可自动做】明日 cron 启动前先确认 SUP-90 (8-17 早报) autopilot 飞书投递 + 是否需要代 flip；同时观察 SUP-88 Mode E 是否复现（comment 齐 + 飞书未投递）。
- 【可选】下周 #36 GC Day cron 本周六 09:00 健康，无需介入；若本周内能拿到 state.db VACUUM 拍板，下周 #36 自动落 `state.db.bak_gc_20260725` 删除 + state-snapshots 滚动保留（保留最近 2 个）。
