# Hermes 日报 - 2026-08-30

## 1. 老板速读

- 今天你没有直接跟 Hermes 对话（静默日第 20 天），自动化任务照常跑。
- AI Builders 早报按时送达飞书（11 位 builder），但 autopilot 又漏翻 issue 状态，已由日报 cron 代收尾。
- **state.db 已到 527 MB，连续 16 天压在 500 MB 红线之上**，仍在等你给一个凌晨 VACUUM 窗口——这是今天唯一真正需要你拍板的事。
- EDN 周报连续 4 周跑空（7-29 / 8-12 / 8-19 / 8-26），昨天 GC Day #14 又没落地 prompt 修复；下次触发是 9-02（周三），不改必定第 5 次跑空。
- 飞书 user 身份的 token 刷新已完全自动化（连续 7 天自愈），这条不再需要你管。
- `/tmp` 工件净 +1 件，是昨天 GC Day 自己留下的脚本，非新增待办。
- 明日优先：给 VACUUM 窗口 + 决定 EDN 周报是修 prompt 还是暂停。

## 2. 今日工作总览

- **无用户直接对话**：全天仅 1 个 cron session（本日报），无人工请求。
- **AI Builders 早报（SUP-103）**：08:03 飞书成功送达，内容齐备；autopilot 未自翻状态，cron 已代 flip `done` 并留核验记录。
- **飞书身份维护**：启动时检测 user token 需刷新，主动触发后恢复 `ready`（有效期推到 8-31 01:50）。
- **系统健康检查**：state.db 体积、`/tmp` 工件、cron job 状态全部完成核对。
- **未闭环的历史项**：EDN 周报 prompt 修复、SUP-88 / SUP-79 两条待你拍板的早报 issue 仍原地不动。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统

- 【已完成】SUP-103 早报 issue 收尾：核验飞书送达证据后代翻 `done`，看板恢复干净。
- 【风险】autopilot「投递成功但不翻状态」的老毛病本月第 9 次出现，成功率约 8 成，仍需保留 cron 兜底，不能撤。
- 【阻塞】昨天 GC Day #14 报告里点名的 EDN 周报 5 条 prompt 修复，一条也没写进 cron 配置。

### 飞书 / 消息平台

- 【已完成】早报正文投递到你的飞书私聊，11 位 builder 覆盖，4 位因内容与 AI 无关被跳过。
- 【已完成】user 身份 token 自动刷新，连续 7 天由 cron 自己搞定，不再是观察项。
- 【说明】早报仍走 bot 身份发送（user 身份缺少代发消息权限），如果你希望以本人身份发出，需要重新走一次飞书授权。

### 数据 / 运维健康

- 【需拍板】state.db 527 MB，日均涨 1-2 MB，已连续 16 天超红线；cron 不敢自己跑 VACUUM（会锁库影响其他任务）。
- 【已完成】`/tmp` 工件扫描：98 件未归档、3 件已归档，净变动 +1 件且属自身产物，无用户交付物滞留。

### 调研 / 内容处理

- 【失败】EDN 周报 8-26 那期实际零产出（数据文件只剩 832 字节模板），监控层却报成功；已累计 31 天缺少这条战略输入。

## 4. 重要交付物 / 系统变更

- Issue｜SUP-103 AI Builders 早报｜已闭环（cron 代翻 done）
- 消息｜飞书早报正文（11 builder）｜已送达 08:03
- 日志｜state.db 体积记录（527 MB）｜已补记，连续无缺口
- 扫描｜`/tmp` 工件快照 2026-08-30｜已存档
- 笔记｜本日报｜已双写

## 5. 风险、阻塞、需要拍板

1. **state.db 527 MB（红线第 16 天）**｜建议：回一句「今晚 3 点可以 VACUUM」，cron 会自动执行并回报回收量。
2. **EDN 周报连续 4 周零产出**｜建议：二选一——授权我直接改 EDN cron 的 prompt（加超时、分步落盘、成功断言），或暂停这条 cron 别再空跑。
3. **SUP-88（8-15 早报）滞留 16 天**｜当天 autopilot 完全没抓数据也没投递，你从未收到这期内容。建议：直接标废弃，不必补。
4. **SUP-79（8-06 早报）阻塞 24 天**｜历史投递失败，digest 还在评论里。建议：一并标废弃。
5. **早报仍以 bot 身份发送**｜若无所谓可忽略；若想改成本人身份，需要你配合走一次飞书重授权。

## 6. Hermes 自我改进

- 昨天 GC Day 自己在 `/tmp` 留下的 `_audit_check.py` 今天才被扫到——这类 cron 内部脚本应该统一写到 `/root/.hermes/state/`，避免每天污染工件统计。已知规则，需在 GC Day 侧真正执行。
- 「GC Day 报告里写了但没落地」已连续 3 周发生（EDN prompt 修复）。GC Day 应该把「本周必须落地的 patch」限制到 1 条并当场执行，而不是列清单。

## 7. 明日建议

1. 给 VACUUM 窗口（需要你确认，一句话即可）。
2. 决定 EDN 周报的去留（需要你确认，9-02 前有效）。
3. SUP-88 / SUP-79 批量标废弃（需要你确认后可自动做）。
4. GC Day 侧内部脚本落盘路径修正（可自动做）。
5. 早报状态收尾继续由 cron 兜底（可自动做，无需关注）。
