# Hermes 日报 - 2026-08-21

## 1. 老板速读
- 静默日第 11 天：今日无用户直接对话，无派发子 agent / autopilot / Multica issue 主线活动。
- Discord `#hermes-agent-notifications` 今日一次性转述约 20 条上游公告（Hermes Bot Mode / Browser Use Mode / v0.20.0 Herald / Pixel Office VSCode / 子代理实时控制等），无 Vault / Get笔记 / 配置写入。
- SUP-94（8-21 AI Builders 早报）autopilot 飞书已送达（om_x100b674d18...，16 builder / 30 tweets / 1 podcast），但 autopilot 漏翻 status，今日 cron **代 flip done**。
- state.db 510 MiB（8-20 507 → 8-21 510，单日 +3 MiB），超 500 MiB 红线第 7 天，VACUUM 窗口仍未拍板。
- EDN 周报连续 2 周跑空（8-12 / 8-19）后今天不是周三触发日，本周**未尝试**——需 8-26（周三）触发时校验 stub-success 修复是否落地。

## 2. 今日工作总览
- 静默日。Hermes 主线仅跑出 1 条 Discord 上游公告转述（无独立产出）+ 1 条本日 cron 自身（本次日报）。
- 无新写 Vault 文件、无 Get笔记 新同步（除日报本体）、无 git commit（除日报本体）、无 fly-cli 主动投递。
- 无 SUP-N 长滞留批量收尾需求（SUP-94 当日补 flip 1 条）。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已闭环】SUP-94 (8-21 早报) 代 flip done：autopilot 飞书 `om_x100b674d183d98a0b1181edd82e9466` + chat_id `oc_d5de551525d3a4ae9e1a52815bb9a92b` + 中文 builder 覆盖齐备 → 仍漏翻 status → cron 兜底；累计 18 样本（autopilot 自愈率仍约 53%）。
- 【风险延续】lark-cli user identity `needs_refresh`，expiresAt = `2026-08-21T10:01:41+08:00`（今日 cron 启动时已过 ~13h50m，**未观察到任何 user API 调用触发 refresh**）。scope 仍全（`im:message.*` / `docx:document:create` 等齐备），但 lark-cli 默认下次 user API 调用时自动 refresh；如期间需要 user-identity 投递会静默 fallback 到 bot。

### 知识库 / Obsidian / Get笔记
- 今日无新写笔记（除日报本体）。无用户主动 sync 请求。

### 飞书 / 日历 / 文档 / 消息平台
- 【已完成】Discord `#hermes-agent-notifications` 上游公告批量中文化转述 1 条 session（约 20 条英文公告 → 中文版，覆盖 Hermes Bot Mode / Browser Use Mode 12→1 / sub-agent 实时控制 / Pixel Office VSCode / v0.20.0 Herald / `/loop` 命令 / 桌面 capabilities 升级 / GPT-5.6 350K→1M 上下文等）。**纯转述，无配置变更**。
- 【提示】Discord 上游公告转述路径仍依赖 agent 实时处理，未迁到 cron/heartbeat 监听；本次 20 条公告一次性堆叠已出现"一条 user message 拼多源"的形态，与 6-30 教训同款。

### 调研 / 内容处理
- 无主动调研 / 内容处理。SUP-94 autopilot fetch + deliver 走通（无本日 cron 介入）。

### 其他 / 系统健康
- `/tmp` 旧工件扫描：连续 2 天与昨天**完全相同**（3 ARCHIVED + 98 NOT_ARCHIVED = 101 行；basename diff 净 0 件）。`wc -l` 当日 101，**未出现新挂起项**。新工件白名单过滤后**今日新增用户可读工件 0 件**（fresh `-mtime -1` 只扫到 hermes-cwd-* / hermes-snap-* / paperclip.log 三个 Hermes/容器 scratch，全部 cron 内部命名约定白名单）。
- state.db 510 MiB（红线第 7 天）。当日增量 +3 MiB（507 → 510），活动量与昨日几乎相同但仍维持 +3-10 MiB/日 趋势。**未拍板 VACUUM 窗口已 9 天**（8-13 首次列入风险位）。
- EDN cron 残件（`/tmp/hermes-results/` / `/tmp/edn-*/`）：今日为空，**本周未堆积**。

## 4. 重要交付物 / 系统变更
- 类型｜名称｜状态
  - 日报｜Hermes 日报 2026-08-21｜撰写中（本笔记）
  - Multica issue｜SUP-94 (2026-08-21 早报)｜**done**（cron 代 flip，8-21 23:52）
  - Discord 转述｜Hermes Bot Mode / Browser Use Mode / v0.20.0 Herald 等 ~20 条｜已转述，无落地变更
- 普通 session / 普通命令验证不列。

## 5. 风险、阻塞、需要拍板
- 【🔴 紧急·第 9 天未拍板】state.db 510 MiB 持续超 500 MiB 红线：上次拍板窗口建议 8-19/8-22 均未确认；cron 不可自行 VACUUM（锁库风险），需你明确"X 日凌晨"窗口。**当前最大阻塞**。
- 【延续】EDN 周报连续 2 周跑空（8-12 + 8-19 stub-success 误报）= 用户战略决策缺失 14 天窗口；下次触发 8-26（周三），需校验 GC Day #22 (8-22) 是否已落地 EDN cron 5 条补丁（含 stub-success 硬断言）。
- 【新增】今日 SUP-94 仍 autopilot 漏翻 → 累计 18 样本（约 53% 自愈率）；follow-builders skill 强制收尾 patch 紧迫性未降。
- 【第 6 天·轻度】lark-cli user identity 自愈证伪已 6 天，今日 cron 启动未观察到任何 user API 调用触发 refresh；下次 cron 必须显式校验 expiresAt 是否更新。

## 6. Hermes 自我改进
- 静默日**复用折叠模式**：`/tmp` 旧工件扫描连续 2 天完全相同（3 + 98 = 101 行），按 SKILL §0-pre 静默日折叠规则今日 §3「其他 / 系统健康」只报一行"与昨天完全相同 + 真零漂移"——不展开文件名清单，避免连续 11 天写"无新增挂起项"造成假稳态错觉。**已验证**：basename diff = 0 件 + wc -l 相同 = 真零漂移，可折叠。
- lark-cli 自愈未刷新观察：今日 cron 启动时 expiresAt 已过 ~13h50m 但未观察到任何 user API 调用触发的 refresh。建议明天 cron 启动时**显式** `lark-cli im messages send` 一条 ping（或调一次 user-scope API）触发 refresh，避免连续 N 天只观测到 `needs_refresh` 状态。
- 静默日模板未启用：连续 11 天静默，§1 / §2 / §6 内容高度同质化（"无用户活动 / 无 autopilot / 无新增工件"），仍按 800-1500 字完整模板生成。**保留**：跨日审计需要完整模板，避免静默日模板切换后回看时漏掉"哪天第一次 SUP-94 漏翻"等关键节点；只在内容层面折叠 `/tmp` 段。

## 7. 明日建议
- **【需立即处理·你拍板】state.db 510 MiB → 确认 8-22 凌晨 VACUUM 窗口**（cron 已连续 9 天呼吁未拍板；建议周五凌晨你不在用时跑 `sqlite3 state.db 'VACUUM;'`，预计释放 ≥ 50 MiB）。
- **【可自动做·你拍板后】明天 cron（8-22 09:00 GC Day #36）应该把 EDN cron prompt 5 条补丁**（含 stub-success 硬断言）一并 patch 上 `/root/.hermes/cron/jobs.json` job id `1d0cf41439ec`，确保 8-26（周三）下次 EDN 触发时验证修复落地。
- **【可自动做】明天 cron 启动时显式触发一次 lark-cli user API 调用**（如 `lark-cli im chats list --limit 1`），验证 expiresAt 是否被自动 refresh；若仍未 refresh → §5 升级措辞 + 报 "OAuth refresh token 过期，需 `lark-cli auth login --refresh`"。
- **【可自动做】明天 cron 启动时复核 SUP-95 (8-22 早报)**——若 autopilot 再次漏翻 status，按 SOP 代 flip done；若 autopilot 自驱翻 done 则不动。
- **【可自动做】明天 23:50 日报**继续按标准模板生成；预计若 8-22 GC Day #36 跑通，state.db 大小会因 GC 清理短暂下降。