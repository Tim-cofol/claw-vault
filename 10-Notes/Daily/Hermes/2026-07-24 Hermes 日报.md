# Hermes 日报 - 2026-07-24

## 1. 老板速读
- 今日核心产出是定位 SupaData 爆量根因：EDN 周报及其 subagent 把 X 账号时间线作为固定信源，历史单轮可产生 30-80 次 scrape。
- 已按你确认的“1+2”落地：X 降为补充信源、停止批量抓 profile，X 相关 SupaData 调用硬限制为每轮 ≤ 3 次，保留信号 ≤ 5 条。
- 复核时发现第一轮修改只清理了 §2.E，prompt 其他位置仍残留“X 是主源”、nitter 与旧 15 条上限；今晚已全局清除并验证 JSON 合法。
- 新建并完善 `supadata-quota-management` skill，固化按天 / 工具 / session / URL 归因、预算修复及 file-mutation verifier 解释流程。
- SUP-61 AI Builders 早报已投递飞书：17 位 Builder、35 条 X、1 期播客；日报已核验送达证据并将 issue 从 `in_review` 收尾为 `done`。
- `/tmp` 旧工件实扫 90 行：12 ARCHIVED + 78 NOT_ARCHIVED，较昨日减少 8 行；未发现新增旧工件，今日新鲜文件均为早报或诊断中间资产。
- 明日优先：GC Day 检查 SupaData 全局预算规则与扫描快照迁移；7 月 29 日 EDN 周报实跑验证 X scrape 是否 ≤ 3。

## 2. 今日工作总览
- 【已完成】调查 SupaData 历史调用，共识别 292 次相关调用及五个周报高峰日，确认爆量来自 EDN 周报批量抓 X profile。
- 【已完成】修订 EDN 周报 cron prompt，补齐预算、按需触发、数据统计与验证约束，并保留可回滚备份。
- 【已完成】解释 file-mutation verifier 警告，验证失败的 `patch` 已被后续替代路径真正落盘；相应经验已写入 skill。
- 【已完成】AI Builders 早报投递与 issue close-out；另完成两条 Hermes 上游公告中文转述。

## 3. 按项目 / 责任域汇报

### EDN / 市场洞察自动化
- 【已完成】把 X 从固定主源降为按需补充信源；不抓 profile 时间线，只在需要互动数据、thread 上下文或紧急弱信号时抓单帖。
- 【已完成】建立硬预算：X scrape 每轮 ≤ 3、最终 X 信号 ≤ 5，并在 `capture_stats` 和验证清单中记录实际调用数。
- 【纠错】全局扫描发现通用规则、工具样例和验证清单仍有旧指令；已统一清理，避免下次 cron 同时收到矛盾要求。

### Hermes / Agent 系统
- 【已完成】新增 SupaData 配额诊断 skill，沉淀从 `state.db` 解析 MCP 工具调用的可复用方法。
- 【已完成】将 file-mutation verifier 的适用边界写入 skill：工具单次失败不代表整轮最终未落盘，必须做最终文件验证并主动说明补救结果。
- 【已转述】Discord 公告频道完成 tldraw offline 技能与 Ling-3.0-flash 免费试用信息的中文转述，无配置变更。

### Multica / AI Builders
- 【已完成】SUP-61 早报已送达飞书；内容覆盖 17 位 Builder 的 35 条动态和 1 期播客。
- 【已闭环】依据明确 message_id 与送达评论完成日报核验，并将 SUP-61 翻为 `done`。

### 飞书 / 消息平台
- 【已完成】AI Builders 正式消息通过 bot 身份送达；误发的字面量 `@file.md` 测试消息已撤回后重发。
- 【观察】user identity 当前为 `needs_refresh`，且仍缺 `im:message.send_as_user`；本日 bot 投递成功，不影响已完成结果。

## 4. 重要交付物 / 系统变更
- 配置｜EDN 周报 X / SupaData 预算规则｜已落地并全局复核｜可回滚备份已保留
- Skill｜SupaData 调用归因与爆量修复｜已创建并完善
- 早报｜SUP-61 AI Builders Digest｜已投递并 `done`
- 公告转述｜tldraw offline 技能、Ling-3.0-flash｜已转述至 Discord

## 5. 风险、阻塞、需要拍板
- 【待实跑验证】prompt 约束已写入，但实际节流效果要等 7 月 29 日 EDN 周报执行后确认；验收线是 `x_scrape_calls ≤ 3`。
- 【需要你确认】SUP-57 仍停留 `in_review`：补发 7 月 20 日早报或取消并关闭，二选一。
- 【需要你确认】SUP-25 飞书 vibe 助手需求仍停留 `in_review`：继续推进或关闭。
- 【观察】飞书 user OAuth scope 缺 `im:message.send_as_user`；如需 user 身份发消息，必须重新授权。

## 6. Hermes 自我改进
- 配置变更验证必须扫描整个解码后的 prompt，而不是只验证目标章节；这条已补进 SupaData skill 的完工检查。
- recurring issue 的送达评论不是收尾，日报继续执行“核验证据 → 补评论 → status done → 再查询”的闭环。
- `/tmp` 审计快照已保存到 `~/.hermes/state/`；明日 GC Day 应迁移遗留的 `/tmp/scan_*`，停止扫描器自污染。

## 7. 明日建议
- 【可自动做】GC Day 核查 SupaData 预算 skill、全局 prompt 残留与扫描快照迁移。
- 【可自动做】为 7 月 29 日 EDN 周报准备调用量对比验收，记录 X scrape 实际值。
- 【需要你确认】处理 SUP-57 的补发 / 取消决策。
- 【需要你确认】处理 SUP-25 的继续 / 关闭决策。
