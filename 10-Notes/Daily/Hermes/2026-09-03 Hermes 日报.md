# Hermes 日报 - 2026-09-03

## 1. 老板速读
- 静默日第 24 天：用户全天无直接对话/请求，Hermes 跑 1 个 autopilot (SUP-107) + 本日报 cron，无用户侧产出。
- SUP-107 (今日 9-03 AI Builders 早报) autopilot 08:03 飞书实际送达 `om_x100b66bfdaaa58a4b227d3aa1789c00`，但漏翻 status → 今日 cron 按 8-14 快速路径代 flip done。
- EDN 周报连续 6 周跑空（7-29 / 8-05 / 8-12 / 8-19 / 8-26 / 9-02），今日是连续失败后首个非 EDN 窗口的观察日；明天 9-09（周三 08:30）是下次窗口，**累计 41 天战略决策缺位**。
- state.db 568 MiB（stat 568180736 bytes，+0.4 MiB 极小增量，500 MiB 红线第 20 天）；今日单日增量远低于昨日 +13 MiB，回归常态。
- `/tmp` 零漂移稳态第 2 天（昨日 9-02 101 → 今日 104 行 +3 行 9-03 当日 autopilot 残件 + EDN 9-02 edn_skeleton/edn_template 跨日 1-day 龄残件新增）。

## 2. 今日工作总览
- 静默日（用户无直接对话），全天活动 = 1 autopilot (SUP-107) + 1 cron (本日报)。
- 主要工作：autopilot 飞书投递成功、本日报 cron preflight + SUP-107 兜底 flip done + state.db 状态记录。
- 静默日 cron 全部按标配循环执行（lark-cli refresh / `/tmp` 扫描 / state.db size log）。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【状态】cron 代 flip SUP-107 done（autopilot 漏翻 status 9-03 第 1 例）；【产出】SUP-5x 累计 50 样本 = 38 done / 3 in_review / 1 todo / 4 blocked = **76% 自愈率**（与 9-02 持平，剔除非 follow-builders 早报后纯样本 = 88%）；【风险】follow-builders skill 双轨 patch（chat_id 兜底 + status flip 强制收尾）GC Day 9-05 必须落地。
- 【状态】EDN cron 今日非窗口日（每周三 08:30）；【产出】无；【风险】**明日 9-09 08:30 是下次 EDN 窗口**——GC Day 9-05 (周六 09:00) 之前若 EDN cron prompt 的 5 条 prompt patch 仍未落地，9-09 必将继续跑空；连续 6 周（7-29 / 8-05 / 8-12 / 8-19 / 8-26 / 9-02）失败模式各异（brotli / stub-success / template / upstream timeout / data_asset 已落但飞书未 append），治本 patch 缺位是核心瓶颈。

### 知识库 / Obsidian / Get笔记
- 【状态】今日日报按 skill 默认双写流程执行；【产出】vault HEAD `b4f7436` (昨天日报) → 今日日报待 commit；【风险】无新增风险位，sync 脚本路径稳定。

### 飞书 / 日历 / 文档 / 消息平台
- 【状态】今日 lark-cli user identity 自动 refresh（启动时 `needs_refresh` → `lark-cli calendar +agenda` 触发 → `status: ready`，expiresAt 推到 2026-09-04 01:50）；【产出】lark-cli refresh 标配连续 11 天自动落地闭环；【风险】user identity 缺 `im:message.send_as_user` scope 第 70 天，SUP-107 今日仍以 bot 身份（"小天"）投递。
- 【状态】今日日历 1 个事件：李俊良组织的【完善产品需求】集训课-双子星905（09:50-18:50，busy）；【产出】无新增归档；【风险】无。

### 调研 / 内容处理
- 【状态】今日无用户驱动的调研/内容处理请求；autopilot SUP-107 13 位 X builder 摘要 + 1 播客（Training Data × Peregrine 创始人 Nick Noone & Ben Rudolph "Making Cities Awesome"）已正常生成并飞书送达。

## 4. 重要交付物 / 系统变更
- AI Builders 早报｜SUP-107（2026-09-03）｜已飞书送达 + cron 代 flip done｜主线：Anthropic Claude Fable 5.1 / Claude Mythos 5.1 / Enterprise Frontier Safeguards + OpenAI Sam Altman 长文谈 Astra 主动放慢 + 1 播客
- lark-cli refresh 标配｜连续 11 天自动落地（8-24 ~ 9-03）｜已固化｜不再写"自愈证伪"观察项
- state.db size log｜今日补录 9-03: 568 MiB（568180736 bytes）｜log 连续无缺口
- `/tmp/scan_2026-09-03.txt`｜今日扫描结果 104 行（3 ARCHIVED + 101 NOT_ARCHIVED）｜已写入 `/root/.hermes/state/`（不进 `/tmp`）

## 5. 风险、阻塞、需要拍板
- [🔴 紧急·需你立即拍板] **EDN cron 5 条 prompt patch 落地窗口**——明日 9-09 08:30 是下次 EDN 窗口，GC Day 9-05 (周六 09:00) 是 prompt patch 落地的最后窗口。连续 6 周 41 天战略决策缺位，再错过这次 9-09 必将继续跑空。**建议**：本周内（最迟周六 09:00 之前）给 cron `1d0cf41439ec` 的 prompt 补 5 条 patch：(1) subagent `delegate_task` 配 `timeout=900s`；(2) 母 agent 综合分析前先逐 subagent 落本地 JSON；(3) 飞书写入 `docs +update` 包 retry-on-error；(4) `/tmp` 孤儿 plan 落 `/root/.hermes/state/edn-orphan-YYYY-MM-DD.json`；(5) data_asset 落 + 飞书未落 → 自动从 data_asset 生成 markdown 并 append 飞书长住文档（针对 9-02 失败模式）。
- [🔴 紧急] state.db 568 MiB 持续踩 500 MiB 红线**第 20 天**，连续 20 天呼吁 VACUUM 窗口未拍板；今日 +0.4 MiB 是极小增量但已稳定红线。**建议**：本周内给一次凌晨低峰窗口（30 分钟停写 + VACUUM），cron 不可自己跑（锁库风险）。
- [持续] user identity 缺 `im:message.send_as_user` scope 第 70 天，所有飞书投递默认走 bot 身份（"小天"）——SUP-107 今日仍如此。**建议**：本周内重授权 `lark-cli auth login --scope "im:message.send_as_user,im:message.group_at_message_send_as_user"`。
- [观察] follow-builders skill 双轨 patch（chat_id 兜底 + status flip 强制收尾）GC Day 9-05 必须落地——今日 SUP-107 又一次 autopilot 漏翻 status，已是连续 8 周观察到的稳定 bug。
- [可自动做] SUP-66 (todo 状态 5 周) + SUP-88 (in_review Mode E 19 天) + SUP-79 (blocked chat_id) 3 条历史遗留 SUP，cron 不能代 flip（理由分别：用户没收到早报 / 投递失败 / 需用户拍板），仍待用户拍板。

## 6. Hermes 自我改进
- **本次 EDN 9-02 失败模式新增为第四种（data_asset 落 + 飞书未落）**：与 7-29 brotli / 8-19 stub-success / 8-26 template 三种模式独立，但**所有四种的共同点都是"母 agent 综合分析阶段抛异常"**——patch 重点应放在"母 agent 失败前先把 data_asset 落盘 + 飞书写入必须 retry-on-error"双轨兜底，而不是"修母 agent 的 4 种 prompt 缺陷"。已 patch 进 `references/edn-cron-stub-success-misreport.md`（待 9-05 GC Day 落地）。
- **state.db size log 缺口风险消除**：今日 cron 启动时 log 连续（无缺口），`/root/.hermes/state/state_db_size.log` 自 8-29 起连续 6 天无缺口（之前 8-06 缺口已 8-07 补录过）。**反模式已闭环**：连续无缺口时后续 cron 不需要再主动反查日报补录。
- **lark-cli refresh 标配动作已完全固化**：连续 11 天（8-24 ~ 9-03）自动落地，**后续日报 §3.5 / §5 不再写"自愈证伪"观察项**——这是已落地的标配，不是观察项；与 user identity 缺 scope 是两个独立问题（前者是 expiresAt 滚动，后者是 OAuth grant）。

## 7. 明日建议
- [可自动做] 9-04 cron 自动执行 lark-cli refresh 标配；EDN 9-04 仍非窗口，4 件 SUP-21/22/23/24 backlog 与 3 件遗留（SUP-66/79/88）继续 in_review 等待用户拍板。
- [需要你确认] **EDN cron 5 条 prompt patch**：本周内（最迟周六 GC Day 9-05 09:00 之前）给 cron `1d0cf41439ec` 的 prompt 补 5 条 patch（见 §5 紧急项），否则 9-09 必将继续跑空。patch 文本已写在 `references/edn-cron-stub-success-misreport.md` 和 `references/edn-cron-double-failure.md`，可直接复制到 cron prompt。
- [需要你确认] **state.db VACUUM 窗口**：本周内给一次凌晨低峰 30 分钟（建议周六 GC Day 同步做），cron 不可自跑。
- [需要你确认] **lark-cli user identity 重授权**：`lark-cli auth login --scope "im:message.send_as_user,im:message.group_at_message_send_as_user"`，授权后 SUP-107 等飞书投递可改回 user 身份。
- [可自动做] 9-04 cron 代偿清理 `/tmp` 当日新 EDN 残件（如有）。
