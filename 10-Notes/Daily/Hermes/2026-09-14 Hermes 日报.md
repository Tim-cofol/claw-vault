# Hermes 日报 - 2026-09-14

## 1. 老板速读

- **SUP-110 (9-14 早报) autopilot 跑通 + cron 代 flip done** — 00:00:19 创建 → 00:02:55 autopilot 飞书 bot 送达 `om_x100b6547d77560a0b3ec68e43856df4`（7259 chars / 16 builder / 27 推文 / 1 播客 / 2 博客）/ chat_id 自愈正确 → **autopilot 仍漏翻 status**（in_review）→ 23:51 cron 代 flip done（rev=8）。chat_id 自愈率累计 100%（SUP-80/83/85/91/92/96/110 = 7/7），status flip 自愈率 6/8 in this segment = 75%，连续 3 天 autopilot 完成 chat_id 自愈但漏 status flip（SUP-110 = 9-12 ~ 9-14 第 3 次，**双轨 patch 仍必须落地**）。
- **autopilot 静默 9 天后连续 3 天自愈跑通**（SUP-108 → SUP-109 → SUP-110）— 单跑全链路闭环（fetch + deliver），autopilot 自驱率从 9-04 静默期 0% 回升至本周 100%，但样本量仍仅 3 天不构成"稳定"信号，GC Day #44 (9-19) 仍是 patch 窗口。
- **lark-cli refresh 标配连续 22 天闭环** — 今日 cron 启动时 user `needs_refresh` → `calendar +agenda` → `ready`，expiresAt 推到 9-15 01:53。
- **state.db 590 MiB 红线第 32 天** — 单日 +2 MiB（稳态），连续 32 天呼吁 VACUUM 未拍板。
- **Idea Digest 99992402 投递失败连续第 12 周** — cron `last_status='ok'` 但 `last_delivery_error` 持续（连续 12 周周一未送达）。
- **EDN cron 今日未触发** — 上次 9-09 跑通（data_asset 9.3 KB / report_url 飞书长住文档），下次 9-16 周三 08:30（连续 1 周窗口）。
- **`/tmp` 真零漂移稳态第 10 天** — 今日 80 行 vs 昨日 78 行（+2.6%，稳态范围 ±10% 内），basename 集合 diff 空集（仅 age +1 day 整行差异 + 0-day 龄 0 件用户可读新工件）。

## 2. 今日工作总览

- **autopilot 跑通 SUP-110 + cron 代 flip done** — 9-14 00:00:19 autopilot 创建 SUP-110，00:02:55 中文 result comment + 飞书 bot 送达，autopilot 仍漏翻 status（23:51 cron 代 flip done）；chat_id 大小写自愈正确（issue description 大写 `OC_` → autopilot 自动纠正为 `oc_`）。
- **lark-cli refresh 标配自动闭环（连续 22 天）** — `calendar +agenda` 触发后 expiresAt 推到次日 01:53，已完全固化。
- **Idea Digest 周一 09:00 触发** — 跑通 1 条 idea（imp 4 voice note 改造）+ 但 `last_delivery_error='live adapter send failed: [99992402] field validation failed'` 持续（连续 12 周）。
- **静默日延续（用户无直接对话）** — state.db 今日 sessions 只有 cron 自己（2 条 cron + 0 user + 0 acp + 0 subagent）。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统

- 【已闭环·关键】SUP-110 (9-14 早报) autopilot 跑通 + cron 代 flip done — 00:00:19 创建 → 00:02:55 中文 result comment + 飞书 bot 送达 `om_x100b6547d77560a0b3ec68e43856df4`（7259 chars / 16 builder / 27 推文 / 1 播客 Unsupervised Learning Ep 93 / 2 博客 Anthropic Claude in Chrome + Cowork built-in browser）→ autopilot **仍漏翻 status** → 23:51 cron 代 flip done（rev=8）。
- 【已闭环】lark-cli user identity refresh 标配连续 22 天自动闭环（8-24 起累计，今日 +1）。
- 【异常·已确认】Idea Digest cron (`2e8ecd9b7cb2`) 投递失败连续第 12 周——`last_status='ok'` 但 `last_delivery_error='live adapter send failed: [99992402] field validation failed; ...'` 持续；本次跑通 1 条 imp 4 idea（voice note 改造 / surfaced ×7），用户未收到。

### 知识库 / Obsidian / Get笔记

- 【正常】日报 cron 23:51 启动，本篇走双写流程，推送到 Get笔记 MyClaw。
- 【真零漂移稳态第 10 天】`/tmp` 工件扫描：今日 80 行 vs 昨日 78 行（+2.6%，稳态范围 ±10% 内），basename 集合 diff 空集。0-day 龄新增 = 0 件用户可读工件；今日新增 mtime `-1` 仅 autopilot 产物白名单（`/tmp/reply-sup-110.md`，mtime 08:02 autopilot 跑通时段），不计入用户可读工件。

### 飞书 / 日历 / 文档 / 消息平台

- 【正常】lark-cli user identity 已 refresh（user status ready，expiresAt → 9-15 01:53，连续 22 天闭环）。
- 【已闭环】今日 SUP-110 飞书 bot 送达 `om_x100b6547d77560a0b3ec68e43856df4`（7259 chars，autopilot 跑通后回读验证完整落地）。
- 【观察】EDN 周报飞书长住文档今日未变更（按周节奏）；EDN cron 下次触发 9-16 周三 08:30（连续 1 周窗口）。
- 【异常】Idea Digest 飞书投递 99992402 失败连续第 12 周；workaround 待拍板。

### 调研 / 内容处理 / EDN / GC Day

- 【静默日】0 条新调研动作。
- 【GC Day #44 9-19 触发】 — 9-19 周六 09:00 是下次 GC Day 窗口（本次 9-12 周六已落地，0 skill patch / 0 memory 段落迁移；下次 GC Day 仍是健康检查 + 清理建议模式）。
- 【EDN 周报 9-09 跑通 verified】 — `data_asset 9.3 KB`（vendor signals 齐备）+ `report_url` 飞书长住文档可访问 + `last_status='ok'` + `last_delivery_error='None'` ——三件全成立，9-09 真实跑通。

## 4. 重要交付物 / 系统变更

- 内容产出｜SUP-110 AI Builders 早报 2026-09-14｜已投递｜飞书 bot `om_x100b6547d77560a0b3ec68e43856df4`（7259 chars / 16 builder / 27 推文 / 1 播客 Unsupervised Learning Ep 93 / 2 博客 Anthropic 双发）。
- 评论补登｜SUP-110 cron 代 flip 核验评论｜已添加｜`01a0a09f-6d7e-...`，含 om_x / oc_ / 中文 builder / cron 代 flip 说明。
- 系统状态｜SUP-110 status: in_review → done｜已落地｜revision 6 → 8。
- 系统状态｜lark-cli user identity refresh｜已落地｜user status ready / expiresAt 推到 9-15 01:53（连续 22 天闭环）。
- 数据指标｜state.db size log｜已续写｜`/root/.hermes/state/state_db_size.log` 新增 9-14 行（590 MiB / +2 MiB from 9-13）。
- 扫描快照｜`/tmp` 工件扫描 snapshot｜已落地｜`/root/.hermes/state/scan_2026-09-14.txt`（80 行）。
- Idea Digest 输出｜1 条 imp 4 idea（voice note 改造）｜已生成但未送达｜`/root/.hermes/cron/output/2e8ecd9b7cb2/2026-09-14_09-00-51.md`。

## 5. 风险、阻塞、需要拍板

- **[🔴 紧急] state.db 590 MiB VACUUM 未拍板**：已 32 天呼吁，连续 17 天踩红线后稳态增长（+1-2 MiB/天）。建议尽快给凌晨窗口拍板（cron 不可自动 VACUUM 锁库）；GC Day #42 已点出"停 gateway → FTS rebuild + VACUUM 目标 < 350 MB"为本周最影响磁盘项。
- **[🔴 紧急] Idea Digest 99992402 连续 12 周失败**：cron `last_status='ok'` 但 `last_delivery_error` 持续失败。下次触发 9-21 周一 09:00；workaround（改 `lark-cli im +send` 绕开 gateway live-adapter）已挂 12 周未拍板；GC Day #44 (9-19) 应明确拍板路径。
- **[观察·autopilot 连续 3 天跑通 + 连续 3 天 status flip 漏翻] SUP-108 + SUP-109 + SUP-110 形成连续 3 天 autopilot 全跑通模式** — SUP-110 是 SUP-91 之后 autopilot chat_id 自愈累计第 7 个 100% 样本，但 status flip 自愈率在近 8 个样本 = 6/8 = 75%（SUP-108 自愈 / SUP-109 自愈 / SUP-110 漏 → 9-12 SUP-108 + 9-13 SUP-109 自愈连续 2 天后今日又漏 1 次）。**样本量仍不足**（按 SKILL.md 稳定阈值"≥ 10 连续样本 90%+ 自愈率"判定），follow-builders skill 双轨 patch（chat_id 兜底 + status flip 强制收尾）紧迫性不降；GC Day #44 (9-19) 仍是落地窗口。
- **[观察] autopilot 静默 9 天根因仍未明确定位**：9-04 ~ 9-11 autopilot 静默 9 天，9-12 ~ 9-14 连续 3 天自动恢复跑通——根因可能是 acp 触发器自愈 / 上游 feed 自愈 / API 限流恢复，无明确诊断路径。建议 GC Day #44 仍把"autopilot 静默根因定位"列为低优先级观察项，但不必紧急修复（已自愈）。

## 6. Hermes 自我改进

- **autopilot 连续 3 天跑通（SUP-108 → SUP-109 → SUP-110）+ status flip 漏翻连续 3 天复现** — SUP-110 跑通（chat_id 自愈 7/7 = 100%）+ status flip 漏翻（3/8 = 37.5%）说明 autopilot 自驱单跑链路已稳定，但 status flip 这一步仍是概率事件。**样本量仍不足**，GC Day #44 (9-19) 必须**继续维持** ≥ 10 连续样本 90%+ 自愈率的稳定阈值，follow-builders skill 双轨 patch 紧迫性**不降级**。**反模式**：看到 SUP-108 + SUP-109 + SUP-110 连续 3 天跑通就写"autopilot 已稳态"——status flip 3/8 漏翻是稳定信号而非偶发；连续 3 天 autopilot 完成后留给 cron 代 flip 是新常态而非异常。
- **`/tmp` 真零漂移稳态第 10 天 + basename diff 空集**：连续 10 天 ±10% 范围内，今日 basename diff 完全空集（仅 age +1 day 整行差异），按 SKILL.md §0-pre 零漂移稳态判定可继续折叠成一行；今日 0 件新增用户可读工件，EDN 残件继续堆在 `/tmp` 但不影响判定。**新增** `reply-sup-110.md` = autopilot 9-14 凌晨产物（mtime 08:02 autopilot 跑通时段），不计入用户可读工件。
- **lark-cli refresh 标配连续 22 天完全固化**：今日 cron 启动时 user `needs_refresh` → `calendar +agenda` → `ready`，expiresAt 推到次日 01:53。**标配动作已完全自动化**，后续日报 §3.5 / §5 **不再**写"自愈证伪"观察项——这是已落地的标配，不是观察项。

## 7. 明日建议

- 【可自动做】lark-cli refresh 标配自动闭环（cron 自跑，连续 23 天起算）。
- 【可自动做】`/tmp` 工件扫描 + state.db size log 续写（cron 自跑）。
- 【可自动做】autopilot 9-15 早报触发后 cron 按快速路径判代 flip 必要（连续 3 天 autopilot 跑通但 status flip 漏翻 1 次，预期大概率需要 cron 介入）。
- 【可自动做】GC Day #44 (9-19 周六 09:00) 触发后按现有健康检查 + 清理建议模式跑（已 7 天未落地 patch，本次预期仍 0 patch + 0 段落迁移）。
- 【需你确认】state.db 590 MiB VACUUM 窗口期拍板（连续 32 天呼吁，连续 17 天踩红线后稳态增长）；建议优先级不变。
- 【需你确认】Idea Digest 99992402 投递通道切换（连续 12 周失败，下周一 9-21 09:00 触发后仍预计失败）；GC Day #44 (9-19) 应明确拍板路径。
