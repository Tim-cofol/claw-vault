# Hermes 日报 - 2026-06-07

## 1. 老板速读
- 今天的核心交付是 Hermes Discord 网关的稳定性修复 + 一个等待合并的上游 PR：把 Discord thread 自动改名的根因（`Platform` 枚举和字符串比较错位）一次性修好，并把"处理占位 → 短标题"的预命名策略作为 PR 推到了 NousResearch/hermes-agent。
- 主模型已从 GPT-5.5 切到 MiniMax-M3，网关已重启生效；后续 Hermes 默认就是 M3。
- GitHub 工具链补齐：`gh 2.93.0` 安装 + PAT 注入 + `gh auth setup-git` 跑通，可正常操作 fork 与 PR。
- 调研侧把 Boris Cherny 关于 Claude Code / 工程未来的访谈整理成了完整中文笔记，落到 Obsidian 并同步 Get笔记；同主题的"使用技巧"播客文稿也已抓取总结。
- 多 profile + 跨 Agent 协同的方案还在规划中：你选了"看板"路线，今天给了方案说明、还没执行。
- 仍需你确认/拍板的两件事：① Hermes 端到端验证 Discord thread 改名是否在生产 thread 上真正生效；② 多 profile 看板方案的执行时机。
- 明日优先：等你看一眼上游 PR 反馈或合并状态、决定是否在本地手动触发一次 rename 验证；如有空把"多 profile 看板"方案按你最终意见落地。

## 2. 今日工作总览
- 把 Discord 自动改名的根因调查 + 修复 + 上游贡献做成了一整条链路，5 处平台枚举比较全部改成 `Platform.DISCORD`，并经语法 / 残留检查。
- 写并推送了一个 feature PR：thread 出现时立刻设"处理占位"名，再异步换成意图短标题；语言跟用户显式要求或消息语言。
- 主模型切换到 MiniMax-M3，含配置写入、`hermes gateway restart` 和重启后状态确认。
- GitHub 工具链从无到可用：装 gh、登录、配置 git credential、能列私有 repo。
- 知识库侧：把 Boris Cherny 访谈做成结构化中文笔记，Obsidian + Get笔记 双写；并把 Every 播客的另一篇 Claude Code 团队对谈（Cat Wu + Boris）抓成中文摘要。
- 工具/记忆侧：把今天确定的 Discord thread 命名偏好沉淀到 `USER.md`（后又按你要求回退为更简洁的版本）。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已完成】定位并修复 Discord thread 自动改名静默失效：根因是 `gateway/run.py` 在 auto-title / fallback 路径上把 `Platform.DISCORD` 枚举当成字符串 `"discord"` 比较，5 处全部修正。
- 【已推进】上游贡献：把"新 thread 立即给处理占位，再异步改成意图短标题"做成 PR 推给 NousResearch/hermes-agent；mergeable、状态 OPEN。同步把命名偏好沉淀为 `USER.md`（已按你要求精简回原版）。
- 【已完成】主模型切换：配置改写为 `minimax-cn / MiniMax-M3`，含备份、`MINIMAX_CN_API_KEY` 校验、外部 shell 触发 `hermes gateway restart`，重启后模型配置生效。
- 【已推进】多 profile + 看板协同方案：先说明了多 profile 在资源占用与跨 profile 通信上的现状，按你要求改成"看板路由"路线，已给出方案、待你确认后再执行。

### 知识库 / Obsidian / Get笔记
- 【已完成】Boris Cherny 访谈笔记：约 11KB 中文结构化笔记，含核心 thesis、Claude Code 起源、抽象层迁移、组织形态变化、给创始人的建议、对 Hermes 的启发；通过 `getnote_sync_obsidian.py` 同步至 MyClaw。
- 【已完成】同一主题的延伸阅读：把 Every 播客的"如何像 Claude Code 团队那样使用 Claude Code"对谈抓成中文摘要。
- 【已推进】本日报按双写流程落到 Obsidian，并同步到 Get笔记 MyClaw。

### 调研 / 内容处理
- 【已完成】视频/播客抓取路线保持现状：海外视频优先 Supadata，海外播客/媒体文章走内置提取；本日两次都拿到完整内容。
- 【已推进】对"Hermes / Agent 工具集是否要做精简"形成了一个新的对照点（Claude Code 只有十几个工具且会主动下线），后续可作为 Hermes 工具审计的输入。

## 4. 重要交付物 / 系统变更
- 代码修复｜Discord thread 自动改名｜已完成｜`gateway/run.py` 中 5 处 `Platform` 枚举比较
- PR｜feat(discord): pre-title auto-threads before agent response｜OPEN / MERGEABLE｜NousResearch/hermes-agent#41283
- 配置｜主模型切换｜已完成｜`/root/.hermes/config.yaml` → `minimax-cn / MiniMax-M3`
- 服务｜Hermes gateway｜已重启｜pid 357722，22:48:46 启动
- 工具链｜GitHub CLI｜已安装 + 已登录｜`gh 2.93.0` + Tim-cofol 账号
- 笔记｜Boris Cherny：Claude Code 与工程未来｜已双写｜Obsidian + Get笔记 MyClaw
- 记忆｜Discord thread 命名偏好｜已回退到简洁版｜`USER.md`

## 5. 风险、阻塞、需要拍板
- 上游 PR #41283 的 review 状态尚未回来；如果你想加快，可在 Discord / GitHub 上对维护者 ping 一下。
- Discord 自动改名修复是否在生产 thread 真的生效还没在 Discord UI 上肉眼确认；建议在下一个真实新 thread 出现时观察一次。
- 多 profile 看板方案当前只到"方案说明"阶段，需要你确认后才执行；不确认就一直停在方案状态。
- 今天知识库推送 GitHub 走的是 HTTPS + gh credential（不是 SSH），如以后切回 SSH 需要先把对应公钥加到目标 repo。

## 6. Hermes 自我改进
- 平台枚举与字符串比较错位这种"语法通过、行为静默失效"的 bug，要把"先列全部命中点 + 一次性 replace_all + 残留扫描"作为模板固化进 runbook，避免类似 patch 漏点。
- "个人偏好"类记忆条目要尽量精简，细节展开容易快速膨胀；今天 USER.md 的回退就是这条原则的实证。
- 调研笔记类任务：拿到原始转写后默认优先做"对 Hermes / 对你本人"两段映射，而不是只做内容摘录；今天 Boris Cherny 笔记的"对我的系统/工作的启发"已按这个改。

## 7. 明日建议
- 【需要你确认】看一眼上游 PR #41283 的状态，决定是否需要再推一下或继续在本地 fork 改。
- 【可自动做】在下一个真实 Discord 新 thread 上肉眼验证一次"处理占位 → 短标题"完整链路，并写回本日报的"风险"或归档。
- 【需要你确认】决定多 profile + 看板方案是否进入执行；我目前只给方案、没动配置。
- 【可自动做】把"主模型切换后 Hermes 在不同任务上的体感"做一次小盘点，纳入 6-08 日报。
