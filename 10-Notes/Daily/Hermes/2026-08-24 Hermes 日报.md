# Hermes 日报 - 2026-08-24

## 1. 老板速读
- 静默日第 14 天：今日无用户直接对话、无 Discord 上游公告转述；Hermes 主线 = 1 个 autopilot（SUP-97 8-24 早报全自驱闭环）+ 1 个 idea-recall cron（[SILENT]）+ 本日日报 cron。
- **SUP-97 autopilot 第二次连续全自驱闭环**：08:03 飞书 bot 身份送达 `om_x100b678cd18700a8b0349c0185ba596` + chat_id `oc_d5de551525d3a4ae9e1a52815bb9a92b`（大小写正确）+ 9 builder / 1 播客 + comment 含完整 om_x + autopilot 自翻 done。**累计 35 样本 SUP-5x 元模式 = 27 done / 4 blocked / 3 in_review / 1 cron 代 flip ≈ 77% 自愈率**；纯 follow-builders 33 样本 ≈ **82% 自愈率**。
- **lark-cli user identity refresh 标配动作今日首次落地**（昨日 8-23 日报 §6 立的标配要求）：cron 启动时 `auth status` 看到 `needs_refresh` → 主动跑 `calendar +agenda`（最小 user-only API 调用）→ `status: ready`，expiresAt 推到 2026-08-25 01:51（+15h）。**反模式规避**：连续 8 天写"自愈证伪"被今日标配动作彻底关闭。
- state.db 514 MiB（红线第 10 天，+1 MiB 稳态），VACUUM 窗口仍未拍板（已连续 12 天呼吁）。
- **新发现**：SUP-66 (7-29 早报) `todo` 状态 **0 comment、autopilot 完全未启动 27 天**——非 Mode A/B/C/D/E 任何一种，是第 6 类"issue 创建后 autopilot 从未运行"。SUP-88 (Mode E) 仍 in_review 第 10 天；SUP-79 blocked 第 19 天。
- EDN 周报连续 2 周跑空延续；**8-26（下周三）EDN cron 触发前 5 条补丁仍未 patch**——已连续 13 天呼吁未拍板，不能跑空第 3 周。

## 2. 今日工作总览
- 静默日。Hermes 主线活动 = 1 个 autopilot（SUP-97 8-24 早报 08:03 飞书 bot 身份投递 + autopilot 自翻 done）+ 1 个 cron（09:00 idea-recall weekly digest [SILENT] 无想法召回）+ 本日 23:50 日报 cron 自身。
- 无新写 Vault 文件、无 Get笔记 新同步（除日报本体）、无 git commit（除日报本体）。
- SUP-97 当日不需补 flip（autopilot 自驱闭环，第 2 个连续 2 天自愈样本）。
- lark-cli user identity refresh 路径今日 cron 主动 trigger 成功（昨日立的标配动作首次落地）。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已闭环·第 2 天连续自驱】SUP-97 (8-24 早报) **autopilot 全自驱闭环**：08:03 飞书 bot 身份投递 `om_x100b678cd18700a8b0349c0185ba596` + chat_id `oc_d5de551525d3a4ae9e1a52815bb9a92b`（大小写正确）+ 9 builder X（Thibault Sottiaux / Peter Yang / Madhu Guru / Amjad Masad / Guillermo Rauch / Aaron Levie / Zara Zhang / Nikunj Kothari / Dan Shipper）+ 1 播客（AI & I by Every × Kevin Scott "Best of the Pod"）+ autopilot 自翻 done。**累计 35 样本 SUP-5x 元模式（SUP-63 ~ SUP-97）= 27 done / 4 blocked / 3 in_review / 1 cron 代 flip ≈ 77% 自愈率**；纯 follow-builders 33 样本 ≈ **82% 自愈率**（仍含 4 blocked: SUP-72/73/74/79）。
- 【已落地·lark refresh 标配】lark-cli user identity refresh 主动 trigger：**昨日 8-23 日报 §6 立的标配动作今日首次落地**。cron 启动时 `auth status` 看到 `needs_refresh` → 主动跑 `lark-cli calendar +agenda`（最小 user-only API 调用）→ `status: ready`，expiresAt 从昨天 8-23 10:03 推到 2026-08-25 01:51（+15h）。**反模式规避**：连续 8 天写"自愈证伪"被今日标配动作彻底关闭；明天 cron 启动时应自动应用此动作。
- 【新发现·待你确认】**SUP-66 (7-29 早报) `todo` 状态 27 天 0 comment**：Multica issue 创建后 autopilot 完全未启动过——既不是 Mode A（已成功漏翻）也不是 Mode B（自报失败），是**第 6 类新失败模式**："issue 创建后 autopilot 从未运行"。可能根因：7-29 是 EDN 周报首次失败的同一天（subagent 600s 超时 + brotli 异常），那天的 follow-builders autopilot 也可能同样被父级故障吃掉。**等你拍板**：手动补跑 7-29 早报（从缓存补）vs 永久 skip 此期。
- 【延续】SUP-88 (8-15 早报 Mode E 失败) 仍 in_review 第 10 天——autopilot 写中文 comment 但**完全跳过 fetch + 完全跳过飞书投递**，用户 8-15 没收到任何 AI Builders 早报。**用户仍未拍板**：今天手动把 SUP-88 comment 转贴飞书 vs 永久 skip 此期。
- 【延续】SUP-79 (8-06 早报 chat_id 大小写) 仍 `blocked` 第 19 天——autopilot 自己没纠正，agent 等用户拍板目标 chat_id。
- 【延续·EDN 业务随行】SUP-77 / SUP-75 仍 in_review（更新于 8-04），是 EDN 业务随行第三方对接项目遗留，非 autopilot 失败模式。

### 知识库 / Obsidian / Get笔记
- 今日无新写笔记（除日报本体）。无用户主动 sync 请求。
- vault HEAD `097fe91` = 8-23 日报 commit，今日日报 commit 即将提交。

### 飞书 / 日历 / 文档 / 消息平台
- 【已完成】SUP-97 autopilot 飞书投递：bot 身份（user identity 仍缺 `im:message.send_as_user` scope，已自动回退 bot），08:03 实际送达。
- 【已自愈】lark-cli user identity refresh 路径今日 cron 主动 trigger 成功——标配动作首次落地。
- 【今日日历观察】日程中有"EDN 解决方案铁三角研讨例会"09:30-11:00（organizer 李斌7，self rsvp: accept），与你 EDN 周报相关但属日常业务例会，不影响日报关键事项。

### 调研 / 内容处理
- 无主动调研 / 内容处理。SUP-97 autopilot fetch + deliver + flip 走通（无本日 cron 介入任何阶段）。

### 其他 / 系统健康
- `/tmp` 旧工件扫描：8-23 102 行 → 8-24 **102 行**（完全相同），basename diff 净 0 件用户可读新工件；按 SKILL §0-pre 折叠规则连续 ≥ 4 天结果一致时可折叠，今日是第 1 天展开确认（含 oc-evolution 子树 26 件已稳定为 chat dump 白名单，不计入）。**`-mtime -1` 今日新增 = 3 件 Hermes 内部 scratch**（`paperclip.log` / `sgdaemon.log` / `ydeye_install.log`），全部 Hermes 容器内部 scratch，不计入用户可读工件。
- state.db 514 MiB（红线第 10 天）：8-23 513 → 8-24 514（+1 MiB 稳态）。**未拍板 VACUUM 窗口已 12 天**（8-13 首次列入风险位）。
- EDN cron 残件（`/tmp/hermes-results/` / `/tmp/edn-*/`）：今日为空（8-19 失败残件已被 GC Day #36 报告覆盖）。
- **EDN cron prompt 5 条补丁（含 stub-success 硬断言）仍未 patch**：GC Day #36 跑通未触及该 prompt（EDN cron 是独立 job `1d0cf41439ec`，GC Day 不会自动 patch）。**8-26（下周三）下次 EDN 触发会重蹈 8-19 stub-success 跑空**——EDN 已连续 2 周跑空，**不能再跑空第 3 周**。

## 4. 重要交付物 / 系统变更
- 类型｜名称｜状态
  - 日报｜Hermes 日报 2026-08-24｜撰写中（本笔记）
  - Multica issue｜SUP-97 (8-24 早报)｜**done**（autopilot 自翻，08-04:01Z）
  - 飞书消息｜SUP-97 中文早报｜**已投递** bot 身份 `om_x100b678cd18700a8b0349c0185ba596`
  - lark-cli user identity｜refresh 标配动作首次落地｜`status: ready`, expiresAt 推到 2026-08-25 01:51
- 普通 session / 普通命令验证不列。

## 5. 风险、阻塞、需要拍板
- **【🔴 紧急·第 12 天未拍板】state.db 514 MiB 持续超 500 MiB 红线**：上次拍板窗口建议 8-19/8-22/8-23 均未确认；cron 不可自行 VACUUM（锁库风险），需你明确"X 日凌晨"窗口。**建议：8-25（周一）凌晨 03:00 你不在用时**跑 `sqlite3 /root/.hermes/state.db 'VACUUM;'`。
- **【🔴 紧急·第 13 天未拍板】EDN cron prompt 5 条补丁（含 stub-success 硬断言）未 patch**：8-26（下周三）下次 EDN 触发前必须 patch 上 `jobs.json` job id `1d0cf41439ec`；**否则 8-26 必然重蹈 8-19 stub-success 跑空**——EDN 已连续 2 周跑空，**不能再跑空第 3 周**。补丁范围：① subagent `delegate_task` 必须配 `timeout=900s`；② 母 agent Step 3 综合分析前先逐 subagent 落本地 JSON；③ 飞书写入 `docs +update` 包 retry-on-error；④ `/tmp` 孤儿 plan 落 `/root/.hermes/state/edn-orphan-YYYY-MM-DD.json`；⑤ stub-success 硬断言（3 subagent 同步返回后立刻 die + 母 agent 卡在 "Waiting for results" 必须显式判失败）。
- **【🟡 待你拍板·新发现】SUP-66 (7-29 早报) `todo` 状态 27 天 0 comment**：第 6 类新失败模式"issue 创建后 autopilot 从未运行"。二选一：① 手动补跑 7-29 早报（从缓存补，已 27 天价值低）；② 永久 skip 此期（标 abandoned）。需要你一句话。
- **【🟡 第 10 天未拍板】SUP-88 (8-15 早报 Mode E 失败) 仍 in_review 第 10 天**：autopilot 写中文 comment 但**完全跳过 fetch + 完全跳过飞书投递**，用户 8-15 没收到任何 AI Builders 早报。今天手动把 SUP-88 comment 转贴飞书 vs 永久 skip 此期——二选一，需要你一句话。
- **【🟡 第 19 天未拍板】SUP-79 (8-06 早报 chat_id 大小写) 仍 `blocked`**：autopilot 没纠正 task description 里的 `OC_xxx`，agent 等用户拍板目标 chat_id（小写 `oc_d5de...92b` 是历史成功投递的目标）。可选项：① 修复 chat_id 后由 autopilot 重投递 8-06 早报；② 永久 skip 此期。

## 6. Hermes 自我改进
- **lark-cli user identity refresh 标配动作今日首次落地**（8-23 日报 §6 第 2 项立的标配）：cron 启动时 `auth status` → `needs_refresh` → 主动跑最小 user API 调用 → `ready`，**反模式规避**：连续 8 天"自愈证伪第 N 天"被关闭。**下次日报 cron 启动时应自动应用此动作**，无需每次在日报正文说明。
- **SUP-5x 元模式累计 35 样本 ≈ 77% 自愈率**（纯 follow-builders 33 样本 ≈ 82%）：今日 SUP-97 是继 SUP-96 之后**连续 2 天 autopilot 全自驱闭环**——这是 8-19 SUP-92 连续自愈信号之后的"再次出现连续 2 天"。**短期不放松 patch 紧迫性**（连续 2 天 ≠ ≥ 10 连续样本 90%+ 自愈率的稳定阈值）；8-30 GC Day #38 可考虑维持 follow-builders skill 双轨 patch（chat_id 兜底 + status flip 强制收尾）的"本周最紧急"评级。**反模式**：看到连续 2 天自愈就宣称"autopilot 自愈稳定"——仅 2 个连续样本不构成稳定信号。
- **新发现 SUP-66 第 6 类失败模式"issue 创建后 autopilot 从未运行"**：与 Mode A/B/C/D/E 全部不同，判别要素 = `status=todo` + `0 comments` + 创建时间已过 ≥ 24h。**GC Day 8-30 应 patch follow-builders skill** 加 hard assertion："Multica issue 创建后 ≥ 24h 仍 `todo` + `0 comments` → autopilot 必须显式在 issue 评论写明"未启动原因 + 待补跑日期""。**反模式**：把 SUP-66 当成"太久远不重要" 跳过——这是结构性失败模式，未来还会复现。

## 7. 明日建议
- **【可自动做·你拍板后】明日 cron 启动时主动 patch EDN cron prompt**（job id `1d0cf41439ec`）— 加 5 条 stub-success 硬断言。**8-26（下周三）下次 EDN 触发前必须落地**——cron 可自动 patch，但你拍板后我做。
- **【需立即处理·你拍板】state.db 514 MiB → 确认 8-25 凌晨 03:00 VACUUM 窗口**：cron 已连续 12 天呼吁未拍板；预计回收 ≥ 50 MiB。**建议明日（周一 8-25）凌晨 03:00 你不在用时**跑 `sqlite3 /root/.hermes/state.db 'VACUUM;'`。
- **【可自动做】明日 cron 启动时自动应用 lark-cli user identity refresh 标配动作**——今日已验证可行，无需每次手动 trigger；若仍未刷新说明 OAuth refresh token 过期，需 `lark-cli auth login --refresh` 重授权。
- **【可自动做·你拍板后】SUP-66 (7-29 早报) 处理路径**：① 手动补跑 7-29 早报（已 27 天价值低）；② 永久 skip 此期（标 abandoned）。两条路径都不复杂，需要你一句话。
- **【可自动做·你拍板后】SUP-88 (8-15 Mode E) 处理路径**：① 手动把 SUP-88 comment 转贴飞书（保存 8-15 早报补档）；② 永久 skip 此期（标记 abandoned）。两条路径都不复杂，需要你一句话。
