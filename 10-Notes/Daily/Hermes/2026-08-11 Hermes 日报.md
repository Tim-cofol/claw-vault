# Hermes 日报 - 2026-08-11

## 1. 老板速读
- 今日没有用户直接对话；主要完成 1 次上游 Hermes 公告转述、1 次日报审计与系统健康核对。
- 上游转述覆盖 Hermes 的 `/learn` 书籍转技能、Browser Use 降低浏览器工具上下文消耗、Hermes Pixel Office，以及 Polymarket 技能移出内置技能等更新。
- `/tmp` 旧工件扫描共 77 件：3 件已归档、74 件未归档；相比昨日 75 行新增 `digest_final.md`、`podcast.txt`，需确认是否保留或清理。
- state.db 当前约 486 MiB，已越过 480 MiB 风险线；主要消息量来自 Discord、cron 和 ACP，建议安排低峰维护窗口。
- 今日无代码项目、GitHub、飞书文档或日历操作；未发现新的业务项目产出。
- 关键待拍板事项：是否处理新增 `/tmp` 文件、是否安排 state.db VACUUM、是否补齐飞书 user identity 的 `im:message.send_as_user` scope。
- 明日优先：先确认工件和数据库维护窗口，再观察 follow-builders 早报的投递与 status 收尾。

## 2. 今日工作总览
- 【已完成】日报 cron 按 Asia/Shanghai 当前时间窗完成活动发现、`/tmp` 双口径扫描、state.db 体积与来源核对。
- 【已完成】转述一组 Hermes 上游公告到 Discord；该活动属于上游信息转述，不涉及配置变更或知识库写入。
- 【已发现风险】`/tmp` 新增 2 个未归档文件；state.db 超过 480 MiB；飞书 user identity 仍显示 needs_refresh 且缺少关键发送 scope。
- 【静默】今日无用户直接对话、无 acp/subagent 业务派发、无代码提交、无 EDN 周报或 GC Day 任务。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已完成】完成今日活动审计；确认唯一非日报 cron 的活动是 Discord 上游公告转述，不把历史 context 误计为多项新产出。
- 【风险】state.db 约 486 MiB，来源消息量以 Discord、cron、ACP 为主；已超过 480 MiB 观察线，未自行执行 VACUUM。
- 【待观察】今日未发现 follow-builders、EDN 或 GC Day 的新业务执行记录。

### 飞书 / 日历 / 文档 / 消息平台
- 【静默】今日没有手动飞书消息、日历、文档或表格操作。
- 【需补 scope】`lark-cli auth status` 显示 user identity needs_refresh，scope 列表未见 `im:message.send_as_user`；未自动发起 OAuth 重授权。

### 知识库 / Obsidian / Get笔记
- 【已完成】生成并保存本日报；本轮 Git push 与 Get笔记同步在正文定稿后执行。
- 【风险】`/tmp` 新增的 `digest_final.md` 与 `podcast.txt` 尚未归档，需确认其是否为用户可保留产出。

### 调研 / 内容处理
- 【已转述】将 Hermes 上游关于 `/learn`、Browser Use、Hermes Pixel Office 和 Polymarket 技能迁移的公告中文化并转述到 Discord；未形成独立 Vault 资料笔记。

## 4. 重要交付物 / 系统变更
- 上游公告转述｜Hermes Agent 近期功能与技能变更｜已转述｜Discord `#hermes-agent-notifications`
- 日报审计快照｜`/root/.hermes/state/scan_2026-08-11.txt`｜已生成｜77 行（3 ARCHIVED + 74 NOT_ARCHIVED）
- state.db 监控｜`state_db_size.log`｜已补齐 8-09 缺口并追加 8-11｜约 486 MiB，超过 480 MiB 风险线
- 日报｜Hermes 日报 2026-08-11｜已写入并同步｜本文件

## 5. 风险、阻塞、需要拍板
- 【需你确认】`/tmp/digest_final.md`、`/tmp/podcast.txt` 是今日相对昨日新增的未归档文件。建议先判断是否为用户需要保留的早报/播客产物；若是，归档到 Vault，否则清理。
- 【需你确认】state.db 已约 486 MiB，接近 500 MiB 红线。建议安排低峰窗口执行 VACUUM；日报 cron 不自行执行，以免锁库影响其他任务。
- 【需你确认】飞书 user identity 仍缺 `im:message.send_as_user`，可能导致后续投递静默 fallback 到 bot 身份。建议补 scope 后再依赖 user 身份投递。
- 【可观察】今日没有新的 follow-builders 结果，无法更新 SUP-5x status flip 自愈率；明日早报是下一次验证点。

## 6. Hermes 自我改进
- 今日按要求完成 `-mtime +0` 与 `-mtime -1` 双扫描，并用 basename diff 区分真实新增，避免把 autopilot 当日产物或年龄变化误报为新工件。
- state.db size log 发现缺少 8-09 行，已按连续记录规则补齐后再追加今日数据；后续应继续保持每日一行，避免错误计算单日增量。
- 对上游公告 session 按“转述产出”定性，不把 context 中的多条历史公告重复计入今日活动。

## 7. 明日建议
- 【需要你确认】确认 `digest_final.md`、`podcast.txt` 的保留策略；若保留，自动归档并双写；若废弃，再清理 `/tmp`。
- 【需要你确认】安排 state.db 低峰 VACUUM 窗口，优先在不运行其他 cron 的时段执行。
- 【需要你确认】补授权 `im:message.send_as_user` 等缺失 scope，避免飞书投递身份错配。
- 【可自动做】运行明日 follow-builders 早报，并独立核验飞书送达与 issue status 是否都完成。
