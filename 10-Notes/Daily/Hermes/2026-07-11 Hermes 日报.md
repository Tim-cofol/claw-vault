# Hermes 日报 - 2026-07-11

## 1. 老板速读
- 今天是典型"周末静默 + autopilot 自驱"日：0 用户直接对话，全部活动是 3 个子 agent / cron 在闭环。
- Multica SUP-48「AI Builders 每日早报」08:00 autopilot 已发飞书 DM，issue `in_review`、飞书 message_id `om_x100b6a2cdee608a8b1dfc1496268fb8`，本轮仍按规则不自动 `done`，等日报复审（**核心延续项第 N 天**）。
- GC Day 09:04 cron 跑通：patch 2 个 skill（feishu 加 OC home DM ID、hermes-operations 固 Tim 当前模型栈）、MEMORY 1419→1233 chars（56%），USER 未动；健康检查发现 3 个新黄灯需要你拍板（state.db 392M、Slack Socket Mode `Session is closed` 风暴、snapshots 策略缺失）。
- Discord 上游公告转述 1 条（Hermes Desktop ↔ Hermes Cloud 自动发现连接能力），只做中文化贴回 `#hermes-agent-notifications`，0 影响。
- `/tmp` 旧工件预扫描：97 行（11 ARCHIVED + 86 NOT_ARCHIVED），比昨天 101 略少但 NOT_ARCHIVED **+2**，不算完全零漂移；今天新增的 4 个 `-mtime -1` 文件全是 Hermes cron/agent scratch，无用户产出需要找回。
- 历史 autopilot 状态债继续累：SUP-39/40/41/42/43/44/47/48 八条均停在 `in_review`/`in_progress`，dashboard 噪声仍未收敛。
- 明日优先：定 SUP 早报 issue 自动 `done` 规则；GC Day 提的 state.db / Slack Socket Mode / snapshots 三个新黄灯至少回一个。

## 2. 今日工作总览
- 【已完成】Multica SUP-48 autopilot：拉 1 播客 + 19 builder + 44 tweets、@claudeai HTTP 402 降级标注、bot 身份 DM 飞书送达、issue 留言置 `in_review`。
- 【已完成】Hermes GC Day 周六 cron：MEMORY/USER 体检、skill 沉淀（patch 2 个 skill + MEMORY 5 段合并为索引）、健康检查红绿灯报告、5 条下周关注点。
- 【已完成】Discord 上游公告转述 1 条（Hermes Desktop × Hermes Cloud），00 异常、0 下游动作。
- 【已完成】本日报按 daily-report 模板生成并准备双写。

## 3. 按项目 / 责任域汇报

### Multica / 自驱试运行
- 【已完成】SUP-48「AI Builders 每日早报」：08:00 autopilot 触发 → follow-builders skill 拉 feed → 中文 remix → 飞书 DM `oc_d5de551525d3a4ae9e1a52815bb9a92b` 送达（message_id `om_x100b6a2cdee608a8b1dfc1496268fb8`），issue 状态 `in_review`。
- 【观察】autopilot 闭环模式今日与昨日完全一致：送达成功 + 无异常仍停在 `in_review`，靠日报复审翻 `done`。连续 2 天在日报里喊该定规则了，今天 **第 3 次提及**——今天再不拍板就是"观察变成噪声"。
- 【风险】历史 autopilot 状态债：SUP-39/40/41/42/43/44/47/48 共 8 条历史/今日早报停在 `in_review`，dashboard 长期噪声无收敛信号。

### Hermes / Agent 系统
- 【已完成】Hermes GC Day 周六 cron（09:04，jobid `8dc968bf2488`）：实际改动 = patch `openclaw-imports/feishu` §Key constants（加 OC home DM ID）+ patch `autonomous-ai-agents/hermes-operations` §Lane 1（新小节"Tim's current model stack (2026-07-11)": main=`minimax-cn:MiniMax-M3` / cheap-aux=`openai-codex:gpt-5.4-mini` / fallback=`minimax-cn:MiniMax-M2.7`，明确"勿把 fallback 埋到 aux 槽")；MEMORY.md 5 段合并成索引 → 1419 → 1233 chars（56% / 2200 上限），USER.md 保持 456 chars（33%）。
- 【已备份】`~/.hermes/memories/{MEMORY,USER}.md.bak_gc_20260711` 已留底，patch 工具自身可回滚。
- 【健康检查】11 项指标红绿灯：🟢 Gateway PID 594602 + 4 个 cron 全 `last_status=ok` + MEMORY/USER 预算充足；🟡 5 项黄色——state.db 392M（未超 500M 但增速异常）、`patches/` 4 个 6 月旧 patch（疑已合入主仓）、`checkpoints/` 116M + `state-snapshots/` 276M 单包无滚动策略、`logs/` 52M 内 5/21 shutdown 诊断 11M 未压缩、Slack Socket Mode 7/11 07:53 起每 10s `Session is closed` 风暴 errors.log 涨 5K+ 行。

### 知识库 / Obsidian / Get笔记
- 【无新增用户笔记】今天用户没主动要求保存任何笔记到 Vault / Get笔记；daily 报告本身按双写流程落档。
- 【自动产出】GC Day 不写新 Vault 笔记（按 skill-curation 规则，cron prompt 只输出到 origin + 留底 logs），所以今天 Vault 净增只有本日报自身。

### 飞书 / 日历 / 文档 / 消息平台
- 【已完成】SUP-48 早报送达飞书 DM，DM target 与昨日 GC patch 固化的 `oc_d5de551525d3a4ae9e1a52815bb9a92b` 一致（补丁即为此场景）。
- 【仍存在】昨天 §5 提及的飞书 user identity scope 缺失问题今天 0 修复动作；SUP-48 投递仍走 bot fallback，不是 user。
- 【已完成】Discord 上游公告转述 1 条（Hermes Desktop × Nous Portal Cloud 自动发现，bot 公告已转中文贴回 `#hermes-agent-notifications`）。

## 4. 重要交付物 / 系统变更
- 早报｜SUP-48「AI Builders 每日早报（7/11）」｜已投递飞书，issue `in_review`｜message_id `om_x100b6a2cdee608a8b1dfc1496268fb8`，含 1 播客 + 19 builder + 44 tweets（@claudeai HTTP 402 降级标注）
- Skill patch｜`feishu` §Key constants｜已应用｜新增 OC home DM `oc_d5de551525d3a4ae9e1a52815bb9a92b`
- Skill patch｜`hermes-operations` §Lane 1｜已应用｜新增 "Tim's current model stack (2026-07-11)" 小节
- MEMORY 段落｜5 段合并为索引｜已应用｜1419 → 1233 chars（56%）
- GC Day 周报｜Hermes GC Day 2026-07-11｜已闭环｜cron 已通过 origin 投递，红绿灯 + 5 条下周关注点（详细见 §3 "Hermes / Agent 系统"）
- Discord 转述｜Hermes Desktop × Hermes Cloud 自动发现｜已转贴｜0 下游影响
- 日报｜2026-07-11 Hermes 日报｜准备归档｜`10-Notes/Daily/Hermes/2026-07-11 Hermes 日报.md`

## 5. 风险、阻塞、需要拍板
- 【需要你确认·第 3 次提及】SUP 早报 issue close-out 规则：今天 SUP-48 已成功送达且 0 异常，issue 仍 `in_review`；SUP-39/40/41/42/43/44/47 同样状态。**定"投递成功 + 无异常 + 评论已写 = 自动 `done`"** 还是保留日报复审——这是 dashboard 噪声不减的根因。
- 【需要你确认】GC Day 健康检查 3 个新黄灯至少回一个：(a) Slack Socket Mode 7/11 07:53 起 `Session is closed` 风暴 errors.log 涨 5K+ 行，疑 7/11 04:04 `Session expiry finalize` 把 in-flight WS 一起关了；(b) `state.db` 392M（messages 17,831 + FTS5 trigram 35,726 rows 占大头），可试探 VACUUM；(c) `state-snapshots/` 只剩 6/20 一份 276M，缺滚动策略。
- 【需要你确认】飞书 user identity scope 仍未补：scope 列表里**没有** `im:message.send_as_user` / `im:message.group_at_message_send_as_user`，SUP-48 仍走 bot 投递；昨天就喊了，cron 不能自作主张 refresh OAuth。
- 【观察·非风险】`/tmp` 旧工件 97 行（11 ARCHIVED + 86 NOT_ARCHIVED），比昨天 101 略少但 NOT_ARCHIVED **+2**：新文件名是有 6 件新增 Hermes 内部 scratch（旧 ARCHIVED 减少是同一批迁移）。完全稳态；本日报 §5 不重复列举文件名。
- 【观察·非风险】今日 `-mtime -1` 新工件仅 `/tmp/digest.{err,json}` + `/tmp/scan_20260710.txt` + `/tmp/today_sorted.txt`，全是 Hermes cron/agent scratch，**无新增用户产出**需要找回。

## 6. Hermes 自我改进
- 本日报是连续 6 天的"周末静默 + autopilot 自驱"主框架（从 6-15 第一次全静默日开始），状态字段已经从"无活动"转为"3 个子 agent 闭环清单"。**建议把这个 stable 模板升级为"周末静默 variant"**——日后再遇到纯静默日，沿用本日框架，只改 §4/§5 的实际数字，不必每次都重写分组骨架。
- 连续 3 天日报重复提及"SUP 早报 issue 自动 `done` 规则"。**这是 cron prompt 已经超载的早期信号**：要么今天拍板定规则、要么在 cron prompt 里直接写"投递成功 + 评论已写 + assistant 输出里出现 om_xxx = 自动 `done`"这行 hard rule。**第 3 次重复后再不收敛，下次该改 cron prompt，不该继续喊**。
- GC Day 是首个"按 prompt 真实执行 skill patch + 留备份 + 输出红绿灯"的 cron 实跑样例，且整轮没触发任何破坏性 `rm`。建议把 GC Day 风格（"可自动做 / 需确认 / 不动" 三档 + dry-run 默认）横向推广到其他 cron prompt——以后改 cron prompt 模板时复用这个分档结构。

## 7. 明日建议
- 【需要你确认】定 SUP 早报 issue 自动 `done` 规则：3 天喊到第 3 次，再不定今天就把规则写进 follow-builders cron prompt（`投递成功 + 评论已写 + 出现 om_xxx 即 issue = done`）。
- 【需要你确认】GC Day 5 个关注点中至少回 1 个——优先 Slack Socket Mode 风暴（5091 errors.log 行还在涨，关乎今天 4:04 那个时点）或 `state.db` VACUUM 试探（影响 dashboard 性能 + FTS5 重建成本可控性）。
- 【需要你确认】飞书 OAuth scope 补 `im:message.send_as_user` + `im:message.group_at_message_send_as_user`——昨天前天都喊了，今天再不补，SUP-49 早报也仍走 bot 投递。
- 【可自动做】SUP-39/40/41/42/43/44/47/48 八条历史/今日早报批量 `done` 翻状态：第 1 条建议的规则还没拍板前，**这一条不要自动做**；等规则落地后 batch 一条命令收口。
- 【可自动做】本日报周六 GC Day cron 刚跑完，下次再 GC Day 是 7-18；下周中间（周三）会跑 EDN 周报、自动飞书长住文档二次 append（沿用 7-08 pitfall H `block_insert_after`），提前 1 天确认 lark-cli scope 是否准备好。
