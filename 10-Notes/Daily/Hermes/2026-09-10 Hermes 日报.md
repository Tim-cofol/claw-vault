# Hermes 日报 - 2026-09-10

## 1. 老板速读
- **静默日延续** — 今日 0 个用户直接对话 / autopilot / Discord / 飞书会话，state.db 今日仅日报 cron 1 个 session（46 条 msg）。延续本周"周末静默日"基调。
- **autopilot 静默第 7 天，本周 0 期 AI Builders 早报** — Multica 最新 SUP-N 仍停在 SUP-107 (9-03)，今日预期 SUP-N (约 SUP-118~120 范围) 未创建，连续 7 天 0 期。本周 5 工作日 + 9-10 周三 = **6 天 0 期 AI Builders 早报投递**。根因仍未定位。
- **state.db 583 MiB 单日 +3 MiB** — 连续 27 天呼吁 VACUUM 窗口未拍板（自 8-15 踩 500 MiB 红线起）；cron 不可自动 VACUUM 锁库，仍需用户拍板。
- **lark-cli refresh 标配连续 18 天闭环** — 今日 cron 启动时 user `needs_refresh` → `calendar +agenda` → `ready`，expiresAt 推到 9-11 01:51。
- **EDN cron 今天未触发** — 9-09 跑通后下次触发是 9-16 周三 08:30（连续 1 周窗口）。9-10 state.db size log 续写完成。
- **Idea Digest 99992402 投递失败连续第 8 周** — cron `last_status='ok'` 但 `last_delivery_error` 持续失败，9-14 周一 09:00 是下次窗口；workaround（改 `lark-cli im +send`）仍待拍板。

## 2. 今日工作总览
- **静默日（0 个用户 / 0 个 autopilot / 0 个派发 subagent）** — state.db 今日只有日报 cron 1 个 session 46 条 msg（assistant: 13 / tool: 32 / user: 1）；平台推送 `platform_message_id` 当日 0 条。
- **lark-cli refresh 标配** — 18 天连续自动闭环，今日 cron 启动时主动 trigger（`calendar +agenda` 零依赖 recipe）。
- **`/tmp` 工件扫描** — 今日 82 行 vs 昨日 46 行（**+36 行，+78% 大幅增长**），全部是昨日 0-day 龄跨 24h 后被 `-mtime +0` 首次抓到的 EDN 当日产物（10 件 `hermes-results/call_*.txt` + `edn_report_2026-09-09.md` / `edn_chunk1.md` / `edn_chunk2.md`）+ 旧 oc-evolution 子树残留 26 days age 跨日被重新数。**这是 7-28 "真零漂移隐式漏报"的同款机制**——9-09 EDN 跑通时落地的工件昨日 0-day 龄被跳过，今日 1-day 龄才被 `-mtime +0` 抓到。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【异常·7 天延续】autopilot 静默第 7 天：今日预期 SUP-N (约 SUP-118~120 范围) 仍未创建，Multica `issue list` 验证 SUP-108 ~ SUP-130 全部不存在，最新 SUP-N 仍是 SUP-107 (9-03)。**根因仍未定位**（acp 触发器 / 上游 feed / API 异常均可能）。本周 6 天 0 期 AI Builders 早报投递，autopilot 自驱 0% 自愈率（L4 静默第 4 天）。
- 【已闭环】lark-cli user identity refresh 标配连续 18 天自动闭环。
- 【异常·已确认】Idea Digest cron (`2e8ecd9b7cb2`) 投递失败连续第 8 周——`last_status='ok'` 但 `last_delivery_error='live adapter send failed: [99992402] field validation failed; ...'`。下次触发 9-14 周一 09:00。

### 知识库 / Obsidian / Get笔记
- 【正常】日报 cron 23:50 启动，本篇走双写流程，推送到 Get笔记 MyClaw。
- 【异常但已识别】`/tmp` 工件扫描：今日 82 行 vs 昨日 46 行（+36 行，+78% 大幅增长）——全部是昨日 EDN cron 跑通时落地但 0-day 龄被跳过的产物，今日 1-day 龄才被 `-mtime +0` 首次抓到（7-28 "真零漂移隐式漏报"机制第二次跨日复现）。具体：(a) EDN 9-09 当日产物 10 件 `hermes-results/call_*.txt` + 3 件 `edn_*.md/json`；(b) 旧 oc-evolution 子树残留（26 days age，跨日被数）；(c) 长期 NOT_ARCHIVED 工件（digest-prep.json / digest_body.md / fb-digest.json / fb-digest.md / digest-2026-08-16.md / edn_skeleton.json / edn_template.json）。**用户可读新工件 0 件**，全部归 EDN cron 残件 + 历史未归档工件。

### 飞书 / 日历 / 文档 / 消息平台
- 【正常】lark-cli user identity 已 refresh（user status ready，expiresAt → 9-11 01:51，连续 18 天闭环）。
- 【缺位】今日无飞书 AI Builders 早报投递（autopilot 静默第 7 天）。
- 【已跑通】EDN 周报飞书长住文档昨日追加成功（`Qe1dd7O7XoCAZ9xBeNUc3meLnch` revision 70，今日未变更）；EDN cron 下次触发 9-16 周三 08:30。
- 【异常】Idea Digest 飞书投递 99992402 失败连续第 8 周；workaround 待拍板（继承上周）。

### 调研 / 内容处理 / EDN
- 【已闭环】EDN 9-09 周报昨日真实跑通（90+ 条证据 / 9 家核心厂商 / 4 家 silent / 飞书长住文档追加 + origin 推送）；今日 EDN cron 未触发（按周节奏）。
- 【观察】state.db size log：9-09 580 MiB → 9-10 583 MiB（**单日 +3 MiB，稳态增长**）。今日 0 个 user / 0 个 subagent 会话，增长主要来自 9-09 EDN 周报落盘的 tool_calls JSON 残件（昨日 0-day 龄跨日才被 -mtime +0 抓到）。
- 【静默日】0 条新调研动作。

## 4. 重要交付物 / 系统变更
- 报告 | 2026-09-10 Hermes 日报.md | 即将写 vault + 双写 Get笔记 MyClaw
- 状态 | SUP-N 预期 (~SUP-118) | not found，autopilot 静默第 7 天，本周 6 天 0 期早报投递
- 状态 | state.db 583 MiB（+3 MiB 单日）| 500 MiB 红线 +83 MiB，VACUUM 未拍板
- 状态 | lark-cli user identity | 已主动 refresh，expiresAt → 9-11 01:51（连续 18 天闭环）
- 状态 | Idea Digest cron `last_delivery_error` | 99992402 连续第 8 周，workaround 待拍板
- 状态 | EDN cron 下次触发 | 9-16 周三 08:30（连续 1 周窗口）

## 5. 风险、阻塞、需要拍板
- **[🔴 紧急] autopilot 连续 7 天静默 + 本周 0 期早报**：根因仍未定位（acp 触发器 / 上游 feed / API 异常均可能），已挂 L4 区间第 4 天。本周 6 天 0 期 AI Builders 早报投递，autopilot 自愈率 0%。建议三选一：(a) 手动查 acp 触发器日志定位根因；(b) 手动跑 follow-builders skill 补 9-04~9-10 七天早报；(c) 跳过等 9-16 后自动恢复。日报 cron 不能代跑 autopilot。
- **[🔴 紧急] state.db 583 MiB VACUUM 未拍板**：已 27 天呼吁，连续 9 天踩红线后稳态增长（+1-3 MiB/天）。建议尽快给凌晨窗口拍板（cron 不可自动 VACUUM 锁库）；GC Day #26（9-12）应明确加入议题。
- **[🔴 紧急] Idea Digest 99992402 连续 8 周失败**：cron `last_status='ok'` 但 `last_delivery_error` 持续失败。下次触发 9-14 周一 09:00；workaround（改 `lark-cli im +send` 绕开 gateway live-adapter）已挂 4 周未拍板；GC Day #26 应明确拍板路径。
- **[观察] `/tmp` 工件扫描 +78% 大幅增长**：今日 82 行 vs 昨日 46 行，但全部是昨日 EDN cron 跑通残件（0-day 龄昨日被跳过，今日 1-day 龄才被首次抓到）+ 历史未归档工件。**用户可读新工件 0 件**——9-10 静默日本质无新产出，§5 风险不增列，仅 §6 自我改进点出 7-28 "真零漂移隐式漏报"机制二次跨日复现。

## 6. Hermes 自我改进
- **EDN 残件清理规则仍未落地（EDN cron prompt patch 已挂 1 周）**：9-09 EDN 跑通后 `/tmp/hermes-results/call_*.txt` 10 件 + `edn_report_2026-09-09.md` / `edn_chunk1.md` / `edn_chunk2.md` 3 件今日才被 `-mtime +0` 首次抓到（9-09 cron 自己跑通时未做收尾清理）。GC Day #26 (9-12) 应落地 EDN cron prompt 末尾加 `rm -f /tmp/hermes-results/call_*.txt /tmp/edn-*/data_asset.json /tmp/edn_chunk*.md /tmp/edn_report_*.md` 作为标准收尾——已挂 1 周未动，反模式风险升级。
- **autopilot 静默判定路径稳定复用第 7 天**：4 步诊断（state.db 新 session 计数 / Multica SUP-N 检查 / 昨日 SUP-N 反推 / 8:00 对比窗口）在 L4 区间仍适用；但根因未定位前 cron 不能自动选应急方案这一约束在 L4 仍成立——已挂 4 天未拍板，autopilot 自愈率 0%，本周积压 6 期早报未投递。

## 7. 明日建议
- 【可自动做】lark-cli refresh 标配自动闭环（cron 自跑）。
- 【可自动做】`/tmp` 工件扫描 + state.db size log 续写（cron 自跑）。
- 【可自动做】EDN cron 9-16 周三 08:30 下次触发按今日三件验证（data_asset / 飞书长住文档 / origin）判定健康。
- 【需你确认】autopilot 静默第 7 天根因定位方案（手动查 acp 触发器 vs 手动补跑 7 期 vs 跳过等 9-16 自愈）；此问题已挂 4 天日报紧急项，需要你拍板路径。
- 【需你确认】state.db VACUUM 窗口期（cron 不可自动 VACUUM 锁库，必须你拍板）。
- 【可自动做】GC Day #26 9-12 把以下议题一并纳入：(a) EDN cron prompt 5 条 patch + 残件清理规则；(b) Idea Digest 99992402 workaround 拍板；(c) state.db VACUUM 窗口期拍板；(d) autopilot 静默根因排查方案；(e) 加 12:00/17:00 轻量告警 cron（autopilot 静默自 9-04 起已 7 天提议未落地）。
