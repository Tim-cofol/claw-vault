# Hermes 日报 - 2026-06-28

## 1. 老板速读
- 今日核心产出 = **SUP-35 AI Builders 每日早报** 自动投递飞书成功（覆盖 15 位 builder、34 条 tweet、1 条 podcast）；以及飞书会话 **京东物流 IT 需求 → 验收标准** 结构化拆解落地。
- **SUP-36「给吴栋栋发 hello」双重路径都堵**——user 身份缺 `im:message.send_as_user` scope，bot 身份又遇 230013「应用可见范围不含此人」。需用户在飞书后台或 scope 补授权二选一拍板。
- /tmp 扫描：仍以 Hermes 内部 scratch 为主（hermes-cwd-*.txt、hermes-results/*.txt、lark_device_code.txt 等）。无新增用户产出挂起；旧工件（ai_security_infographic / cisco_nhi_v3 / beautiful-feishu-whiteboard / esis-prd.md）已闭环，未产生新的档 1 归档候选。
- lark-cli user identity `needs_refresh`（expiresAt 2026-06-28 10:06），scope 列表完整。下次 user API 调用会自动 refresh；如果今天或明天又跑需要 user 身份的脚本，先手动触发一次刷新以免冷启动失败。
- 明日优先：处理 SUP-36 用户拍板（scope 补授权 vs 应用可用范围），跑 SUP-36 早报（如 cron 还设着）确认 user identity refresh 后能正常发。

## 2. 今日工作总览
- 【已闭环】AI Builders 每日早报 SUP-35 自动跑通并成功投递飞书，state-feed.json 已更新 seen 集。
- 【已闭环】京东物流 IT 需求验收标准——基于用户发的需求截图，按 4 要素（功能/性能/边界/兼容性）拆 7 条核心功能 + 6 条硬指标 + 边界条件 + 兼容性矩阵 + 隐含验收点 + 验收方式。
- 【阻塞】SUP-36 给吴栋栋飞书发 hello——双路径（user scope 缺 / bot 可用范围不含）均失败，已贴 issue 等待用户拍板。
- 【无活动】11:15 启动的 Multica session `8566f8d4-...` 0 消息空跑，未产生任何任务；可能为 ACP stub session。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已推进】SUP-35 AI Builders 每日早报 cron 化第一日跑通——15 builder / 34 tweet / 1 podcast，digest 已发飞书 bot 身份（message_id `om_x100b6b3216f900a0b039046bd513ff1`），state-feed.json 防重复机制生效。
- 【已推进】follow-builders skill 适配 Multica + lark-cli 投递路径跑通——原 deliver.js 只支持 telegram/email/stdout，本轮通过 Python subprocess + lark-cli `+messages-send --as bot` 解决，dry-run 验证后实际投递。
- 【风险】follow-builders skill 提示文件缺失：`skills/find` 返回不在 registry，agent 通过文件系统直接定位 `.agent_context/skills/follow-builders/`。建议把这个 skill 正式注册进 Hermes skills（一次性动作），避免下次 Multica agent 重新摸索路径。

### 知识库 / Obsidian / Get笔记
- 【观察】/tmp 累积大量 `hermes-cwd-*.txt`（41+ 个，2-8 天）+ `hermes-results/*.txt`（7 个）+ `lark_device_code.txt`（16 天）。这些是 Hermes 自身 scratch / ACP cwd 临时文件 / 飞书 device-code 凭据，**不属于用户产出**，与之前日报定调一致——不归档，按内部 scratch 处理。
- 【观察】/tmp 唯一值得归档的候选：`beautiful-feishu-whiteboard/{CATALOG.md, README.zh.md, RULES.md, SKILL.md}`（5 天 NOT_ARCHIVED）——但这是 OpenClaw 内部的 skill 草稿，已经在 OpenClaw 仓库内，未到用户产出层级；列入候选但暂不动。

### 飞书 / 日历 / 文档 / 消息平台
- 【阻塞】SUP-36：user 身份缺 `im:message.send_as_user` scope（一次性授权扩展，非 token 过期）；改 bot 路径后遇 230013「应用可见范围不含此人」——两条路堵在不同层级。修复路径二选一：
  - (a) `lark-cli auth login --scope "im:message.send_as_user"` 重授权（用户浏览器确认）；
  - (b) 在飞书开发者后台把应用可见范围改为全员或包含锐捷 EBG-软件产品事业部。
- 【状态】lark-cli user identity `needs_refresh`（token expiresAt 2026-06-28 10:06），scope 列表本身完整。下次 user API 调用会自动 refresh——SUP-35 早报今天跑通靠的是 bot 身份，不需要 refresh，所以今日未受影响。

### 调研 / 内容处理
- 【已闭环】京东物流 IT 需求验收标准——基于用户截图（哑终端资产 + 无线准入 + AI 定界 + 资产管理对接），结构化拆解为：F1-F7 功能优先级（F1-F6 红线，F7 加分）、6 条硬指标（40W 资产 / 10W 白名单 / 150 台·秒 / 10000 AP / ≤2s 准入 / ≤30s 调拨同步）、边界条件、兼容性矩阵、隐含验收点、6 项验收方式。下一步等用户选 A（飞书表格）/ B（UAT 用例）/ C（MVP/二期拆分）。

## 4. 重要交付物 / 系统变更
- 类型｜名称｜状态｜路径
- 投递｜AI Builders 每日早报 06-28｜已投递｜飞书 message_id `om_x100b6b3216f900a0b039046bd513ff1`
- 文档｜京东物流 IT 需求验收标准｜已交付（feishu 会话内）｜session `20260628_091631_42116eb9`
- 代码｜follow-builders lark-cli 投递路径｜已落地｜supplement: `lark-cli im +messages-send --as bot` + Python subprocess
- 配置｜state-feed.json seen 集｜已更新｜34 tweet + 1 podcast 去重，下次不会重发
- 代码｜hermes-cwd / hermes-results / lark_device_code / pc.py / probe.py｜识别为 Hermes 内部 scratch｜/tmp，未归档（按日报定调）

## 5. 风险、阻塞、需要拍板
- **SUP-36 双重路径阻塞**：user scope 缺 + bot 可用范围不含——建议优先走 (b) 后台改可见范围（一次配置长期解决，scope 路径只解决单点）。**需用户拍板走哪条**，agent 不能擅自改飞书后台配置或 OAuth 重授权。
- **follow-builders skill 未注册进 Hermes skills registry**：今天 agent 通过文件系统 fallback 找到 `.agent_context/skills/follow-builders/`，下次同类任务可能再花时间定位。建议下次有用户空闲时一行命令注册进 registry（low cost）。
- **lark-cli user identity needs_refresh**：今日未受影响（投递走 bot），但明天任何 user 身份任务（如 SUP-36 修复后重发）首次调用会自动 refresh——若要心里有数，可现在手动跑一次 `lark-cli auth status` 触发 refresh，让 token 提前进入有效期。

## 6. Hermes 自我改进
- **idea-recall 联动 follow-builders digest**：今天 SUP-35 早报里 swyx / Dan Shipper / Aaron Levie 三条值得固化——Swyx「AI FDE 是新增高需求方向」、Dan Shipper「GPT-5.6 Sol 讽刺文 / 行业垄断担忧」、Aaron Levie「GPT-5.6 知识工作 + 长跑 agent 强」。建议明天（手动或 cron）追加为 idea 笔记。
- **skill 注册一致性**：follow-builders 这次靠文件系统 fallback 找到，AGENTS.md 已要求「高频 playbook 提到 skill」但 follow-builders 仍是 OpenClaw / Multica 内部 skill。建议固化一条规则：任何被 cron / Multica 调用的 OpenClaw / Multica skill，都应镜像到 `/root/.hermes/skills/`（一次性脚本批量同步即可）。

## 7. 明日建议
- **SUP-36 用户拍板**：等用户在「scope 重授权 / 后台改可见范围」中二选一后立即重发，验证 message_id。可自动做。
- **SUP-35 早报明日照常**：cron 已设置，跑 user identity 路径（消化早报要 user 身份 + 应用可见范围都通）——若 user identity refresh 后仍报 scope 问题，按今天的 lark-cli auth login 流程补 scope。可自动做，但需先验证 refresh。
- **idea 笔记补 3 条**：把今天的 swyx / Dan Shipper / Aaron Levie 三条触发写入 `30-Resources/Ideas/idea-2026-06-29-XXX-...md`。可自动做。
- **follow-builders skill 注册进 Hermes skills**：一行命令。需用户确认。
- **京东物流验收标准后续**：等用户选 A / B / C 之一，按选项推进 MVP 拆分 / UAT 用例 / 飞书表格化。
