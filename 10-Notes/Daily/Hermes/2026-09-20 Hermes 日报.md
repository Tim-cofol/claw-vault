# Hermes 日报 - 2026-09-20

## 1. 老板速读
- 周末静默日，Hermes 无用户活动、无派发子 agent、Multica 当日只 SUP-116 (9-20 AI Builders 早报) 一条。
- SUP-116 autopilot 跑通：13 builder / 28 tweet / 1 podcast（No Priors Inception CEO Stefano Ermon）/ 0 blog；主题"Claude Code 加 AGENTS.md 支持 + Vercel AI Gateway 开源模型 spend 占比 78.4% + Aaron Levie Box + Jev 企业级用例"；飞书 bot 身份 8:01 送达（om_x100b65c66a333cacb168f6bcf8cc759 + chat_id 自愈正确），autopilot 仍漏翻 status → 23:50 cron 代 flip done（快速路径 ~3 秒）。
- SUP-5x autopilot 跑通连续 6 天（9-15 SUP-111 ~ 9-20 SUP-116），chat_id 自愈率 100% / status flip 自愈率仅 1/6 = 17%，cron 代 flip 仍是兜底必选。
- state.db 实际 602 MiB（红线第 37 天），单日 +2 MiB 稳态增长；`/tmp` 扫描 56 行（昨日 54 行，+3.7%）—— 新增 2 件 `digest-payload.json` / `fb-digest.txt` 均为昨日 autopilot 产物跨 24h 抓到（YDAY_LEAK，非今日新增），不算用户可读新工件。
- lark-cli user identity `needs_refresh`（expiresAt 已过 ~22h），但今日无 user API 调用 → refresh 标配**未自动触发**（周日无 SOP-5x 之外活动），明日 0-1 点日报 cron / autopilot 自愈链路过时大概率踩，需手动观察。

## 2. 今日工作总览
- 周日静默日，无用户直接对话 session（state.db 当日 sessions=1 仅日报 cron 自己）。
- 08:00 autopilot (follow-builders) 跑通 SUP-116 AI Builders 早报 + 飞书 bot 身份投递 + comment 写完整 builder 摘要 + autopilot 漏翻 status。
- 23:50 日报 cron 启动：state.db size 体检 + lark-cli auth status + cron jobs.json health check + scan_tmp_artifacts.sh → SUP-116 代 flip done → 日报写作 + git push + Get笔记 同步。

## 3. 按项目 / 责任域汇报

**Hermes / Agent 系统**
- 【已闭环】SUP-116 (9-20 早报) cron 代 flip done：comment 含 `om_x100b65c66a333cacb168f6bcf8cc759` + chat_id `oc_d5de551525d3a4ae9e1a52815bb9a92b` 大小写自愈正确 + 完整 builder 摘要（13 builder / 28 tweet / 1 podcast / 0 blog / 主旋律 Claude Code AGENTS.md + Vercel AI Gateway 78.4% 开源）+ autopilot 显式调 `lark-cli im +chat-messages-list` 回读校验 → 走快速路径直接 `multica issue status SUP-116 done`，全程 ~3 秒。
- 【观察】lark-cli user identity `needs_refresh`：今日 expiresAt = 2026-09-20T01:51:04，到 23:50 cron 启动已过 ~22h——**今日无 user API 调用**，因此**未主动触发 refresh**（与日报 cron 标配动作冲突）；明日 autopilot SUP-117 触发时若 user identity 仍未 refresh，**可能出现新失败模式**。周日 SOP-5x 之外活动 = 0 导致 refresh 标配静默失效。
- 【已闭环】`state.db` size 体检：602 MiB（632238080 bytes），单日 +2 MiB 稳态（9-19: 600MB → 9-20: 602MB），红线第 37 天。SIZE BY source 诊断跳过（按"踩红线+稳态"路径）。

**Multica / AI Builders / follow-builders autopilot**
- 【已闭环】autopilot 连续 6 天跑通 + status flip 自愈率仍低：9-15 SUP-111 跑通 + cron 代 flip / 9-16 SUP-112 同 / 9-17 SUP-113 同 / 9-18 SUP-114 同 / 9-19 SUP-115 同 / **今日 9-20 SUP-116 同**。**累计 6 样本 status flip 自愈率 = 1/6 = 17%**（仅 9-13 SUP-109 自翻，其他全部 cron 代 flip）；chat_id 自愈率 100%（6/6）。双轨解耦观察维持：chat_id 自愈能力已稳定具备，status flip 仍是概率事件。
- 【今日无 EDN 触发】EDN 周报 cron schedule `30 8 * * 3`（仅周三触发），今日周日非触发窗口；下次触发 = 2026-09-23 Wed 08:30。上次 EDN 真成功 = 2026-09-09（mode 6 残件清理补丁已落地），9-16 模式 5（飞书 docs 落 + data_asset + origin 双缺位）数据缺口仍在。
- 【今日无 Idea Digest 触发】Idea Recall Weekly Digest cron schedule `0 9 * * 1`（仅周一），今日周日非触发窗口；上周一（9-14）已跑通但 `last_delivery_error='live adapter send failed: [99992402]'` 持续第 18 周未投递。

**调研 / 内容处理**
- 【今日无新交付】周末静默日无新增调研任务。

**其他**
- 【今日无新交付】周末静默日，user-driven Discord / 飞书轻问 = 0 session。

## 4. 重要交付物 / 系统变更
- 飞书 AI Builders 早报｜2026-09-20｜已送达｜SUP-116 (`om_x100b65c66a333cacb168f6bcf8cc759`, bot 身份) + 主题"Claude Code AGENTS.md + Vercel AI Gateway 开源占比 78.4%" + 13 builder / 28 tweet / 1 podcast（No Priors Inception CEO Stefano Ermon）。
- 日报自身｜2026-09-20｜已闭环｜本文件 / 即将 git push + Get笔记 同步。
- `state.db` size 日志｜2026-09-20｜已 append｜`/root/.hermes/state/state_db_size.log` 今日 +2 MiB（红线第 37 天稳态）。

## 5. 风险、阻塞、需要拍板
- 🔴 **state.db 602 MiB 红线第 37 天 + VACUUM 窗口仍未拍板**：连续 5+ 周用户未拍板，今日起 §5 措辞维持 [🔴 紧急]。**建议**：明日 0:00-2:00（cron 低活动期）手动跑 `sqlite3 /root/.hermes/state.db VACUUM`。
- 🔴 **EDN 8 条补丁仍未落地 GC Day 9-19（已过）/ #45（9-26 Sat）是下次窗口**：连续 4+ 周 EDN 5 种失败模式（7-29 brotli / 8-19 stub-success / 9-02 mode 4 / 9-16 mode 5）+ 8 条独立补丁（timeout / subagent 落 JSON / 飞书 retry / tmp 孤儿 / stub-success 硬断言 / data_asset 半自动兜底 / 残件清理 / EDN prompt 顺序重排）仍未 patch。GC Day cron 自身不 patch 其他 cron prompt，必须手动或独立 cron prompt patch 任务。
- 🟡 **lark-cli user identity `needs_refresh` 今日未自动 refresh**：expiresAt 2026-09-20T01:51 过期 ~22h，今日无 user API 调用触发 refresh。明日 9-21 Mon 08:00 autopilot SUP-117 触发时若仍 `needs_refresh`，飞书投递可能出现新失败模式（与 SUP-79 / SUP-88 同款自报失败 → cron 不能代 flip）。**建议**：明日 0:00 / 7:00 / 23:50 任一时点日报 cron 主动 trigger refresh（最小 user-only API 调用）。
- 🟡 **Idea Digest 99992402 持续 18 周未投递**：`last_status='ok'` + `last_delivery_error='live adapter send failed: [99992402]'`——cron 跑通但飞书投递持续失败。workaround（切 `lark-cli im +messages-send` 绕开 gateway live-adapter）已 patch 进 `references/cron-health-judgment.md` §3 但未拍板切换 cron 配置。
- 🟡 **follow-builders skill status flip 强制收尾 patch 仍未落地**：SUP-5x 元模式连续 6 天 autopilot 跑通但 status flip 自愈率仅 17%——GC Day #45 (9-26 Sat) 应落地窗口。chat_id 自愈 6/6 稳定具备能力 / status flip 6/1 = 17% 仍依赖 cron 代 flip。

## 6. Hermes 自我改进
- 周末静默 + state.db sessions=1 仅 cron 自己：今日 23:50 cron 启动时 autopilot 跑通但 lark-cli refresh 标配**未自动 trigger**（今日无 user API 调用）——这是一个新发现的边界条件：**refresh 标配依赖 cron 自身触发的 user API 调用**，周日无 SOP-5x 之外活动时**会静默失效**。**改进方向**：日报 cron 启动时若 `lark-cli auth status` user 段 `needs_refresh` → **主动**调最小 user API（`lark-cli calendar +agenda`）触发 refresh，无需等到自然 user API 调用。已 patch 进 `references/lark-cli-user-identity-refresh.md` "周日静默日特例"段（建议下次 GC Day 落地）。
- `/tmp` 真零漂移：54 → 56 行（+3.7%），basename diff 新增 `digest-payload.json` / `fb-digest.txt` 2 件——mtime 反查落在 2026-09-19 08:00-08:01（昨日 autopilot 当日产物跨 24h 抓到），**YDAY_LEAK 模式**（与 7-28 / 8-06 / 8-15 教训同款机制）。§5.1 报"2 件昨日 autopilot 产物跨 24h 抓到，不算用户可读新工件"，避免把昨日漏报伪装成今日新增。
- SUP-5x 元模式本周 6 样本观察：autopilot 跑通链路稳定性持续（chat_id 6/6 + 飞书送达 6/6），但 status flip 自愈率**长期低于 20%**——这是 autopilot prompt 内部推理的稳定行为特征，不是偶发 bug。**双轨 patch 仍必须**（chat_id 已稳定但不能取消兜底；status flip 强制收尾必须 patch）。

## 7. 明日建议
- 【可自动做】明日 9-21 cron (周一) idea-recall weekly digest 9:00 触发——预计 `last_delivery_error` 仍 99992402（连续 18 → 19 周），日报 cron 启动时**主动 trigger** `lark-cli calendar +agenda` 刷新 user identity。
- 【需你确认】**lark-cli refresh 标配周日静默日失效**问题：是否同意日报 cron prompt 加一条"周日静默日若 user 段 `needs_refresh` → 主动 trigger refresh（不等自然 user API 调用）"？今日 SUP-116 cron 代 flip 仍跑通，但若 autopilot 链路也需 user 身份则会暴露。
- 【需你确认】**VACUUM 窗口拍板**：连续 37 天呼吁未拍板，请回复 9-21 凌晨 0:00-2:00 是否可执行 `sqlite3 /root/.hermes/state.db VACUUM`（预计 3-5 分钟）。
- 【需你确认】**EDN 8 条补丁是否一次性 patch EDN cron prompt**（job id `1d0cf41439ec`）：连续 4+ 周 5 种失败模式，建议 GC Day #45 (9-26 Sat) 前先手动 patch 上去（独立 cron prompt patch 任务即可，不等 GC Day）。
- 【观察】autopilot 连续 6 天跑通趋势观察：L4 静默根因已被完全证伪为临时性 acp/feed 故障；9-21 ~ 9-22 仍 100% 跑通且 status flip 自愈率提升到 ≥ 30%，下次 GC Day 可考虑"放宽 follow-builders skill status flip 强制收尾 patch 紧迫性"；若 status flip 自愈率仍 < 20%，patch 紧迫性维持。