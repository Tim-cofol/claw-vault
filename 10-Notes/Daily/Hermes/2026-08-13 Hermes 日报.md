# Hermes 日报 - 2026-08-13

## 1. 老板速读
- 今日 0 用户直接对话、0 派发子 agent；仅有 SUP-86（AI Builders 每日早报）一组 autopilot 产出。
- SUP-86 飞书已成功送达（chat_id 自愈稳定），但 autopilot 仍漏翻 status → 今日 cron 代 flip done（与 8-12 SUP-85 同款双轨模式）。
- SUP-5x 元模式 12 样本累计：50% 投递后自愈率，其中 **chat_id 自愈 100% / status flip 自愈 0%** —— 8-15 GC Day 双轨 patch 仍是本周最紧急。
- `/tmp` 旧工件 72 行（3 ARCHIVED + 69 NOT_ARCHIVED），与昨天 74 行 basename 集合无新增用户可读漂移；4 件 1-day 龄新增全是 cron 内部 scratch 残件（c67ee10a shell 调试残留）。
- `~/.hermes/state.db` 单日 +1 MiB（496 → 497 MiB），仍超 490 MiB 红线，今日活动量与昨日持平但未缓解趋势。
- 8-15 GC Day **必须落地**的 3 件事：① follow-builders skill 强制收尾段（status flip）；② EDN cron 末尾加 `rm -f /tmp/hermes-results/call_*.txt /tmp/edn-*/data_asset.json`（已呼吁 7 天，今日 cron 仍手动代偿 13 件）；③ 确认低峰 VACUUM 窗口拍板（已连续 6 天呼吁）。

## 2. 今日工作总览
- **0 用户直接对话**（state.db messages 当天仅 cron session 自身 14 条）。
- **0 派发子 agent**：1 个 cron session（当前日报 cron 自身）。
- **autopilot 自驱 1 次**：follow-builders skill 跑通 8-13 AI Builders 早报（10 builder + 0 博客 + 1 播客因缺视频 URL 跳过），飞书 08:01:38 送达。
- **状态**：cron 代 flip SUP-86 done + 本日报归档。延续"静默日 + autopilot 自驱 + cron 代 flip"模式。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【已闭环】SUP-86 (8-13 AI Builders 早报) cron 代 flip done：autopilot 08:01:38 飞书实际送达 `om_x100b68e4d8d3dcacb4ac687608378ae`，chat_id `oc_d5de551525d3a4ae9e1a52815bb9a92b` 自愈成功（issue 描述里大写 `OC_` 自动改小写），中文 builder 覆盖 10 位/16+ tweet/0 博客齐备，但 autopilot 仍漏翻 status —— 今日 cron 代 flip done。
- 【数据点】SUP-5x 元模式 12 样本累计：7-26 / 7-30 / 7-31 / 8-01 / 8-02 / 8-03 / 8-05 / 8-06(blocked) / 8-07 / 8-10 / 8-12 / 8-13 = 6 自愈 / 5 漏 / 1 blocked = 50% 投递后自愈率。**关键解耦**：chat_id 自愈率 100%（SUP-80/83/85/86 四次稳定），status flip 100% 依赖 cron 代 flip。
- 【状态】state.db 497 MiB（昨日 496，单日 +1 MiB），超 490 MiB 红线第 3 天。今日活动量与 8-12 几乎相同（仅 SUP-86 + 当前 cron），但仍未缓解 490+ 趋势。

### 知识库 / Obsidian / Get笔记
- 【已完成】本日 8-13 日报生成、双写归档路径走 skill 标配（写 vault → git commit/push → 同步 getnote）。

### 飞书 / 日历 / 文档 / 消息平台
- 【静默】今日无用户主动会话。

### OpenClaw / 代码项目 / GitHub
- 【无活动】今日无任何项目推进。

### 调研 / 内容处理
- 【已完成】SUP-86 autopilot 抓取 10 位 builder 完整 X 摘要：Josh Woodward (Gemini 跨平台 1 亿 iOS 用户) / Boris Cherny (Claude Code bug 形态变化) / Thibault Sottiaux (Codex Linux 落地) / Peter Yang (/human-review 717 ⭐) / Madhu Guru (DevRel 高光 + 开源垂直领域) / Thariq (Claude 文本水印)、播客 The MAD Podcast × Samsara CEO Sanjit Biswas（physical AI / 380k 车险规避 / 25T 数据点）。

## 4. 重要交付物 / 系统变更
- **type**: 日报 / **name**: `2026-08-13 Hermes 日报.md` / **status**: 已写入 vault 待 git commit + Get笔记同步
- **type**: Multica issue / **name**: SUP-86 (8-13 AI Builders 早报) / **status**: cron 代 flip done（chat_id `oc_d5de551525d3a4ae9e1a52815bb9a92b` · message_id `om_x100b68e4d8d3dcacb4ac687608378ae`）
- **type**: autopilot 产物 / **name**: `/tmp/digest-data.json` (104KB) + `/tmp/fb-digest-2026-08-13.md` (4.3KB) / **status**: autopilot 当日 08:01 产出，autopilot 白名单，不归档（已落 `/tmp` 标准产物路径）

## 5. 风险、阻塞、需要拍板
- 【需立即处理】`~/.hermes/state.db` 497 MiB 超越 490 MiB 红线第 3 天，单日 +1 MiB 趋势延续。**建议**：你本周内确认一个 0:00-2:00 低峰窗口，让我跑 VACUUM；cron 自身不会自动跑（锁库风险）。**已连续 6 天呼吁**。
- 【GC Day 8-15 必须落地】① follow-builders skill 末尾加强制收尾段 `multica issue status SUP-N done`（即使 chat_id 自愈成功也要执行）；② patch autopilot 内部 prompt 显式包含 "deliver 后立即 flip" 硬指令。**已累积 12 样本证实双轨分离必要性**。
- 【GC Day 8-15 必须落地】EDN cron 末尾加 `rm -f /tmp/hermes-results/call_*.txt /tmp/edn-*/data_asset.json` 收尾段。**已呼吁 7 天**，今日 cron 仍手动代偿 13 件（8-12 EDN 残件 + 8-12 历史 `data_asset.json`），治本 patch 一直未落地。
- 【可继续观察】SUP-5x 元模式本周 2 次样例（SUP-85 / SUP-86）chat_id 自愈 100% 稳定，已是事实规范。autopilot 内部 prompt 是否要把"chat_id 自愈"从隐式决策升级为 start 阶段硬约束？**建议 GC Day 评估**。

## 6. Hermes 自我改进
- **【反模式·已规避】今天 0 用户活动不是"无显著活动"**——本日仍按模板生成日报并落档，因为静默日也是 audit trail 的一环（避免再次踩 6-15 全静默日漏档教训）。同时再次确认 cron 仅做"必要工作"（SUP-86 代 flip + 写日报 + 双写），不画蛇添足。
- **【新坑点名】autopilot 当日产物与 cron 内部 scratch 同时落到 `/tmp`**，今日 1-day 龄新增 4 件（`today_not.txt` / `yday_not.txt` / `hermes-cwd-c67...` / `hermes-snap-c67...`）全是昨日 cron 调试残留 + Hermes 内部 scratch。今日 preflight 已通过 basename diff 白名单过滤后判为 0 件用户可读新工件。**反模式反向证据**：若不跑 basename diff 直接 `wc -l` 报"昨夜漏报 4 件"就是假报警。
- **【8-15 GC Day 议题清单】**（已呼吁 3+ 天）：① follow-builders status flip 强制收尾；② EDN cron 残件清理；③ state.db 低峰 VACUUM 窗口拍板；④ 是否改 cron prompt 加自动低峰 VACUUM（仅 SIZE > 490 时触发）让用户明确拍板。

## 7. 明日建议
- 【可自动做】明日 cron 启动前先确认 SUP-87（8-14 早报）飞书送达 + 是否需要代 flip，预计同款模式。
- 【需要你确认】**本周内给一个 state.db VACUUM 窗口拍板**（任何凌晨 0:00-2:00 时段都行），或回复"同意 cron 在 SIZE > 497 时自动低峰 VACUUM"——这是 490+ 红线第 3 天，再不处理下周可能踩数据库锁。
- 【需要你确认】8-15 GC Day 议题清单 4 项已在日报里喊 3+ 天，可直接拍板"同意 GC Day 一次性把 4 项都 patch" / 或"只 patch 前 2 项急的"。
- 【可自动做】继续监控 1-2 周 SUP-5x 元模式自愈率：若 ≥ 10 连续样本 90%+ 自愈率可放宽 patch 紧迫性；目前 50% 远低于稳定阈值，patch 紧迫性不变。
- 【可选】今天 0 用户活动，无新讨论议题；周末前若无主动追问，明日日报会保持"静默日 + autopilot 自驱"模板。
