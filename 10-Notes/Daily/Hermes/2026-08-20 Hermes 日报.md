# Hermes 日报 - 2026-08-20

## 1. 老板速读
- **静默日第 10 天**：今日无用户主动会话，Hermes 实质产出 = SUP-93 autopilot 单次早报。
- **SUP-93 (8-20 早报) autopilot 完整闭环**：19 builder / 32 X / 1 podcast + 2 博客（Anthropic《How we contain Claude》、Claude《Code supports artifacts》），飞书已送达；今日 cron 代 flip done。
- **EDN 周报连续 2 周跑空风险延续**：8-19 stub-success 误判导致本周 weekly digest 0 产出；下周 8-26 触发前用户未拍板 patch 路径将形成 3 连空。
- **state.db 507 MiB 持续踩红线**：与昨日持平，未继续增长（EDN 无 subagent 跑 + autopilot 未留 /tmp 残件是部分原因）；VACUUM 窗口连续 13 天未拍板。
- **`lark-cli user identity` 自愈已部分修复**：今日 23:50 cron 启动时 expiresAt 已滚动到 2026-08-20T22:28:22（refreshExpiresAt 2026-08-27），不再是昨日的 2026-08-19T10:02:28——用户侧或后台触发了 refresh token；tokenStatus 仍 `needs_refresh` 但机制不再完全证伪。
- **SUP-88 Mode E 延续第 6 天 / SUP-79 chat_id blocked 第 15 天**：两条都等待用户拍板。

## 2. 今日工作总览
- 【静默】用户全程未直接对话；今日 Hermes 活动 = 1 次 SUP-93 autopilot + 1 次日报 cron。
- 【已闭环】SUP-93 (8-20) 飞书投递成功 + cron 代 flip done。
- 【已清理】EDN 8-19 / 8-05 残留 `/tmp/hermes-results/` + `/tmp/edn-*/data_asset.json` 今日 cron 代偿 `rm -rf`。
- 【零漂移确认】`/tmp` 旧工件扫描 = 3 ARCHIVED + 98 NOT_ARCHIVED，与昨日完全相同；今日新增用户可读工件 = 0 件（autopilot 当日产物未落 /tmp）。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统
- 【静默】今日 Hermes 用户直接对话 = 0 次；autopilot 自驱产出 SUP-93 一条。
- 【已维护】`state_db_size.log` 追加今日 507 MiB（与昨日持平，无暴涨），log 连续 20 天无缺口。
- 【已清理】EDN cron 历史残件 `/tmp/hermes-results/` + `/tmp/edn-2026-08-05/` 已 `rm -rf`，治本 patch（EDN cron prompt 末尾加清理）仍待 GC Day 8-22 落地。

### 知识库 / Obsidian / Get笔记
- 【完成】今日日报已写入 vault + 待 git commit/push + 同步 Get笔记（操作见 §4）。
- 【零写入】今日无新条目（非日报）需要双写。

### 飞书 / 日历 / 文档 / 消息平台
- 【已自愈部分】`lark-cli auth status` user identity `needs_refresh` 但 `expiresAt` 已滚动到 2026-08-20T22:28:22（昨日为 2026-08-19T10:02:28），`refreshExpiresAt` 2026-08-27T20:28:22——今日白天有 user API 调用触发 refresh。今日 SUP-93 飞书送达仍走 bot 身份（autopilot 默认 `--as bot`），未验证 user 身份是否仍 fallback。
- 【活跃】SUP-93 飞书送达 + 中文 builder 覆盖齐备。

### OpenClaw / 代码项目 / GitHub
- 【无活动】今日无任何代码项目推进。

### Multica / 自驱工作流
- 【成功】**SUP-93 (8-20 早报) autopilot 完整闭环**：19 builder / 32 X / 1 podcast (Rich Sutton & Khurram Javed《Why AI Models Stop Learning, and How to Start It Again》) + 2 博客（Anthropic《How we contain Claude》、Claude《Code supports artifacts》）。核心信号 = OpenAI/Sam Altman 主动暂停前沿 RL 训练对齐安全门槛 + Anthropic 系统复盘 containment 架构 + Rich Sutton LLM 部署后不持续学习是错误范式——三件事同指向 AI 行业从"卷能力"转入"卷落地与安全"。飞书送达 `om_x100b67500fcf8934b3bcd3010bdb1b0`，cron 代 flip done（91 秒级同款行为）。
- 【持续】**SUP-88 (8-15 早报) Mode E 第 6 天未自愈**：autopilot 仅写中文 comment、未拉 builder 数据、未投递飞书；cron 不能代 flip（无 om_x）。
- 【持续】**SUP-79 (8-06 早报) chat_id `blocked` 第 15 天**：autopilot 跑完 digest 但飞书投递因 `OC_` 大写失败，digest 仍贴在 comment 待用户拍板。

### 调研 / 内容处理
- 【完成】SUP-93 autopilot 飞书投递 + 中文汇总完成（19 builder / 32 推文 / 1 播客 / 2 博客）。
- 【风险·连续 2 周】EDN 本周 weekly digest **0 产出**（8-19 stub-success 误判，cron 监控层 `last_status='ok'`）——下周 8-26 触发前若无 patch，3 连空将造成 EDN 战略决策窗口 21 天连续缺失。

## 4. 重要交付物 / 系统变更
- Multica issue｜**SUP-93**｜**已完成**：autopilot 完整闭环（19 builder / 32 X / 1 podcast / 2 blog / 飞书送达 `om_x100b67500fcf8934b3bcd3010bdb1b0` + cron 代 flip done）。
- cron 失败｜**EDN-Weekly-Market-Insight (`1d0cf41439ec`) 本周周报**｜**未完成**：weekly digest 0 产出（与 8-12 形成连续 2 周跑空）；下周 8-26 触发前必须 patch。
- 风险｜**state.db 507 MiB 红线**｜**持续**：`state_db_size.log` 今日 +0 MiB（持平 507 MiB），VACUUM 窗口连续 13 天未拍板。
- 风险｜**`lark-cli user identity`**｜**部分自愈**：expiresAt 已滚动到今日 22:28，tokenStatus 仍 `needs_refresh`（明日 cron 必须验证 user API 是否真正可用）。
- 风险｜**SUP-88 Mode E**｜**第 6 天**：等待用户拍板（手动转贴飞书 vs 跳过本期）。
- 风险｜**SUP-79 chat_id `blocked`**｜**第 15 天**：等待用户拍板（人工补投目标 chat_id）。

## 5. 风险、阻塞、需要拍板
- **【🔴 紧急】EDN 周报连续 2 周失败 + 8-22 GC Day 强制窗口**：8-19 stub-success 已确认是 subagent 同步返回的 stub 被误判为 completed；下周 8-26 触发前若不落 `completion-token + data_asset.json 硬断言` patch，将形成 3 连空（21 天窗口缺失）。**需要用户拍板**：是否授权 GC Day 8-22 落 EDN cron prompt 强制 patch。
- **【🔴 紧急·第 13 天】state.db 507 MiB VACUUM 窗口**：今日与昨日持平（+0 MiB），趋势可能因 EDN 无 subagent 跑暂缓；用户工作流需要明确 X 日凌晨低峰窗口。**需要用户拍板**：cron prompt 自动低峰 VACUUM（SIZE > 490）还是手动停机跑。
- **【🟡 关注】`lark-cli user identity` 部分自愈**：今日 expiresAt 已滚动到 22:28 但 tokenStatus `needs_refresh`——明日 cron 必须验证 user API 是否真正可用（SUP-94 飞书送达身份是 user 还是 bot）；若继续走 bot，则明日日报需重新升级为"必须用户手动 `lark-cli auth login --refresh`"。
- 【持续·第 6 天】**SUP-88 Mode E**：是否手动转贴飞书 vs 跳过本期。
- 【持续·第 15 天】**SUP-79 chat_id `blocked`**：人工补投目标 chat_id。

## 6. Hermes 自我改进
- **EDN stub-success 误判检测加进日报 cron preflight**：今日确认 8-19 失败模式后，未来日报 cron 启动时若 EDN cron `last_status='ok'` 但**当日 0 user-API 调用** + **`edn-insight-data/<date>.json` 不存在** + **飞书长住文档本周增量未写入** 三件全成立 → 默认按 EDN 失败报告（即使 cron 监控显示 ok）。该规则应在下次日报 cron 自动跑前先发到 vault 作为补丁路径（已在本日报 §7 标注为"可自动做"）。
- **EXP 自愈率统计需要增加 user-identity-bot-fallback 维度**：今日发现 lark-cli expiresAt 滚动 + SUP-93 仍走 bot 身份，意味着 tokenStatus 与实际身份可能脱钩；下次日报 cron 应记录 `autopilot 飞书送达消息的 metadata 身份` 而非仅看 lark-cli `auth status`——避免"自愈证据"二次误判。
- **`/root/.hermes/state/state_db_size.log` 连续无缺口维护**：今日追加 8-20 507MB（持平），log 连续 20 天无缺口；cron 启动时若发现 log 最后一行不是昨天日期 → 主动从昨日日报反查补一行（不要跳）。

## 7. 明日建议
- 【可自动做】明日 8-21 daily-report cron 启动时增加 EDN stub-success 检测 + lark-cli user identity 实际可用性验证（不只看 auth status，而跑一次 user API 测试调用）。
- 【可自动做】明天 SUP-94 autopilot 飞书送达后，今日 cron 模式翻 SUP-94（如适用）。
- 【需要你确认】**EDN cron stub-success patch 是否本周落地**：连续 2 周 EDN 跑空，下周 8-26 触发前必须拍板（否则 3 连空 = 21 天窗口缺失）。
- 【需要你确认】**state.db VACUUM 窗口**：连续 13 天未拍板；建议本周内（X 日凌晨 02:00-04:00） + cron 提前 1h 通知。
- 【需要你确认】**SUP-88 / SUP-79 处理路径**：是否授权 cron 在第 7 天 / 第 16 天 ghost（不再持续列风险位，由用户主动恢复）。