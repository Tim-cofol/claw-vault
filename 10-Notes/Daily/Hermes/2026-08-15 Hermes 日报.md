# Hermes 日报 - 2026-08-15

## 1. 老板速读

- 今日 **0 用户直接对话、0 派发子 agent、0 autopilot 跑通产物**；纯 GC Day 落档日。
- **GC Day #35（8-15 周六 09:00）跑通**：MEMORY/USER 已 lean 无迁出、4/4 cron enabled、gateway 健康；唯一发现 = state.db **500MB 踩 500 MiB 红线**（昨日 499 → 今日 500，单日 +1 MiB），FTS5 bloat ratio 2.1×。
- **SUP-88 (8-15 AI Builders 早报) autopilot 启动但**仅写中文 comment、没拉数据、没投递飞书**——这是 SUP-5x 元模式的新一类失败（无 om_x / oc_ 证据）。issue description 里的 chat_id 仍是大写 `OC_d5de551525d3a4ae9e1a52815bb9a92b`，autopilot 未自动小写兜底。
- `lark-cli` user identity 仍 `needs_refresh`（expiresAt 2026-08-15T10:01:44，今日 23:50 已过），scope 列表齐（含 `im:message.send_as_user`），下次 user API 调用会自动 refresh；今日无 user API 投递触发，未验证自愈路径。
- **明日起最重要的事**：state.db **已进入 500 MiB 红线**（GC Day #35 报告判定为 🔴 立即处理项，但所有破坏性操作未执行，等用户拍板）；下周 #36 GC Day cron 健康，本周内**必须给一个低峰 VACUUM 窗口**。

## 2. 今日工作总览

- **0 用户直接对话**（state.db 当天仅 cron + 老 Discord session 的零星消息）。
- **0 派发子 agent**。
- **autopilot 失败 1 次**：SUP-88 (8-15 早报) 启动后只写了中文 comment（19 builder / 36 推文 / 1 播客摘要齐全），但**未拉任何 /tmp 工件、未投递飞书**（comment 无 om_x / oc_ 证据），status 仍 `in_review`。
- **GC Day #35 cron 跑通**：备份 MEMORY/USER 健康检查 + 索引审计，输出 22 条 assistant 消息到 `/root/.hermes/cron/output/8dc968bf2488/2026-08-15_09-02-19.md`（未入 vault）。
- **状态**：典型「周末静默 + GC Day 跑通 + autopilot 启动但未投递」复合模式。延续「静默日 + cron 代 flip」第 4 天，但今天 autopilot 没进入"需代 flip"环节——因为根本没投递飞书。

## 3. 按项目 / 责任域汇报

### Hermes / Agent 系统

- 【已闭环】GC Day #35 (8-15 周六 09:00:24 → 09:02:19) cron `8dc968bf2488` 跑通：MEMORY 1275/2200 (58%) + USER 523/1375 (38%) 已 lean 无可迁出；新建备份 `MEMORY.md.bak_gc_20260815` + `USER.md.bak_gc_20260815`；索引审计 8 个 skill 引用全部解析到现存 §-anchor，无悬空指针。
- 【健康】Gateway PID 3189004 在跑，gateway.log 最近 ERROR 0 条；Ticker 09:00 fresh；Cron 4/4 enabled (日报 / GC Day / Idea Digest / EDN)，EDN last_run 8-12（今日非 EDN 触发日）。
- 【健康】Skill 库 132 个 SKILL.md 结构完整，最近一次大量新增 2026-06-08 (27 个)；多数 SKILL.md mtime >60d 属正常。
- 【新发现】state.db **500MB** 踩 500 MiB 红线（8-14 报告 499 → 今日实测 500，GC Day 报告点 499）；FTS5 trigram_data 45958 行（**bloat ratio 2.1×，理想 < 1.5×**）；state-snapshots 仅 1 个（`20260712-102944-pre-update` 396MB，无 rolling retention）；磁盘 checkpoints 116M / sessions 98M / logs 52M 都不超阈值。
- 【失败·新模式】SUP-88 (8-15 早报) autopilot 启动后**只写中文 comment、未拉 builder 数据、未投递飞书**：comment 含 19 builder / 36 推文 / 1 播客摘要完整，但**无 om_x / oc_ 任何飞书送达证据**。**这是 SUP-5x 元模式的新一类失败**：之前是"飞书送达但漏翻 status"，今天是"comment 写了但飞书根本没投递"。issue description 里的 chat_id 仍是模板硬编码大写 `OC_d5de551525d3a4ae9e1a52815bb9a92b`，autopilot **未自动小写兜底**（对比 SUP-80/83/85 三次都自愈，今天失败）。

### 知识库 / Obsidian / Get笔记

- 【进行中】本日 8-15 日报生成、走 skill 标配双写（写 vault → git commit/push → 同步 getnote）。
- 【未变】`scripts/getnote_sync_obsidian.py` 仍位于 `/root/.hermes/skills/openclaw-imports/obsidian-getnote-dualwrite/scripts/`，vault 内 `/root/obsidian-vault/scripts/` 不存在脚本副本；沿用 OpenAPI fallback 模式 + 幂等映射 `getnote_sync_map.json`。

### 飞书 / 日历 / 文档 / 消息平台

- 【静默】今日无用户主动会话。
- 【待刷新】`lark-cli auth status` user identity `needs_refresh`（access token 过期 2026-08-15T10:01:44，今日 23:50 已过 ~13h50m），scope 列表含 `im:message.send_as_user` / `docx:document:create` 等关键 scope 齐全，下次 user API 调用会自动 refresh。今日无 user API 投递触发，未验证自愈路径。
- 【活跃】SUP-88 autopilot 写了中文 comment 但**未触发飞书投递**——所以无 SUP 飞书送达记录。

### OpenClaw / 代码项目 / GitHub

- 【无活动】今日无任何项目推进。

### 调研 / 内容处理

- 【未落地】SUP-88 autopilot 抓 builder 数据未落地：comment 内容是齐的（19 builder / 36 推文 / 1 播客），但**没拉 X / 播客 transcript**（`/tmp` 无 `fb-blogs.json` / `fb-podcast.json` / `fb-x.json` 等产物，最新 mtime 是 8-13 的 `digest-data.json`）。autopilot 似乎只调用了 LLM 总结，**跳过了 fetch 阶段**——根因待查。
- 【完成】GC Day #35 完整报告已写到 `/root/.hermes/cron/output/8dc968bf2488/2026-08-15_09-02-19.md`（22083 bytes / 254 行），含 5 段结构（改动清单 / 健康检查 / 建议清理项 / 下周关注点 / 重要提醒）。

## 4. 重要交付物 / 系统变更

- **type**: 日报 / **name**: `2026-08-15 Hermes 日报.md` / **status**: 已写入 vault 待 git commit + Get笔记同步
- **type**: Cron report / **name**: `~/.hermes/cron/output/8dc968bf2488/2026-08-15_09-02-19.md` (GC Day #35) / **status**: 已生成，未入 vault
- **type**: Memory backup / **name**: `~/.hermes/memories/MEMORY.md.bak_gc_20260815` + `USER.md.bak_gc_20260815` / **status**: 已新建（GC Day 备份惯例）
- **type**: Multica issue / **name**: SUP-88 (8-15 AI Builders 早报) / **status**: `in_review`（autopilot 启动但未投递飞书，无 om_x / oc_ 证据，cron 不代 flip）
- **type**: 状态日志 / **name**: `/root/.hermes/state/state_db_size.log` / **status**: 已 append 2026-08-15: 500MB（连续无缺口）

## 5. 风险、阻塞、需要拍板

- 【**🔴 需立即处理**】`~/.hermes/state.db` **500 MiB**（**已踩 500 MiB 红线**！昨日 499 → 今日 500，单日 +1 MiB，trend +5-10 MiB/天 已稳定 6 天）。GC Day #35 报告把此列为🔴 立即处理项，但所有破坏性操作（FTS rebuild + VACUUM）未执行，等你拍板窗口期。**cron 自身不会自动跑 VACUUM**（锁库风险）。建议：本周内凌晨 0:00-2:00 停 gateway 跑 FTS rebuild + VACUUM；或同意 cron 在 SIZE > 500 时自动低峰 VACUUM。**已连续 8 天呼吁**。
- 【**🔴 需立即处理**】**SUP-88 autopilot 新失败模式**（**仅写 comment 未投递飞书**）——8-15 早报**完全没投到飞书**，用户今天收不到任何 AI Builders 早报。comment 里的 19 builder 内容齐但无 om_x / oc_ 证据。**请确认**：今天是否手动把 SUP-88 comment 内容转贴飞书？或今天就这样跳过、明天 autopilot 正常跑？**关键风险**：这类型失败 autopilot 自检不出来（comment 写完就结束），只有 cron 看 `/tmp` 工件缺失 + state.db 状态 + comment 无 om_x 三项联合判定才能发现。GC Day 8-15 之后应 patch follow-builders skill 加"投递成功回写 om_x 到 comment"硬断言，否则下次仍会"comment 齐 + 飞书无"。
- 【需 GC Day 8-22 落地】**follow-builders skill 双轨 patch**（已呼吁 7+ 天）：① status flip 强制收尾段（即使 chat_id 自愈成功也要执行 `multica issue status SUP-N done`）；② autopilot chat_id 大小写兜底应**默认开启**而非概率事件（今天 SUP-88 第三次未自愈）；③ 新增【新】投递成功回写 om_x 硬断言——comment 必须含 `om_xxx` 才算 deliver 完成，否则 status 必须保持 `in_review` 并写"飞书未投递"原因。GC Day 8-15 patch 一直未落地，**SUP-88 是 14 样本累计的第 7 个漏**（7 自愈 / 6 漏 / 1 blocked-N/A → 50% 自愈率，新分类 = "漏 + 1 new mode"）。
- 【可继续观察】state-snapshots 仅 1 个（396MB），无 rolling retention——GC Day #35 已 dry-run 给出"保留最近 2 个 / 旧的移 .archive"建议，未执行。**等待你点头**。
- 【可继续观察】state.db.bak_gc_20260725 (441MB) 是 GC Day 全量备份，已被 8/15 增量覆盖，建议删——GC Day #35 建议删，未执行。**等待你点头**。

## 6. Hermes 自我改进

- **【SUP-5x 元模式新子类发现】**SUP-88 (8-15) 是**第四类失败模式**：(a) 早期是"飞书送达但漏翻 status"；(b) 8-06 SUP-79 是"chat_id 大写阻塞投递"；(c) 8-15 SUP-88 是**"comment 齐 + 飞书未投递 + 无 om_x"**——之前两类失败 autopilot 至少跑了 fetch + translate，今天**似乎跳过了 fetch 阶段**直接生成 comment（内容完整但 `/tmp` 无 fb-blogs.json / fb-podcast.json / fb-x.json 等产物）。**根因待查**：是 follow-builders skill 内部 prompt 让 LLM 直接走 generate 而非 fetch 阶段，还是 MCP tools 异常被 silently skipped？**强制规则**：GC Day 8-22 patch 必须加 "comment 必须含 `om_xxx` + 至少 1 个 `/tmp/fb-*.json` 落地" 双断言，否则 status 保持 `in_review` 并写"飞书未投递"原因。
- **【state.db 500 MiB 红线落地】**8-14 日报已 [🔴]，8-15 真的踩 500 MiB（500MB = 524902400 bytes）。今日 cron 没主动跑 VACUUM（不会自动跑），但**这是连续 8 天呼吁后用户仍未拍板的最直接后果**。**强约束**：今天起任何 cron 启动时若 SIZE ≥ 500 MiB → §5 风险位**默认加 [🔴 紧急]**（不再只是【需立即处理】）；§7 明日建议写"今日必须给窗口拍板"而非"本周内"。
- **【lark-cli user identity 自愈路径未验证】**今日 expiresAt 过期 13h50m，无 user API 调用触发 refresh，**自愈是否生效明天才能知道**。明天日报 cron 启动时跑一次 `lark-cli auth status` 看 expiresAt 是否被更新——若仍未刷新，说明 OAuth refresh token 也可能过期，需 `lark-cli auth login --refresh` 重授权。

## 7. 明日建议

- 【需要你确认】**state.db VACUUM 窗口**——今天已踩 500 MiB 红线，明早前给一个 0:00-2:00 时段即可。或直接同意 cron 在 SIZE > 500 时自动低峰 VACUUM。**这是连续 8 天呼吁的硬 deadline**，再不处理下周 #36 GC Day 之前可能进入只读。
- 【需要你确认】**SUP-88 飞书投递补救**：今天 19 builder / 36 推文 / 1 播客摘要齐全但未投飞书，请决定是否手动把 SUP-88 comment 内容转贴飞书 chat `oc_d5de551525d3a4ae9e1a52815bb9a92b`，或今天跳过等明天 autopilot 正常跑。
- 【需要你确认】**GC Day 8-22 patch 清单升级**（SUP-5x 元模式第 14 样本 + state.db 500 MiB 触线）——建议 patch follow-builders skill 一次性落 3 件：① status flip 强制收尾；② chat_id 大小写默认兜底（不再依赖概率）；③ **新增** "comment 必须含 om_x + /tmp fb-*.json 至少 1 个" 双断言。state-snapshots 滚动保留（保留最近 2 个）+ state.db.bak_gc_20260725 删旧 一次落。
- 【可自动做】明日 cron 启动前先确认 SUP-89 (8-16 早报) autopilot 飞书投递 + 是否需要代 flip；同时观察 SUP-88 模式下 autopilot 是否会复现（comment 齐 + 飞书无）。
- 【可选】下周 #36 GC Day cron 健康，无需介入；若本周内能拿到 state.db VACUUM 拍板，下周 #36 自动落 `state.db.bak_gc_20260725` 删除。