# Hermes 日报 - 2026-05-18

## 1. 老板速读
- 今天最有价值的产出是完成了 Get笔记对 YouTube 链接的实测：`note_type:"link"` 可以直接 ingest YouTube，并在 `note/detail` 的 `web_page.content` 中暴露带时间戳的原始字幕；`content` 字段是 AI 总结，不能当原文。
- 基于这次实测，Hermes 更新了 YouTube 内容处理路径：以后“原始字幕归档到 Get笔记”优先走 Get笔记 link ingest，Supadata 作为 fallback，而不是默认先抓 Supadata。
- 已把这条新经验固化到技能库：更新了 `youtube-content`、新增参考文件 `references/getnote-youtube-link-ingest.md`，并更新 `obsidian-getnote-dualwrite` 的 YouTube 原始字幕规则。
- 同步写入了长期 memory：Get笔记 YouTube link ingest 的字段位置、`progress` API 的误导风险、Supadata fallback 边界。注意 memory 使用率已到约 85%，后续需要做 memory 压缩/清理。
- 另完成一条 Discord 公告翻译：Hermes Agent 创意黑客松获奖名单从英文翻译为中文，未见持久化落盘或发布动作。
- 后台 skill curator 执行了一次技能库整理检查，只发现 `linux-server-maintenance` 一个 agent-created umbrella skill，未做合并/删除，状态正常。
- 重要风险：`/root/obsidian-vault/scripts/getnote_sync_obsidian.py` 仍缺失；今天日报同步只能使用 OpenAPI fallback，对日报这种 recurring note 有重复创建 Get笔记的结构性风险。
- 明日建议：优先恢复或迁移 Get笔记同步脚本到 `/root/obsidian-vault/scripts/`，其次清理/压缩 memory，避免后续高频任务被老规则污染。

## 2. 今日工作总览
今天处理了 4 类任务：

- 已完成：3 类
  - Get笔记 YouTube 原始字幕能力实测。
  - YouTube/Get笔记路由规则技能固化。
  - Hermes 黑客松获奖公告中文翻译。
- 已推进：1 类
  - 技能库 curator 检查，确认当前没有明显可合并的 agent-created sibling skill。
- 阻塞或失败：1 类
  - 日报的 Get笔记同步脚本缺失，需使用 OpenAPI fallback。
- 建议：2 项
  - 恢复幂等同步脚本。
  - 压缩 memory 中重复或过期的工具路由规则。

## 3. 按项目 / 责任域汇报

### A. Hermes / Agent 系统
- 状态：已推进
- 做了什么：
  - 通过后台 skill curator 扫描技能库，目标是做 umbrella-building consolidation，而不是简单查重。
  - 检查结果显示仅发现一个 agent-created skill：`linux-server-maintenance`。
- 产出：
  - 未做技能合并、删除或归档；确认 `linux-server-maintenance` 已是 class-level umbrella skill，并带有支持文件：
    - `references/oz-lite-disk-cleanup-2026-05.md`
    - `scripts/worker_capacity_probe.py`
- 证据：
  - session：`session_20260518_202639_5d90ec.json`
  - session 标题/来源：后台 skill curator，2026-05-18 20:26:39 左右。
  - 工具输出：`consolidations: []`，`prunings: []`。
- 风险 / 后续：
  - curator 本轮没有发现可操作对象，不代表技能库整体没有冗余；只是本轮候选集很小。

### B. 知识库 / Obsidian / Get笔记
- 状态：已完成
- 做了什么：
  - 按用户要求验证 Get笔记是否能直接访问 YouTube 并取得完整原始字幕，从而判断后续是否还需要 Supadata 抓原始数据。
  - 使用测试链接：`https://youtu.be/awRQWBvE5eQ?si=LHiB1PNB7gHTqbke`
  - 使用 Get笔记 OpenAPI 创建 `note_type:"link"` 链接笔记，并轮询/读取详情。
- 产出：
  - 第一次测试创建链接任务：`task_id=6a0a9e4c43b8e81cae83749e`，`note_id=1910273441761253528`。该次详情中 `web_page.content` 仅为测试说明文本，不能证明抓到视频字幕。
  - 第二次测试创建链接任务：`task_id=6a0a9ec6bae7fe6ba4804d8b`，`note_id=1910273572757894200`。详情中：
    - title：`零成本10倍提升生产力：Hermes AI Agent结合DeepSeek V4的三模Triad工作流搭建教程`
    - `content_len=6443`，为 AI 总结。
    - `web_content_len=28684`，`web_page.content` 包含带时间戳字幕，例如 `[00:00 - 00:16] When you combine Hermes' agent...`。
  - 结论：Get笔记 link ingest 对该 YouTube 链接可拿到原始字幕，后续“归档到 Get笔记”场景应优先使用 Get笔记，而不是 Supadata。
- 证据：
  - session：`session_20260518_131014_02f7c4.json`
  - 关键工具输出：`SAVE 200`、`POLL ... status processing/success`、`DETAIL 200`。
  - Get笔记 note_id：`1910273572757894200`。
  - 字段证据：`web_content_len=28684`，`content_len=6443`。
- 风险 / 后续：
  - Get笔记 `task/progress` 可能返回 `processing` 且带有陈旧/误导性的 `error_msg="生成笔记失败，请手动重试"`，但 `note/detail` 里已经出现可用内容。后续不能只看 progress，需要同时按 `note_id` 查 detail。
  - `content` 是 AI 总结；保存“原始字幕”时必须使用 `web_page.content`。

### C. 飞书 / 日历 / 文档 / 消息平台
- 状态：已完成
- 做了什么：
  - 将 Hermes Agent 创意黑客松获奖公告从英文翻译为中文。
- 产出：
  - 中文版公告文本，保留主赛道、技术赛道、研究赛道、学生赛道等结构和 Discord submission thread 链接。
- 证据：
  - session：`session_20260518_144424_c39bffa1.json`
  - session 标题：`Hermes Hackathon Winners Announcement`
  - 时间：2026-05-18 14:44 左右。
- 风险 / 后续：
  - 未见发布、保存到 Obsidian 或同步 Get笔记动作；本日报只记录“翻译完成”，不记录为“已发布”。

### D. OpenClaw / 代码项目 / GitHub
- 状态：已推进
- 做了什么：
  - 检查本机 OpenClaw/Get笔记环境能力，确认存在 `/usr/bin/openclaw`、`/usr/bin/node`、Hermes Python、`jq`。
  - 发现 Get笔记同步脚本存在于 OpenClaw 工作区路径：`/root/.openclaw/workspace/scripts/getnote_sync_obsidian.py`。
- 产出：
  - 明确当前 Vault 路径下日报技能期望的脚本 `/root/obsidian-vault/scripts/getnote_sync_obsidian.py` 不存在。
- 证据：
  - 今日日报执行前检查：`test -f /root/obsidian-vault/scripts/getnote_sync_obsidian.py` 输出 `script_missing`。
  - session：`session_20260518_131014_02f7c4.json` 中读取过 OpenClaw 工作区脚本。
- 风险 / 后续：
  - 两个路径之间的脚本分布不一致导致日报只能 fallback。建议把脚本迁移/复制到 Vault `scripts/`，或修改技能约定统一使用 OpenClaw workspace 脚本。

### E. 调研 / 内容处理 / 资料阅读
- 状态：已完成
- 做了什么：
  - 读取并核对 Get笔记技能文档，确认链接笔记工作流：`note/save` 创建任务，`task/progress` 轮询，`note/detail` 读取详情。
  - 对 YouTube 原始字幕路径做实测，而不是只根据文档推断。
- 产出：
  - 明确新路由：Get笔记 link ingest → `note/detail.web_page.content` → 原始字幕归档；Supadata 只作为失败/不想创建 Get笔记 note 时的 fallback。
- 证据：
  - 技能文档路径：`/root/.openclaw/workspace/skills/getnote/SKILL.md`
  - 关键接口：`POST /open/api/v1/resource/note/save`、`POST /open/api/v1/resource/note/task/progress`、`GET /open/api/v1/resource/note/detail?id={note_id}`。
- 风险 / 后续：
  - 该结论基于一个 YouTube 测试链接验证。不同视频若字幕禁用、地区限制、会员限制或抓取失败，仍需 fallback。

### F. 其他
- 状态：已完成
- 做了什么：
  - 更新长期 memory，将 YouTube/Get笔记字段规则写入稳定偏好。
- 产出：
  - memory 条目替换为：YouTube raw subtitle archival into Get笔记 优先 Get笔记 link ingest；Supadata fallback；避免 youtube-transcript-api/yt-dlp。
- 证据：
  - session 工具输出：memory `usage: 85% — 1,887/2,200 chars`，`entry_count: 10`，`Entry replaced.`
- 风险 / 后续：
  - memory 已接近容量上限；需要压缩重复条目，尤其是 Get笔记/YouTube routing 与旧 Supadata-first 规则之间的潜在冲突。

## 4. 交付物索引

| 类型 | 名称 | 路径/ID/链接 | 状态 | 备注 |
|---|---|---|---|---|
| Get笔记 | YouTube 链接实测笔记（有效结果） | `note_id=1910273572757894200` | 已完成 | `web_page.content` 约 28684 字，含时间戳字幕 |
| Get笔记 | YouTube 链接实测笔记（初次探测） | `note_id=1910273441761253528` | 已完成但证据不足 | 只抓到测试说明文本，不能证明字幕能力 |
| 技能 | `youtube-content` | `~/.hermes/skills/media/youtube-content/SKILL.md` | 已更新 | 加入 Get笔记 link ingest 优先规则 |
| 技能参考 | Get笔记 YouTube link ingest 经验 | `~/.hermes/skills/media/youtube-content/references/getnote-youtube-link-ingest.md` | 已新增 | 记录实测 URL、note_id、字段位置、坑点 |
| 技能 | `obsidian-getnote-dualwrite` | skill library | 已更新 | 加入 YouTube 原始字幕归档规则 |
| Memory | YouTube/Get笔记 routing | Hermes memory | 已替换 | 使用率约 85% |
| 消息内容 | Hermes 黑客松获奖公告中文翻译 | `session_20260518_144424_c39bffa1.json` | 已完成 | 未发布/未落盘 |
| 后台任务 | skill curator umbrella pass | `session_20260518_202639_5d90ec.json` | 已完成 | 无 consolidations/prunings |
| Obsidian 笔记 | Hermes 日报 - 2026-05-18 | `/root/obsidian-vault/10-Notes/Daily/Hermes/2026-05-18 Hermes 日报.md` | 已生成 | 本文件 |

## 5. 配置与系统变更
- 变更内容：更新 `youtube-content` 技能。
  - 原因：Get笔记 YouTube link ingest 实测成功，旧规则偏向 Supadata first，不再适合作为“归档到 Get笔记”的默认路径。
  - 验证方式：技能更新工具返回 `Patched SKILL.md in skill 'youtube-content'`；新增参考文件成功。
  - 回滚/修改方式：通过 `skill_manage(action='patch')` 修改或删除新增段落。
- 变更内容：新增 `youtube-content/references/getnote-youtube-link-ingest.md`。
  - 原因：保存本次实测的字段位置、note_id、异常表现和推荐流程，避免下次重新踩坑。
  - 验证方式：工具返回文件写入成功，路径为 `/root/.hermes/skills/media/youtube-content/references/getnote-youtube-link-ingest.md`。
  - 回滚/修改方式：用 `skill_manage(action='remove_file')` 删除，或继续 patch。
- 变更内容：更新 `obsidian-getnote-dualwrite` 技能。
  - 原因：补充 YouTube 原始字幕归档时应优先使用 Get笔记 `web_page.content` 的规则。
  - 验证方式：工具返回 `Patched SKILL.md in skill 'obsidian-getnote-dualwrite'`。
  - 回滚/修改方式：用 `skill_manage(action='patch')` 修改该段规则。
- 变更内容：更新 Hermes memory。
  - 原因：这是稳定路由偏好，会影响后续所有 YouTube 原始字幕任务。
  - 验证方式：memory 工具返回 `Entry replaced`，使用率约 85%。
  - 回滚/修改方式：需要后续 memory 管理/压缩操作。
- 变更内容：本日报写入 Obsidian Vault。
  - 原因：每日 cron 日报归档要求。
  - 验证方式：文件已写入 `/root/obsidian-vault/10-Notes/Daily/Hermes/2026-05-18 Hermes 日报.md`。
  - 回滚/修改方式：编辑该 Markdown 后重新 git commit/push 并重新同步 Get笔记。

## 6. 质量与验证记录
- 已验证：当前执行时间与时区。
  - 命令输出：`2026-05-18 23:50:57 CST +0800`。
- 已验证：今日 session 文件存在。
  - 关键文件：
    - `session_20260518_130050_0afa3e91.json`
    - `session_20260518_131014_02f7c4.json`
    - `session_20260518_144424_c39bffa1.json`
    - `session_20260518_202639_5d90ec.json`
    - `session_cron_7be51b336d4f_20260518_235051.json`
- 已验证：Get笔记 YouTube link ingest。
  - `SAVE 200`，`DETAIL 200`。
  - 有效 note：`1910273572757894200`。
  - `web_page.content` 约 28684 字，含时间戳字幕。
- 已验证：技能更新。
  - `youtube-content` patch 成功。
  - `youtube-content/references/getnote-youtube-link-ingest.md` 写入成功。
  - `obsidian-getnote-dualwrite` patch 成功。
- 已验证：日报同步脚本缺失。
  - `/root/obsidian-vault/scripts/getnote_sync_obsidian.py` 检查结果：`script_missing`。
- 逻辑上完成但需后续验证：
  - 新路由对更多 YouTube 视频的稳定性。当前只有一个有效链接实测样本。

## 7. 问题、风险、阻塞
- 风险：Get笔记 progress API 状态不可靠。
  - 表现：`task/progress` 可持续返回 `processing` 和 `error_msg="生成笔记失败，请手动重试"`，但 `note/detail` 已经能读到完整内容。
  - 建议：未来实现探测脚本时同时轮询 `task/progress` 与 `note/detail`。
- 风险：`content` 与 `web_page.content` 容易混淆。
  - 表现：`content` 是 AI 总结；`web_page.content` 才是原始网页/字幕内容。
  - 建议：所有“不总结/原始字幕”任务强制读取 `web_page.content`。
- 风险：日报 Get笔记同步脚本缺失。
  - 表现：`/root/obsidian-vault/scripts/getnote_sync_obsidian.py` 不存在。
  - 建议：恢复脚本到 Vault，或修改日报技能使用 `/root/.openclaw/workspace/scripts/getnote_sync_obsidian.py`；否则 recurring note 继续有重复创建风险。
- 风险：memory 接近上限。
  - 表现：memory 使用率约 85%。
  - 建议：压缩重复条目，把流程性规则尽量迁到 skills，只在 memory 保留稳定偏好和关键路径。
- 风险：黑客松公告只完成翻译，未见发布/归档。
  - 建议：如需公开发布或沉淀，应由用户明确授权发布或要求保存。

## 8. Hermes 自我复盘

### 做得好的地方
- 没有停留在“文档说支持 link note”的推断，而是实际调用 Get笔记 API 验证。
- 正确区分了 AI 总结与原始字幕字段，避免把 `content` 错当 transcript。
- 把实测经验固化进技能和 reference，减少下次重复探索。

### 做得不够的地方
- 初次测试把测试说明文本放进 link note，导致 `web_page.content` 只出现说明文本，证据无效；第二次才得到有效视频字幕。
- 仍存在路径分裂问题：OpenClaw workspace 有同步脚本，但 Vault `scripts/` 下没有，导致日报 fallback 风险延续。
- memory 已接近上限，还写入了较长规则，需要后续精简。

### 可改进 / 可固化
- 应新增或补充一个可复用探测脚本：输入 YouTube URL，创建 Get笔记 link note，轮询 progress + detail，输出 `note_id`、`content_len`、`web_page.content_len`、前几条时间戳样例。
- 应恢复 `getnote_sync_obsidian.py` 到 `/root/obsidian-vault/scripts/`，并确认映射文件 `/root/.openclaw/workspace/config/getnote_sync_map.json` 可用。
- 应安排 memory 清理，把旧的 Supadata-first 规则与新的 Get笔记-first-for-archival 规则合并成一条短规则。

## 9. 明日建议 / 需要你拍板
- 建议 Hermes 自动做：恢复或迁移 Get笔记同步脚本到 `/root/obsidian-vault/scripts/getnote_sync_obsidian.py`，优先解决日报重复创建风险。
- 建议 Hermes 自动做：写一个 Get笔记 YouTube link ingest 验证脚本，作为 `youtube-content` 或 Get笔记技能的支持脚本。
- 建议 Hermes 自动做：压缩 memory，保留稳定事实，移除重复或流程性过强的条目。
- 需要你确认：是否把今天 Get笔记 YouTube 实测结论整理成一篇独立专题笔记，而不是只留在日报和 skill reference 里。
- 需要你确认：黑客松获奖公告中文翻译是否需要保存到 Obsidian/Get笔记，或发布到指定渠道。
